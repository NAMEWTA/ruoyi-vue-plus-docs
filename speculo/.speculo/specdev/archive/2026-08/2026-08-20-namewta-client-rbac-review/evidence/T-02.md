# Evidence T-02: 后端 Client RBAC 与认证合同

## Result

- Parent：`af4bf65c71087c6080f52fed6155c542a162419b`
- RBAC commit：`737232002` `fix(rbac): enforce client-scoped role contracts`
- Auth/session commit：`017797212` `fix(auth): fail closed on identity and sessions`
- Workflow consumer commit：`eeab21d91` `fix(workflow): scope assignee roles to client`
- Result HEAD：`eeab21d91`

## Contract Evidence

- 删除 `selectExplicitRolesByUserId`，角色列表、授权、运行时角色和默认角色均要求明确且启用的 Client 与登录域。
- `GET /system/user/{userId}` 未传 `clientId` 时 `roles`、`roleIds` 均为空数组；传入时只返回该 Client 角色上下文。
- `GET /system/user/authRole/{userId}` 的 `clientId` 改为必填。
- 无效/停用/无 Client 角色、无效 Client/登录域、跨 Client 角色和显式默认角色均抛出 `ServiceException`，不再静默跳过。
- 配置了但无效的默认角色在角色与菜单运行时路径失败关闭。
- `RegisterBody` 接受可选邮箱/手机号；非空时校验格式并复用账号、手机、邮箱唯一性检查，用户和登录域关系仍处于同一事务。
- `LoginHelper.getUserType()` 只读取 Token extra；删除 LoginUser/loginId 回退。
- 删除 `ClientSessionService.kickoutUser`；用户禁用/删除先捕获登录域，再逐域调用 `kickoutUserType`。
- 工作流办理人角色查询从当前登录快照读取 `clientPk`；无 Token/Client 上下文仍由严格角色服务拒绝，不恢复跨 Client 查询。

## Verification

- 定向编译：`bash mvnw -pl ruoyi-modules/ruoyi-system,ruoyi-admin -am -DskipTests compile`，35/35 SUCCESS，耗时 8.459s。
- 测试：`bash mvnw -Dmaven.test.skip=false test`，40/40 SUCCESS；实际 tests run 1，failures 0，errors 0，skipped 0，耗时 10.746s。
- 构建：`bash mvnw clean package`，40/40 SUCCESS，耗时 14.555s。按项目配置 package 跳过测试，因此不替代上一条测试证据。
- CR-002 前复核发现并修复工作流调用方缺失 Client 上下文；最终 `eeab21d91` 再次通过定向编译（21/21）、全量 `test`（40/40）和 `clean package`（40/40，14.872s）。
- `git diff --check`：通过。
- `rg`：`selectExplicitRolesByUserId` 与 `kickoutUser(` 均无残留；唯一 optional `clientId` 是按合同允许的基础详情接口。
- 变更审计：10 个 Java 文件；无 POM、lockfile、SQL、schema、测试源码或依赖变化；仓库 clean。

## Deferred E2E

MySQL/Redis/HTTP Token 的跨边界负向矩阵由 T-04 统一执行；本 Ticket 的 E2E disposition 已明确，不以编译或构建冒充运行时证据。

## 2. Lead Dispatch And Candidate Return

- Implementation owner：Lead `codex-root`，current workspace 直接实现与提交。
- 返回：三个 focused commits、clean child、Maven 结果；Lead 复核全部严格角色服务调用方并补齐工作流消费者。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| backend auth/system Java | writable | RBAC、注册、Token、会话严格合同 |

- read-only 修改、未声明路径、POM/SQL/测试源码/锁文件：无。

## 4. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001 | role services/API | scoped query 与负向静态审计 | pass |
| AC-003 | LoginHelper/auth | Token-only 登录域 | pass |
| AC-004 | register transaction | 可选身份校验与唯一性 | pass |
| AC-005 | session invalidation | 三方法服务与逐域清理 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| targeted compile | current-workspace | pass | 最终 21/21 SUCCESS |
| `bash mvnw -Dmaven.test.skip=false test` | current-workspace | pass | 40/40 SUCCESS |
| `bash mvnw clean package` | current-workspace | pass | 40/40 SUCCESS，14.872s |
| diff/path audit | current-workspace | pass | 无依赖/schema/SQL/test-source 变化 |

- 失败后修复与重跑：首次工作流补丁误用不存在 helper 方法，编译失败后改读 LoginUser 快照并完整重跑通过。
- E2E：not-run；required runtime matrix owner 为 T-04。

## 6. 双轴审查

### 标准轴

- 固定输入：`af4bf65c7..eeab21d91`；结果：pass；无未关闭 finding。

### 规范轴

- 来源：Spec/AC-001,003,004,005/CR-001；代码结果 pass，运行验收留给 T-04。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `af4bf65c71087c6080f52fed6155c542a162419b` |
| Implementation/source SHA | `eeab21d91689b1e5b40fcd87017c85658749fdc9` |
| Candidate branch/workspace | current |
| Method/conflicts | direct-parent；无 |
| Integration checks | current-workspace compile/test/package/diff passed |
| E2E disposition/result | required at T-04 / pending |
| Parent result/re-read | `eeab21d91689b1e5b40fcd87017c85658749fdc9` |

## 8. 偏差与决策

- 偏差：不重写已发布旧历史；整改 commits 保持聚焦并由 CR-002 复核。

## 9. 残余风险与交付定位

- 残余风险：真实 DB/Redis/Token 行为未运行，由 T-04 阻塞。
- Source/Parent result：`eeab21d91689b1e5b40fcd87017c85658749fdc9`。
- Source workspace：`current`。
