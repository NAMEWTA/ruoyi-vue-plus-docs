# Evidence T-03: 前端 Client 角色与认证上下文

## Result

- Parent：`3122cbac38e9118da8bd1e8cdc82b7dd22671f05`
- User/RBAC commit：`08001c8` `fix(user): load roles by client context`
- Auth context commit：`adf5a0c` `fix(auth): reject malformed client context`
- Result HEAD：`adf5a0c`

## Contract Evidence

- `UserInfoVO` 只保留实际后端字段；删除合成的 `userTypeIds`、`explicitRoleIds`、`defaultRoleIds`、`defaultRoles`，ID 数组改为明确联合类型。
- `getAuthRole(userId, clientId)` 的 Client 主键改为必填；授权页首次使用基础 `getUser`，选中合格 Client 后才请求授权角色。
- 用户编辑先加载基础详情，再对用户已拥有登录域对应的启用 Client 逐个顺序调用 `getUser(userId, client.id)`；全部成功后才打开弹窗。
- 显式角色按 scoped `roleIds` 并集形成，默认角色按 `clientDefault` 只读汇总；切换/移除登录域会裁剪失去 Client 资格的角色。
- 用户编辑和授权页均使用请求代次，旧用户/旧 Client 响应不会覆盖当前选择；未使用无界 `Promise.all`。
- 认证上下文 parser 只接受对象中两个精确 Boolean；缺字段、数字、字符串、null 等全部抛错。
- 登录页在上下文加载中或不可用时禁用密码与第三方登录提交；注册链接仅在精确 `true` 时展示。注册页只有两个开关均为 `true` 才加载验证码和允许提交。

## Verification

- `pnpm lint`：通过，0 error。
- `pnpm exec oxfmt --check`（7 个修改文件）：通过。
- `pnpm exec vue-tsc --noEmit --forceConsistentCasingInFileNames false`：通过，0 error。
- 原始 `pnpm exec vue-tsc --noEmit`：仅失败于未修改的上游 `views/monitor/logininfo` 与 `api/monitor/loginInfo` 大小写冲突（TS1149）；没有本次文件错误。
- `pnpm build:prod`：通过，3357 modules transformed，最终构建 3.25s。
- `git diff --check`：通过。
- 搜索审计：无 `isEnabledFlag`、Boolean 注册开关 coercion、缺 Client 的 `getAuthRole`、伪角色字段或 `Promise.all` 残留。
- 变更审计：7 个源码文件；无 package.json、pnpm-lock.yaml、依赖或 dist 变化；仓库 clean。

## Deferred E2E

真实浏览器与后端的多 Client 角色、畸形上下文和过期响应矩阵由 T-04 执行；本 Ticket 不以静态门禁冒充 E2E。

## 2. Lead Dispatch And Candidate Return

- Implementation owner：Lead `codex-root`，current workspace 直接实现与提交。
- 返回：两个 focused commits、clean child、lint/type/build 结果；Lead 独立核对请求代次与所有 scoped API 调用。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| frontend user/auth API and views | writable | scoped 角色加载与严格认证上下文 |

- read-only 修改、未声明路径、package/lock/dist 交付变化：无。

## 4. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-002 | user edit/authRole | 顺序 scoped 请求与请求代次 | pass |
| AC-003 | login/register context | exact Boolean、失败关闭 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| `pnpm lint` / focused `oxfmt` | current-workspace | pass | 0 error |
| casing-relaxed `vue-tsc --noEmit` | current-workspace | pass | 0 error |
| original `vue-tsc --noEmit` | current-workspace | fail-known | 仅上游 TS1149 大小写冲突 |
| `pnpm build:prod` | current-workspace | pass | 3357 modules transformed |

- 失败后修复与重跑：本次文件无类型错误；原始 TS1149 未在本 Ticket 扩张处理。
- E2E：not-run；required browser/backend matrix owner 为 T-04。

## 6. 双轴审查

### 标准轴

- 固定输入：`3122cbac3..adf5a0c`；结果：pass；无未关闭 finding。

### 规范轴

- 来源：Spec/AC-002,003/CR-001；代码结果 pass，运行验收留给 T-04。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `3122cbac38e9118da8bd1e8cdc82b7dd22671f05` |
| Implementation/source SHA | `adf5a0c5db3f7ebb1431c4c2fb87e29f987ef1bc` |
| Candidate branch/workspace | current |
| Method/conflicts | direct-parent；无 |
| Integration checks | current-workspace lint/format/type/build/diff passed |
| E2E disposition/result | required at T-04 / pending |
| Parent result/re-read | `adf5a0c5db3f7ebb1431c4c2fb87e29f987ef1bc` |

## 8. 偏差与决策

- 偏差：原始 TS1149 是未修改上游 casing 冲突，未放宽正式配置，仅以命令行诊断本次范围。

## 9. 残余风险与交付定位

- 残余风险：真实浏览器与后端联调未运行，由 T-04 阻塞。
- Source/Parent result：`adf5a0c5db3f7ebb1431c4c2fb87e29f987ef1bc`。
- Source workspace：`current`。
