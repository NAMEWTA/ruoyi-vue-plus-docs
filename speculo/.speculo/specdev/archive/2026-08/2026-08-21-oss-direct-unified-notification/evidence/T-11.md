# Evidence: T-11 — 通知监控前端

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/11-notify-monitoring-frontend.md</Path>`
- **Lead：** `cursor-agent`
- **Workspace/branch：** `current/frontend` / `main`
- **Base/result SHA：** `1dba1ea71be891127fa4ee67a91a0b136016f4e7` / `783da5759bbc0b978bec90a410ac940957ed9cc7`
- **状态：** done

## 1. 实现摘要

新增 `/monitor/notify` API、类型、动态菜单页面和详情抽屉。页面支持请求、业务、Channel、Provider、状态、Provider Message ID、来源 Client PK 与时间筛选；列表只展示后端返回的脱敏目标。详情在 `system:notify:query` 权限下展示完整正文、模板参数、完整目标、Delivery 与附件，关闭抽屉立即清理敏感状态。

正文始终以纯文本 `pre` 展示，不使用 `v-html` 或 `innerHTML`。附件通过已授权的短时 OSS URL 下载。单删、批删和清空受 `system:notify:remove` 控制，清空需要两次确认。页面复用 T-02 的动态组件路径 `monitor/notify/index`，未增加静态路由；Client 字段明确标注为“来源 Client PK”，不表达数据隔离或所有权。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead
- **Dispatch Packet/checkpoint：** Lead direct；frontend base `1dba1ea71be891127fa4ee67a91a0b136016f4e7`
- **允许动作：** current workspace changes、implementation commit、direct-parent verification；无 worktree/candidate
- **返回：** frontend commit `783da5759bbc0b978bec90a410ac940957ed9cc7`；5 个 Ticket 文件
- **Lead 独立核对：** pass；复核动态路径、权限指令、服务端脱敏边界、敏感状态清理、HTML 非执行、清空双确认、附件短 URL 和 Client 文案
- **只读 Agent findings：** 无；本计划未启用 subagent

## 3. 修改范围与路径所有权

- **API/types：** `src/api/monitor/notify/**`
- **View：** `src/views/monitor/notify/**`
- **路由：** 未修改静态 router；继续由 DSL 动态下发
- **集成方式：** current workspace direct-parent，无 candidate/worktree、无冲突

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-027 | 全局筛选和分页页面只消费后端 `targetMasked` | pass |
| AC-028 | query 权限详情显示完整数据；正文只作文本，安全测试禁止 `v-html/innerHTML` | pass |
| AC-029 | remove 权限控制单删、批删、清空；清空双确认；操作后刷新 | pass |
| AC-030 | 来源 Client PK 只是可选筛选/展示；无隐式隔离或所有权表达 | pass |

## 5. Workspace Verification

本 Ticket 在 `current-workspace` 完成实现、验证和 direct-parent 集成。

| 命令或步骤 | 结果 | 摘要 |
|---|---|---|
| TDD red security test | expected fail | API/View 尚不存在时测试失败，证明安全合同先于实现 |
| `pnpm exec vitest run src/views/monitor/notify/security.test.ts` | pass | 2 tests；禁止可执行 HTML，验证动态路径、权限键、脱敏字段与清空确认 |
| `pnpm lint` | pass | Oxlint/仓库 lint 门禁通过 |
| `pnpm exec oxfmt --check src/api/monitor/notify src/views/monitor/notify` | pass | 新增目录格式通过 |
| `pnpm build:prod` | pass | production build 成功 |
| `pnpm exec vue-tsc --noEmit` | baseline exception | 退出 2；仅有基线两个 TS1149：`loginInfo` / `logininfo` 大小写冲突，本 Ticket 新目录无诊断 |
| staged diff/security audit | pass | 5 个 Ticket 文件；无静态路由、`v-html`、`innerHTML` 或客户端明文拼接 |

E2E 为 `not-required`：用户明确不执行 E2E。本 Ticket 未启动 Playwright 或浏览器自动化，也未伪报真实账号权限矩阵、后端联调或恶意 HTML 浏览器验收；安全行为由聚焦合同测试、静态审查、lint 和 production build 覆盖。

## 6. 双轴审查

- **标准轴：** `1dba1ea71..783da5759b` 的安全测试、lint、格式和 production build 通过；补充 vue-tsc 只保留已记录的两个仓库基线诊断。
- **规范轴：** 动态路由、query/remove 权限、列表服务端脱敏、正文非执行、短时附件 URL、Client 非隔离和关闭详情清理状态均与 ADR/Spec/Ticket 一致。
- **审查修正：** 状态值改为后端真实枚举 `PARTIAL_FAILURE`；Client 列名明确为来源维度；清空增加第二次危险动作确认。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `1dba1ea71be891127fa4ee67a91a0b136016f4e7` |
| Implementation/source/result SHA | `783da5759bbc0b978bec90a410ac940957ed9cc7` |
| Candidate branch/workspace | `current`；candidate not-applicable |
| Method/conflicts | `direct-parent`；无冲突 |
| Integration checks | security test、lint、format、production build、type baseline diagnostic 与 diff audit |
| E2E disposition/result | `not-required`：用户明确不做 E2E |

## 8. 偏差与决策

Ticket 原计划包含人工浏览器权限/恶意 HTML 验收；用户明确要求不做 E2E，本次没有运行浏览器自动化或冒充真实账号联调。该项由可复现安全合同测试与静态门禁替代，不改变 API、权限或安全语义。

## 9. 残余风险与交付定位

- **残余风险：** 未使用真实后端、动态菜单和不同权限账号做浏览器联调；生产开放菜单前仍应验证角色授权、短 URL、CORS 和清空权限。
- **类型基线：** 仓库既有 `loginInfo/logininfo` 大小写冲突使全量 vue-tsc 非绿色，本次没有新增诊断。
- **Source commit / Parent result：** `783da5759bbc0b978bec90a410ac940957ed9cc7`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/evidence/T-11.md</Path>`
