# Evidence: T-13 — 通知敏感审计与物理清理修复

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/13-review-notify-audit-cleanup.md</Path>`
- **Lead：** `codex`
- **Workspace/branch：** `current/backend main`
- **Base/result SHA：** `4d5a3b5bce660e1d529383006b8252ff0161f430` / `b2d50248acfb13c46fc490f248c43500e0f4f7cf`
- **状态：** done

## 1. 实现摘要

引入 `FULL/REDACT_SENSITIVE` 审计策略，验证码不持久化正文/参数并脱敏目标；请求 ID 改为普通索引；管理员清理按 500 条物理删除父子日志且先解除附件引用。

## 2. Lead Dispatch And Candidate Return

Lead direct；无 subagent/worktree/candidate。DDL 仅在末尾追加 `NAMEWTA-OSS-NOTIFY-DDL-002`。

## 3. 修改范围与路径所有权

修改 common-notify、Captcha 调用方、system notify monitor、Mapper XML、admin tests 与追加 DDL；未触碰用户 POM。

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-020/025/028/029 | 敏感落库、重复日志、附件解绑与分批物理清理单测/DDL 审查 | pass |

## 5. Workspace Verification

current-workspace：最终 admin reactor 102 tests，0 failure/error、3 external skip；36 模块 package 通过。E2E not-required。

## 6. 双轴审查

标准轴 pass：审计策略由请求显式携带并由 Dispatcher 保留。规范轴 pass：Mapper 复用现有 MyBatis 边界，清理事务使用动态数据源事务。

## 7. Integration Verification

direct-parent；result `b2d50248acfb13c46fc490f248c43500e0f4f7cf`，最终 backend HEAD 为 `d836894f969d8f4134d72af129f3c452f8265c46`。

## 8. 偏差与决策

DDL-002 同时承载后续 T-14 的 `delete_state`，避免拆分同一追加迁移块；T-14 Evidence 单独验证状态机。

## 9. 残余风险与交付定位

生产执行 DDL 和保留期策略仍需独立授权；本次未执行生产迁移。
