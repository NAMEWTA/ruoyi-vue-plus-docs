# Evidence: T-19 — OSS 引用集合协调契约

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/19-oss-reference-reconciliation-contract.md</Path>`
- **Lead：** `codex`
- **Workspace/branch：** `current/backend main`
- **Base/result SHA：** `d836894f969d8f4134d72af129f3c452f8265c46` / `c658669af`
- **状态：** done

## 1. 实现摘要

`OssService` expand `reconcileReferences`；生命周期层验证并规范化集合，按 ID 排序先绑定新增、再解绑移除，复用既有行锁、幂等与 TEMP 状态机。公共原始 `bind/unbind` 留待 T-22 contract。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead 自行实现，未派遣 subagent。
- **Dispatch/checkpoint：** current workspace，base `d836894f969d8f4134d72af129f3c452f8265c46`。
- **返回：** 非空 implementation commit `c658669af9f6f44ddc92899b4fa3d52b3711179d`。
- **Lead 独立核对：** commit/diff、聚焦测试、package、祖先关系和 dirty POM 均已重读。
- **E2E：** not-required；无 HTTP/UI 行为，用户已确认不建设 E2E。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `ruoyi-api/.../OssService.java` | shared:T-19 contract owner | 增加集合协调公共契约 |
| `ruoyi-system/.../OssLifecycleManager.java` | writable | 实现规范化集合差分与失败传播 |
| `ruoyi-system/.../SysOssServiceImpl.java` | writable | API adapter 委托生命周期协调器 |
| `ruoyi-admin/.../OssReferenceReconciliationUnitTest.java` | writable | 覆盖差分、顺序、幂等与非法输入 |

- **read-only 修改：** 无。
- **未声明路径：** 无。
- **用户 dirty 文件：** `ruoyi-admin/pom.xml` 未暂存、未提交。

## 4. 验收与合同映射

| Contract | 验证接缝 | 结果 |
|---|---|---|
| AC-009/AC-010 | 旧新集合规范化与差分 | pass |
| fail-closed | 新增绑定失败后不继续解绑旧引用 | pass |
| 非法输入 | DB 访问前拒绝 null/非正数 ID | pass |
| 兼容边界 | T-19 仅 expand；公共 raw API 由 T-22 收缩 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| TDD red：聚焦测试编译 | current-workspace | expected fail | 缺少 `OssLifecycleManager.reconcileReferences` |
| `sh mvnw -pl ruoyi-admin -am -Dmaven.test.skip=false -Dtest='OssLifecycle*UnitTest,OssReferenceReconciliationUnitTest' -Dsurefire.failIfNoSpecifiedTests=false test` | current-workspace | pass | 24 tests |
| `sh mvnw -pl ruoyi-admin -am -DskipTests package` | current-workspace | pass | 36-module reactor |

- **未运行检查：** E2E not-required；无外部协议或 UI 变化。

## 6. 双轴审查

- **标准轴：** pass；公共契约明确，排序后先绑定新增、再解绑移除，异常不被吞掉。
- **规范轴：** pass；机械协调不读取业务表、不推断 ACL，不新增前端入口或运行时注册。
- **未关闭 finding：** 无。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `d836894f969d8f4134d72af129f3c452f8265c46` |
| Implementation/source SHA | `c658669af9f6f44ddc92899b4fa3d52b3711179d` |
| Candidate | not-applicable；current/direct-parent |
| Method/conflicts | direct-parent；无冲突 |
| Integration checks | 24 focused tests + 36-module package pass |
| E2E disposition/result | not-required / not-required |
| Parent result/re-read | `c658669af9f6f44ddc92899b4fa3d52b3711179d` |

## 8. 偏差与决策

- **偏差：** 无。
- **决策：** T-19 采用 expand-first，raw `bind/unbind` 暂留至 T-22，在 Owner 完成迁移后再删除。

## 9. 残余风险与交付定位

- **残余风险：** 事务整体回滚需由真实 Owner 的 `@DSTransactional` 边界保证，T-20/T-21 继续关闭。
- **后续 Ticket：** T-20、T-21、T-22。
- **Source/Parent result：** `c658669af9f6f44ddc92899b4fa3d52b3711179d`。
- **Workspace：** `current/backend main`。
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/evidence/T-19.md</Path>`。
