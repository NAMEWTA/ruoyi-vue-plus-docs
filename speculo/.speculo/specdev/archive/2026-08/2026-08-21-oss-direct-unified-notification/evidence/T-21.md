# Evidence: T-21 — 接入 Workflow 历史任务附件 Owner

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/21-workflow-history-attachment-owner.md</Path>`
- **Lead：** `codex`
- **Workspace/branch：** `current/backend main`
- **Base/result SHA：** `3cd24fa2e63362e2c970a93773187c24e057a0ed` / `44049525e24b688366b889f0771d096ef9aa3f30`
- **状态：** done

## 1. 实现摘要

新增 workflow 内部 `WorkflowHistoryOssOwner`。完成和退回在引擎转换前捕获同一 `task_id` 的历史 ID，转换后只对新生成且实际持久化附件的 `flow_his_task.id` 建引用；历史实例物理删除在 WarmFlow 删除历史行前逐条协调附件为空。三个外层入口使用 `@DSTransactional`。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead 自行实现，未派遣 subagent。
- **Dispatch/checkpoint：** current workspace，base `3cd24fa2e63362e2c970a93773187c24e057a0ed`。
- **返回：** 非空 implementation commit `44049525e24b688366b889f0771d096ef9aa3f30`。
- **Lead 独立核对：** commit/diff、写入口、依赖扫描、测试、package、祖先关系和 dirty POM 哈希均已重读。
- **E2E：** not-required；无前端/HTTP 合同变化，用户已确认不建设 E2E。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `ruoyi-workflow/.../oss/WorkflowHistoryOssOwner.java` | writable | 捕获历史 ID、解析持久化附件并协调/释放引用 |
| `ruoyi-workflow/.../CompleteExecuteComponent.java`、`InstanceDeleteExecuteComponent.java` | writable | 完成与历史删除生命周期接缝 |
| `ruoyi-workflow/.../FlwTaskServiceImpl.java`、`FlwInstanceServiceImpl.java` | writable | 事务边界与退回/删除入口接缝 |
| `ruoyi-admin/.../WorkflowHistoryOssOwnerUnitTest.java` | writable | 真实历史主键、CSV、失败传播与删除合同 |

- **read-only 修改：** 无。
- **未声明路径：** 无。
- **用户 dirty 文件：** `ruoyi-admin/pom.xml` SHA-256 保持 `53683d824e306dcb8584e22ad0cc39acdb7526d11e887dca6674e27f2310df97`，未暂存、未提交。

## 4. 验收与合同映射

| Contract | 验证接缝 | 结果 |
|---|---|---|
| AC-009 | `flow_his_task.ext` 由 workflow Owner 维护 | pass |
| AC-010 | 以新生成 `flow_his_task.id` 作为 refId | pass |
| fail-closed | 创建/清理异常向上传播；外层入口为 `@DSTransactional` | pass |
| dependency direction | workflow 只消费 ruoyi-api `OssService` | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| TDD red：`WorkflowHistoryOssOwnerUnitTest` 编译 | current-workspace | expected fail | 目标 workflow OSS 接缝不存在 |
| `sh mvnw -pl ruoyi-admin -am -Dmaven.test.skip=false -Dtest=WorkflowHistoryOssOwnerUnitTest -Dsurefire.failIfNoSpecifiedTests=false test` | current-workspace | pass | 7 tests |
| workflow 写入口与依赖扫描 | current-workspace | pass | 两处写入口和历史删除接缝完整；无 system 实现依赖 |
| `sh mvnw -pl ruoyi-admin -am -DskipTests package` | current-workspace | pass | 36-module reactor |

- **未运行检查：** E2E not-required；无外部协议或 UI 变化。

## 6. 双轴审查

- **标准轴：** pass；CSV 严格解析、历史 ID 差集、持久化值一致性和失败传播均 fail-closed。
- **规范轴：** pass；不修改 WarmFlow、schema、DTO、前端或历史数据，不引入 workflow 到 system 实现依赖。
- **未关闭 finding：** 无。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `3cd24fa2e63362e2c970a93773187c24e057a0ed` |
| Implementation/source SHA | `44049525e24b688366b889f0771d096ef9aa3f30` |
| Candidate | not-applicable；current/direct-parent |
| Method/conflicts | direct-parent；无冲突 |
| Integration checks | 7 tests + write-entry/dependency scans + 36-module package pass |
| E2E disposition/result | not-required / not-required |
| Parent result/re-read | `44049525e24b688366b889f0771d096ef9aa3f30` |

## 8. 偏差与决策

- **偏差：** 无。
- **决策：** 使用 `flow_his_task.id` 作为真实 refId；`task_id` 只用于转换前后定位新历史记录，不回填历史数据。

## 9. 残余风险与交付定位

- **残余风险：** 最终 manifest 与公共 API 收缩由 T-22 关闭；当前基座无历史回填负担。
- **后续 Ticket：** T-22。
- **Source/Parent result：** `44049525e24b688366b889f0771d096ef9aa3f30`。
- **Workspace：** `current/backend main`。
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/evidence/T-21.md</Path>`。
