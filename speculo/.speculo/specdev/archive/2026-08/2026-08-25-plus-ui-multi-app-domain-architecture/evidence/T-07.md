# Evidence: T-07 - 迁移共享认证授权与动态路由核心

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/07-identity-access-migration.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `native:/root/t01`
- **Source workspace / branch：** `specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-07` / `speculo/2026-08-25-plus-ui-multi-app-domain-architecture/T-07`
- **Base / source / candidate / result SHA：** `6b873176daef467d935d01750cd9e88493bade4e` / `d755cba4729fab4dcc098e3eeb87fac374543d36` / `d755cba4729fab4dcc098e3eeb87fac374543d36` / `d755cba4729fab4dcc098e3eeb87fac374543d36`
- **Result tree：** `806ad80093219244fcbb1cf67d6f77a5fb901b83`
- **状态：** done

## 1. 实现与合同结果

激活无头 `@namewta/platform-permission`，扩充 identity domain 的登录、注册、OAuth、info、menus、logout 与 session rotation，并将根 API、stores、guard、directives、plugin 和 permission utils 改成公共包兼容 facade。动态菜单通过 admin manifest registry 和 RouteAssembler 映射；受保护导航严格保持 `getInfo -> getRouters -> addRoute -> replace`。

ClientContext 在验证码和任一 auth 请求前严格校验，非法或畸形 Client 零 auth 请求并 fail-close。401 恢复为单例 logout/redirect；消息通道关闭是 best-effort，即使其 401/拒绝也不阻断真实 logout、token 清理和 redirect。任意无物理 facade 的 typo key `system/user/not-a-physical-facade` 进入稳定可见诊断，诊断包含 App、推断 domain 和原始 key。

`superadmin` 是规范超管角色；基线扫描证明旧 directive/plugin/utils 实际兼容 `admin`，因此本 Ticket 显式保留 legacy alias。`*:*:*` 保持全权限语义；空要求、畸形 snapshot 和无关权限均 fail-close。

## 2. Lead Dispatch And Candidate Return

- **实现提交：** `85c1b621696c58fe64d3f28fa5dd8d269d5ac9a6` - `feat(architecture): migrate shared identity access runtime`
- **追加修复：** `d755cba4729fab4dcc098e3eeb87fac374543d36` - `fix(auth): preserve logout recovery chain`
- **最终 source：** clean；tree `806ad80093219244fcbb1cf67d6f77a5fb901b83`；source 未声明 Playwright pass
- **Lead 接收：** 重读固定累计 diff、commit/tree、ancestor、clean、manifest/lock 和路径 owner；第一轮 review finding 以追加提交关闭，未改写 checkpoint 历史

## 3. 修改范围与路径所有权

T-07 修改 identity/web-domain、platform permission/app-runtime、根 auth/menu/store/guard/permission facade、登录页面和 auth E2E。生产 `<Path>src/utils/request.ts</Path>`、adapters 与 `<Path>apps/client-web/**</Path>` 保持只读。

| 路径 | 所有权 / 结果 |
|---|---|
| `<Path>packages/domains/identity-access/**</Path>`、`<Path>packages/web-domains/identity-access/**</Path>` | T-07 writable；完整 auth/session use case 与 web facade |
| `<Path>packages/platform/permission/**</Path>`、`<Path>packages/platform/app-runtime/**</Path>` | T-07 writable；求值矩阵、RouteAssembler 与恢复序列 |
| `<Path>src/permission.ts</Path>`、router/stores/directive/plugin/utils/API facade | T-07 shared owner；兼容入口保留 |
| `<Path>package.json</Path>`、`<Path>pnpm-lock.yaml</Path>` | `DEV-T07-001` 精确授权；仅三个 root workspace dependency 及匹配 importer/specifier |
| `<Path>src/utils/request.test.ts</Path>` | `DEV-T07-002` 精确测试授权；生产 request adapter 未改 |

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-012 | 无物理 facade 的 typo key 输出含 App/domain/key 的可见诊断 | pass |
| AC-013/014 | unit 事件序列与 E2E 验证 `getInfo -> getRouters -> addRoute -> replace`；不二次 Client 裁剪 | pass |
| AC-015/016 | invalid Client 零 auth 请求；close 失败不阻断单例 logout/clear/redirect | pass |
| AC-017/018 | superadmin/admin/wildcard/fail-close matrix；双 App Client/session 隔离 | pass |
| AC-021 | candidate full suite、双 build 和 required Playwright 全部通过后才推进 parent | pass |

## 5. Workspace Verification

最终 `source-worktree` 运行 targeted unit、architecture、lint、typecheck、全 unit、双 build、frozen install 和静态 E2E list，均 exit 0；source 的真实 E2E 为 `not-run`，未声明 Playwright pass。Lead `current-workspace` 候选复跑 frozen install、17 workspace list、architecture `0 + 92/92`、lint、typecheck、root `29/129` 与全部 workspace unit、admin/client production build，全部通过。

Playwright 在 Lead candidate 上实际运行，Chromium `15/15` passed in `15.0s`。覆盖非法 Client 零 auth 请求、动态恢复顺序、真实 typo 可见诊断、权限 wildcard/deny、401 单例恢复及双 App Client header/session namespace 隔离。

## 6. 双轴审查

- **固定输入：** `6b873176daef467d935d01750cd9e88493bade4e..d755cba4729fab4dcc098e3eeb87fac374543d36`
- **标准轴：** 第一轮发现 SSE close 失败会截断 401 恢复，以及 OAuth facade 类型与运行时不一致；追加提交修复并补测试后，固定累计 diff 复审 PASS，无新 finding。
- **规范轴：** PASS；AC-012/013/014/015/016/017/018/021、真实 no-facade typo、严格 Client 顺序、权限 alias/matrix、多 Client 隔离和 `DEV-T07-001/002` 路径范围均满足。

## 7. Integration Verification

| 项目 | 实测结果 |
|---|---|
| Candidate | `speculo/integration/2026-08-25-plus-ui-multi-app-domain-architecture/T-07`；`specdev-worktree/.integration/2026-08-25-plus-ui-multi-app-domain-architecture/T-07`；从 `main@6b873176...` fast-forward，无冲突 |
| Reproducibility | `pnpm install --frozen-lockfile` exit 0；17 workspace projects |
| Architecture | check `17 workspaces / 0 violations`；tests `92/92` |
| Static gates | `pnpm lint`、`pnpm typecheck` 均 exit 0 |
| Unit | root `29 files / 129 tests`；全部 workspace tests exit 0；identity `18`、app-runtime `18`、permission `13`、auth `2` |
| Production build | admin `3393` modules；client-web `1718` modules；全部 workspace build gates exit 0 |
| Browser E2E | `pnpm test:e2e` exit 0；Chromium `15/15` passed in `15.0s`；client `4174`，Playwright-owned admin `4173` |
| Promotion | 父 HEAD 未漂移；`git merge --ff-only` 推进 `plus-ui-namewta/main` 到 result；SHA、tree、ancestor 与 clean 复核通过 |
| Cleanup | 临时 candidate worktree/branch 已删除；source worktree/branch 按未授权 cleanup 合同保留 |

## 8. 偏差与决策

- **DEV-T07-001：** 根 manifest 仅新增 identity-access、web-domain-identity-access、platform-permission 三个实际消费的 `workspace:*`；lock 仅同步对应 root specifier 与 permission importer。无 scripts、workspace/catalog、外部版本、既有 resolution 或无关节点漂移。
- **DEV-T07-002：** 仅更新 `<Path>src/utils/request.test.ts</Path>` 的迁移 characterization 为 `context -> code -> login` 并补 session mock；既有 header、401、download 和错误安全断言未放宽。
- **Alias 决策：** 基线存在 `admin` 超管兼容行为，故显式保留；`superadmin` 仍是规范角色。
- **Expand-migrate-contract：** 根兼容入口保留到 T-15，本 Ticket 未提前删除 contract。

## 9. 残余风险与交付定位

- client 首屏 chunk 约 `1.34 MB`，仍有 Vite >500 kB 非阻塞警告；后续按真实性能基线做 code splitting，不通过提高阈值掩盖。
- admin build 报告 `src/api/login.ts` 同时静态和动态导入，动态导入不能形成独立 chunk；不影响正确性，后续 manifest 收口时可消除重复导入。
- 并发动态导航失败交错主要由代码审查覆盖；401 singleton 与正常恢复顺序已有 unit/E2E。
- **前端结果：** `plus-ui-namewta main@d755cba4729fab4dcc098e3eeb87fac374543d36`
- **下一 Ticket：** `T-08 / Gate G2`
