# Evidence: T-08 - 迁移工作流定义管理纵向切片

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/08-workflow-definition-slice.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `native:/root/t01`
- **Source workspace / branch：** `specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-08` / `speculo/2026-08-25-plus-ui-multi-app-domain-architecture/T-08`
- **Base / source / candidate / result SHA：** `d755cba4729fab4dcc098e3eeb87fac374543d36` / `262c06d38be0d123e4ec07d8905fc13f87a8fcab` / `262c06d38be0d123e4ec07d8905fc13f87a8fcab` / `262c06d38be0d123e4ec07d8905fc13f87a8fcab`
- **Result tree：** `47614e25eb129c64d55aaf1d967ef4ab3b8fa1e6`
- **状态：** done

## 1. 实现与合同结果

激活无头 `@namewta/domain-workflow` 与 Vue 表现层 `@namewta/web-domain-workflow`。domain 固化 category、definition、SpEL 的 24 个既有 transport 合同；web-domain 提供分类、定义、设计器和 SpEL 完整页面，并通过精确的四个 component key 贡献 admin manifest。admin 显式选择 definition slice，client-web 与 T-09 runtime key 均保持未选择。

根 API/types/views 已收敛为兼容 facade，待 T-15 执行零引用收缩。现有 TreePanel 通过 typed host port 注入复用；definition package chunk 自带页面布局 CSS。E2E 覆盖分类嵌套新增、定义选择/导入/导出/发布、设计器 deep link 与 iframe 参数、SpEL 新增、权限隐藏、业务失败可见、未知请求失败以及展开/收起布局几何。

## 2. Lead Dispatch And Candidate Return

- **实现提交链：** `41f50f9`、`3f864f1`、`1a98790`、`9906afe`、`d028d17`、`93cc826`、`6f8ec3a`、`e79ada7`、`d588a3b`、`262c06d`
- **最终 source：** `262c06d38be0d123e4ec07d8905fc13f87a8fcab`；tree `47614e25eb129c64d55aaf1d967ef4ab3b8fa1e6`；clean
- **Lead 接收：** 每个修复追加提交并在固定累计 diff 上重做工程标准与 Spec/AC 双审；最终两轴均 PASS，无 finding
- **Candidate：** 每次从未推进的 `main@d755cba...` 重建并 `--ff-only` 合入 source；前三次失败时 parent 保持不动，第四次全绿后才提升

## 3. 修改范围与路径所有权

| 路径 | 所有权 / 结果 |
|---|---|
| `<Path>packages/domains/workflow/**</Path>` | T-08 writable；无头 transport、DTO/VO 与 URL 编码 |
| `<Path>packages/web-domains/workflow/**</Path>` | T-08 writable；四个页面、composables、runtime host ports 与 manifest |
| `<Path>src/api/workflow/{category,definition,spel}/**</Path>`、对应 views | T-08 writable；兼容 facade/wrapper，保留到 T-15 |
| `<Path>src/router/adminManifestRegistry.ts</Path>` | `DEV-T08-001`；只追加 workflow inputs 与 selected IDs |
| `<Path>src/router/adminManifestRegistry.test.ts</Path>` | `DEV-T08-003`；只追加 definition selected/runtime unselected 断言 |
| `<Path>package.json</Path>`、`<Path>pnpm-lock.yaml</Path>` | `DEV-T08-001`；仅两个 workflow 根依赖、匹配 specifiers/importers |
| `<Path>e2e/workflow-definition.spec.ts</Path>` | T-08 专用 required E2E；未知请求保持严格失败 |

T-09 task/instance/leave、`src/components/Process/**`、identity/platform 和 system user seam 均未修改。

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-009 | workflow domain/web-domain 可追踪；definition 与 runtime exports 边界明确 | pass |
| AC-010 | admin 选择四个 definition keys；client-web 与 runtime key 未选择 | pass |
| AC-021 | full candidate Gate、双 build 与 required Playwright 全绿后才推进 parent | pass |
| DEV-T08-002 | domain tests 锁定全部 24 个既有方法/路径；PUT/DELETE 未被单边改写 | pass |

## 5. Workspace Verification

最终 `source-worktree` 运行 scoped Oxfmt/Oxlint、静态 E2E TypeScript、workflow domain/web-domain unit、architecture check 与 `diff --check`，均 exit 0；source 的真实 Playwright 为 `not-run`，未声明 E2E pass。

Lead `current-workspace` 最终 candidate 实测 `pnpm install --frozen-lockfile`、`pnpm architecture:check`、`pnpm architecture:test`、`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build:prod` 与 `pnpm test:e2e` 全部 exit 0。冻结安装发现 19 个 workspace；architecture 为 `19 / 0`，规则测试 `92/92`；root Vitest `31 files / 135 tests`，workflow domain `2/2`、web-domain `4/4`；admin/client production build 分别转换 `3405` / `1718` modules。

Playwright 在 Lead candidate 上实际运行，Chromium `18/18` passed in `23.8s`。client preview 使用 `4174`，admin `4173` 由 Playwright webServer 管理；运行后两个服务均停止。

## 6. 双轴审查

- **固定输入：** `d755cba4729fab4dcc098e3eeb87fac374543d36..262c06d38be0d123e4ec07d8905fc13f87a8fcab`
- **标准轴：** 最终 PASS，无 finding。确认 domain 无 Vue/DOM/concrete adapter，manifest/lock/shared path 未越界，TreePanel 复用真实 host component，E2E 未通过 sleep 或弱断言制造绿色。
- **规范轴：** 最终 PASS，无 finding。AC-009/010/021、四个精确 key、selected-only、T-09/client 排除、完整页面行为、兼容 facade 与 `DEV-T08-001/002/003` 均满足。
- **残余审查意见：** typed host port 的 `Component` 类型较宽，但已由集成测试覆盖；WarmFlow token query 与 origin-agnostic close message 是继承行为，不由本 Ticket 引入。

## 7. Integration Verification

| 项目 | 实测结果 |
|---|---|
| Candidate | `speculo/integration/2026-08-25-plus-ui-multi-app-domain-architecture/T-08`；从 `main@d755cba...` fast-forward，无冲突 |
| Reproducibility | frozen install exit 0；19 workspace projects |
| Architecture | check `19 workspaces / 0 violations`；tests `92/92` |
| Static gates | lint、typecheck、root/workspace unit、admin/client production build 全部 exit 0 |
| Browser E2E | attempt 4：`pnpm test:e2e` exit 0；Chromium `18/18` passed in `23.8s` |
| Failed candidates | attempt 1 隐藏 native checkbox；attempt 2 SpEL mutation 竞态；attempt 3 缺少两个 live dict fixtures；均未推进 parent |
| Promotion | 父 HEAD 保持 `d755cba...` 至最终 Gate 全绿；Lead `git merge --ff-only` 推进 `plus-ui-namewta/main` 到 result |
| Result audit | HEAD `262c06d...`；tree `47614e2...`；ancestor 与 clean 复核通过 |
| Cleanup | 临时 candidate worktree/branch 已删除；source worktree/branch 按未授权 source cleanup 合同保留 |

## 8. 偏差与决策

- **DEV-T08-001：** admin composition 只追加 workflow inputs/selected IDs；root/lock 只含两个实际消费的 workspace 依赖及机械 importer，无 resolver、workspace、catalog、script、外部版本或 resolution 漂移。
- **DEV-T08-002：** 后端不在范围内，保留既有 workflow PUT/DELETE。例外仅在后端协调迁移并同步全部消费者后到期。
- **DEV-T08-003：** registry unit 只追加 definition keys selected 与 runtime key unselected 断言，既有 identity/demo 测试未改写。
- **DEV-T08-004：** candidate integration 实际 4 次，超过 Goal Plan 默认上限 3。前三次均为专用 E2E 真实性/确定性缺口，parent 未动；每次均 source-only 追加修复、双审并从 parent 重建。最终结果不降低 Gate，但后续 Ticket 达到第 3 次失败时必须停止并在下一次候选前登记偏差，不再边运行边追加尝试。

## 9. 残余风险与交付定位

- WarmFlow 设计器继续在 iframe URL 携带 token，并以未限制 origin 的 message 关闭；这是迁移前兼容行为，安全收紧需与设计器协议协同，不能由本 Ticket 单边改变。
- client 首屏 chunk 约 `1.34 MB`，仍有 Vite >500 kB 非阻塞警告；后续按真实性能基线拆包。
- 旧 workflow API/types/views facade 仍存在；T-15 必须在零引用扫描、双 App build 和全 E2E 后再删除。
- **前端结果：** `plus-ui-namewta main@262c06d38be0d123e4ec07d8905fc13f87a8fcab`
- **下一 Ticket：** `T-09 / Gate G3`
