# Evidence: T-09 - 迁移工作流运行时并建立 system user 公共接缝

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/09-workflow-runtime-slice.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `native:/root/t01`
- **Source workspace / branch：** `specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-09` / `speculo/2026-08-25-plus-ui-multi-app-domain-architecture/T-09`
- **Base / source / candidate / result SHA：** `262c06d38be0d123e4ec07d8905fc13f87a8fcab` / `39f7eaef8bff2002ba6824a2616e2050991e6959` / `39f7eaef8bff2002ba6824a2616e2050991e6959` / `39f7eaef8bff2002ba6824a2616e2050991e6959`
- **Result tree：** `6d62a305098c787bc586adc360f01c14e630fbd9`
- **状态：** done

## 1. 实现与合同结果

workflow domain 已覆盖 task、instance、leave transport 与 DTO/VO，并仅通过 `@namewta/domain-system-admin/public/user` 的最小公开接缝消费用户查询合同。workflow web-domain 提供 task/instance/leave 页面、用户选择、办理/干预动作和全部 Process 公共组件；admin composition 显式选择 runtime registrations，并注入 FileUpload、关闭当前页、流程图 URL、OSS 元数据解析和附件下载宿主端口。

根 workflow API/views、UserSelect 和八个 Process 路径均收敛为兼容 facade，保留至 T-15 零引用收缩。行为等价覆盖候选人逗号 scalar 查询、管理员独立干预动作、实例变量更新、真实附件上传与退回、请假日期范围/自动天数/发起/完成关闭、撤销原因、权限 fail-close 和错误状态保留。

## 2. Lead Dispatch And Candidate Return

- **实现提交链：** `a852e31`、`00b3f6d`、`24b8470`、`0c0f5cf`、`0cf6f93`、`3e2cb76`、`88bb0cb`、`2d27d0c`、`e5afd8d`、`01c999b`、`39f7eae`
- **最终 source：** `39f7eaef8bff2002ba6824a2616e2050991e6959`；tree `6d62a305098c787bc586adc360f01c14e630fbd9`；clean
- **双轴接收：** engineering standards/architecture 与 Spec/行为等价在最终 fixed checkpoint 均 PASS，无 finding
- **Candidate：** 第 4 次从未漂移的 `main@262c06d...` 创建并 `--ff-only` 合入 source；完整 Gate 通过后才提升

## 3. 修改范围与路径所有权

| 路径 | 所有权 / 结果 |
|---|---|
| `<Path>packages/domains/workflow/**</Path>` | T-09 writable；task/instance/leave 无头合同与序列化 |
| `<Path>packages/domains/system-admin/{README.md,package.json,tsconfig.json,public/user/**}</Path>` | `DEV-T09-001`；仅激活最小 user public seam |
| `<Path>packages/web-domains/workflow/**</Path>` | T-09 writable；runtime pages、Process/UserSelect、host ports 与 manifest |
| `<Path>src/api/workflow/**</Path>`、`<Path>src/views/workflow/**</Path>`、`<Path>src/components/{Process,UserSelect}/**</Path>` | 兼容 facade；旧公共入口不复制实现 |
| `<Path>src/router/adminManifestRegistry.ts</Path>` | `DEV-T09-002/003`；仅注入已批准宿主能力并公开同一 runtime |
| `<Path>pnpm-lock.yaml</Path>` | `DEV-T09-001`；仅 system-admin importer 与 workflow dependency specifier |
| `<Path>e2e/workflow-runtime.spec.ts</Path>` | T-09 required E2E；11 条真实运行时行为与反向夹具 |

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-006 / AC-007 | 20-workspace architecture check 0 violation；规则 tests `92/92`；domain 无 Vue/DOM/concrete adapter/deep import | pass |
| AC-009 / AC-010 | workflow runtime 可追踪；admin selected-only composition 与 client 隔离由 full E2E 覆盖 | pass |
| AC-021 | frozen install、lint、typecheck、unit、双 build、workflow `11/11` 与 full `29/29` 全部真实通过 | pass |
| DEV-T09-001..003 | system user seam、宿主 ports、全部 Process facade 与行为等价均经双轴审查 | pass |

## 5. Workspace Verification

最终 source-worktree 实测 workflow web-domain `4 files / 13 tests`、包级与根级 typecheck、root `34 files / 145 tests`、全部 workspace unit、architecture check 和 `diff --check`，均 exit 0；source 不声明浏览器通过。

Lead 第 4 次 parent-candidate 实测 `pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build:prod` 全部 exit 0。冻结安装发现 20 个 workspace；architecture 为 `20 / 0`，规则测试 `92/92`；root Vitest `34 files / 145 tests`，workflow web-domain `4/13`；admin/client production build 分别转换 `3416` / `1718` modules。

Playwright 在正确双 App 环境运行：client-web preview `4174`，admin `4173` 由 Playwright webServer 管理。专用 workflow Chromium `11/11` passed in `29.6s`；随后 full Chromium `29/29` passed in `23.9s`。运行后 `4174` 已停止并确认端口释放。

## 6. 双轴审查

- **固定输入：** `262c06d38be0d123e4ec07d8905fc13f87a8fcab..39f7eaef8bff2002ba6824a2616e2050991e6959`
- **标准轴：** PASS，无 finding。确认 `nextTick` 同时关闭 node filter 写入和普通 selector 清除的同 tick 竞态；async handler、依赖方向、facade、host port、manifest/lock 与路径边界合规。
- **规范轴：** PASS，无 finding。候选 URL/forbidden fixture、任务错误局部可见、审批意见保留、退回首次失败的 message/附件保留、编辑后重试与精确 payload 均未弱化。

## 7. Integration Verification

| 项目 | 实测结果 |
|---|---|
| Candidate | `speculo/integration/2026-08-25-plus-ui-multi-app-domain-architecture/T-09`；从 `main@262c06d...` fast-forward 到 `39f7eae...`，无冲突 |
| Reproducibility | frozen install exit 0；20 workspace projects |
| Architecture | check `20 workspaces / 0 violations`；tests `92/92` |
| Static gates | lint、typecheck、root/workspace unit、admin/client production build 全部 exit 0 |
| Browser E2E | attempt 4：workflow `11/11`，随后 full dual-App `29/29`，均 exit 0 |
| Failed candidates | attempt 1：full `18/29`，client `4174` 缺失及 workflow locator；attempt 2：workflow `6/11`，locator/fixture；attempt 3：workflow `8/11`，props 同 tick、重复错误 locator、repeat-submit；均未推进 parent |
| Promotion | 父 HEAD 保持 `262c06d...` 至第 4 次完整 Gate 全绿；Lead `git merge --ff-only` 推进 `plus-ui-namewta/main` 到 result |
| Result audit | main/source/candidate 均复核为 `39f7eae...` 且 clean；一次 compound preflight 使用错误相对 source 路径并打印 fatal，后续独立绝对路径审计确认 source clean，实际 `--ff-only` 成功 |
| Cleanup | 临时 candidate worktree/branch 已删除；source worktree/branch 按未授权 source cleanup 合同保留 |

## 8. 偏差与决策

- **DEV-T09-001：** T-09 只激活 system-admin 最小 user public seam 和机械 lock 节点；T-10 保留其余实现所有权。
- **DEV-T09-002：** admin composition 只注入 FileUpload/close-current-page 并公开同一 runtime；附件、实例变量、请假、撤销、候选与干预等价均由真实行为覆盖。
- **DEV-T09-003：** 仅追加 chart/OSS/download host ports；八个根 Process 路径全部为 facade，未 reverse/deep import 宿主实现。
- **DEV-T09-004：** 第 3 次失败后先停止、登记偏差并复盘；source 追加修复且双轴 PASS 后才允许第 4 次。前三次 parent 均保持 `262c06d...`，最终第 4 次依次通过 workflow `11/11`、full `29/29` 与完整 Gate 后才推进。

## 9. 残余风险与交付定位

- 旧 workflow API/views/Process/UserSelect facade 仍存在；T-15 必须在零引用扫描、双 App build 和 full E2E 后再删除。
- admin 首屏及 client chunk 仍有 Vite >500 kB 非阻塞警告；后续以真实性能基线拆包。
- **前端结果：** `plus-ui-namewta main@39f7eaef8bff2002ba6824a2616e2050991e6959`
- **下一 Ticket：** `T-10 / Gate G4`；T-12/T-14 fan-out 已由 Gate G3 解锁，但 Lead candidate 继续串行。
