# Evidence: T-12 - 迁移 AI 领域与 Web 表现层

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/12-ai-domain.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `native:/root/t01_spec_review`
- **Base / source / candidate / result SHA：** `9ce426c20be49209d24b40083e14eede4ea31109` / `8639b9bb19c65d37b792b61b00bb7fc12c3ef765` / `e7086d4bc0dabe82e09aac6dada5e41b9892766d` / `e7086d4bc0dabe82e09aac6dada5e41b9892766d`
- **状态：** done；Gate G5 已关闭，source worktree/branch 保留

## 1. 实现摘要

T12 激活 headless AI domain 与 AI web-domain，原样迁移 `POST /snail-ai/user/register` 和 `ai/chat/index`。当前产品事实是嵌入式 Snail AI iframe，而非前端流式协议；Web session 负责 credential/root-relative base 前置、注册、same-origin HTML probe、iframe load timeout、URL 清理与重试，旧 API/view 保留 facade，admin 选择而 client-web 不选择。

## 2. Lead Dispatch And Candidate Return

- source-worktree：`specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-12`
- 初始 checkpoint：`425c7ed3e0bef6e8bf76fc9de4c9916b3ffe7467`；第一追加修复 `eb6afb470a57cce00d4ea15f3a03b36c2730fc60`；第二追加修复/source checkpoint：`8639b9bb19c65d37b792b61b00bb7fc12c3ef765`，tree `ce2385a694283ea3c842732ef473e69a111b648b`，worktree clean。
- 前两个固定点的两个独立审查轴均为 `REQUEST CHANGES`；第三固定点两个独立审查轴均 PASS。candidate 与父分支推进 pending，integration attempts 仍为 `0`。

## 3. 修改范围与路径所有权

- AI domain/web-domain、旧 AI API/view facade 和专用 E2E 位于 Ticket writable paths。
- `DEV-T12-001` 仅添加两个 root workspace dependencies、机械 lock importers 与 additive admin registry selection/runtime ports。
- `DEV-T12-002` 忠实采用当前 embedded-chat 生命周期，不复活已删除的 Fetch/SSE/ReadableStream/prompt 协议；实现与测试不记录 credential。
- `DEV-T12-003` 仅扩展 typed `probeFrame` runtime port、唯一 admin registry 的 same-origin HTML fetch adapter 及其聚焦测试；它以真实 probe/timeout/abort/retry 替代不可靠的 iframe `error` 信号，不开放其他 registry 行为。

## 4. 验收与合同映射

| Contract | Source 证据 | 当前结果 |
|---|---|---|
| AC-009 / AC-010 | domain/manifest、admin selected、client unselected 声明 | source pass；candidate pending |
| AC-019 | typed Web runtime 与 iframe session，不复制 domain transport | source pass；candidate pending |
| AC-021 | failure tests、全量静态 Gate、双 build | source pass；required E2E pending |

## 5. Workspace Verification

- **source-worktree：** frozen install 通过；architecture check `23 workspaces / 0 violations`，architecture tests `92/92`。
- 第二次修复后聚焦 session/registry tests `2 files / 23 tests`；full lint/typecheck 通过。
- 第二次修复后 full test：root `39 files / 181 tests` 及全部 workspace tests 通过。
- full build：admin/workspaces/client 均通过；仅既有大 chunk warning。
- Playwright `--list` 识别 `e2e/ai-domain.spec.ts` 3 项；按 required-worktree 合同未在 source 运行 E2E。

## 6. 双轴审查

- 固定输入：`9ce426c20be49209d24b40083e14eede4ea31109..425c7ed3e0bef6e8bf76fc9de4c9916b3ffe7467`
- 标准轴：`REQUEST CHANGES`。iframe base URL 仅判空，可能把 credential 发布到非法 scheme、带 userinfo 或不可导航地址；iframe 网络失败不能可靠依赖父页 `error` 事件，当前 E2E 手工派发事件未证明真实失败恢复；立即注册重试可能被既有 500ms repeat-submit 防护拦截。
- 规范轴：`REQUEST CHANGES`。独立确认 iframe 网络失败不能靠父页 `error` 事件恢复、base URL 缺少可信边界，并发现重试 E2E 只等 iframe 元素和 registration count，未证明第二次 frame 请求/load 与 loading 终态。
- 已确认通过：headless domain、注册 endpoint/method 与旧 API/view facade、admin selected/client unselected、空权限贡献、`DEV-T12-001` 精确 root/lock/registry 范围，以及无 credential 日志。
- 满足条件：为 base URL 建立明确 allowlist/fail-close 且错误不回显原值；采用不发明后端协议的可验证网络/readiness/timeout 合同并用真实 route failure 证明恢复；明确允许注册重试并证明立即重试发送第二次注册与 frame/probe 请求、loading 收敛；然后对新固定点重新执行两个独立审查轴。
- 修复事实：base 只接受 root-relative same-origin path；注册使用既有 `repeatSubmit: false`；abortable GET probe 要求成功 HTML 且拒绝 redirect，probe 成功后才发布 iframe URL；session 覆盖 probe/timeout/dispose/并发清理；E2E 用真实 probe failure 后立即重试并等待第二次 probe、iframe load 与 loading 终态。
- 第二固定点：`9ce426c20be49209d24b40083e14eede4ea31109..eb6afb470a57cce00d4ea15f3a03b36c2730fc60`。
- 标准轴复审：`REQUEST CHANGES`。`probeFrame` 在 15 秒 timer 建立前被无限期 await，悬挂 probe 会永久保持 loading 且不自动 abort；成功 adapter 校验 headers 后也未显式取消未读 response body。要求 timeout 覆盖 probe、超时 abort 并进入可立即重试终态，probe settle 后释放未读 body，并以 never-resolving probe/fake timer 与 adapter body cancellation 聚焦测试证明。
- 规范轴复审：`REQUEST CHANGES`。独立确认 probe 无 timeout/abort 边界和成功响应体未释放，并指出现有 E2E 只证明 `route.abort()` 后恢复，尚未证明 hanging probe 的客户端 timeout/abort、credential URL 清理与可重试终态。要求在既有 failure E2E 内补真实悬挂 probe 恢复证据，无需增加测试数量。
- 第二次修复事实：15 秒 lifecycle timeout 覆盖 never-settling probe 并 abort/清空 URL/结束 loading；timeout、重试与 dispose 都释放旧等待；adapter 在 `finally` 释放未读 response body 并覆盖 null/locked/consumed；既有 3 条 E2E 保留 `route.abort()`，同时追加 hanging route、客户端 abort、终态清理与再次重试成功。
- 第三固定点：`9ce426c20be49209d24b40083e14eede4ea31109..8639b9bb19c65d37b792b61b00bb7fc12c3ef765`。
- 标准轴：`PASS`，无 findings。确认 timeout 在 probe 前启动并覆盖 never-settling request，abort/稳定失败/URL 清理/loading 收敛/立即重试完整；retry/dispose 解除旧等待且 stale continuation 不覆盖新状态；adapter 仅取消 non-null/unread/unlocked body，cleanup failure 不替换原结果；E2E 保留真实 `route.abort()` 并追加悬挂 probe 的浏览器取消、终态清理与第三次成功证据。append 范围精确，DEV-T12-001/002/003、credential、registry/lock/path 边界无回归；未运行浏览器 E2E，worktree clean。
- 规范轴：`PASS`，无 findings。确认 never-settling probe 在 15 秒终止并 abort/允许立即重试，未读未锁 body 在成功/失败释放，hanging E2E route handler 随 browser abort 释放；3 条 E2E 分别覆盖成功终态、失败/timeout/retry/credential 不入 DOM 与 client-web 未选择。DEV-T12-001/002/003 的 package/lock/registry 增量、唯一 endpoint、iframe 解释、same-origin HTML probe、credential 发布时点与 selected-only 无回归。定向 domain `3/3`、web-domain `17/17`、registry `9/9`、architecture `23 / 0`、Playwright `--list` 3 项通过；未运行浏览器 E2E，worktree clean。

## 7. Integration Verification

T12 已从最新 `main@b79687427b9b00d6e5ab5990f104d4a4bb8dc984` 建立 attempt 1。由于 T11/T12 均从 `9ce426c` 并行开发，Lead 以 `--no-ff --no-commit` 组合 source，并在唯一冲突 `src/router/adminManifestRegistry.test.ts` 中保留 T11 已选 OSS/Dict、未选 devtools 与 T12 已选 AI chat/未选 AI model、probe/body cleanup 测试；聚焦 registry `9/9`、domain `3/3`、web-domain `17/17`、architecture `23 / 0` 与 diff-check 通过。Lead-owned merge commit/candidate 为 `e7086d4bc0dabe82e09aac6dada5e41b9892766d`，parents 为 `b79687427b9b00d6e5ab5990f104d4a4bb8dc984 8639b9bb19c65d37b792b61b00bb7fc12c3ef765`，tree `896dc3f1bc75baf52d4859a529dc42e14c843625`；生成声明噪音精确恢复后 candidate clean。完整 Gate、AI 专用与 full dual-App E2E、result SHA pending。

### Attempt 1 Result

- 完整 Gate 通过：23-workspace frozen install；architecture `23 / 0 + 92/92`；root/workspace lint、typecheck；root Vitest `40 files / 191 tests` 与全部 workspace tests；admin/client production builds。构建声明噪音精确清理，candidate HEAD/tree 保持 `e7086d4bc0dabe82e09aac6dada5e41b9892766d` / `896dc3f1bc75baf52d4859a529dc42e14c843625` 且 clean。
- preview `127.0.0.1:4173/4174` 下 targeted `e2e/ai-domain.spec.ts` 为 `3/3`（18.1s）：成功 load、真实 failed probe、15 秒 hanging probe 客户端 abort、敏感 URL 不入 DOM、立即重试与 client-web exclusion 均通过；full dual-App Playwright 为 `39/39`（1.0m，workers=1），包含 T11 system-resource `4/4` 回归。
- Lead 停止 preview 并确认端口释放，重验 merge parents/source ancestor、candidate/source/main clean 与 tree 后，将前端 `main` 从 `b79687427b9b00d6e5ab5990f104d4a4bb8dc984` 以 `--ff-only` 推进到 `e7086d4bc0dabe82e09aac6dada5e41b9892766d`。transient candidate worktree/branch 已移除；T12 source 保留。未 push、部署、发布或清理 source。

## 8. 偏差与决策

- `DEV-T12-001/002/003` 全部按批准边界实现；唯一 candidate 冲突仅合并 T11 与 T12 的 selected/unselected registry 测试事实，没有新增产品决定。
- iframe credential query 是既有后端嵌入合同；实现不记录该值，并在 probe 失败、timeout 与 dispose 时不发布或清空本地 URL state。

## 9. 残余风险与交付定位

- iframe credential query 是既有后端嵌入合同；实现不记录该值，且在 probe 失败、timeout 与 dispose 时不发布或清空本地 URL state。
- **Source commit：** `8639b9bb19c65d37b792b61b00bb7fc12c3ef765`
- **Candidate / parent result：** `e7086d4bc0dabe82e09aac6dada5e41b9892766d`；tree `896dc3f1bc75baf52d4859a529dc42e14c843625`。
