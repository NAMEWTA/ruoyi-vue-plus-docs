# Evidence: T-14 - 迁移监控与运维领域

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/14-operations-domain.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `native:/root/t01_standard_review`
- **Base / source / candidate / result SHA：** `9ce426c20be49209d24b40083e14eede4ea31109` / `6dd9bf1f9ac638dd06f49dd940e6b01a8f4fa311` / `8144f98bd8a005c1386e7de8462fd354776b5d34` / `8144f98bd8a005c1386e7de8462fd354776b5d34`
- **状态：** done；Gate G6 已关闭，source worktree/branch 保留

## 1. 实现摘要

T14 激活 headless operations domain 与 operations web-domain，迁移 17 个 monitor API seam、模型、8 个 legacy component keys 和页面，旧 API/views 保留 facade。domain 产生经过验证的 external URL/download intent，宿主只执行已验证 intent；admin 选择 operations，client-web 不选择。

## 2. Lead Dispatch And Candidate Return

- source-worktree：`specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-14`
- 初始 checkpoint：`b5e3f53096e9c13bd2fc59a75c1e8f49599ca524`；结构化 URL/README 修复：`e0a09e353244d8965910161ca7ae63113ac57eb4`。
- attempt 1 浏览器验证暴露 Chromium 会把 raw-space URL 编码为 `%20`，且通知 E2E 以未显示字段定位；追加实现修复 `cb3578dd70b4f45412b400e27da97757bf088753` 与测试 ratchet `6dd9bf1f9ac638dd06f49dd940e6b01a8f4fa311`，最终 tree `efc513dbb282f9a82cdaea7d23011fd49ecf38ab`，worktree clean。
- 最终固定点两个独立审查轴均 PASS。Lead attempt 2 candidate/result 为 `8144f98bd8a005c1386e7de8462fd354776b5d34`。

## 3. 修改范围与路径所有权

- operations packages、旧 monitor API/views 与专用 E2E 位于 Ticket writable paths。
- `DEV-T14-001` 仅添加两个 root workspace dependencies、机械 lock importers 与 additive admin registry operations selection/typed host ports。
- 既有 identity/platform/router/request/App、scripts/catalog/external versions 与无关 lock nodes zero diff。

## 4. 验收与合同映射

| Contract | Source 证据 | 当前结果 |
|---|---|---|
| AC-009 / AC-010 | domain/manifest、admin selected/client unselected 声明 | source/candidate pass；result integrated |
| AC-019 | typed runtime 与安全 intent 分离 domain/Web/host | source/candidate pass；result integrated |
| AC-021 | security/unit/architecture/双 build/required E2E | Gate G6 pass |

## 5. Workspace Verification

- **source-worktree：** frozen install 通过；architecture check 0 violations，architecture tests `92/92`。
- 最终修复后 operations domain `18/18`，raw-space absolute/root-relative 必须拒绝而合法 `%20` 保持允许。
- full lint、typecheck、build:prod 通过；full test root `40 files / 165 tests` 及全部 workspace tests 通过。
- Playwright `--list` 识别 `e2e/operations-domain.spec.ts` 3 项；按 required-worktree 合同未在 source 运行 E2E。

## 6. 双轴审查

- 固定输入：`9ce426c20be49209d24b40083e14eede4ea31109..b5e3f53096e9c13bd2fc59a75c1e8f49599ca524`
- 标准轴：`REQUEST CHANGES`。除与规范轴独立识别出相同 malformed URL 问题外，domain 与 web-domain README 在激活包时删除了职责、非职责、依赖、公开入口、后端映射、安全失败和验证门禁，违反当前权威包文档同步要求。
- 规范轴：`REQUEST CHANGES`。`safeUrl` 使用正则解析绝对 URL authority，会接受浏览器 URL parser 拒绝的 malformed host、port 与 percent encoding，违反外部导航与下载 intent 的 fail-close 边界。
- 已确认通过：17 个现有 monitor path/method/shape、逐段 segment encoding、8 个 legacy component key/name/facade、manifest permissions、通知无 raw HTML sink、授权 API 失败不调用 download port、selected/unselected composition 以及 `DEV-T14-001` 精确路径变化。
- 满足条件：改用结构化 URL parser，并对 relative URL 定义明确 path policy；限定 http/https、空 credentials、合法 host/port，增加 malformed host/port/percent encoding/IPv6 反向测试；恢复两包九类真实合同，并补 unsafe configured URL 与 URL API failure 不导航的 E2E 声明；随后对新固定点重新执行两个独立审查轴。
- 修复事实：iframe/download intent 共用结构化 URL parser；domain 负向矩阵增至 `15/15`；两包 README 恢复九类真实合同；E2E 增至 3 项，覆盖 unsafe/500 attachment URL、无 anchor/download、以及由 `OPERATIONS_EXPECT_UNSAFE_URL=1` 驱动的真实非法 Vite 配置 build variant。
- 复审固定输入：`9ce426c20be49209d24b40083e14eede4ea31109..e0a09e353244d8965910161ca7ae63113ac57eb4`。
- 标准轴：`PASS`，无阻断 findings。确认结构化 URL parser 对控制字符、反斜杠、非法 percent encoding、host/port/IPv6、credentials 与非 HTTP(S) fail-close；两份 README 九类合同完整；unsafe build marker 不能绕过真实配置，下载 hook 不吞真实 click，API failure/unsafe URL 均保持可见且零下载副作用。定向 domain `15/15`、web-domain `5/5` 与 architecture check 通过，worktree clean；未运行 build/E2E。
- 规范轴：`PASS`，无阻断 findings。确认 17 个 monitor endpoint、8 个 legacy component key/name/facade、`monitor:online:batchLogout` 权限、逐段 segment encoding、共享 URL fail-close、文本通知、admin selected/client unselected 与 `DEV-T14-001` 精确增量均符合合同；三条 E2E 声明覆盖正常授权、permission/client exclusion 和附件 unsafe/API failure，并为 Lead 非法 Vite 配置变体保留真实反向验证。worktree clean，tree `7e3b2d7f16c40abcc2e9a67c85e96816e1ff32c8`；未运行浏览器 E2E。
- attempt 1 后复核固定输入：`e0a09e353244d8965910161ca7ae63113ac57eb4..6dd9bf1f9ac638dd06f49dd940e6b01a8f4fa311`。
- 标准轴首轮 `REQUEST CHANGES`：仅用 `https://bad host/path` 会被 Node 原生 URL parser 拒绝，不能证明新增空白 guard；要求增加原生 parser 可接受的 raw-space absolute/root-relative 反向样例及合法 `%20` 正向样例。条件落实后最终复审 `PASS`，无 findings。
- 规范轴最终 `PASS`，无 findings。确认共享 `safeUrl` 同时约束 iframe/download，raw-space fail-close 与 `%20` 正向边界准确；通知行改用可见 exact ID cell，不弱化 unsafe/500/零下载/strict-unknown 终态。

## 7. Integration Verification

### Attempt 1

- 从 `main@e7086d4bc0dabe82e09aac6dada5e41b9892766d` 合并 `e0a09e353244d8965910161ca7ae63113ac57eb4`，候选 `5b2f5a3061833529bef2becd320473e6b4ce31ae`；两个冲突位于 admin registry/test，解决时保留 T11 system resources、T12 AI 与 T14 operations 的 additive selection/tests。
- 完整非浏览器 Gate 通过：25-workspace frozen install、architecture `25/0 + 92/92`、lint/typecheck、root `44 files / 210 tests`、全部 workspace tests、admin/client builds。
- 非法 `VITE_APP_MONITOR_ADMIN='https://bad host/path'` 专项为 `1/3`：Chromium 把空格编码为 `%20` 后创建 iframe；通知数据已渲染，但 locator 使用未显示的 requestId。父分支未推进，临时候选移除；问题在 source 以 append-only commits 修复并重新双审。

### Attempt 2 Result

- 从仍未漂移的 `main@e7086d4bc0dabe82e09aac6dada5e41b9892766d` 重建并合并最终 source；Lead merge candidate `8144f98bd8a005c1386e7de8462fd354776b5d34`，parents `e7086d4bc0dabe82e09aac6dada5e41b9892766d 6dd9bf1f9ac638dd06f49dd940e6b01a8f4fa311`，tree `f820600b4c702f5a606b25b728c8dc1f73ea962a`。同两个 registry 冲突再次按 additive composition 解决。
- 完整 Gate 通过：25-workspace frozen install；architecture `25/0 + 92/92`；root/workspace lint、typecheck；root Vitest `44 files / 213 tests` 与全部 workspace tests；admin/client production builds。
- 真实非法配置重新 build 后，`OPERATIONS_EXPECT_UNSAFE_URL=1` 专项 `3/3`；恢复正常 build 后 operations 专项 `3/3`，full dual-App Playwright `42/42`（workers=1，约 1.0m）。预览停止且 `4173/4174` 端口释放。
- Lead 重验 candidate/source/main clean、parents/source ancestor/tree 后，以 `--ff-only` 将 plus-ui `main` 推进到 `8144f98bd8a005c1386e7de8462fd354776b5d34`。transient candidate worktree/branch 已移除；T14 source 保留。未 push、部署、发布或清理 source。

## 8. 偏差与决策

- `DEV-T14-001` 按批准边界实现；两个 candidate 冲突只合并已批准的 system/AI/operations selection 与测试事实，没有改变 resolver/router/request/App 行为。
- 浏览器与 Node 对 malformed/raw-space URL 的解析差异由显式 raw-whitespace policy 封闭；合法 percent-encoded space 仍允许。

## 9. 残余风险与交付定位

- 外部 monitor URL 仅接受 domain 安全策略生成的 intent；Chromium 已由 required E2E 验证，其他浏览器依赖显式空白预检与共享单元反向矩阵。
- **Source commit：** `6dd9bf1f9ac638dd06f49dd940e6b01a8f4fa311`
- **Candidate / parent result：** `8144f98bd8a005c1386e7de8462fd354776b5d34`；tree `f820600b4c702f5a606b25b728c8dc1f73ea962a`。
