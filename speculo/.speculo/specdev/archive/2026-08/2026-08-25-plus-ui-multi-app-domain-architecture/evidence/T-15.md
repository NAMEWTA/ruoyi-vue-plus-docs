# Evidence: T-15 - 激活 admin-web 并收缩根应用兼容入口

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/15-admin-web-contract.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `codex:/root`
- **Base SHA：** `a73007e54dcb517fccf4e5470e679dfee7ac0c00`
- **状态：** integrated；Gate H attempt 1 全绿，`main` fast-forward 到 `50ff220`

## 1. 实现摘要

admin-web 独立入口已激活。初轮 review 证明原 `dd14be9` 仅是 expand checkpoint，不能批准 `da8e21f` 的同步迁移/删除；整改后 `ad4e971` 在 dormant 根副本仍保留时完成零活动消费者与全 Gate，`3fc3a68` 再以独立 contract commit 删除旧入口并收缩根 runtime dependencies。Playwright 现在自主管理双 App，dev/prod 构建模式均已实证。

## 2. Lead Dispatch And Candidate Return

- source-worktree：`specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-15`
- branch：`speculo/2026-08-25-plus-ui-multi-app-domain-architecture/T-15`
- base：`a73007e54dcb517fccf4e5470e679dfee7ac0c00`，worktree clean。
- expand checkpoint：`dd14be9fa75078e6cc565b5864d04d960a445085`，tree `2db9fd53150e9043cf27533b0095738785cbd8f3`；初轮 review 判定它尚非删除批准点。
- rejected contract fixed point：`da8e21f20b0ecd6621fd73f1498dc9c44445d0d5`，tree `a8acb0e656bd1d743accbbdadcccfca03a9c0924`；未进入 candidate、未推进 parent。
- approved pre-contract checkpoint：`ad4e971`；保留 dormant 根副本，所有真实入口/脚本已切换，零活动消费者与非浏览器 Gate 全绿。
- source contract：`50ff2207791c87d10ff8f618090a6d82f160b645`，tree `a3b748e2d48ddbb96f9eba5e1bdc3bb868780e71`；在 `3fc3a68` 的 root contract 后追加 Oxfmt ignore 与两轮边界 README 收口，worktree clean。

## 3. 修改范围与路径所有权

- `DEV-T15-001` 授权 admin package/lock、root App contract、Playwright preview 与 shared tsconfig 的最小必要变化。
- `DEV-T15-002` 授权保留既有共享 dev toolchain、显式 test-only `crypto-js`、增加最小根 Vitest Vue 配置，并修正 admin 内第三方类型公开入口。
- `DEV-T15-003` 记录初轮 review 整改、真正的删除批准点与双服务/dev-mode 修复。
- `DEV-T15-004` 记录第二轮 review 的生成声明格式保护与 package 边界文档同步。
- `DEV-T15-005` 记录第三轮 review 的剩余边界当前态与真实 App 消费关系修正。
- 除 DEV-T15-004/005 明确列出的 README 外，`packages/**`、workspace/catalog、外部版本、architecture rules、client-web、后端与既有 E2E 断言保持只读。

## 4. 验收与合同映射

| Contract | 计划证据 | 当前结果 |
|---|---|---|
| AC-002 | admin-web independent build + Playwright | passed：dev/prod 双 App build，Playwright 47/47 |
| AC-024 | pre-contract、root src zero scan、dual build/E2E | passed：`ad4e971` pre-contract，contract 后扫描/构建/E2E 全绿 |
| AC-021 | full Gate H | passed：attempt 1 完整 Gate 后 fast-forward |

## 5. Workspace Verification

- **source-worktree / expand：** `dd14be9` frozen install 识别 28 workspaces；architecture `28/0 + 92/92`；admin lint/typecheck、unit `10 files / 44 tests`、production build `3466 modules` 通过。根 `src` 与 admin `src` 除两张 SVG 的 4 处行尾空格规范化外内容等价，index/public/Vite/Uno/env 字节一致。
- **source-worktree / approved pre-contract：** `ad4e971` 保留 dormant 根文件与 dependency declarations，但 package scripts、root HTML、Playwright 和 tsconfig 已无旧入口调用；零活动消费者、frozen install、architecture `28/0 + 92/92`、lint、全 workspace type/unit、dev/prod 双 App build 均通过。dev artifact 包含 `/dev-api` 且不含 `/prod-api`，prod artifact相反；Playwright discovery `47 tests / 11 files`。
- **source-worktree / contract：** `3fc3a68` root App 零扫描、frozen install、architecture `28/0 + 92/92`、lint、全部 workspace typecheck/unit 与双 production build 通过。admin build `3466 modules`，client build `1718 modules`；根 package 无 runtime dependencies，admin package 持有 45 个 App runtime dependencies，E2E 的 `crypto-js` 为 root test-only devDependency。
- **source-worktree / documentation-format remediation：** `27c3c52` 将 Oxfmt ignore 从已删除的根声明迁到 admin 声明，并同步 12 份直接受 contract 影响的 package README。聚焦 `oxfmt --check` 只匹配配置文件且跳过两个生成声明，architecture `28/0`、完整 lint、`git diff --check` 通过。
- **source-worktree / current-state remediation：** `50ff220` 修正 11 份剩余 README；宽扫描不再发现 root App/compatibility window 的失效当前态，architecture `28/0`、完整 lint 与 `git diff --check` 通过。
- **current-workspace/parent-candidate attempt 1：** frozen install 识别 28 workspaces；root App 入口、Vite/Uno/env/public 扫描为零，root runtime dependencies 为零；architecture check `28 packages / 0 violations`，architecture tests `92/92`；完整 workspace lint、typecheck、unit 全绿；`build:dev` 与 `build:prod` 均通过（admin `3466 modules`，client `1718 modules`）。development admin 产物包含 `/dev-api` 且不含 `/prod-api`，production 产物包含 `/prod-api` 且不含 `/dev-api`。构建/预览产生的 3 行组件声明已作为生成噪声精确移除，候选恢复 clean 且 tree 未变。
- **source-worktree E2E disposition：** not-run；浏览器验证由 Lead 在独立 parent-candidate 执行，结果归入第 7 节。

## 6. 双轴审查

- 初轮标准轴 `REQUEST CHANGES`：Playwright 只启动 admin 4173，fresh full E2E 缺 client 4174；root `build:dev` 通过通用 build 静默使用 production mode。
- 初轮规格轴 `REQUEST CHANGES`：`dd14be9` 未达到零旧调用点，`da8e21f` 同时迁移和删除，未形成可验证的 AC-024 批准点；另指出本 Evidence 的旧残余风险文案矛盾。
- 整改：`ad4e971` / `3fc3a68` append-only 双 checkpoint、双 Playwright webServer readiness、显式 admin development build、Evidence 修正；待新 fixed point 双轴复审。
- 第二轮规格轴对 `3fc3a68` PASS；标准轴 REQUEST CHANGES：Oxfmt 仍会改写迁移后的生成声明，package README 仍陈述已退役 facade。整改 source 为 `27c3c52`，待同一 fixed point 重新双轴复审。
- 第三轮规格轴对 `27c3c52` PASS；标准轴 REQUEST CHANGES：platform-auth 被误述为双 App 消费，且数份未纳入首批的 README 仍陈述根 App 当前态。整改 source 为 `50ff220`，待同一 fixed point 重新双轴复审。
- 最终固定点 `50ff2207791c87d10ff8f618090a6d82f160b645`（tree `a3b748e2d48ddbb96f9eba5e1bdc3bb868780e71`）标准轴 PASS、规格轴 PASS；两轴均确认 worktree clean、pre-contract `ad4e971` 仍为祖先、旧 root App 为零，E2E 留给 Lead candidate。

## 7. Integration Verification

- parent before：`main@a73007e54dcb517fccf4e5470e679dfee7ac0c00`，clean。
- source/candidate：`50ff2207791c87d10ff8f618090a6d82f160b645`；candidate tree `a3b748e2d48ddbb96f9eba5e1bdc3bb868780e71`。
- attempt：1；method：`git merge --ff-only`；conflicts：无。
- candidate E2E：Playwright 自主管理 4173/4174 两个预览服务，`47/47` 通过（约 1.5 分钟），覆盖 admin/client ClientContext、会话隔离、认证/401、动态路由、权限和七领域选装/失败路径；测试完成后候选 clean。
- result：`main@50ff2207791c87d10ff8f618090a6d82f160b645`，tree 与已审查候选一致；Gate H passed。
- cleanup：一次性 `T-15-attempt-1` candidate worktree 与 candidate branch 已删除；T15 source worktree/branch 按授权保留。

## 8. 偏差与决策

- `DEV-T15-001`：见 Ticket/Goal Plan/change status；删除批准点仍以 pre-contract 真实绿色为前置。
- `DEV-T15-002`：删除旧根 `vite.config.ts` 后，operations 等 SFC unit 首次揭示其曾被 Vitest 隐式向上加载；最小根 `vitest.config.ts` 恢复 Vue SFC 转换。现有 packages 同时依赖根 devDependency hoist，故只移除 root runtime dependencies，保留既有共享开发工具版本。admin typecheck 还证明 `vxe-table` 顶层声明未公开 `VxeTableInstance`，改从已安装的 `vxe-pc-ui/types/components/table` 声明入口导入；完整 typecheck/test/build 通过。
- `DEV-T15-003`：见 Ticket/Goal Plan/change status；`da8e21f` 是 rejected fixed point，未建立 candidate。整改全过程 append-only，parent 始终保持 `a73007e`。
- `DEV-T15-004`：见 Ticket/Goal Plan/change status；只修改 `.oxfmtrc.json` 与 12 份 package README，未改变任何运行代码、manifest、依赖或测试。
- `DEV-T15-005`：见 Ticket/Goal Plan/change status；只修改 11 份 README，client-web 源码保持只读且其自持 unauthorized 流程不变。

## 9. 残余风险与交付定位

- 根 App contract 已进入 `main@50ff220`。失败恢复点仍为 `ad4e971`；source worktree/branch cleanup 未获授权且已保留。剩余工作是 T16 OpenAPI transport contract 与 T17 上游能力文档，不再保留根 App 兼容入口。
