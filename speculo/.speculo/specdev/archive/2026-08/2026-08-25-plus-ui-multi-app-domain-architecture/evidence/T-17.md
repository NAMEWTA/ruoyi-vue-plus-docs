# Evidence: T-17 - 固化上游能力映射与本地架构维护合同

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/17-upstream-capability-map.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `codex:/root`
- **状态：** integrated；Gate J closed

## 1. 实现摘要

将上游维护入口改为 capability-first 治理账本：本地 `main` 架构是实现权威，上游仅用于发现能力、缺陷修复与安全变化，不追求路径同构。账本固定十字段 schema、adopt/adapt/reject/defer 决策与 completed/rejected/deferred 状态流，覆盖双 App、七组 domain/web-domain、platform、browser adapters、web-kit、api-contracts、tooling 以及 README-only 移动端/小程序占位；安全关键项不得静默 defer。

## 2. Lead Dispatch And Candidate Return

- plus-ui base：`22c10d202c069d851e61de83bf4ff5f376da8419`。
- plus-ui source/candidate/result：`60540c412fd00ebb5b948ff26dcab6ae9763d7ef`，tree `7b7c9baa447f0d241fdd847d4a083acc1fbcb515`。
- plus-ui source worktree/branch 保留；transient candidate worktree/branch 已删除。
- docs product result：`0e31753ab1c8929951ee9f5aafbe913b5dc11440`，tree `e67fe1efd2587f931c3562070b7d86ee0f5fd842`。
- docs product commit 只包含 `docs/upstream/customization-map.md`、`docs/upstream/README.md` 和必要的 `plus-ui-namewta` submodule pointer；未暂存 docs 父仓库其他既有脏改动。

## 3. 修改范围与路径所有权

- plus-ui source-worktree 只修改根 `README.md`，apps、packages、tooling、配置与运行代码零变化。
- docs current-workspace 只修改 Ticket 授权的两份 upstream 文档；submodule pointer 由 Lead 在 scoped product commit 中推进。
- SpecDev Ticket/Map/Goal/status/Evidence 由 Lead 按完成合同维护，不转移产品路径所有权。

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-029 | 十字段 schema、capability-first 流程、真实 owner boundary、验证/Evidence 门禁 | passed |
| 安全 defer | threat、mitigation、owner、deadline、issue/change 缺一即 Gate 失败 | passed |
| 未来终端 | mobile-web、miniapp-taro、taro-request、taro-storage 均为 README-only、无 package manifest | passed |

## 5. Workspace Verification

- **source-worktree：** commit `60540c4` / tree `7b7c9ba` clean，根 README 本地维护入口可解析。
- **current-workspace：** docs schema、链接、固定 hash 和 scoped staged-path 检查在 docs 父仓库执行。
- Markdown 表格解析得到 4 张十字段表、16 条有效样例/基线记录；13 条 completed 均无空字段、`not-run` 或 `unassigned`。
- 无效 `defer + blocked` 反例被拒绝；合成的普通优先级 `defer + deferred` 组合被接受；reject/defer 状态转移无歧义。
- 22 个 docs 相对链接与 plus-ui README 本地入口均存在。
- 七领域、apps、platform、adapters、web-kit、contracts、tooling 和四个未来终端目录覆盖通过；completed owner 均使用真实 `apps/**`、`packages/**` 或 `tooling/**` 路径。
- customization map 最终 SHA-256：`2d8cc01f4bc32f59d71a2b07664fa69cff959518cb8b2e99749b887ee1ef977a`；docs upstream README SHA-256：`74dcb44017c6af6b40e31e2736ddce5b5ca3a645fd0f8352e032d6958e025030`。
- Speculo tickets/complete 校验使用 `<Path>plus-ui-namewta</Path>` 仓库解析；最终结果见本 Evidence 第 7 节。

## 6. 双轴审查

- 第一轮 fixed point：plus-ui source/tree `60540c4` / `7b7c9ba`，docs hashes `43435bca...` / `74dcb440...`。规格轴 PASS；标准轴 FAIL，发现 completed owner 概念路径、defer 状态流歧义及 taro-request 占位遗漏。
- `DEV-T17-002` 整改后 fixed point：plus-ui source/tree 不变，customization map hash 更新为 `2d8cc01f...`，README hash 不变。
- 第二轮 Spec/Intent 与 Standards/Maintainability 对同一 fixed point 均 PASS，无 findings；审查只读且未运行 E2E。

## 7. Integration Verification

Attempt 1 从未漂移的 plus-ui parent `22c10d2` 创建 candidate 并 fast-forward 到 exact reviewed source。candidate 相对 base 只修改根 `README.md`，SHA/tree、单文件范围、diff-check、本地链接和 clean worktree 全部通过；docs schema 正反规则、22 个链接、固定 hashes 与 Speculo validator 同轮通过。Lead 复核 parent 未漂移后以 `--ff-only` 推进 plus-ui `main`，result 与 source/candidate 完全一致。

E2E 为 `not-required`：T17 只修改维护文档和 README，不改变 App UI、路由、API、权限或浏览器运行行为；Gate H 的双 App Playwright `47/47` 仍是最近运行时基线。

## 8. 偏差与决策

- `DEV-T17-001`：保留用户已修正的 `DML.sql` 术语、`ry_vue.sql -> DDL.sql -> DML.sql` 顺序和本地 SQL append-only 不变量，同时用 capability-first schema 替换已退役的路径热点表。
- `DEV-T17-002`：首轮标准轴 findings 触发真实 packages owners、唯一 deferred 终态/re-triage 流和 taro-request 占位补齐；新 fixed point 重新双轴 PASS。
- Ticket、Map、Goal Plan、Evidence、change status 与 Git 结果完成对齐后，`validate-specdev --stage complete --repo plus-ui-namewta` 必须为 `0 error / 0 warning` 才关闭 Gate J。

## 9. 残余风险与交付定位

- 未拉取、合并或自动吸收任何上游代码；未 push、建 PR、部署、发布或归档。
- source branches/worktrees 按授权保留；只清理 transient candidate。
- 移动端与小程序仍是占位，激活必须另立 change 并定义终端专属 Gate。
