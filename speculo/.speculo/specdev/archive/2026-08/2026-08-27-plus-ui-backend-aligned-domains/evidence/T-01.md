# Evidence: T-01 - 原子迁移后端模块对齐的前端领域布局

- **Change：** `2026-08-27-plus-ui-backend-aligned-domains`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-27-plus-ui-backend-aligned-domains/ticket/01-align-domain-layout.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `codex:/root`
- **Base SHA：** `8a5cd0dfb9bd56b7be94d78e29957a54cc218805`
- **Result SHA：** `d60b807a195336e4430e92d830eef7b707df7f6f`
- **状态：** integrated；G1 attempt 2 全绿，前端 `main` 已 fast-forward

## 1. 实现摘要

前端第一层已经收敛为 `admin/system/gen/workflow/demo/ai` 六个 domain 与同名 web-domain。每个实际消费的 Controller 资源均有 kebab-case 目录、`index.ts`、`types.ts` 和显式 package export；旧 `identity-access/system-admin/devtools/operations` 包、目录和 imports 已清零。`admin` 通过 App 注入的 system identity port 获取用户与动态菜单，system 同时接管监控资源；Admin 独占的三个外部控制台页面保留在 App 内。中文 README 与领域开发 Skill 已同步。

## 2. Lead Dispatch And Candidate Return

- 本 Ticket 由 Lead 直接实现，未派遣 subagent；原因是单 Ticket 的包重命名、锁文件和 manifest 共享路径必须原子修改。
- source-worktree：`specdev-worktree/2026-08-27-plus-ui-backend-aligned-domains/T-01`。
- source branch：`speculo/2026-08-27-plus-ui-backend-aligned-domains/T-01`。
- 实现 commits：`6848040a27a1f7c833c0c9310495c6784c572dff`、`d60b807a195336e4430e92d830eef7b707df7f6f`。
- 最终 source/candidate tree：`f84b11a29e4a48cbd6d51186330e0f5b26043ce1`；两者 clean 且 SHA/tree 一致。
- parent-candidate：`specdev-worktree/.integration/2026-08-27-plus-ui-backend-aligned-domains/T-01`，branch `speculo/integration/2026-08-27-plus-ui-backend-aligned-domains/T-01`。

## 3. 修改范围与路径所有权

- 修改落在 Ticket 授权的 `packages/domains/**`、`packages/web-domains/**`、`apps/**`、`e2e/**`、`tooling/architecture/**`、锁文件、根 README、`platform/auth/README.md` 与前端领域 Skill。
- 后端、OpenAPI 生成文件、platform 运行代码、adapters 和移动/小程序占位源码保持只读。
- 格式化产生的既有样式、Vite 插件、通用 README 与 OpenAPI 测试噪声已从 source diff 排除。
- 主工作区原有 `<Path>plus-ui-namewta/apps/admin-web/src/types/components.d.ts</Path>` 修改未进入任何实现 commit；`main` 推进后仍是唯一未提交修改。

## 4. 验收与合同映射

| Contract | 结果 |
|---|---|
| AC-001 | passed：六个 canonical domain/web-domain 唯一存在；架构测试禁止四个旧包回流 |
| AC-002 | passed：Controller/base path、类型文件与 domain/web-domain 显式 exports 均由 architecture tests 校验 |
| AC-003 | passed：Admin 登录、菜单、路由、权限、gen/system/workflow 页面由 unit/build/E2E 验证 |
| AC-004 | passed：Client 显式组合 admin + headless system + demo，独立 Client/session 与未选能力失败关闭通过 |
| AC-005 | passed：OpenAPI 当前，HTTP path/method、动态组件键集合和 transport tests 无漂移 |
| AC-006 | passed：受影响 README 与 Skill 为中文且只描述新包名、资源和组合边界 |

## 5. Workspace Verification

- **source-worktree：** `pnpm install --frozen-lockfile`、`pnpm architecture:check`、`pnpm architecture:test`、`pnpm --filter @namewta/tooling-openapi openapi:check`、`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build:dev`、`pnpm build:prod` 全部通过；source-worktree 按合同未运行 E2E。
- **source-worktree 静态审计：** architecture 为 `28 packages / 0 violations`，architecture tests `98/98`；OpenAPI 为 `651675 bytes`、SHA-256 `cb1d2e0857f7ed99319423ac10a59d50c4795fac92840ff4c88178853ecb383f`；旧 workspace 包名/import、空 domain 目录、根级兼容 `src/` 均为零。
- **parent-candidate attempt 2 / final SHA：** frozen install、architecture `28/0 + 98/98`、OpenAPI、完整 lint、typecheck、workspace unit、`build:dev` 全部通过；最终 `pnpm test:e2e` 的 webServer 在 `d60b807` 上执行 `build:prod` 并启动 Admin/Client 预览。
- **构建结果：** Admin 与 Client development/production 构建成功；保留项目既有 ineffective dynamic import 与大 chunk 警告，无构建错误。
- **E2E disposition：** source-worktree `not-run`；E2E 只在 parent-candidate 运行，详见第 7 节。

## 6. 双轴审查

- **规格轴：** 对照 AC-001..006、HTTP 与 component key 不变量复核。首次发现登录组件键随包名误改，已恢复 `identity-access/login/index`；迁移前后全部静态 component key 集合逐项 diff 为零。
- **标准轴：** 复核包边界、exports、cross-domain ownership、错误关闭、测试和目录清洁度。新增门禁强制 domain 唯一后端模块、资源 `index.ts/types.ts`、Controller/base path 与显式 exports。
- **标准轴整改：** 清除 Oxfmt 带入的 35 个无关 diff；恢复 gen 页面稳定 CSS 类 `devtools-generator-page`；扩展 workflow E2E fixture 按合法 leave ID 响应。
- **最终结论：** PASS；source 与 candidate clean、tree 一致，旧包/兼容门面为零，产品组件键、HTTP、权限和会话语义保持不变。

## 7. Integration Verification

- parent before：`main@8a5cd0dfb9bd56b7be94d78e29957a54cc218805`；仅有用户自持的 `components.d.ts` 修改。
- attempt 1：candidate `6848040` 的非 E2E 全绿；Playwright `42/47`，4 项因 gen 根 CSS 类漂移，1 项因 workflow fixture 只硬编码 `leave-1`。`main` 未推进。
- remediation：source 追加 `d60b807`；5 个失败场景定向复跑 `5/5`。
- attempt 2：candidate/source/result `d60b807a195336e4430e92d830eef7b707df7f6f`，tree `f84b11a29e4a48cbd6d51186330e0f5b26043ce1`；无冲突，完整非 E2E 全绿，Playwright `47/47`（约 38 秒）。
- promotion：在原主工作区执行 `git merge --ff-only speculo/integration/2026-08-27-plus-ui-backend-aligned-domains/T-01`；`main` 精确推进到 result SHA，用户未提交修改仍保留。
- cleanup：未授权；source 与 candidate worktree/branch 均保留。

## 8. 偏差与决策

- `DEV-T01-001`（local）：候选 worktree 初始名称不符合 Speculo 固定 locator；完成验证后仅重命名 branch 并移动 clean worktree 到标准 `.integration/.../T-01`，SHA/tree 未改变。
- `DEV-T01-002`（local）：首轮 E2E 揭示稳定 CSS 测试接缝被语义重命名，以及既有 workflow fixture 样本过窄；均在既定“产品行为不变”合同内修复，无需改变 Spec/Ticket 范围。
- 没有范围、公共 HTTP、权限、后端或数据偏差；没有 push、PR、部署或远端动作。

## 9. 残余风险与交付定位

- Vite 仍报告既有动态/静态重复导入和大 chunk 警告，本次未扩大为独立性能重构。
- `components.d.ts` 的 `ElAlert` 行是用户主工作区已有生成修改，刻意未纳入 commits；后续由用户按自身生成文件策略处理。
- 前端交付定位：`plus-ui-namewta/main@d60b807a195336e4430e92d830eef7b707df7f6f`。worktree 未清理，未执行 push 或部署。
