# Evidence: T-03 - 永久清除生成器数据库表与菜单权限

- **Change：** `2026-08-28-retire-runtime-code-generator`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-28-retire-runtime-code-generator/ticket/03-hard-retire-generator-database.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-28-retire-runtime-code-generator/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-28-retire-runtime-code-generator/goal-plan.md</Path>`
- **Lead：** `codex:/root`
- **Workspace/branch：** `current` / aggregate `main`, backend `main`
- **Base/implementation-or-source/candidate/result SHA：** `ae2d180eee05da1c5b335127abec78becb332516` / `8d401907b6be81c36f92cf88e73e1dee61fd26a4` / `null` / `40df0cd25d41375979b5aa21dbc0dd209d578733`
- **状态：** done

## 1. 实现摘要

在 `script/sql/namewta/DDL.sql` 末尾追加 `NAMEWTA-RUNTIME-GEN-RETIRE-DDL-001`，先永久删除 `gen_table_column`，再删除 `gen_table`。在 `DML.sql` 末尾追加 `NAMEWTA-RUNTIME-GEN-RETIRE-DML-001`：先验证九个固定菜单完整匹配冻结基线或已完整删除，并拒绝系统工具目录被非目标菜单复用的状态；随后先删除全部目标 `sys_role_menu`，再按功能、页面、目录顺序删除九个菜单。不创建备份、归档、兼容表或恢复脚本。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `codex:/root`
- **Dispatch Packet/checkpoint：** Lead direct；backend base `aedebb975dc61413d02ee734ce26dc855ea42c55`
- **允许动作：** current workspace changes、backend scoped implementation commit、aggregate scoped gitlink commit、Lead-owned disposable MySQL E2E
- **返回：** backend commit `8d401907b6be81c36f92cf88e73e1dee61fd26a4`；两个 SQL 文件只追加 168 行；既有 backend `.claude/.codex` 删除与 `README.md` 修改仍未纳入提交
- **Lead 独立核对：** pass；重读 append-only diff、冻结基线、固定 ID、父仓库 gitlink 与 MySQL 最终状态
- **只读 Agent findings：** 无；未派发 subagent

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>ruoyi-vue-plus-namewta/script/sql/namewta/DDL.sql</Path>` | shared:T-03 | 末尾追加两张生成器表的不可逆物理删除 |
| `<Path>ruoyi-vue-plus-namewta/script/sql/namewta/DML.sql</Path>` | shared:T-03 | 末尾追加固定菜单前置保护、角色关系和菜单删除 |

- **read-only 修改：** 无；`script/sql/ry_vue.sql`、其他方言和 Docker Compose 未修改
- **未声明路径：** 无
- **历史前缀哈希：** DDL `a143b83c6648b22c6e138be8ea3bac88f4ff2f7aa592458671fb19354ef570da`；DML `9f52c97b9803b8079bf503746af8e96382e99c20a7cdf5330d6c7ac3c89ede66`
- **冻结基线哈希：** `ry_vue.sql` 为 `7aad4cdbd50de11abf32202a7a5fc0609e3d281f4b4aa69099d5cee26899bb16`

## 4. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-005 | `sys_menu`、`sys_role_menu` 与权限反向查询 | 九个 menu_id、目标关系、`tool:gen:*`、`tool/gen%` 与 `/tool/gen` 均为 0 | pass |
| AC-006 | append-only review 与 `information_schema` | diff 仅在 EOF 新增；子表先于主表；两张表最终为 0 | pass |
| AC-007 | 全新临时 MySQL 8.4 完整初始化 | MySQL 8.4.11 按 10/20/30/40/50/60 顺序执行成功并正常关闭 | pass |
| 失败保护 | 非目标子菜单哨兵 | 前置检查以 `ERROR 3819` 拒绝，哨兵仍存在，未发生宽泛删除 | pass |
| 集成 | backend source 与 aggregate gitlink | backend `8d401907b`；aggregate `40df0cd` 精确指向该提交 | pass |

## 5. Workspace Verification

- **Workspace：** current-workspace / backend + aggregate direct-parent

| 命令或步骤 | 结果 | 摘要 |
|---|---|---|
| `git diff --check` | pass | 两个 SQL 无 whitespace error |
| `git diff --unified=0` | pass | DDL 仅 `@@ -225,0 +226,12`；DML 仅 `@@ -289,0 +290,156` |
| 历史前缀与冻结 SQL SHA-256 | pass | 三个哈希与实施前快照一致 |
| 固定 ID、DDL/DML 删除顺序与恢复语义 review | pass | 仅九个固定 ID；关系先删、子项先删；无备份/恢复面 |
| SpecDev implement validator | pass | 0 errors，0 warnings |

### Required MySQL E2E

- **环境：** Homebrew MySQL `8.4.11`，全新 `/tmp/namewta-t03-mysql.1rnGrC/data`，socket `/tmp/namewta-t03-mysql.1rnGrC/mysql.sock`，本机端口 `43316`，`utf8mb4/utf8mb4_general_ci`，`lower_case_table_names=1`
- **隔离：** 只创建临时 `ry-vue` 数据库；未连接或修改任何现有/生产数据库
- **初始化顺序：** `ry_vue.sql` → `ry_job.sql` → `ry_workflow.sql` → `ry_ai.sql` → `namewta/DDL.sql` → `namewta/DML.sql`
- **生命周期：** `mysqld` ready 后执行全部查询，随后由 `mysqladmin shutdown` 正常关闭；临时目录已精确删除

### 清理前冻结基线

| 指标 | 数量 |
|---|---:|
| `gen_table` + `gen_table_column` | 2 |
| 九个目标菜单 | 9 |
| 目标角色关系 | 0 |
| 非目标菜单 | 147 |
| 非目标角色关系 | 117 |

### 完整初始化最终状态

| 指标 | 数量 |
|---|---:|
| 两张目标表 | 0 |
| 九个目标菜单 | 0 |
| 目标角色关系 | 0 |
| `tool:gen:*` 权限 | 0 |
| `tool/gen%` component 或 `/tool/gen` active menu | 0 |
| 非目标菜单 | 160 |
| 非目标角色关系 | 123 |
| 代表性用户菜单 `1761400000000000100` | 1 |

冻结基线与最终非目标总数不同，是因为既有 NAMEWTA DML 会新增 13 个业务菜单和 6 个角色关系；该变化不来自退役删除。以完整初始化后的 `160/123` 为基准重跑本次退役块，计数仍为 `160/123`。

### 重跑与失败路径

- 从唯一标识 `NAMEWTA-RUNTIME-GEN-RETIRE-DML-001` 起重跑本次追加块成功；目标菜单、关系、perms/component 继续为 0，非目标计数不变。
- 整份历史 `namewta/DML.sql` 重跑会在到达本次追加块前，于既有第 20 行因 `sys_user_type` 主键重复返回 `ERROR 1062`。T-03 的 append-only writable contract 禁止改写该历史块；因此不把整文件重跑记为通过，本次只声明新退役块的删除态幂等性。
- 插入非目标子菜单哨兵 `2999999999999999003` 后重跑退役块，check constraint 返回 `ERROR 3819`；哨兵计数仍为 1、目标菜单仍为 0。随后精确删除哨兵，最终哨兵计数为 0。

## 6. 双轴审查

### 标准轴

- **固定输入：** backend `aedebb975dc61413d02ee734ce26dc855ea42c55..8d401907b6be81c36f92cf88e73e1dee61fd26a4`、工程 Skills、append-only SQL 规则
- **结果：** pass
- **Findings 与修正：** 无；DDL/DML 分类正确，前置保护在删除语句前失败，未引入 pattern-based 宽泛删除

### 规范轴

- **固定输入与来源：** Spec、T-03、Goal Plan、ADR-002、用户永久删除批准
- **结果：** pass
- **Findings 与修正：** 无；两表、九菜单和授权投影共同消失，且冻结 SQL 与非目标数据保留

两个轴隔离检查，均未以一轴结果抵消另一轴。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `ae2d180eee05da1c5b335127abec78becb332516` |
| Implementation/source SHA | backend `8d401907b6be81c36f92cf88e73e1dee61fd26a4` |
| Candidate branch/workspace | current；not-applicable |
| Method/conflicts | direct-parent；backend scoped SQL commit + aggregate gitlink commit；无冲突 |
| Required E2E | passed；Lead 在临时 MySQL 8.4.11 fresh datadir 执行 |
| Parent result/re-read | `40df0cd25d41375979b5aa21dbc0dd209d578733`；aggregate gitlink 指向 `8d401907b6be81c36f92cf88e73e1dee61fd26a4` |

## 8. 偏差与决策

- **批准偏差：** Docker 不可用，按 Goal Plan 已记录的等价接缝使用 Homebrew MySQL 8.4.11；数据库版本、初始化顺序和最终查询合同不变
- **未批准偏差：** 无
- **批准来源及影响：** Goal Plan 的本地可丢弃 MySQL 8.4 接缝；不扩大到现有数据库、生产、部署或远端动作

## 9. 残余风险与交付定位

- **已知限制：** 既有历史 DML 全文件非幂等；本 Ticket 不越界改写历史前缀，只证明新增退役块重跑安全
- **残余风险：** 无 T-03 阻塞项；当前 OpenAPI 仍由 T-04 收缩
- **Source commit：** `8d401907b6be81c36f92cf88e73e1dee61fd26a4`
- **Parent result：** `40df0cd25d41375979b5aa21dbc0dd209d578733`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-28-retire-runtime-code-generator/evidence/T-03.md</Path>`
