# Evidence: T-11 - 交付管理员动态页与个人中心 OpenAPI 双入口

- **Lead：** `codex:/root`
- **Change：** `2026-08-30-openapi-common-module`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/11-frontend-dual-openapi-ui.md</Path>`
- **Workspace/branch：** `specdev-worktree/2026-08-30-openapi-common-module/T-11` / `speculo/2026-08-30-openapi-common-module/T-11`
- **Base/source/candidate/result：** `a0a1a1d788e1359dd1432e21b05c939f67ebc6f8` / `ea32aa1b1c9911e430f406631199e30589ba007b` / `ea32aa1b1c9911e430f406631199e30589ba007b` / `ea32aa1b1c9911e430f406631199e30589ba007b`
- **Candidate tree：** `5aebefaebdc1418a05a9677b9298157d8aee6e78`
- **状态：** done

## 1. 实现摘要

新增共享 `OpenApiWorkspace` 及纯状态控制器，以 `current-user` / `target-user` 判别 scope 绑定 T-10 service。工作区呈现凭据摘要、enabled/disabled/expired 状态、create/reset/enable/disable/delete、实时接口目录与 cURL/Java 合同；404 凭据与服务关闭分开处理，forbidden/conflict/unavailable 均有稳定恢复态。

system manifest 注册精确 key `system/openApi/index`，管理员页先搜索并持续显示显式目标用户；个人中心新增静态 OpenAPI tab，固定本人 scope 且不构造 userId。创建/重置的 AppSecret 只存在于一次性对话框状态，footer、右上角或 ESC 关闭都会清空；复制失败在页面与 host feedback 同时可见。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead；未使用子 Agent
- **Source checkpoint：** clean commit `ea32aa1b1c9911e430f406631199e30589ba007b`，tree `5aebefaebdc1418a05a9677b9298157d8aee6e78`
- **依赖恢复：** source/candidate 均使用 `pnpm install --offline --frozen-lockfile`，391 packages 全由本机 store 复用，lockfile 未修改
- **Lead 核对：** 16 个文件均在原 writable scope 或已登记的最小 App composition 修订内；无生成文件、lockfile 或其他 App 变化

## 3. 修改范围与路径所有权

| 路径 | 改动目的 |
|---|---|
| `<Path>plus-ui-namewta/packages/web-domains/system/src/open-api/**</Path>` | 双 scope controller、共享工作区、管理员目标页与测试 |
| `<Path>plus-ui-namewta/packages/web-domains/system/src/index.ts</Path>` / `runtime.ts` / package | manifest key、权限清单、OpenAPI runtime port 与稳定 export |
| `<Path>plus-ui-namewta/apps/admin-web/src/application/services.ts</Path>` | 组合唯一 `createOpenApiService(domainHttp)` 实例 |
| `<Path>plus-ui-namewta/apps/admin-web/src/router/adminManifestRegistry.ts</Path>` | 仅向 `SystemWebRuntime` 注入 OpenAPI service |
| `<Path>plus-ui-namewta/apps/admin-web/src/views/system/user/profile/**</Path>` | current-user 静态 tab/wrapper 与组合测试 |

未修改 T-10 domain、router resolver/manifest 选择语义、权限指令、lockfile、其他 App 或后端。

## 4. 验收与合同映射

| 合同 | 结果 |
|---|---|
| AC-023 | `system/openApi/index` 注册为 `OpenApi`，admin registry 可解析 |
| AC-024 | 管理员目标用户先选择后执行，每项 target service 都绑定显式 userId，目标标签持续可见 |
| AC-025/026 | profile 固定 current-user，不发送或持有 target userId；UI 权限只改善体验，后端仍裁决 |
| AC-027 | AppSecret 仅在 issued modal；所有关闭路径清空，复制失败可见，无 storage/cache/URL |
| 可恢复状态 | empty/disabled/expired/forbidden/conflict/unavailable/loading 均有控制器或组件断言 |

## 5. Workspace Verification

| 命令 | 环境 | 结果 |
|---|---|---|
| `pnpm --filter @namewta/web-domain-system test` | source-worktree | pass；6 files，20 tests |
| `pnpm --filter @namewta/admin-web test` | source-worktree | pass；8 files，41 tests |
| `pnpm --filter @namewta/domain-system test` | source-worktree | pass；9 files，63 tests |
| architecture + affected tests + dual typecheck/lint + admin production build | parent-candidate | pass；24 packages，0 baseline violation；63 + 20 + 41 tests；3370 modules transformed |
| `git diff --check`、writable-path 与 secret storage/log scan | source/candidate | pass |

E2E disposition 为 not-required：本票按用户批准的轻量门禁使用真实 SFC 编译、组件状态测试、App manifest/profile 集成测试与 production build。未启动浏览器或真实后端，完整 Playwright 明确保留为 residual risk。

## 6. 双轴审查

- **标准轴：** pass；domain -> web-domain -> App 依赖方向正确，HTTP 只在 App composition 注入，组件使用既有 Element Plus/icon/host ports，无全局状态或敏感持久化。
- **规范轴：** pass；双入口共享行为但各自持有瞬态状态，target owner 明确，current scope 无 userId，权限与稳定错误态可重复验证。

## 7. Integration Verification

- **Parent before：** 前端 `main@a0a1a1d788e1359dd1432e21b05c939f67ebc6f8`
- **Candidate：** `speculo/integration/2026-08-30-openapi-common-module/T-11`，fast-forward，无冲突，tree `5aebefaebdc1418a05a9677b9298157d8aee6e78`
- **Parent result：** 前端 `main@ea32aa1b1c9911e430f406631199e30589ba007b`；source 为 ancestor；仅保留未跟踪 `specdev-worktree/` 容器，无 tracked 改动

## 8. 偏差与决策

- **组合 scope 修订：** T-10 按独立 slice 导出 `createOpenApiService(http)`，既有 `SystemService` 不包含 OpenAPI。为避免 web-domain 反向依赖 App 或复制 HTTP，T-11 增补 `application/services.ts` 与 registry runtime 对象两处最小写权限；resolver、manifest 选择与路由语义未改变。
- **依赖环境：** 新 worktree 首次测试因没有 `node_modules` 报 `vitest: command not found`，不是行为失败；frozen offline install 后红灯精确落在缺失 workflow/registration，随后实现转绿。
- **视觉验证：** production build 生成独立 `OpenApiAdminPage` / `OpenApiWorkspace` chunks；因后端仍 default-off 且本票不要求 Playwright，没有伪造浏览器全链结果。

## 9. 残余风险与交付定位

- **残余风险：** 未在浏览器真实登录态、真实后端或窄移动视口执行 Playwright；T-12 仍需 frontend full gate 与发布边界复验。
- **后续 Ticket：** T-12
- **恢复边界：** 后端保持关闭时 UI 显示 disabled/unavailable；移除菜单 DML 即可停止动态入口，secret 不可恢复。
- **Source commit / Parent result：** `ea32aa1b1c9911e430f406631199e30589ba007b`
- **Evidence：** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-11.md</Path>`
