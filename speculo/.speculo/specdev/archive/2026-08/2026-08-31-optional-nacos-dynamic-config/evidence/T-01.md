# Evidence: T-01 - 建立可选 Nacos 稀疏覆盖运行时

- **Change：** `2026-08-31-optional-nacos-dynamic-config`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/01-nacos-config-runtime.md</Path>`
- **Workspace/branch：** current / aggregate `main` + backend `main`
- **Parent before/result SHA：** `01c815f8952ec5a5a3e92d75341ab3af3fd36895` / `62d1aca6afb8c0b783d340db0446335f10b835fe`
- **Backend implementation SHA：** `344c1bed54e7e80c5a14b6d8fa0d997f994c14ca`
- **状态：** done

## 1. 实现摘要

新增 `ruoyi-common-nacos`，直接使用 `nacos-client:2.5.1` 提供默认关闭的 Config-only 稀疏覆盖。启动读取使用 `getConfigAndSignListener`，避免离线重启读取客户端磁盘 snapshot；部署层保持在远端覆盖之上。候选 YAML 经保护键、已知标量与参与者 prepare 校验后原子替换，空文档撤销覆盖，断连保留内存版本。Actuator info 只输出连接、摘要、结果、时间与分类计数。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `codex:/root`
- **执行模式：** current workspace，未派发 subagent，未创建 source/candidate worktree
- **Backend return：** `344c1bed54e7e80c5a14b6d8fa0d997f994c14ca`
- **Aggregate return：** `62d1aca6afb8c0b783d340db0446335f10b835fe`
- **Lead 核对：** backend commit、aggregate gitlink、允许路径、测试执行数和制品内容均已独立读取

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 目的 |
|---|---|---|
| `<Path>ruoyi-vue-plus-namewta/ruoyi-common/ruoyi-common-nacos/**</Path>` | writable:T-01 | SDK 生命周期、覆盖状态机、SPI、info 与测试 |
| `<Path>ruoyi-vue-plus-namewta/pom.xml</Path>` | writable:T-01 | 固定客户端版本与 dependencyManagement |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-common/pom.xml</Path>` | writable:T-01 | 注册 reactor 模块 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-common/ruoyi-common-bom/pom.xml</Path>` | writable:T-01 | 注册 common artifact |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/pom.xml</Path>` | writable:T-01 | full/core 直接装配 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/main/resources/application.yml</Path>` | writable:T-01 | 默认关闭的部署参数 |

- 未修改 Ticket read-only 路径；未提交 `target/**` 或 `.flattened-pom.xml`。
- 聚合仓既有用户 dirty 未清理、未暂存；Ticket commits 通过精确 path staging 隔离。

## 4. 验收与合同映射

| 合同 | 验证接缝 | 结果 |
|---|---|---|
| AC-001/006/023 | 默认关闭 context、fake unavailable、full/core package | pass |
| AC-002/003/005 | 稀疏覆盖、PropertySource 优先级、profile namespace tests | pass |
| AC-004/009/011/012/013 | 保护键、空删除、已知标量、participant 原子拒绝、分类 | pass |
| AC-007/008/014 | 无 `getConfig` snapshot 路径、内存 snapshot、逐实例 manager/state | pass；真实故障由 T-06 复验 |
| AC-022 | info/log/source/build scan | pass；不输出正文、键值或凭据 |

## 5. Workspace Verification

| current-workspace 命令 | 结果 | 摘要 |
|---|---|---|
| `./mvnw -pl ruoyi-common/ruoyi-common-nacos -am -DskipTests=false test` | pass | 11 tests，0 failure/error/skipped |
| `./mvnw -pl ruoyi-admin -am -Dtest='*Nacos*Test' -Dsurefire.failIfNoSpecifiedTests=false test` | pass | 37-module reactor；11 Nacos tests 实际执行 |
| `./mvnw -pl ruoyi-admin -am -Pbundle-full -DskipTests package` | pass | full bundle 成功 |
| `./mvnw -pl ruoyi-admin -am -Pbundle-core -Dmaven.test.skip=true package` | pass | core 28-module 生产制品成功 |
| `jar tf ruoyi-admin/target/ruoyi-admin.jar` | pass | core fat jar 包含 common-nacos 与 nacos-client |
| `dependency:tree`、`git diff --check`、路径/输出扫描 | pass | 无 Spring Cloud/Server/Discovery；无泄密输出；路径合规 |

- **E2E：** not-required；真实 Nacos/MySQL、断连、双实例与 snapshot 隔离由 T-03/T-06 required E2E 负责。

## 6. 双轴审查

### 标准轴

- 模块依赖显式且可独立编译；公共库只依赖 SLF4J API，不携带日志实现。
- 远端 source 位于 CLI/system/env 下、本地 ConfigData 上；原子快照同时承载 overlay、prepared config 和状态。
- 关闭条件同时约束 EnvironmentPostProcessor 与 auto-configuration，不创建客户端 Bean。

### 规范轴

- 默认关闭、本地完整基线、Config-only、profile namespace、固定 group/dataId、保护键、删除与脱敏状态均符合 T-01。
- 即时清单未扩展；common 只提供参与者合同和分类，真实业务读取由 T-02 实现。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Aggregate parent before | `01c815f8952ec5a5a3e92d75341ab3af3fd36895` |
| Backend implementation | `344c1bed54e7e80c5a14b6d8fa0d997f994c14ca` |
| Aggregate result | `62d1aca6afb8c0b783d340db0446335f10b835fe` |
| Method/conflicts | direct-parent；无冲突；attempt 1 |
| Gitlink | aggregate result 指向 backend implementation SHA |
| Verification/E2E | passed / not-required |

## 8. 偏差与决策

- 第一次模块构建暴露缺少显式 `slf4j-api`，已按公共库边界补齐。
- 项目 Surefire 只运行 active profile 同名 `@Tag`；补齐 `@Tag("dev")` 后从 `0 tests` 收敛为真实 11 tests。
- core 的 `-DskipTests` 仍编译仓库既有 full-only admin 测试并因 demo/workflow 类缺失失败；使用 `-Dmaven.test.skip=true` 验证 core 生产制品，Nacos tests 已在模块和 full reactor 中完整执行。
- Goal Plan 已批准以当前 dirty 工作树为权威并要求精确暂存；Speculo `--repo` clean 检查无法区分既有用户 dirty，因此不清理用户文件，也不虚报 repository clean。结构验证不带 `--repo`，Git SHA/gitlink 另由上述命令核对。

## 9. 残余风险与交付定位

- 真实 Nacos 2.5.4 鉴权、MySQL 持久化、运行断连、双实例 digest 与离线 snapshot 隔离尚未在本 Ticket 运行，分别由 T-03/T-06 关闭。
- `NacosConfigParticipant` 的三组业务实现和跨字段约束由下一依赖 Ticket T-02 完成。
- 未执行 push、部署、生产迁移、角色授权、secret 设置或工作树清理。

## 10. T-06 前置修正与关闭

T-06 源码审查确认 `getConfigAndSignListener` 本身不能关闭 Nacos SDK 的磁盘 snapshot。backend `4ea0bd6fc39f37a4b7b13cebff3bb7a96d80399b` 在创建客户端前调用 `SnapShotSwitch.setIsSnapShot(false)`，并以真实执行的 policy test 固化；aggregate `401659a` 提升 gitlink。最终远程矩阵在 Nacos 停止时重启单实例，观察到 `LOCAL_BASELINE`、无 digest、0/0 count，恢复 Nacos 后重新收敛，AC-007 已由真实环境关闭。
