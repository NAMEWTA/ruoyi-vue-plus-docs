# Evidence: T-02 - 让三组安全配置即时生效

- **Change：** `2026-08-31-optional-nacos-dynamic-config`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/02-safe-live-refresh.md</Path>`
- **Workspace/branch：** current / aggregate `main` + backend `main`
- **Parent before/result SHA：** `f8470f717910f2f272afc46e4edd6b83404c05ee` / `a8f993eb401f67f166ecc87e8cf99e7781fb28a5`
- **Backend implementation SHA：** `c68cb55d50a1ab874da1ae428b2bd1bd7828dc74`
- **状态：** done

## 1. 实现摘要

`CaptchaProperties`、`NotifyIdempotencyProperties` 与 `OssLifecycleProperties` 直接实现 T-01 participant 合同：prepare 阶段从完整 effective Binder 生成不可变 snapshot 并校验，业务调用从 `NacosConfigAccessor` 的单一原子 snapshot 读取；Nacos 关闭或键删除时回退本地字段。验证码请求在开始时捕获完整 snapshot，通知幂等一次读取三项窗口，OSS 只动态读取默认下载 TTL。清单严格限定为 `captcha.*`、`notify.idempotency.*` 和精确键 `oss.lifecycle.download-ttl`。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `codex:/root`
- **执行模式：** current workspace，未派发 subagent，未创建 source/candidate worktree
- **Backend return：** `c68cb55d50a1ab874da1ae428b2bd1bd7828dc74`
- **Aggregate return：** `a8f993eb401f67f166ecc87e8cf99e7781fb28a5`
- **Lead 核对：** implementation commit、aggregate gitlink、12 个修改文件、测试执行数和制品内容均已独立读取

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 目的 |
|---|---|---|
| `<Path>ruoyi-vue-plus-namewta/ruoyi-common/ruoyi-common-web/**</Path>` | writable:T-02 | Captcha 不可变 snapshot、participant 与依赖 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-common/ruoyi-common-notify/**</Path>` | writable:T-02 | 幂等窗口 snapshot、约束与单次读取 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/**</Path>` | writable:T-02 | OSS 精确 TTL participant 与动态读取 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/main/java/org/dromara/web/controller/CaptchaController.java</Path>` | writable:T-02 | 请求级完整 snapshot |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/nacos/refresh/**</Path>` | writable:T-02 | 跨三域行为/并发/装配合同 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-common/ruoyi-common-nacos/{SPI,Manager,test}</Path>` | approved exception:T-02 | 精确键分类与晚注册拒绝回退 |

- 未修改 `application*.yml` 或其他即时生效前缀；精确 SPI 例外已在 Ticket 路径合同中同步。
- 聚合仓既有用户 dirty 未清理、未暂存；父提交只提升 backend gitlink。

## 4. 验收与合同映射

| 合同 | 验证接缝 | 结果 |
|---|---|---|
| AC-009/010 | real manager + Captcha/Notify/Oss consumers | pass；发布后下一调用变化，空文档立即回本地 |
| AC-011/012 | mixed YAML + state counts | pass；5 个即时键、`server.port` 1 个等待重启 |
| AC-013 | participant prepare + invalid cross-field version | pass；三组行为与上一 digest 全部保持 |
| AC-022 | source/output scan | pass；日志与状态不含正文、键值或 secret |

## 5. Workspace Verification

| current-workspace 命令 | 结果 | 摘要 |
|---|---|---|
| T-02 红灯 Maven tests | expected fail | 缺少 `exactKeys()`，证明精确键合同未实现 |
| Nacos manager 定向测试 | pass | 8 tests；exact key、跨参与者原子拒绝、晚注册本地回退 |
| Captcha/Notify/Oss + T-02 reactor tests | pass | 65 tests，0 failure/error/skipped |
| T-02 live refresh contract | pass | 7 tests；三行为、稀疏绑定、并发、真实 manager、关闭装配 |
| `bundle-full -DskipTests package` | pass | 37-module full reactor |
| `bundle-core -Dmaven.test.skip=true package` | pass | 28-module core 生产制品 |
| fat jar / diff / output scan | pass | 相关 common/system jars 均存在；路径合规且无敏感输出 |

- **E2E：** not-required；真实 Nacos 推送与双实例行为由 T-06 required E2E 复验。

## 6. 双轴审查

### 标准轴

- participant `prepare` 无副作用，manager 仅在全部成功后以一个 `AtomicReference` 切换 overlay、三组 prepared snapshot 与状态。
- 业务请求不访问 Nacos；每次调用只读取一个不可变 snapshot，验证码并发切换测试未观察到字段撕裂。
- 三个模块显式依赖 common-nacos，full/core 构建均能按 reactor 拓扑解析。

### 规范轴

- 精确键 API 避免把 `download-ttl-min/max` 误报为即时生效；其他合法键仍进入等待重启。
- 启动先收到远端、后注册 participant 时若领域校验失败，manager 清空候选 overlay、重新 prepare 本地基线并清除候选 digest。
- Nacos 关闭上下文没有 manager Bean，但三个原属性 Bean 和 participant 均正常创建，既有构造方式保持可用。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Aggregate parent before | `f8470f717910f2f272afc46e4edd6b83404c05ee` |
| Backend implementation | `c68cb55d50a1ab874da1ae428b2bd1bd7828dc74` |
| Aggregate result | `a8f993eb401f67f166ecc87e8cf99e7781fb28a5` |
| Method/conflicts | direct-parent；无冲突；attempt 1 |
| Gitlink | aggregate result 指向 backend implementation SHA |
| Verification/E2E | passed / not-required |

## 8. 偏差与决策

- 原 T-01 SPI 只有前缀分类，无法正确表达 OSS 单键。T-02 在 Ticket 中显式扩展了三个 common-nacos 可写文件，增加向后兼容的 `exactKeys()` 默认方法；既有参与者无需修改。
- 启动时序测试进一步发现 participant 晚注册拒绝可能保留早期 overlay，已在同一最小 Manager 接缝修正为本地基线恢复并补回归。
- core 继续使用 `maven.test.skip=true`，原因是仓库既有 admin tests 引用 full-only demo/workflow；本 Ticket 的 65 个相关测试已在 full reactor 单独真实执行。
- Speculo `--repo` clean 检查无法区分聚合仓既有用户 dirty 和顺序 Ticket 后的新 HEAD；结构校验不带 `--repo`，SHA/gitlink 与路径边界独立核对。

## 9. 残余风险与交付定位

- 真实 Nacos listener、双实例收敛、断连与恢复仍由 T-06 关闭；本 Ticket 使用真实 manager 但不访问网络。
- 当前清单不会自动扩展。新增动态键必须新增 participant、领域约束、分类和真实业务行为证据。
- 未执行部署、生产配置发布、push、secret 设置或工作树清理。

## 10. T-06 启动边界修正与关闭

真实矩阵发现跨字段非法配置若恰好驻留于 Nacos，EnvironmentPostProcessor 会早于 participant Bean 创建；原实现可先把非法值绑定进本地字段，再在晚注册时失去可信基线。backend `ebc2fef99d4ee33121de298d68a75781cbd2109c` 在通用候选阶段用本地有效值补全并预检已锁定的 notify/OSS 跨字段合同，participant prepare 仍作为第二道门；aggregate `60d5ae9` 提升 gitlink。common-nacos 16 tests 与最终真实 `PARTICIPANT_REJECTED`/后续合法发布/离线重启矩阵全部通过。
