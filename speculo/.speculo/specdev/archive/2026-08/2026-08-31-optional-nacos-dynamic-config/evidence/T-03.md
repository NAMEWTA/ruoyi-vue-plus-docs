# Evidence: T-03 - 交付鉴权持久化的 Nacos Docker 基础设施

- **Change：** `2026-08-31-optional-nacos-dynamic-config`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/03-nacos-docker-infrastructure.md</Path>`
- **Workspace/branch：** current / aggregate `main`
- **Parent before/result SHA：** `a8f993eb401f67f166ecc87e8cf99e7781fb28a5` / `2cfc2cbfe80bed5e18556694c22ce2158ffead05`
- **Implementation SHAs：** `87fdf058e5ba0e4b120a2c99c3854781192781b0`、`2cfc2cbfe80bed5e18556694c22ce2158ffead05`
- **状态：** done

## 1. 实现摘要

发布基础设施新增固定官方 `nacos/nacos-server:v2.5.4` standalone 服务、MySQL 持久化、鉴权必填变量、回环端口绑定、真实 readiness healthcheck 和可选 `nacos-enabled` override。Nacos 使用独立数据库与仅 `SELECT/INSERT/UPDATE/DELETE` 的账号；官方 2.5.4 schema 被固定并提供 fresh volume hook 与 existing volume 幂等初始化脚本。基础 backend/infrastructure 仍可独立解析，只有组合 override 时才为两台 admin 注入启用变量与 Nacos healthy gate。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `codex:/root`
- **执行模式：** current workspace，未派发 subagent，未创建 source/candidate worktree
- **Implementation checkpoints：** `87fdf058e5ba0e4b120a2c99c3854781192781b0`、`2cfc2cbfe80bed5e18556694c22ce2158ffead05`
- **Aggregate result：** `2cfc2cbfe80bed5e18556694c22ce2158ffead05`
- **Lead 核对：** 精确路径 diff、release tests、Compose 正反向解析、官方 schema 来源与真实远程 Docker E2E 均已读取

## 3. 修改范围与路径所有权

| 路径 | 目的 |
|---|---|
| `<Path>release-artifacts/docker/docker-compose-infrastructure.yml</Path>` | 固定 Nacos 服务、MySQL、鉴权、持久目录与健康检查 |
| `<Path>release-artifacts/docker/overrides/nacos-enabled.yml</Path>` | 显式启用双 admin、相同配置单元与健康门 |
| `<Path>release-artifacts/docker/infrastructure/mysql/init/nacos/**</Path>` | 固定官方 schema、来源摘要与 fresh hook |
| `<Path>release-artifacts/scripts/init-nacos-mysql-container.sh</Path>` | existing-volume 幂等初始化 |
| `<Path>release-artifacts/.env.example</Path>` | 无可工作默认值的变量合同 |
| `<Path>release-artifacts/tests/release-config.test.mjs</Path>` | 四类 Compose、缺 secret、端口、权限与清理回归 |

## 4. 验收与合同映射

| 检查 | 结果 |
|---|---|
| `node --test release-artifacts/tests/release-config.test.mjs` | pass；15 tests |
| Shell/YAML/schema 校验 | pass；两个初始化脚本语法有效，schema 精确 10 表 |
| 官方 schema SHA-256 | `5f8292d8add62e4275ad103cdd5757ec6b2abb77a01e2d06cf0434c3f0b6317d` |
| Compose 5.5.0 base config | pass；backend 与 infrastructure 各自可解析 |
| Compose 5.5.0 enabled override | pass；两 admin 启用且依赖 healthy Nacos |
| 六类必填变量逐一缺失 | pass；均在 configuration stage 明确失败 |
| release-manage staging | pass；保留 Nacos 受管资产并清理旧顶层生成 SQL |

## 5. Workspace Verification

current-workspace 完成静态验证；用户授权使用远程中间件服务器执行 required E2E，测试资产隔离在 `/data/namewta-data/specdev/optional-nacos-dynamic-config`。未停止或修改既有 CDE Nacos，未替换现有 Compose，未删除卷。

| 行为 | 结果 |
|---|---|
| Docker/Compose | Docker 26.1.4、Compose 2.27.1 |
| 官方镜像 | `nacos/nacos-server@sha256:ac1ed5b84d5246fcfe051817f38e5bd8e3a0aa91a6430bc394d929476a3241f4`，amd64 |
| 端口与健康 | `127.0.0.1:18848/19848`；readiness HTTP 200；restart count 0 before test restart |
| existing-volume init | 对既有 MySQL 连续运行两次均成功；其他数据库列表不变 |
| fresh-volume init | MySQL 8.4.9 新数据目录自动导入，再次运行 hook 仍成功 |
| schema/权限 | 两条路径均为精确 10 表；schema 权限仅 DML，global 仅 USAGE；DDL probe 被拒绝 |
| 鉴权 | 匿名 config GET HTTP 403；首次强密码初始化与登录 HTTP 200 |
| namespace | `local`、`dev`、`prod` 创建成功并可列出 |
| 发布与读取 | `ruoyi-namewta.yml` 发布 HTTP 200；读取 SHA-256 `8a92880823f803facf9f283f988701c43fd53ebbea82bfe99633facb945cd9ce` 一致 |
| 持久化 | 主动重启 Nacos 后重新健康、重新登录；读取同一 SHA-256，MySQL history 有写入记录 |
| secret scan | DB 密码、auth token、identity key/value、admin 密码均未出现在容器日志；临时 env 权限均为 `0600` |

主动 `docker restart` 时日志出现 9 条 `Event listener exception`，上下文均为关闭阶段队列线程收到 `InterruptedException`；重启后 readiness、登录与持久化读取全部通过，因此不属于运行故障。

### 合同结果

| 合同 | 结果 |
|---|---|
| AC-006 | pass；Nacos 是可选 override，基础 backend/infrastructure 独立有效 |
| AC-018 | pass；固定官方镜像、standalone、MySQL、鉴权和真实健康检查 |
| AC-019 | pass；六类必填 secret 缺失均失败，仓库无可工作默认值，日志扫描无命中 |
| AC-020 | pass；只有 enabled override 为两实例注入配置并增加 healthy gate |
| AC-021 | pass；发布内容在 Nacos 容器重启后由 MySQL 持久化读回同一 digest |
| AC-024 | pass；fresh/existing 两种初始化路径均幂等，独立 DB/user 最小权限成立 |

## 6. 双轴审查

- 标准轴发现 release staging 可能遗留旧顶层 SQL，已在 hardening checkpoint 中改为保留受管 Nacos 目录并删除旧生成文件。
- 规范轴发现历史账号可能保留更高权限，初始化改为先撤销旧权限再授予 DML；真实 `SHOW GRANTS` 与 DDL probe 共同验证。
- base profile 使用 `[nacos]`，override 以 `profiles: !reset []` 显式启用；固定四类顶级 Compose 未被增加或破坏。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Aggregate parent before | `a8f993eb401f67f166ecc87e8cf99e7781fb28a5` |
| Implementation checkpoints | `87fdf058e5ba0e4b120a2c99c3854781192781b0`、`2cfc2cbfe80bed5e18556694c22ce2158ffead05` |
| Aggregate result | `2cfc2cbfe80bed5e18556694c22ce2158ffead05` |
| Method/conflicts | direct-parent；无冲突；attempt 2（首次因本机无运行时失败） |
| Verification/E2E | passed / passed |

## 8. 偏差与决策

- 本地没有容器运行时，用户随后明确提供并授权远程中间件服务器；E2E 改在服务器的 specdev 隔离目录执行，未改变产品或验收合同。
- 既有 NAMEWTA MySQL 只执行可重复的独立 `nacos` DB/user/schema 初始化；fresh path 使用独立容器和数据目录，既有 CDE Nacos 完全不动。

## 9. 残余风险与交付定位

- standalone Nacos 仅面向可信网络，不是生产 HA；端口默认回环绑定，明文配置与简单鉴权风险仍需在 T-06 文档明确。
- 双 ruoyi-admin 真实收敛、运行断连与 iframe 浏览器链路分别由 T-06、T-05 关闭。
- T-06 最终 Gate 已精确清理全部 T-03/T-05/T-06 测试容器、网络和专用 specdev 目录；既有 CDE Nacos 与 NAMEWTA 中间件保持运行。

## 10. T-06 Fresh-volume 修正

最终门禁首次导入完整业务 SQL 时发现 MySQL entrypoint 客户端未显式使用 UTF-8，15 个中文字符被按 31 个字节解释并撞上 `varchar(30)`。backend `15fa812cb7cd56a16b4c10bb76b0c454b8f81586` 为五份缺声明的源 SQL 增加 `SET NAMES utf8mb4`（`ry_job.sql` 已有），aggregate `52ba232` 增加六份源/stage 一致性回归。后续 fresh MySQL 完整初始化与最终运行矩阵通过。
