# Evidence: T-04 - 在系统管理中提供 Nacos 官方控制台入口

- **Change：** `2026-08-31-optional-nacos-dynamic-config`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/04-nacos-console-menu.md</Path>`
- **Workspace/branch：** current / aggregate `main` + frontend/backend `main`
- **Parent before/result SHA：** `62d1aca6afb8c0b783d340db0446335f10b835fe` / `f8470f717910f2f272afc46e4edd6b83404c05ee`
- **Frontend implementation SHA：** `da5318e254238a2965ddf62bbd20da39413d1617`
- **Backend implementation SHA：** `3161aa769d834a67ff9ef9a8833f0a49cf25c74d`
- **状态：** done

## 1. 实现摘要

新增 `nacos` external target，将唯一入口权限固定为 `system:nacos:console`，并通过共享 domain 映射供通用 external view 与 admin manifest 使用。开发 URL 固定为 `http://localhost:8848/nacos/`，生产 URL 固定为 `/nacos/`。NAMEWTA DML 以固定菜单 ID `2094360621561675790` 在系统管理下幂等新增“配置中心”，带冲突 preflight，不授予任何普通角色。iframe 仍使用 Nacos 官方独立登录，不实现 CRUD、SSO 或凭据注入。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead `codex:/root`
- **执行模式：** current workspace，未派发 subagent，未创建 source/candidate worktree
- **Frontend return：** `da5318e254238a2965ddf62bbd20da39413d1617`
- **Backend return：** `3161aa769d834a67ff9ef9a8833f0a49cf25c74d`
- **Aggregate return：** `f8470f717910f2f272afc46e4edd6b83404c05ee`
- **Lead 核对：** 两个 implementation commit、aggregate gitlink、允许路径、测试数量和 DML 镜像摘要均已独立读取

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 目的 |
|---|---|---|
| `<Path>plus-ui-namewta/packages/domains/system/src/monitor/**</Path>` | writable:T-04 | target、统一权限映射与测试 |
| `<Path>plus-ui-namewta/apps/admin-web/src/views/monitor/external/index.vue</Path>` | writable:T-04 | 消费 domain 权限映射 |
| `<Path>plus-ui-namewta/apps/admin-web/src/router/adminManifestRegistry*</Path>` | writable:T-04 | 注册动态 component |
| `<Path>plus-ui-namewta/apps/admin-web/.env.*</Path>`、`env.d.ts` | writable:T-04 | 开发/生产 URL 合同 |
| `<Path>ruoyi-vue-plus-namewta/script/sql/namewta/DML.sql</Path>` | writable:T-04 | 固定且幂等的菜单增量 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/nacos/menu/**</Path>` | writable:T-04 | 菜单合同回归 |
| `<Path>release-artifacts/docker/infrastructure/mysql/init/60-namewta-dml.sql</Path>` | generated:T-04 | 当前发布树的机械镜像 |

- 未修改只读的 `ry_vue.sql` 或 Nginx 路径，未新增后端 Nacos CRUD/API。
- release 的 `60-namewta-dml.sql` 是 `.gitignore` 下生成物，当前工作区与后端源 DML 字节一致；权威可提交源是 backend DML，T-06 会通过 release 生成器复建。
- 聚合仓既有用户 dirty 未清理、未暂存；父提交只提升两个 gitlink。

## 4. 验收与合同映射

| 合同 | 验证接缝 | 结果 |
|---|---|---|
| AC-015 | domain permission、menu DML、manifest tests | pass；无权限不产生安全 intent |
| AC-016 | external target/env/manifest | pass；进入官方 iframe，仍由 Nacos 登录 |
| AC-022 | source/build/DML scan | pass；无账号、密码、token 或自动填写 |
| AC-024 | DML preflight/idempotent insert/mirror digest | pass；固定 ID 且无角色授权 |

## 5. Workspace Verification

| current-workspace 命令 | 结果 | 摘要 |
|---|---|---|
| domain Vitest | pass | 9 files / 69 tests |
| admin-web Vitest | pass | 8 files / 41 tests |
| `NacosMenuContractUnitTest` Maven reactor | pass | 37-module reactor，1 contract test |
| domain lint + typecheck | pass | target union/permission map 类型闭环 |
| admin-web lint + typecheck | pass | env/manifest/view 类型闭环 |
| admin-web production build | pass | 仅保留项目既有 ineffective dynamic import warnings |
| `shasum` backend DML + release 60-DML | pass | 两者均为 `1281fd1a...`，字节一致 |
| `git diff` / `git show` 路径审计 | pass | 子提交范围与 Ticket writable paths 一致 |

- **Red evidence：** 实现前 domain 测试因缺 `nacos` 权限映射失败，manifest 测试因缺 component 注册失败。
- **E2E：** not-required；真实同源 iframe、资源加载与官方独立登录由 T-05 required E2E 关闭。

## 6. 双轴审查

### 标准轴

- target、permission、component 与 env URL 各自有单一注册点；view 不再自行拼接权限字符串。
- DML 在写入前检查父菜单、固定 ID 现有值以及 component/permission 冲突，重复执行不产生第二行。
- 前端沿用现有 external view 与 URL 安全策略，未建立平行页面或额外 API。

### 规范轴

- 菜单位于系统管理，唯一 RuoYi 权限为 `system:nacos:console`；未自动绑定普通角色。
- 生产只声明根相对 `/nacos/`，代理实现留给 T-05；RuoYi 与 Nacos 身份边界保持独立。
- 固定官方 Nacos 登录没有 secret 注入，符合无 SSO/无凭据自动填充决策。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Aggregate parent before | `62d1aca6afb8c0b783d340db0446335f10b835fe` |
| Frontend implementation | `da5318e254238a2965ddf62bbd20da39413d1617` |
| Backend implementation | `3161aa769d834a67ff9ef9a8833f0a49cf25c74d` |
| Aggregate result | `f8470f717910f2f272afc46e4edd6b83404c05ee` |
| Method/conflicts | direct-parent；无冲突；attempt 1 |
| Gitlinks | aggregate result 同时指向上述 frontend/backend SHA |
| Verification/E2E | passed / not-required |

## 8. 偏差与决策

- backend 单仓测试不能依赖聚合仓中被忽略的 release 生成物，因此 Java 合同测试只验证权威 DML；发布镜像用聚合工作区 `cmp`/`shasum` 独立证明，最终由 T-06 生成器复验。
- 聚合仓保持既有 dirty 基线，Speculo `--repo` clean 检查不适合顺序 Ticket 后的 current workspace；结构 validator 不带 `--repo`，Git SHA/gitlink 和路径边界另行核对。
- 没有为了控制台加入自建 API。该取舍遵循用户确认的官方页面内嵌方案，并把配置操作授权保留给 Nacos。

## 9. 残余风险与交付定位

- `/nacos/` 的 Nginx rewrite、静态资源和 WebSocket 行为尚未真实浏览器验证，由 T-05 关闭。
- 菜单脚本尚未在 fresh/existing MySQL 双场景真实重放，由 T-06 最终 Gate 复验。
- 未执行生产 DML、角色授权、部署、push、secret 设置或工作树清理。
