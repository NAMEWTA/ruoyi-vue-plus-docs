# Evidence: T-01 - 收敛 Admin 运行能力数据库最终态

- **Change：** `2026-09-01-admin-runtime-capability-reconciliation`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/01-reconcile-admin-runtime-database.md</Path>`
- **Workspace/branch：** current / aggregate `main` + backend `main`
- **Aggregate parent before/result：** `1bae24677e601b6313820a3198fc0e8690ab8248` / `a21d0fdb7fa8782cfd83afa9237c74a5311cfa45`
- **Backend implementation/follow-up：** `9f5d382ada11e4bbc01bb7b49ca8ed4c6770f6ef` / `c7e926c8ac663b1fc83e1df2abf39ffcebbecef3`
- **状态：** done

## 1. 实现摘要

- 在 NAMEWTA DDL/DML 尾部追加固定标识块；历史 SQL 字节不改写。
- OpenAPI 固定六条菜单收敛到“系统管理 > OpenAPI管理”，Nacos 固定入口收敛到“系统监控 > Nacos配置中心”。
- 精确删除九条生成器菜单/角色关系；仅当两表为空、均为 `BASE TABLE` 且冻结 ID 列为主键时删除两张表。
- 冲突测试推动生成器菜单身份和表身份检查收紧。

## 2. Lead Dispatch And Candidate Return

- **Implementation/integration owner：** Lead `codex:/root`
- **执行模式：** current/direct-parent；未创建 subagent、source worktree 或 candidate worktree
- **Backend return：** `9f5d382ada11e4bbc01bb7b49ca8ed4c6770f6ef`，无备份说明 follow-up `c7e926c8ac663b1fc83e1df2abf39ffcebbecef3`
- **Aggregate return：** SQL checkpoint `1bae24677e601b6313820a3198fc0e8690ab8248`，T-01 result `a21d0fdb7fa8782cfd83afa9237c74a5311cfa45`
- **Lead 核对：** commit、gitlink、允许路径、测试计数、目标 DB 写前/最终态及重放均已独立读取

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 目的 |
|---|---|---|
| `<Path>ruoyi-vue-plus-namewta/script/sql/namewta/DDL.sql</Path>` | writable/shared:T-01 | 追加生成器表身份、0-row preflight 与幂等退役 |
| `<Path>ruoyi-vue-plus-namewta/script/sql/namewta/DML.sql</Path>` | writable/shared:T-01 | 追加菜单冲突 preflight 和最终态收敛 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/migration/adminruntime/**</Path>` | writable:T-01 | 静态及隔离 MySQL 正负向矩阵 |
| 两个既有 SQL prefix contract test | writable:T-01 | 以实施前 HEAD 重建失真的历史字节门 |

- 未修改 Ticket read-only 路径；未提交生成物或凭据。
- 聚合仓其他用户 change 与全局状态保持未暂存、未修改。

## 4. 验收与合同映射

| 合同 | 结果 |
|---|---|
| AC-005 | 六条 OpenAPI 菜单存在，主入口为“OpenAPI管理”且位于系统管理 |
| AC-006 | Nacos 为“Nacos配置中心”，父菜单为系统监控，稳定 path/component/perms 未变 |
| AC-007/008/009 | 九条生成器菜单与角色关系为 0，两张表不存在，四项诊断均为 `1` |
| AC-010/011 | 真实目标库重放指纹稳定；隔离冲突场景在目标写入前失败 |
| AC-013 | OpenAPI/Nacos 普通角色授权为 `0`，迁移未扩大权限 |

## 5. Workspace Verification

| current-workspace Gate | 结果 | 摘要 |
|---|---|---|
| 静态 + 隔离 MySQL 矩阵 | pass | `9/9`；真实数据库场景 `7/7`，含 fresh/upgrade/replay/conflict/非空/身份错误 |
| `./mvnw -pl ruoyi-admin -am test` | pass | `328` tests，`0` failure，`23` 个环境型 skip |
| 无备份说明聚焦回归 | pass | `14` tests，`0` failure；无 JDBC 时 `7` 个环境型 skip |
| Speculo implement validator | pass | 实施候选阶段 `0 error / 0 warning` |
| `git diff --check` | pass | backend 与 aggregate 均通过 |

### 开发库写前 Gate

- **目标：** DBX 连接中的 `ry-namewta`，MySQL `8.4.9`；未记录 endpoint 或凭据。
- 用户通过 `USER-DECISION:2026-09-01-no-backup` 明确接受本次无迁移前备份和前向恢复边界。
- `gen_table` / `gen_table_column` 均为 `BASE TABLE`，`table_id` / `column_id` 均为 `PRI bigint`，行数为 `0 / 0`。
- 九条生成器菜单完整匹配历史固定身份，三个父节点无额外子菜单。
- OpenAPI 固定 ID 数量为 `0`；Nacos 唯一历史行完整匹配；OpenAPI/Nacos component/perms 冲突均为 `0`。
- OpenAPI/Nacos 及生成器目标菜单的迁移前普通角色关系均为 `0`。

### 目标库迁移与重放

- DDL 自身 preflight 返回 `gen_table_rows=0`、`gen_table_column_rows=0`、`preflight_ok=1` 后删除空表。
- DML 自身 preflight 返回 `preflight_ok=1`。
- 从已提交 SQL 再次重放 DDL/DML，前后菜单指纹均为 `2d76cc06b7238381aead6178ed5bf0ba`，目标 role grants 均为 `0`，生成器表计数均为 `0`。

## 6. 双轴审查

### 标准轴

- append-only 块没有改写历史 SQL；表删除同时要求空表、`BASE TABLE` 和冻结主键身份。
- DML 在持久写入前完成父菜单、固定 ID、完整生成器身份、额外子节点、component 和 permission 冲突检查。
- 删除范围只含九个固定 menu ID 和对应 role-menu；不按名称或前缀宽泛删除。

### 规范轴

- OpenAPI/Nacos 最终名称、父菜单、固定 ID、path/component/perms 与 ADR/Spec 一致。
- 未自动向普通角色授权；开发库真实迁移、重放及最终态覆盖 AC-005..011/013。
- 无备份决定只覆盖目标开发库，没有扩展到生产/CDE 或降低 T-02 secret/逐实例 Gate。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Backend implementation | `9f5d382ada11e4bbc01bb7b49ca8ed4c6770f6ef` |
| Backend no-backup follow-up | `c7e926c8ac663b1fc83e1df2abf39ffcebbecef3` |
| Aggregate SQL checkpoint | `1bae24677e601b6313820a3198fc0e8690ab8248` |
| Aggregate result | `a21d0fdb7fa8782cfd83afa9237c74a5311cfa45` |
| Gitlink | aggregate result 指向 backend follow-up `c7e926c8ac663b1fc83e1df2abf39ffcebbecef3` |
| Method/conflicts | direct-parent continuation；无 Git 冲突；attempt 2 |
| Verification/E2E | passed / passed |

## 8. 偏差与决策

- 用户在 G1 后明确决定“无需备份”；ADR-005 接管恢复边界，0-row、对象身份和冲突 Gate 未降低。
- DBX 在第一条持久化 DML 前拒绝显式 `START TRANSACTION`，当时没有菜单写入。Lead 随后在同一 pinned session 设置 `autocommit=0`，重跑完整 preflight 和 DML，恢复 `autocommit=1` 一次性提交；错误路径固定为关闭 session 触发回滚。
- 目标重放采用同一事务方法，最终指纹、角色授权和 schema 状态均稳定。

## 9. 残余风险与交付定位

- 本 change 没有生成器表或迁移前菜单的恢复副本；数据库失败只允许保持 OpenAPI disabled、修正冲突并前向重放。
- 数据库最终态已经关闭 T-02 的依赖 Gate；OpenAPI 运行开关、双实例 HTTP/log 和登录浏览器验收仍由 T-02 完成。
- 未执行 push、生产/CDE 写入、普通角色授权、volume 删除或工作树清理。
