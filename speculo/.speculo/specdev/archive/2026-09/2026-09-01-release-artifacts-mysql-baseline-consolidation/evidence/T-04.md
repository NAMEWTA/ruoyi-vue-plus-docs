# Evidence：T-04 删除后端旧 script 树

- **Lead：** `codex:/root`
- **Workspace/branch：** 父仓与后端 `current-workspace` / `main`
- **Backend source/result：** `6927ee9786ba87f432127f4feda61c0f43c01849`
- **Parent result：** `9dc99e715d8ba542dcee547205504cf9d74c32a4`
- **状态：** done

## 1. 实现摘要

在 SQL、Workflow 和测试消费者迁移后，删除后端整个 `script/`，包括旧 MySQL 副本、Docker/bin、Workflow JSON 以及 Oracle、PostgreSQL、SQL Server 方言；没有创建软链接或兼容副本。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** `codex:/root`，current-workspace 直接实施。
- **返回：** 后端删除提交 `6927ee9`、路径扫描、构建和 JAR 校验结果。
- **Lead 独立核对：** pass；逐项复核删除清单和保留资产。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 目的 |
|---|---|---|
| `ruoyi-vue-plus-namewta/script/**` | writable:T-04 | 退出旧资产树及非 MySQL 方言 |
| 后端 README/配置注释 | writable:T-04 | 去除旧路径引用 |

删除仅作用于 Git 跟踪的源码资产，不触达服务器或数据库数据。

## 4. 验收与合同映射

AC-004/005/006：`script/` 不存在；活动测试、配置、README 和上游说明中的旧路径为零；保留 SQL/Workflow 资产仍完整。

## 5. Workspace Verification

| 命令 | 环境 | 结果 |
|---|---|---|
| `test ! -d script` 与活动引用扫描 | 后端 current-workspace | pass |
| `./mvnw test` | 后端 current-workspace | pass，42 模块 |
| full/core `clean package` + JAR 校验 | 后端 current-workspace | pass |

E2E 为 not-required：目录收缩由静态扫描、全量测试和产物校验覆盖。

## 6. 双轴审查

- **标准轴：** pass；先扩展迁移再收缩，Git 历史可恢复。
- **规范轴：** pass；项目当前仅支持 MySQL 8.4。

## 7. Integration Verification

后端 current-workspace direct-parent 结果 `6927ee9`，父仓 gitlink 结果 `9dc99e7`；无冲突。

## 8. 偏差与决策

无活动偏差；非 MySQL 方言按用户明确决定直接删除且不迁移。

## 9. 残余风险与交付定位

历史版本仍可从 Git Tag 审计；新迭代不得恢复 `script/`。结果为后端 `6927ee9`、父仓 `9dc99e7`。
