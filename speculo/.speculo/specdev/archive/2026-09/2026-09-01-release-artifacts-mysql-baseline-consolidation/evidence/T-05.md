# Evidence：T-05 对齐治理与活动实施工件

- **Lead：** `codex:/root`
- **Workspace/branch：** `current-workspace` / `main`
- **Source/result：** `9dc99e715d8ba542dcee547205504cf9d74c32a4`
- **状态：** done

## 1. 实现摘要

父/后端 README、增强说明、上游边界、OSS 运维文档和相关 Skill 已统一为：父仓六份 MySQL 8.4 完整基座是唯一事实源，50 只含 DDL、60 只含 DML，迭代直接修改。已有库必须按源/目标 Git Tag、备份、差异评审、隔离演练和私密报告升级。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** `codex:/root`，current-workspace 直接实施。
- **返回：** 治理 diff、当前权威引用扫描、活动 Change 协调记录。
- **Lead 独立核对：** pass。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 目的 |
|---|---|---|
| `.agents/skills/**` 当前相关规范 | writable:T-05/Lead | 固化唯一基座和升级边界 |
| 当前 README、增强说明、上游与 OSS 文档 | writable:T-05 | 同步调用规范 |
| 活动 account-profile Change | writable:T-05/Lead | 迁移路径并保留 checkpoint 偏差 |

未改写 completed、archived 或历史 Evidence。

## 4. 验收与合同映射

AC-005/006/011/013/014：当前权威只声明新 owner、MySQL-only、直接修改完整基座和 Tag 差异升级；历史材料保持审计语义。

## 5. Workspace Verification

| 命令 | 环境 | 结果 |
|---|---|---|
| `release-integration-contract.test.mjs` | current-workspace | pass |
| 全部发布合同 | current-workspace | pass，31/31 |
| 当前权威旧 owner 扫描 | current-workspace | pass，仅剩明确否定旧合同的文字 |

E2E 为 not-required：这是治理和活动实施工件迁移。

## 6. 双轴审查

- **标准轴：** pass；项目、模块和部署 Skill 采用同一 owner。
- **规范轴：** pass；已有库禁止重放完整基座，敏感报告仍位于被忽略的 `temp/relase/`。

## 7. Integration Verification

current-workspace direct-parent 结果为 `9dc99e7`，无冲突；相关活动 Change 的 source checkpoint 只保留为审计记录。

## 8. 偏差与决策

活动 account-profile 的旧 checkpoint 禁止直接集成或恢复后端 `script/`，后续必须从最新后端重放代码并把 SQL 直接移植到 50/60。该约束已记录，不阻塞本 Change。

## 9. 残余风险与交付定位

后续活动 Change 恢复时需遵循其偏差记录。当前 Change 的治理结果提交为 `9dc99e7`。
