# Evidence: T-01 - module and public contracts

- Change: `2026-09-04-third-party-http-integration`
- Ticket: `T-01`
- Lead: `codex:/root`
- Workspace/branch: `current` / backend `main`
- Base/implementation/result: `a63c83ac3e8f999013a40fc350098023ebcbdf11` / `82cb1febf` / `82cb1febf`
- Status: `done`

## 1. Implementation

Added the `ruoyi-third` Maven module, explicit full/core Admin dependency wiring, and `org.dromara.third.api` contracts (`ThirdPartyGateway`, request/response records, and stable failure categories). Public contracts accept only provider/endpoint codes and constrained values.

## 2. Verification

| Command | Environment | Result |
|---|---|---|
| `./mvnw.cmd -pl ruoyi-modules/ruoyi-third -am -DskipTests compile` | backend current workspace | pass |
| `git diff --check` | backend current workspace | pass |

## 3. Review

- Standard axis: pass; module compiles on Spring Boot 4.1/Java 21.
- Contract axis: pass; no Entity, credential, or HTTP client is exposed.
- E2E: not-required; this Ticket is the API/module prefactor.

## 4. Integration

Direct-parent commit `82cb1febf` is the backend result. No remote action, deployment, production migration, or real provider call was performed.

## 5. Residual risk

Runtime persistence and execution behavior are intentionally delivered by T-02 through T-09.

## 2. Lead Dispatch And Candidate Return

Lead-owned current-workspace execution; implementation checkpoint `82cb1febf` was returned directly on backend `main`.

## 3. 修改范围与路径所有权

Backend module and public API paths are owned by T-01. Later persistence and runtime paths remain owned by their respective tickets.

## 4. 验收与合同映射

The module compiles and the public gateway contracts satisfy the API boundary acceptance criteria.

## 5. Workspace Verification

`current-workspace` compile and diff checks passed. No external provider or production service was contacted.

## 6. 双轴审查

Standard engineering and contract/API boundary reviews passed.

## 7. Integration Verification

Direct-parent integration passed with result `82cb1febf`; full suite and external E2E are covered by the release gate.

## 8. 偏差与决策

No deviation. Async delivery and provider-specific SPI remain explicitly out of scope for this prefactor ticket.

## 9. 残余风险与交付定位

Runtime integration is delivered by T-02 through T-09; release-level environment verification remains tracked by T-13.
