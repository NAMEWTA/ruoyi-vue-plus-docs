# Evidence: T-04 - endpoint metadata and URI security

- Owner: `codex:/root`
- Status: `done`
- Backend result: `751ecd439` (latest ruoyi-third integration checkpoint; supersedes `2dacefcfb`)

## Implementation

Endpoint metadata supports JSON/query/form requests and JSON/text/bytes responses. Provider inheritance and endpoint limits are enforced; the only endpoint override is a validated `headers` object, while the Provider base URL remains trusted and immutable at runtime. Relative path, template variable, HTTP method, request mode, response mode, declared query/header/body names, and runtime revalidation reject absolute URLs, host authorities, traversal, malformed templates, scripts, and undeclared values.

## Verification

| Check | Result |
|---|---|
| `ThirdEndpointSecurityTest` | 6 tests pass, including provider-origin, configured-header, declared-schema, and endpoint-override rejection |
| Backend compile | pass |
| Local HTTP zero-request SSRF/redirect matrix | pass; deterministic loopback Gateway/ApplicationContext tests reject SSRF and redirects with zero outbound requests |

## 2. Lead Dispatch And Candidate Return

Lead-owned current-workspace execution; backend result `751ecd439` was returned directly on `main`.

## 3. 修改范围与路径所有权

T-04 owns endpoint metadata validation and endpoint management. Provider identity, credential storage, cache invalidation, and transport execution remain behind their declared boundaries.

## 4. 验收与合同映射

AC-002/003/006/007/016/017 are covered by endpoint security tests, metadata whitelist checks, inherited configuration validation, and zero-request rejection cases.

## 5. Workspace Verification

`current-workspace` `ThirdEndpointSecurityTest` passed 6/6. Application-context and loopback HTTP tests also passed, covering absolute URL, `//host`, traversal, undeclared values, scripts, and redirect refusal.

## 6. 双轴审查

Endpoint paths are constrained relative metadata only; provider base URLs remain trusted and runtime validation is repeated before client construction. Arbitrary headers and executable templates are excluded.

## 7. Integration Verification

Direct-parent result `751ecd439` passed module and application-context checks; every rejected unsafe endpoint produced zero HTTP requests.

## 8. 偏差与决策

No deviation. The former unavailable-loopback note is superseded by the deterministic loopback test evidence.

## 9. 残余风险与交付定位

Multipart, streaming, and complex templates remain future explicit SPI work and are not enabled by this Ticket.
