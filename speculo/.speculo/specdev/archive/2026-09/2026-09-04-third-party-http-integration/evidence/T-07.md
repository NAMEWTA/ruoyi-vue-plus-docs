# Evidence: T-07 - RestClient gateway pipeline

- Owner: `codex:/root`
- Status: `done`
- Backend result: `751ecd439` (latest ruoyi-third integration checkpoint; supersedes `2dacefcfb`)

## Implementation

`ThirdHttpClientFactory` centralizes JDK client connection/read timeouts, redirect policy, and Spring `RestClient` conversion; a loopback contract proves typed `@HttpExchange` clients reuse that transport. Dynamic metadata calls use the fixed gateway pipeline. Provider adapters are selected by a duplicate-checked registry with provider-owned endpoint hook validation; endpoint `adapter_code` cannot select an unowned implementation, and adapters may explicitly limit supported endpoint codes. Runtime validation re-applies endpoint method/path/mode/schema, adapter code, and server-controlled header rules on every snapshot read; safe endpoint header overrides are merged between Provider shared headers and credentials. Each physical send enters a recorder start/finish hook with bounded sanitized headers, request/response bodies, and relative path. The final implementation keeps the layered chain intact and exposes persistence, crypto, resilience, configuration, and observability through ports instead of adapter-to-DAO imports.

## Verification

| Check | Result |
|---|---|
| Backend module compile | pass |
| URI/header/body whitelist and adapter registry source review | pass |
| `ParameterizedTypeReference<T>` JavaType response conversion | pass |
| `validate-module-mode.mjs ... --mode layered` | pass (71 Java files) |
| Transport failure attempt-count preservation | pass; failures record elapsed time and actual attempts |
| Caller header/path/metadata rejection and response-body redaction | pass; configured headers remain server-controlled |
| Deterministic loopback Gateway and typed client tests | pass; `ThirdGatewayAdapterTest` 5/5 verifies constrained JSON POST, provider-owned adapter rejection, provider-disabled and rate-limit zero-request behavior, and 302 classification without following; `ThirdHttpClientFactoryTest` verifies typed `@HttpExchange` over the shared `RestClient` |
| `ThirdGatewayApplicationContextTest` | pass; 3 tests, 0 failures/errors/skips, covering no-data startup, duplicate/missing adapter startup validation, typed responses, provider error mapping, 503/no-retry, 302 refusal, SSRF zero-request rejection, non-idempotent single attempt, and bounded idempotent retry |

## 2. Lead Dispatch And Candidate Return

Lead-owned current-workspace execution; backend result `751ecd439` was returned directly on `main`.

## 3. 修改范围与路径所有权

T-07 owns the gateway, pipeline, adapter registry, client factory, and URI/security ports. Resilience, persistence, and observability are consumed through explicit ports rather than bypassing their owners.

## 4. 验收与合同映射

AC-003/006/007/008/011/012 are covered by dynamic metadata validation, typed `@HttpExchange`, provider-owned SPI selection, stable response mapping, and zero-request unsafe input rejection.

## 5. Workspace Verification

`current-workspace` module-mode validation and deterministic loopback HTTP/ApplicationContext tests passed. Redirects and SSRF attempts produced no outbound request; typed calls reused the shared RestClient transport.

## 6. 双轴审查

Facade, Registry/Strategy, Factory, Pipeline, and interceptor ownership boundaries remain explicit. Callers cannot inject arbitrary headers, complete URLs, scripts, or persistence entities.

## 7. Integration Verification

Direct-parent result `751ecd439` passed all gateway and Spring context checks, including bounded retry and provider business-error mapping.

## 8. 偏差与决策

No deviation.

## 9. 残余风险与交付定位

Future non-standard payloads require a reviewed Java SPI; database scripts and arbitrary reflection remain forbidden.
