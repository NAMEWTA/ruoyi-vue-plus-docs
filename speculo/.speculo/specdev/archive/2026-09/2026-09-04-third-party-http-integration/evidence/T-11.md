# Evidence: T-11 - third-party administration pages

- Owner: `codex:/root`
- Status: `done`
- Frontend result: `cc6a6f2` (latest web-domain/Admin integration checkpoint; supersedes `d45a480`)

## Implementation

Added `@namewta/web-domain-third` with exactly four views: provider, endpoint, invocation, and statistics. Credential replacement/removal is a permission-guarded nested dialog in provider/endpoint workflows; no standalone credential page is registered. Endpoint editing exposes the constrained shared-header override JSON. Forms and tables use the typed domain service, explicit component keys, permission metadata, masked credential fields, and provider/endpoint edit, enable/disable, and delete controls.

## Verification

| Command | Result |
|---|---|
| `pnpm --filter @namewta/web-domain-third typecheck` | pass |
| `pnpm --filter @namewta/web-domain-third lint` | pass |
| `pnpm --filter @namewta/admin-web typecheck` | pass |
| `pnpm --filter @namewta/web-domain-third test` | pass; manifest test verifies exactly four page registrations |
| Admin manifest registry test mock and third Provider route | pass; `thirdService` is provided by the service mock and the route resolves to `ThirdProvider` |
| Full workspace typecheck/lint/test/build | pass; architecture includes the `third -> ruoyi-third` mapping and reports 101/101 tests |

Browser routing and permission behavior are verified by T-12 when the backend/menu stack is available.

## 2. Lead Dispatch And Candidate Return

Lead-owned current-workspace execution; frontend result `cc6a6f2` was returned directly on `main`.

## 3. 修改范围与路径所有权

T-11 owns `packages/web-domains/third` and exactly four page views. App composition, menu DML, and backend permission integration remain owned by T-12.

## 4. 验收与合同映射

AC-015/016 are covered by the four page registrations, nested credential workflow, endpoint override form, masked fields, and permission-aware mutation controls.

## 5. Workspace Verification

`current-workspace` web-domain/Admin typecheck, lint, tests, and full build passed. Component tests cover CRUD, refresh, credential masking, and permission states.

## 6. 双轴审查

Pages consume domain exports and use explicit component keys; no standalone credential route or sensitive DOM field is exposed.

## 7. Integration Verification

Direct-parent frontend result `cc6a6f2` passed web-domain and Admin package checks; menu/browser behavior is verified by T-12.

## 8. 偏差与决策

No deviation.

## 9. 残余风险与交付定位

New pages must be added through the manifest and permission metadata, preserving the four-page initial surface.
