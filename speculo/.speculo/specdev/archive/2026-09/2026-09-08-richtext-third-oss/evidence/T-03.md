# Evidence: T-03 — OSS 开发环境默认不再依赖固定前端 IP 才能通过 CORS

- **Change：** 2026-09-08-richtext-third-oss
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-09-08-richtext-third-oss/ticket/03-oss-dev-cors-default.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-09-08-richtext-third-oss/spec.md</Path>`
- **Goal Plan：** 不适用
- **Lead：** grok-build
- **Workspace/branch：** current / main
- **Base/implementation-or-source/candidate/result SHA：** `31096b665be5b6812d09d6400d365d08727e528a` / `35a871dc8b7541927c2dda4d2e8635500180809d` / not-applicable / `35a871dc8b7541927c2dda4d2e8635500180809d`
- **状态：** done

## 1. 实现摘要

本地 release compose 默认 `MINIO_API_CORS_ALLOW_ORIGIN` 从固定 Origin 白名单改为 `*`；`.env.example` 同步；排障文档写明 `*` 仅本地/联调，共享/生产必须显式 HTTPS 白名单。未改 Bucket policy 或对象访问语义。

实现提交：父仓 `35a871dc8b7541927c2dda4d2e8635500180809d`。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** 历史 current-workspace commit（2026-09-08）；本次 Lead 只审查与关闭
- **Dispatch Packet/checkpoint：** Lead direct
- **允许动作：** 审查、Evidence、状态关闭；不改项目代码
- **返回：** 父仓 result `35a871dc8b7541927c2dda4d2e8635500180809d`
- **Lead 独立核对：** pass；重读 compose、`.env.example` 与排障文档
- **只读 Agent findings：** `<Path>{roots.state}/specdev/changes/2026-09-08-richtext-third-oss/reviews/CR-001.md</Path>` approved

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>release-artifacts/docker/docker-compose-infrastructure.yml</Path>` | writable | 开发默认 CORS `*` |
| `<Path>release-artifacts/.env.example</Path>` | writable | 示例默认 `*` |
| `<Path>docs/error/oss-login-and-direct-upload-troubleshooting.md</Path>` | writable | 本地/生产策略说明 |

- **read-only 修改：** 无
- **未声明路径：** 无
- **生成文件/锁文件：** 无

## 4. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-003 | compose 默认值 + 文档 | `:-*` 与 `.env.example=*`；文档禁止生产沿用 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| 阅读 compose / `.env.example` / 排障文档 | current-workspace | pass | 默认 `*`；生产必须覆盖 |
| `docker compose --env-file release-artifacts/.env.example -f release-artifacts/docker/docker-compose-infrastructure.yml config --quiet` | current-workspace | not-run | 本机无 Docker CLI |
| 浏览器 OPTIONS/PUT 预检 | current-workspace | not-run | 无 MinIO/浏览器会话 |

- **失败后修复与重跑：** 无
- **未运行检查：** compose 展开与真实预检
- **E2E：** not-required。Docker 不可用；配置文本已满足“不再绑死前端 IP”

## 6. 双轴审查

### 标准轴

- **固定输入：** `31096b665be5b6812d09d6400d365d08727e528a..8b75866903c3fafc26b9f67f186c32005e690f5e`
- **结果：** pass
- **Findings 与修正：** `.env.example` 同时含 `SPRING_PROFILES_ACTIVE=prod` 与 CORS `*`。文档已禁止生产沿用。

### 规范轴

- **固定输入与来源：** Spec / T-03
- **结果：** pass
- **Findings 与修正：** 开发默认与生产收口说明与锁定决策一致。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `31096b665be5b6812d09d6400d365d08727e528a` |
| Implementation/source SHA | `35a871dc8b7541927c2dda4d2e8635500180809d` |
| Candidate branch/workspace | current / not-applicable |
| Method/conflicts | direct-parent；无 |
| Integration checks | 配置与文档复核 |
| E2E disposition | not-required: 无 Docker，未做真实预检 |
| E2E result | not-required |
| Parent result/re-read | `35a871dc8b7541927c2dda4d2e8635500180809d` 是当前 `main` 祖先 |

### Failure History And Lead Recovery

| 轮次 | 阶段 | Checkpoint/candidate | 失败事实 | 下一轮变化 |
|---|---|---|---|---|
| 1 | review | `35a871dc8b7541927c2dda4d2e8635500180809d` | 无 blocker | 关闭 Ticket |

- **共同失败模式：** not-applicable
- **最可能原因：** not-applicable
- **下一轮具体改变：** not-applicable
- **下一 owner/路由：** not-applicable

## 8. 偏差与决策

- **偏差：** 无
- **记录：** 不适用
- **批准来源及影响：** 用户要求以代码库为准审查后归档。

## 9. 残余风险与交付定位

- **残余风险/已知限制：** 未做真实 OPTIONS/PUT；若生产直接使用 `.env.example` 的 `*` 会放宽 Origin。
- **后续 Ticket：** 无
- **监控或回滚触发：** 共享/生产环境出现 `MINIO_API_CORS_ALLOW_ORIGIN=*`。
- **Source commit：** `35a871dc8b7541927c2dda4d2e8635500180809d`
- **Parent result：** `35a871dc8b7541927c2dda4d2e8635500180809d`
- **Source workspace：** current
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-09-08-richtext-third-oss/evidence/T-03.md</Path>`
