# Evidence T-04: 集成快照与最终验收

## Result

- Parent before：`0909fb782ad41da88dc8ccafa9e528c3e77a1c1a`
- Contract/pointer commit：`1cf768652444d23e1a41200d5ac02f888395768a`
- Backend pointer follow-up：`a64f04f808c74b7c31ee77a7e4ada79530eba56a`
- Final product snapshot commit：`a64f04f808c74b7c31ee77a7e4ada79530eba56a`
- Backend gitlink/HEAD：`eeab21d91689b1e5b40fcd87017c85658749fdc9`
- Frontend gitlink/HEAD：`adf5a0c5db3f7ebb1431c4c2fb87e29f987ef1bc`
- Delivery status：代码、文档、构建与父快照完成；required runtime E2E blocked。

## Integration Evidence

- `plan/update.md` 明确注册邮箱/手机号是后端可选字段，当前公开前端仍只提交核心字段。
- `docs/upstream/customization-map.md` 固化严格 Boolean、Long Client PK、默认角色失败关闭、Token-only 登录域与逐域会话清理合同。
- `git ls-tree HEAD plus-ui-namewta ruoyi-vue-plus-namewta` 与两个 clean child HEAD 精确一致。
- parent `git diff 0909fb782..HEAD --check`、两个 child `git diff --check` 均通过。
- 无 POM、package.json、lockfile、SQL、schema、依赖、测试源码或 dist 交付变化。
- CR-002 在最终组合上未发现新的代码或工程规范 finding；复核过程中发现的工作流角色调用回归已在 `eeab21d91` 修复并重新验证。

## Final Gates

- Backend targeted compile：21/21 SUCCESS。
- Backend `bash mvnw -Dmaven.test.skip=false test`：40/40 SUCCESS。
- Backend `bash mvnw clean package`：40/40 SUCCESS，14.872s；项目配置下测试跳过，因此仅作构建证据。
- Frontend `pnpm lint`：通过。
- Frontend 7 个修改文件 `oxfmt --check`：通过。
- Frontend `vue-tsc --noEmit --forceConsistentCasingInFileNames false`：通过。
- Frontend原始 `vue-tsc --noEmit`：仅有未修改上游 `logininfo`/`loginInfo` 的 TS1149 大小写冲突。
- Frontend `pnpm build:prod`：通过，3357 modules transformed。

## Blocked Runtime Acceptance

- `docker` 不存在；未创建 disposable MySQL/Redis 环境。
- 本机无 3306/6379 listener；`mysqladmin --connect-timeout=2 ping` 失败于 `/tmp/mysql.sock` 不存在。
- 因数据库与 Redis 不可用，后端、HTTP Token 和浏览器无法形成真实联调环境。
- 未执行且不得视为通过：fresh `ry_vue.sql -> 001 -> 002 -> 003`、004 两次幂等、五组多登录域/注册开关/跨 Client/会话失效人工矩阵。
- T-04、Goal Plan 与 change 保持 blocked/active；完成这些环境 Gate 后才能批准交付。
