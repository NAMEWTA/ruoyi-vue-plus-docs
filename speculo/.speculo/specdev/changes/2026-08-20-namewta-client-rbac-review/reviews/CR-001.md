---
schema_version: 1
artifact: code-review
change: 2026-08-20-namewta-client-rbac-review
review_id: CR-001
fixed_point: a16f2490247d3ba33683c46c74782c5dc2827f0c
head: a426d5f7aab841442f93864a8277a96155a7fbb6
status: request-changes
standards_result: request-changes
specification_result: request-changes
spec_sources: ["plan/update.md", "docs/upstream/customization-map.md"]
standards_sources: ["AGENTS.md", "ruoyi-vue-plus-namewta/.editorconfig", "plus-ui-namewta/.editorconfig", "ruoyi-vue-plus-namewta/pom.xml", "plus-ui-namewta/package.json", "adjacent implementation patterns", "git history"]
created_at: 2026-08-20T15:26:45+08:00
---

# Code Review CR-001

## Fixed Input

- **Backend diff:** `git diff a16f2490247d3ba33683c46c74782c5dc2827f0c...a426d5f7aab841442f93864a8277a96155a7fbb6`
- **Backend commits:** `git log a16f2490247d3ba33683c46c74782c5dc2827f0c..a426d5f7aab841442f93864a8277a96155a7fbb6 --oneline`
- **Frontend diff:** `git diff 0870ce17514895854ccff03600e102546d8c5046...3122cbac38e9118da8bd1e8cdc82b7dd22671f05`
- **Frontend commits:** `git log 0870ce17514895854ccff03600e102546d8c5046..3122cbac38e9118da8bd1e8cdc82b7dd22671f05 --oneline`
- **Parent snapshot:** `0909fb782ad41da88dc8ccafa9e528c3e77a1c1a`; recorded backend `8b802717caa37c58396f374b0b78524239ddf344`, frontend `03813b58fc54d88a219201dfff293ca0fb5458d4`.
- **Current upstream:** backend `upstream/6.X=387c4f0a20e9232f44e762ef5a46c462f54bd464`; frontend `upstream/6.X-Vue=0870ce17514895854ccff03600e102546d8c5046`.
- **Scope:** backend and frontend product diffs, parent submodule delivery snapshot, `plan/update.md`, upstream drift, build and acceptance gates. Speculo runtime files are review artifacts, not product diff.
- **Source discovery order:** root instructions and repository configuration; build/lint configuration; adjacent implementation patterns; product history; then `plan/update.md` and `docs/upstream/customization-map.md`. The two axes were evaluated independently and retain their own ordering.

## 标准

| Severity | Path / block | Finding | Authority | Satisfied when |
|---|---|---|---|---|
| P1 | Parent gitlinks for `ruoyi-vue-plus-namewta` and `plus-ui-namewta` | The parent records `8b802717c`/`03813b58f`, but the reviewed product heads are `a426d5f7a`/`3122cbac3`. A clean checkout therefore omits both final integration commits and is not a reproducible representation of the reviewed release. | Git submodule delivery integrity | Commit the two final gitlink values together after the child heads are accepted, then verify a recursive clean checkout resolves those exact SHAs. |
| P1 | `SysUserServiceImpl.java:937-953`; `SysRoleServiceImpl.java:782-794` | Both role-assignment validation paths treat missing roles, missing `role.clientId`, missing Client rows, and missing `client.userTypeId` as success via `continue`/`return`. Corrupt, stale, or manually seeded records can bypass the domain boundary instead of failing closed. | Authorization-boundary defensive validation | Reject every unresolved role/Client/domain relationship and reject requested role IDs that were not resolved; only a fully resolved enabled Client/domain may reach the ownership check. |
| P1 | `SysUserInfoVo.java:13-40`; frontend `src/api/system/user/types.ts:102-120`; frontend `src/views/system/user/index.vue:971-985` | The frontend contract declares `userTypeIds`, `explicitRoleIds`, and `defaultRoles`, but the backend VO cannot serialize those fields. It receives cross-Client role IDs with only current/no-Client role metadata, then silently falls back, so role labels and Client default roles are incomplete and raw IDs can be shown. | End-to-end API contract consistency | Define and populate explicit roles, their metadata, owned domains, and default roles in one authoritative response contract, or load each selected Client explicitly without declaring fields the backend never emits. |
| P2 | `LoginHelper.java:205-235` | Token context lookup catches broad `Exception` twice and reconstructs `userType` from two legacy sources. This hides invalid session context and makes authorization behavior depend on stale snapshot/login-id formats. | Explicit failure semantics; no swallowed authorization-context errors | Read the documented token extra directly and make missing/invalid context produce the caller's explicit empty/reject behavior without legacy reconstruction. |
| P2 | Backend `e3e69281`; frontend `719a2b83` | The product ranges begin with unrelated edits to local agent/skill instructions, and those commits point development guidance at the product branch being reviewed. They expand upstream conflict surface and are not a cohesive product change. | Focused commits and minimal change surface | Remove these operational-instruction changes from the product range or move them to a separately reviewed repository/process change. |

## 规范

| Severity | Requirement | Finding | Source | Satisfied when |
|---|---|---|---|---|
| P1 | Invalid Client/domain and cross-Client role operations must always be rejected | Role assignment fails open for unresolved role/Client/domain relations. Default-role resolution also accepts a role with `clientId == null` (`SysRoleServiceImpl.java:745-753`, `SysMenuServiceImpl.java:499-506`) instead of proving same-Client ownership. | `plan/update.md:169-170,199,205,260,264` | Every path requires an existing enabled domain, an existing Client, exact non-null Client ownership, and user-domain ownership; all other states throw/reject. |
| P1 | Role query entry points and user role management must be Client-scoped | `selectRoleListByUserId` deliberately returns all explicit roles when `clientId` is absent (`SysRoleServiceImpl.java:182-195`), backed by the unscoped mapper at `SysRoleMapper.java:103-119`. `SysUserController.getInfo` keeps `clientId` optional, and the user page calls it without a Client, contradicting the required Client-first flow and causing the incomplete display contract described above. | `plan/update.md:203-205,278,280` | Require `clientId` for scoped role reads/assignments, remove the unscoped mapper fallback, and return/display owned domains, explicit roles, and each Client's default role from an explicit contract. |
| P1 | Every stage needs fresh DB initialization and the manual acceptance matrix | No persisted evidence demonstrates fresh execution of the four SQL scripts or the five multi-domain/registration/cross-Client scenarios. This host has a MySQL client but no running server, and Docker is unavailable, so these gates could not be reproduced during review. | `plan/update.md:172,298-306` | Run the scripts against an empty supported database and record all five manual scenarios, including token isolation, routing, registration toggles, and forged cross-Client requests. |
| P1 | Parent repository must snapshot completed child milestones | The final backend and frontend heads are not recorded by the parent gitlinks; the parent stopped at each `docs(upstream)` commit. | `plan/update.md:152` | Bump both submodules to `a426d5f7a...` and `3122cbac3...` (or accepted successors) and validate the parent snapshot. |
| P2 | No compatibility layer or fuzzy fallback | Frontend `src/api/login.ts:10-24,79-84` accepts inverted numeric/string flags, coerces arbitrary values, and defaults a missing `clientEnabled` to `true`, although the documented API is JSON boolean. | `plan/update.md:169-170`; `docs/upstream/customization-map.md:25,49` | Use the Boolean response contract directly and reject/make unavailable a malformed or missing Client context; do not interpret `'0'`/`'1'`. |
| P2 | `LoginHelper.getUserType()` reads the token extra | `LoginHelper.java:218-235` first reads the extra, then falls back to `LoginUser.userType`, then parses the login ID. This is an explicit compatibility chain prohibited by the plan. | `plan/update.md:169,187,228` | Return only the token extra (with explicit missing-context semantics at the caller) and delete both fallback branches. |
| P2 | Registration validates username, phone, and email uniqueness | `RegisterBody` contains only username/password and `SysRegisterService.java:81-86` checks only username. Phone/email uniqueness is not implemented or expressible by the current request contract. | `plan/update.md:256` | Either add optional phone/email to the registration contract and validate each supplied value, or revise the plan explicitly if public registration is intentionally username-only. |
| P3 | `ClientSessionService` only exposes three listed methods | The service adds a fourth public `kickoutUser(Long)` at `ClientSessionService.java:65-75`; multiple user-service paths depend on the enlarged surface. | `plan/update.md:268-270` | Keep the three-method public contract and route global-user invalidation through an established session primitive or revise the stated contract with rationale. |
| P3 | Required commit split and no unrelated changes | The required baseline commit name is absent, both ranges include unrelated agent/skill edits, frontend registration is folded into `feat(auth)` instead of `refactor(register)`, and later broad `fix(api)`/final feature commits repair earlier stages. All functional commits build, but the specified reviewable split is not met. | `plan/update.md:288-294` | Rework the unpublished history to the prescribed cohesive stages, or document an approved deviation before release. |

### Requirement Coverage

| Stage | Result | Evidence / exception |
|---|---|---|
| 1-3: UserType persistence, legacy field removal, Client policy | pass | Models, relations, SQL, enum/field removal, and Client policy are present; final backend builds. |
| 4: Client-scoped roles | request-changes | Unscoped explicit-role fallback and fail-open assignment validation remain. |
| 5: Client-scoped menus/routes | pass with risk | Client-scoped entry points are present; null-Client default roles are not rejected. |
| 6: permission/token context | request-changes | Permission reads are scoped, but `LoginHelper.getUserType()` retains legacy fallback. |
| 7: five-strategy login admission | pass | All five strategies call the shared access service and construct Client/domain snapshots. |
| 8: registration | request-changes | Client/domain policy and transaction are present; phone/email uniqueness is missing. |
| 9-10: user management/default role | request-changes | Transactions and explicit/default separation exist; role validation and response/display contracts are incomplete. |
| 11: session invalidation | request-changes | Required triggers are broadly wired; public surface exceeds the mandated three methods. |
| 12-15: UserType/Client/role/menu UI | pass | CRUD and Client selectors are present; scoped user-role flow is covered under stage 16. |
| 16: user UI | request-changes | Client-first selector exists, but initial data is unscoped and the backend never emits declared summary fields. |
| 17: public auth context/register UI | request-changes | Both pages consume context and registration fields are removed; Boolean compatibility/defaulting violates the contract. |
| 18: dynamic routes | pass | Existing dynamic route mechanism is retained; backend performs Client filtering. |
| Git/upstream | request-changes | Current backend upstream drift is one non-overlapping Redis cache commit with no merge-tree conflict; parent final gitlinks are stale. |
| Builds | pass | Backend final build and 12/12 functional commits passed `clean package`; frontend final build and 8/8 functional commits passed lint/build; no dependency or lockfile changes; both diffs pass `git diff --check`. |
| Runtime acceptance | unverified | Fresh DB initialization and manual matrix were unavailable. Supplemental `vue-tsc --noEmit` fails on an unchanged upstream `logininfo`/`loginInfo` casing collision, not on this diff. |

## Summary

- **Standards findings:** 5 (`P1`: 3, `P2`: 2)
- **Specification findings:** 9 (`P1`: 4, `P2`: 3, `P3`: 2)
- **Most severe standards finding:** role-assignment validation fails open at the authorization boundary; the parent snapshot is also not reproducible.
- **Most severe specification finding:** strict Client/domain rejection and Client-scoped role management are not satisfied, and mandatory runtime acceptance evidence is absent.
- **Next Work:** `<Path>{roots.workflows}/specdev/T-tickets/T-tickets.md</Path>`
