# Evidence: T-03 — OSS TEMP、引用与下载授权

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/03-oss-lifecycle-reference-download.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/goal-plan.md</Path>`
- **Lead：** `cursor-agent`
- **Workspace/branch：** `current/backend` / `main`
- **Base/implementation-or-source/candidate/result SHA：** `7a37357b6f62b4464081d410e1a0baae6560d959` / `2a94b4dfa28241be9a702b7c2713bfd7362c3ef9` / `not-applicable` / `2a94b4dfa28241be9a702b7c2713bfd7362c3ef9`
- **状态：** done

## 1. 实现摘要

新增 `OssLifecycleManager`、`sys_oss_ref` 映射、Provider 接缝、默认关闭且 dry-run 的 TEMP 清理任务，以及 ruoyi-api additive bind/unbind/snapshot/presign 合同。生命周期变更、通用删除和清理均先锁定 `sys_oss` 行：首个引用退出 TEMP，最后解绑重新获得 24 小时窗口，有引用对象拒绝删除，清理在锁内重新检查引用。Provider 删除失败不删除数据库元数据。

`ref_type` 只接受实际物理表名格式，`ref_id` 保存该表真实主键；实现不动态查业务表、不推导 ACL，也没有 Client/tenant 行隔离。管理列表返回 TEMP 和真实引用坐标但不返回可用 URL；`system:oss:download` 专用入口返回结构化短时授权。普通业务只可在完成自己的权限和数据权限校验后调用内部 `OssService.presignDownload`。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead
- **Dispatch Packet/checkpoint：** Lead direct；backend base `7a37357b6f62b4464081d410e1a0baae6560d959`
- **允许动作：** current workspace changes、disposable local MySQL verification、implementation commit、direct-parent verification
- **返回：** backend commit `2a94b4dfa28241be9a702b7c2713bfd7362c3ef9`；用户 POM 保持 dirty；20 个 Ticket 路径；非 E2E 检查全部通过
- **Lead 独立核对：** pass；重读公共 API、锁顺序、Provider 失败边界、管理 URL 权限、缓存复制、测试报告、staged diff、commit 与 HEAD
- **只读 Agent findings：** 无；本计划未启用 subagent

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>ruoyi-vue-plus-namewta/ruoyi-api/src/main/java/org/dromara/system/api/OssService.java</Path>` | writable | 生命周期、引用和授权下载公共合同 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/src/main/java/org/dromara/system/oss/**</Path>` | writable | 引用映射、协调器、Provider 接缝和清理入口 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/src/main/java/org/dromara/system/{controller,domain,mapper,service}/**</Path>` | exact writable paths | 现有 OSS 管理面接入与生命周期字段 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/oss/lifecycle/**</Path>` | writable | 集中生命周期和合同测试 |

- **read-only 修改：** 无；common-oss 与 SQL diff 为空
- **未声明路径：** 无
- **既有用户改动：** `ruoyi-admin/pom.xml` 未暂存、未提交，patch SHA-256 保持 `1ea9de0c55c2718576868a792a7f8638a2c0d8fb9126eea365563d3d9c56d958`

## 4. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-008 | TEMP 清理 | 24h 默认、到期索引扫描、锁内 recheck、dry-run 默认开启 | pass |
| AC-009/010 | 引用状态机 | 真实表名/主键验证、幂等 bind、最后 unbind 重新 TEMP | pass |
| AC-011/029 | 删除保护 | 批量有引用时 Provider 零调用；Provider 失败保留元数据 | pass |
| AC-012/013 | 下载授权边界 | 管理入口精确权限、结构化 2 分钟 URL、内部 API 无 Client 参数 | pass |
| AC-026 | TEMP 兜底能力 | 未绑定对象可由关闭默认的 dry-run 清理接缝识别 | pass |
| AC-030 | Client/RBAC | OSS API、Mapper、引用和生命周期扫描无 Client/tenant 行隔离 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| focused lifecycle tests | current-workspace/backend | pass | 14 tests，0 failure/error/skip；首轮缺实现编译错误为预期红灯 |
| disposable MySQL row-lock competition | local MySQL 8.4 temp datadir | pass | 第二事务等待 1s，拿锁后观察第一事务提交的 `is_temp=N` |
| `sh mvnw -pl ruoyi-admin -am -Dmaven.test.skip=false test` | current-workspace/backend | pass | 36/36 reactor；32 tests，0 failure/error/skip |
| `sh mvnw -pl ruoyi-admin -am package -DskipTests` | current-workspace/backend | pass | 36/36 reactor SUCCESS |
| staged path/dynamic SQL/Client/hash audits | current-workspace/backend | pass | 无动态查表、无 Client/tenant 字段、用户 POM 哈希不变 |

- **失败后修复与重跑：** 首轮按 TDD 因生命周期包未实现产生预期编译错误；实现后仅测试 matcher 遇到 MyBatis-Plus insert 重载二义性，收窄为 `SysOssRef` 后全部转绿。复核时修正管理 VO 原地修改缓存对象和旧内部 API 回退持久 URL 的问题。
- **临时资源：** 一次性 MySQL 正常关闭后移入 `/Users/wta/.Trash`，未触碰共享或生产数据库。
- **E2E：** not-required；用户明确不执行 E2E，本 Ticket 以 ruoyi-admin 测试、静态权限合同和 disposable MySQL 竞争验证判卷。

## 6. 双轴审查

### 标准轴

- **固定输入：** `7a37357b6f62b4464081d410e1a0baae6560d959..2a94b4dfa28241be9a702b7c2713bfd7362c3ef9`
- **结果：** pass
- **Findings 与修正：** 管理视图复制后再置空 URL，避免污染缓存；Mapper 多参数显式 `@Param`；批量锁按 ossId 排序；测试集中 admin；用户 POM 未入提交。

### 规范轴

- **固定输入与来源：** `spec.md`、`ADR-001/002/005/009`、Ticket T-03、Goal Plan 与 implementation commit
- **结果：** pass
- **Findings 与修正：** 引用仅反向定位/生命周期；无动态查表或 ACL；Client 仅参与调用入口 RBAC 求值而非资源归属；管理下载专用权限；清理部署后默认不执行。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `7a37357b6f62b4464081d410e1a0baae6560d959` |
| Implementation/source SHA | `2a94b4dfa28241be9a702b7c2713bfd7362c3ef9` |
| Candidate branch/workspace | `current`; candidate not-applicable |
| Method/conflicts | `direct-parent`；无冲突 |
| Integration checks | focused/full admin tests、36-module package、MySQL row lock、path/hash/static audits 全部通过 |
| E2E disposition/result | `not-required`：用户明确不做 E2E |
| Parent result/re-read | `2a94b4dfa28241be9a702b7c2713bfd7362c3ef9`；HEAD、commit paths、锁/引用/权限边界与 dirty POM 已重读 |

## 8. 偏差与决策

- **偏差：** 无
- **批准来源及影响：** 用户授权 current workspace 本地 commit/direct-parent；未扩大到部署、生产清理、Bucket policy 或角色授权。

## 9. 残余风险与交付定位

- **残余风险/已知限制：** T-04 才使 Direct Complete 新对象默认进入 TEMP；T-05 才移除旧字节代理入口；清理任务需在部署环境先审核 dry-run 再单独批准启用；跨 Provider/DB 删除无法形成分布式原子事务，依赖 Provider delete 幂等重试。
- **后续 Ticket：** `T-07` Redis 幂等；`T-04` 上传控制面；`T-08` 附件快照；`T-05` 最终协议收缩
- **Source commit / Parent result：** `2a94b4dfa28241be9a702b7c2713bfd7362c3ef9`
- **Source workspace：** `current/backend`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/evidence/T-03.md</Path>`
