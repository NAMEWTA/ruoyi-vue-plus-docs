# Evidence: T-04 — OSS 上传控制面

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/04-oss-upload-control-plane.md</Path>`
- **Lead：** `cursor-agent`
- **Workspace/branch：** `current/backend` / `main`
- **Base/implementation/result SHA：** `3e35a9c3646f5f3313b7223f3f31771d132caea1` / `69f8b07e4910c9b48d7e9ce4b4b8f9410d265c18` / `69f8b07e4910c9b48d7e9ce4b4b8f9410d265c18`
- **状态：** done

## 1. 实现摘要

后端新增 `/resource/oss/uploads` JSON 控制面，覆盖 SINGLE 初始化、Multipart 初始化/窗口签名/ListParts 恢复、Complete 和幂等 Abort。对象 Key、Provider、Bucket、uploadId 与策略参数均由服务端生成并冻结在 Redis UploadTicket；会话以 userId 为所有者，`client_pk` 只保存入口审计快照并可用于初始化策略准入，不参与对象所有权或数据隔离。

Complete 使用 Provider ListParts/HEAD 交叉验证 Part 编号、ETag、总大小、Content-Type、指纹 metadata 与已知 magic bytes，成功登记一个 TEMP `sys_oss` 并只返回字符串 ossId。Redis 以 Ticket、补偿记录、到期 ZSET 和 watchdog 分布式锁维护状态；应用 cleanup 与 Bucket Lifecycle 形成两层保护。DB 已登记但 Redis 完成态写入失败时，重复 Complete 复用原 ossId，过期清理也会先查元数据，避免误删正式对象。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead
- **Dispatch Packet/checkpoint：** Lead direct；backend base `3e35a9c3646f5f3313b7223f3f31771d132caea1`
- **允许动作：** current workspace changes、一次性本地 Redis、implementation commit、direct-parent verification
- **返回：** backend commit `69f8b07e4910c9b48d7e9ce4b4b8f9410d265c18`；27 个白名单文件；用户 POM 保持 dirty
- **Lead 独立核对：** pass；重读状态机、Provider/Redis/DB 接缝、清理竞态、Client 语义、测试报告、staged diff、commit 与 HEAD
- **只读 Agent findings：** 无；本计划未启用 subagent

## 3. 修改范围与路径所有权

- **控制面：** `SysOssUploadController.java` 与 `system/oss/upload/**`
- **配置：** `ruoyi-admin/src/main/resources/application.yml`
- **集中测试：** `ruoyi-admin/src/test/java/org/dromara/test/oss/upload/**`
- **read-only 修改：** 无；T-01 common-oss 与 T-03 lifecycle API 未修改
- **既有用户改动：** `ruoyi-admin/pom.xml` 未暂存、未提交；完成时 patch SHA-256 为 `1ea9de0c55c2718576868a792a7f8638a2c0d8fb9126eea365563d3d9c56d958`
- **集成方式：** current workspace direct-parent，无 candidate/worktree、无冲突

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-001 | SINGLE init 返回结构化 PUT；Complete 仅 `R<String>`；后端入口无 MultipartFile | pass（浏览器网络验收留 T-05） |
| AC-002 | Multipart 只返回 partSize/count，Part URL 按最多 20 个的窗口申请 | pass |
| AC-004 | 指纹校验与 Provider ListParts 恢复；不同指纹稳定拒绝 | pass |
| AC-005 | Provider Part/HEAD 校验、分布式锁、DB objectKey 恢复与重复 Complete 同 ossId | pass |
| AC-006 | size/content-type/metadata/magic 失败不登记 ossId，并删除或登记补偿 | pass |
| AC-007 | 用户 Abort、到期 ZSET、应用主动清理、dry-run 和 Lifecycle 诊断 | pass |
| AC-032 | 类型化策略启动校验；CORS/Expose ETag/Lifecycle 只诊断、不改外部配置 | pass |

## 5. Workspace Verification

以下检查均在 `current-workspace/backend` 执行。

| 命令或步骤 | 结果 | 摘要 |
|---|---|---|
| T-04 focused tests | pass | 17 tests，0 failure/error/skip；REST、策略、状态机、TEMP metadata 和失败恢复 |
| disposable Redis integration | pass | 1 test；Ticket/Cleanup JSON 往返、到期索引、12 路并发最大临界区为 1 |
| `sh mvnw -pl ruoyi-admin -am -Dmaven.test.skip=false test` | pass | 60 tests，0 failure/error，2 个环境型测试按设计 skip |
| `sh mvnw -pl ruoyi-admin -am -Dmaven.test.skip=false package` | pass | 36/36 reactor modules SUCCESS；60 tests，生成 admin jar |
| staged/path/privacy audit | pass | 27 个 Ticket 白名单文件；无签名 URL、Secret 或 Authorization 日志；POM 未 staged |

一次性 Redis 已正常关闭，临时目录移入 `/Users/wta/.Trash`。E2E 为 `not-required`：用户明确不执行 E2E；真实浏览器直传、CORS、取消和刷新网络矩阵由破坏性切换 Ticket T-05 统一人工验收。T-01 已用项目自有 wire server 与真实 AWS S3 client 验证公共 Provider 协议；T-04 用 Provider/metadata 测试替身聚焦验证控制面状态机。

## 6. 双轴审查

- **标准轴：** `3e35a9c36..69f8b07e4` 编译、17 项聚焦测试、真实 Redis、60 项 admin 回归和 36 模块 package 全部通过。
- **规范轴：** Client 不是对象所有者或隔离键；服务端独占 key/provider/uploadId；失败分类对 HTTP 保留机器可读 `error`；cleanup 在删除前保护已登记对象。
- **审查修正：** 修复 Part ceiling division 的 long 溢出、固定 30 秒 Redis lease 的提前解锁风险，以及 Redis COMPLETED 写失败后 cleanup 误删 DB 已登记对象的竞态。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `3e35a9c3646f5f3313b7223f3f31771d132caea1` |
| Implementation/source SHA | `69f8b07e4910c9b48d7e9ce4b4b8f9410d265c18` |
| Candidate branch/workspace | `current`；candidate not-applicable |
| Method/conflicts | `direct-parent`；无冲突 |
| Integration checks | focused/full admin tests、真实 Redis、36-module package、path/privacy/HEAD audit 全部通过 |
| E2E disposition/result | `not-required`：用户明确不做 E2E；T-05 负责人工浏览器合同 |

## 8. 偏差与决策

- **偏差：** 未连接真实云账号或启动完整 HTTP 应用；该组合验证不冒充 E2E，Provider wire 合同复用 T-01 Evidence，浏览器/API 发布验收留 T-05。
- **批准来源及影响：** 用户授权 current workspace、本地 commit/direct-parent，并明确测试集中 ruoyi-admin、不做 E2E；未扩大到 Bucket CORS/Lifecycle 修改、部署或生产对象操作。

## 9. 残余风险与交付定位

- **残余风险：** 各 S3-compatible Provider 对 CORS、ETag 暴露、Multipart metadata 与 Lifecycle 的实际差异仍需目标环境发布前检查；主动 cleanup 默认关闭且 dry-run 默认开启。
- **后续 Ticket：** `T-08` 通知附件快照；随后 T-05 完成前后端 OSS 协议切换。
- **Source commit / Parent result：** `69f8b07e4910c9b48d7e9ce4b4b8f9410d265c18`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/evidence/T-04.md</Path>`
