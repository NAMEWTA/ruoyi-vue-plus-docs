# Evidence: T-05 — 前后端 OSS 协议切换

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/05-oss-protocol-cutover.md</Path>`
- **Lead：** `cursor-agent`
- **Workspace/branch：** `current/backend main` + `current/frontend main`
- **Backend base/result：** `bb2a8114f24ebe25fc9546272f44927d3c69dff3` / `68473cbbb9a36e8298ef954c3f43d0d18df24915`
- **Frontend base/result：** `adf5a0c5db3f7ebb1431c4c2fb87e29f987ef1bc` / `1dba1ea71be891127fa4ee67a91a0b136016f4e7`
- **状态：** done

## 1. 实现摘要

前端新增固定 JSON 控制面 API、首尾采样文件指纹、仅保存 token/fingerprint/非敏感元数据的 IndexedDB 会话、裸 `XMLHttpRequest` OSS 数据面和统一上传状态机。SINGLE 直接 PUT；Multipart 按 8 Part 窗口签名并并发上传，单 Part 最多三次重签重试，进度单调不回退，重新选择同文件时通过 Ticket + ListParts 只补缺失 Part。取消会 Abort 数据面并通知后端清理；普通网络失败保留续传会话。

FileUpload、ImageUpload、Editor 和头像均切换到统一直传入口。上传 Complete 的业务成功值是 `ossId`，预览 URL 通过独立短时下载授权取得。下载插件不再请求后端 blob，而是取得短时 URL 后让浏览器直连 OSS。裸 transport 只发送签名合同中的 `requiredHeaders`，不引用业务 Axios、Authorization 或 clientid。

后端删除旧 `POST /resource/oss/upload`、`GET /resource/oss/download/{ossId}`、MultipartFile Service 和 `ResponseEntity<byte[]>` 字节方法；保留 T-04 JSON 控制面和 T-03 权限化 download-url。内部 `File` 上传能力继续供通知附件等服务端流程使用。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead
- **Dispatch Packet/checkpoint：** Lead direct；backend base `bb2a8114f24ebe25fc9546272f44927d3c69dff3`，frontend base `adf5a0c5db3f7ebb1431c4c2fb87e29f987ef1bc`
- **允许动作：** current workspace changes、两个 implementation commits、direct-parent verification；无 worktree/candidate
- **返回：** backend `68473cbbb9a36e8298ef954c3f43d0d18df24915`；frontend `1dba1ea71be891127fa4ee67a91a0b136016f4e7`
- **Lead 独立核对：** pass；重读前端状态机、裸 transport、全部调用点、后端旧合同删除、提交范围与门禁报告
- **只读 Agent findings：** 无；本计划未启用 subagent

## 3. 修改范围与路径所有权

- **Backend commit：** `68473cbbb9a36e8298ef954c3f43d0d18df24915`，4 个白名单文件。
- **Frontend commit：** `1dba1ea71be891127fa4ee67a91a0b136016f4e7`，11 个白名单文件。
- **集成方式：** current workspace direct-parent；无 worktree/candidate、无冲突。
- **用户既有改动：** `ruoyi-admin/pom.xml` 未暂存、未提交；完成时 patch SHA-256 仍为 `1ea9de0c55c2718576868a792a7f8638a2c0d8fb9126eea365563d3d9c56d958`。
- **父仓库：** 本 Ticket 不单独提交 gitlink；按 Goal Plan 在全部 Ticket 完成后选择性形成聚合 commit。

## 4. 验收与合同映射

| Contract | 本地证据 | 结果 |
|---|---|---|
| AC-001 | SINGLE 初始化、裸 PUT、Complete 只取 ossId；旧 Multipart endpoint 删除 | pass |
| AC-003 | Part 最多三次重签重传；窗口失败保留已上传 Part，重新选择可续传 | pass（真实 Provider 故障注入留发布前验收） |
| AC-004 | v1 指纹 + IndexedDB token + resume/ListParts；短暂控制面故障不删除会话 | pass（真实刷新/断网留发布前验收） |
| AC-012/013 | 管理下载只经 `system:oss:download` download-url；普通业务仍由自身 Service 授权 | pass |
| AC-014 | FileUpload/ImageUpload/Editor/Avatar/download 全部迁移；旧生产 URL/Builder 扫描为零 | pass |

## 5. Workspace Verification

以下检查分别在 `current-workspace/backend` 和 `current-workspace/frontend` 执行。

| 命令或步骤 | 结果 | 摘要 |
|---|---|---|
| `sh mvnw ... -Dtest='OssProtocolCutoverUnitTest' ... test` | pass | 2/2；Controller/Service 不再接受 MultipartFile 或返回字节 ResponseEntity |
| `sh mvnw -pl ruoyi-admin -am -Dmaven.test.skip=false test` | pass | 36/36 reactor；72 tests，0 failure/error，2 个外部 Redis 环境测试按设计 skip |
| `sh mvnw -pl ruoyi-admin -am -Dmaven.test.skip=false package` | pass | 36/36 reactor；72 tests；生成 admin jar |
| `pnpm lint` | pass | Oxlint 0 error |
| `pnpm build:prod` | pass | Vite production build 成功 |
| `pnpm exec vue-tsc --noEmit` | baseline diagnostic | exit 2；仅既有 `loginInfo/logininfo` 两个 TS1149，无 T-05 新诊断 |
| 旧 URL/类型与 OSS Header 静态扫描 | pass | 旧 upload/download、`uploadOss`、后端 Multipart/byte route 为零；裸 transport 无业务 Header/request import |
| staged/path audit | pass | 两仓库提交均只含 Ticket writable paths；用户 POM 未 staged |

E2E 为 `not-required`，遵守用户明确决定。本地未配置已授权的真实 OSS Bucket、CORS、登录和业务权限环境，因此没有把真实浏览器网络/cancel/resume 伪报为通过；这些项目继续作为发布前人工验收与 Bucket CORS/Lifecycle 检查。该环境缺口不改变本地代码 Gate，但禁止据此宣称生产发布已验收。

## 6. 双轴审查

- **标准轴：** 前后端格式、lint、build、聚焦测试、72 项 admin 回归、36 模块 package 和 staged diff 均通过。
- **规范轴：** 浏览器字节不再经过后端；objectKey/uploadId 不暴露为业务权威；本地不保存 signed URL；Client 只随业务控制面认证请求，裸 OSS transport 不发送 clientid。
- **审查修正：** 恢复查询的网络故障不再误删 Ticket；会话确实过期/失效时才丢弃；Part 重试进度保持单调；所有 XHR 终态移除 abort listener。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Backend parent before/result | `bb2a8114f24ebe25fc9546272f44927d3c69dff3` -> `68473cbbb9a36e8298ef954c3f43d0d18df24915` |
| Frontend parent before/result | `adf5a0c5db3f7ebb1431c4c2fb87e29f987ef1bc` -> `1dba1ea71be891127fa4ee67a91a0b136016f4e7` |
| Method/conflicts | `direct-parent`；无冲突 |
| Candidate/worktree | not-applicable |
| Verification | passed（本地非 E2E Gate）；真实 OSS 浏览器验收为发布前置条件 |
| Source commits | backend `68473cbbb...`；frontend `1dba1ea71...` |

## 8. 偏差与决策

无 Spec、ADR、schema、安全语义或 Ticket 路径偏差。真实 OSS/CORS 浏览器矩阵未在无目标环境的本地执行，按既定 E2E not-required 与发布权限边界保留为发布前运维验收；没有将其伪报成实现 Gate 结果。

## 9. 残余风险与交付定位

- **残余风险：** 目标 Provider 必须暴露 Multipart ETag，并正确配置 CORS origin/method/header/expose-header；签名 URL 获取仍受管理或业务权限约束；前后端必须成对发布。
- **后续 Ticket：** T-09 消费 T-08 Delivery Event，完成两层监控、附件绑定和管理 API。
- **Source commit / Parent result：** backend `68473cbbb9a36e8298ef954c3f43d0d18df24915`；frontend `1dba1ea71be891127fa4ee67a91a0b136016f4e7`。
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/evidence/T-05.md</Path>`
