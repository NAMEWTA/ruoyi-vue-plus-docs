# Evidence: T-07 — Redis 通知幂等

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/07-notify-redis-idempotency.md</Path>`
- **Lead：** `cursor-agent`
- **Workspace/branch：** `current/backend` / `main`
- **Base/implementation/result SHA：** `2a94b4dfa28241be9a702b7c2713bfd7362c3ef9` / `3e35a9c3646f5f3313b7223f3f31771d132caea1` / `3e35a9c3646f5f3313b7223f3f31771d132caea1`
- **状态：** done

## 1. 实现摘要

`common-notify` 新增可选 Redis 幂等状态机。存储 Key 是 `channel + business idempotencyKey` 的 SHA-256，不包含 target、正文、tenant、Client 或用户上下文；请求摘要覆盖 Provider、业务坐标、顺序目标、内容、附件和排序 metadata。窗口默认 5 分钟，并受 30 秒至 24 小时边界约束。

首次请求原子进入 `IN_PROGRESS`，完成后以 CAS 写入可还原的 `NotifyResult`；并发请求立即抛 `NotifyInProgressException`，同 Key 不同摘要抛冲突异常，Redis 故障对带 Key 请求 fail-closed。相同摘要的完成态复用首次成功或失败结果，同时发布无 Delivery 的 `SKIPPED_DUPLICATE` Event 并关联 `originalRequestId`。无 Key 路径不访问 Redis，保持 T-06 行为。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead
- **Dispatch Packet/checkpoint：** Lead direct；backend base `2a94b4dfa28241be9a702b7c2713bfd7362c3ef9`
- **允许动作：** current workspace changes、disposable local Redis verification、implementation commit、direct-parent verification
- **返回：** backend commit `3e35a9c3646f5f3313b7223f3f31771d132caea1`；用户 POM 保持 dirty；15 个 Ticket 路径；非 E2E 检查全部通过
- **Lead 独立核对：** pass；重读状态机、CAS、自动装配、测试报告、staged diff、commit 与 HEAD
- **只读 Agent findings：** 无；本计划未启用 subagent

## 3. 修改范围与路径所有权

- **可写实现：** `ruoyi-common/ruoyi-common-notify/**`
- **集中测试：** `ruoyi-admin/src/test/java/org/dromara/test/notify/idempotency/**`
- **read-only 修改：** 无；`ruoyi-common-redis` 未修改
- **既有用户改动：** `ruoyi-admin/pom.xml` 未暂存、未提交；回写时当前 patch SHA-256 为 `444c85f8255c8e5fd808604dfc097a79d4e1b2513fdf1290e183c4b0d9c5e65c`
- **集成方式：** current workspace direct-parent，无 candidate/worktree、无冲突

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-018 | 默认 5 分钟、边界配置、首次占位后保存完整结果 | pass |
| AC-019 | 一次性 Redis 20 路并发只有 1 个 acquired、19 个 in-progress | pass |
| AC-020 | 完成态不再调用 Provider；复用原成功/失败结果；发布 `SKIPPED_DUPLICATE`、零 Delivery | pass |
| AC-021 | acquire/complete Redis 故障均为 typed fail-closed；Provider 不被错误重入 | pass |
| AC-022 | 无 Key 时不依赖 Redis，原同步发送行为不变 | pass |

## 5. Workspace Verification

| 命令或步骤 | 结果 | 摘要 |
|---|---|---|
| notify core/idempotency focused tests | current-workspace/backend | pass；17 tests，0 failure/error/skip |
| disposable Redis integration | current-workspace/backend | pass；10 tests；真实原子竞争、结果序列化、冲突和 TTL；0 skip |
| `sh mvnw -pl ruoyi-admin -am -Dmaven.test.skip=false test` | current-workspace/backend | pass；42 tests，0 failure/error，1 个无一次性 Redis 参数时按设计 skip |
| `sh mvnw -Dmaven.test.skip=false package` | current-workspace/backend | pass；41/41 reactor modules SUCCESS |
| staged/path/privacy audit | current-workspace/backend | pass；15 个白名单文件；Key/日志无敏感明文；Client/tenant/user 不入作用域 |

一次性 Redis 正常关闭，临时目录移入 `/Users/wta/.Trash`。E2E 为 `not-required`：用户明确不做 E2E，本 Ticket 的内部并发状态机由真实 Redis 集成覆盖。

## 6. 双轴审查

- **标准轴：** `2a94b4dfa..3e35a9c36` 编译、自动装配、并发、异常、CAS、完整打包均通过。
- **规范轴：** 幂等只保证窗口内 Provider 首次进入，不是可靠队列或跨窗口 exactly-once；Client 仅保留在 T-06 Event 审计上下文，不参与幂等。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `2a94b4dfa28241be9a702b7c2713bfd7362c3ef9` |
| Implementation/source SHA | `3e35a9c3646f5f3313b7223f3f31771d132caea1` |
| Candidate branch/workspace | `current`；candidate not-applicable |
| Method/conflicts | `direct-parent`；无冲突 |
| Integration checks | focused/full admin tests、41-module package、真实 Redis 并发与 path/privacy audit 全部通过 |
| E2E disposition/result | `not-required`：用户明确不做 E2E |

## 8. 偏差与决策

- **偏差：** 无。用户 POM 在 Ticket 执行期间有新补丁，但未与白名单路径冲突，也未进入提交。
- **批准来源及影响：** 用户授权 current workspace 本地 commit/direct-parent；未扩大到远程、部署或共享 Redis 操作。

## 9. 残余风险与交付定位

- **残余风险：** Redis 完成态写入失败时 Provider 可能已接受请求，因此明确向调用方暴露 `COMPLETE` 故障，不能自动重发；T-09 才持久化 duplicate Event。
- **后续 Ticket：** `T-04` OSS 上传控制面，随后 `T-08` 附件快照。
- **Source commit / Parent result：** `3e35a9c3646f5f3313b7223f3f31771d132caea1`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/evidence/T-07.md</Path>`
