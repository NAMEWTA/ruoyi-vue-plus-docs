# Evidence: T-09 — 通知监控后端

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/09-notify-monitoring-backend.md</Path>`
- **Lead：** `cursor-agent`
- **Workspace/branch：** `current/backend` / `main`
- **Base/result SHA：** `68473cbbb9a36e8298ef954c3f43d0d18df24915` / `63a05b8eec82b5cc65927c326c51ca8f25a811a6`
- **状态：** done

## 1. 实现摘要

`ruoyi-system` 新增全局通知监控实体、Mapper、Service、异步 Event Listener 和 `/monitor/notify` 管理 API。一次逻辑通知写入 `sys_notify_log`，每个实际 Provider target attempt 写入 `sys_notify_delivery_log`；`SKIPPED_DUPLICATE` 只写逻辑行并通过 `original_request_id` 关联首次请求。Listener 使用 Event 中已经冻结的 userId、traceId 和可空来源 clientPk，不读取异步线程的 LoginHelper、Tenant、MDC 或 Client 上下文。

正文、模板参数、内容快照和完整目标按已批准风险永久明文保存。列表 VO 不包含正文和完整目标，只返回服务端脱敏目标；详情在 `system:notify:query` 权限下返回完整数据。`client_pk` 只显式展示/筛选，不是默认 SQL 条件、数据所有权或隔离维度。删除、批删和清空具备 `system:notify:remove`、OperLog 与事务边界，并在父子逻辑删除前解除 `sys_notify_log` 附件引用。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead
- **Dispatch Packet/checkpoint：** Lead direct；backend base `68473cbbb9a36e8298ef954c3f43d0d18df24915`
- **允许动作：** current workspace changes、implementation commit、direct-parent verification；无 worktree/candidate
- **返回：** backend commit `63a05b8eec82b5cc65927c326c51ca8f25a811a6`；18 个白名单文件；用户 POM 保持 dirty
- **Lead 独立核对：** pass；重读 Event 映射、列表/详情投影、Client 查询、删除/解绑顺序、Controller 权限、真实 Mapper SQL、提交范围和测试报告
- **只读 Agent findings：** 无；本计划未启用 subagent

## 3. 修改范围与路径所有权

- **System：** `system/notify/**` 与 `SysNotifyController.java`
- **集中测试：** `ruoyi-admin/src/test/java/org/dromara/test/notify/monitor/**`
- **SQL：** T-02 所有的 DDL/DSL 只读复用，本 Ticket 未修改
- **既有用户改动：** `ruoyi-admin/pom.xml` 未暂存、未提交；完成时 patch SHA-256 仍为 `1ea9de0c55c2718576868a792a7f8638a2c0d8fb9126eea365563d3d9c56d958`
- **集成方式：** current workspace direct-parent，无 candidate/worktree、无冲突

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-020 | distinct duplicate requestId 写 SKIPPED_DUPLICATE、originalRequestId，Delivery 为零 | pass |
| AC-025 | partial failure 单测；MySQL 8.4 实际写入逻辑/Delivery、ACCEPTED 状态保持不变 | pass |
| AC-026 | Listener 持久化异常被吞并且同步调用不受影响；附件未绑定时仍由 TEMP 清理兜底 | pass |
| AC-027 | 全局分页无隐式条件，列表只返回服务端脱敏目标 | pass |
| AC-028 | detail 返回完整明文正文、模板参数、目标和附件 ID；权限注解为 query | pass（HTML 执行隔离留 T-11） |
| AC-029 | 单/批/清空权限与 OperLog；删除前解绑附件；MySQL 父子逻辑删除 | pass |
| AC-030 | 空查询不追加 client_pk；仅显式 clientPk 条件可筛选；权限由 Sa-Token 注解求值 | pass |
| AC-031 | Event clientPk 可空且审计创建者使用系统占位，不要求 SYSTEM scope | pass |

## 5. Workspace Verification

本 Ticket 在 `current-workspace` 完成实现、验证和 direct-parent 集成。

| 命令或步骤 | 结果 | 摘要 |
|---|---|---|
| T-09 focused monitor tests | pass | 8 tests，0 failure/error/skip；Event 映射、duplicate、监听失败、权限、脱敏、明文详情、删除解绑 |
| `NotifyMonitorMySqlIntegrationTest` + disposable MySQL 8.4.11 | pass | 1 test，0 skip；真实 MyBatis-Plus insert/page/detail/duplicate/父子逻辑删除 |
| `sh mvnw -pl ruoyi-admin -am -Dmaven.test.skip=false test` | pass | 81 tests，0 failure/error；3 个外部 Redis/MySQL 条件测试按设计 skip，MySQL 用例已单独真实运行 |
| `sh mvnw -DskipTests package` | pass | 41/41 reactor modules SUCCESS |
| path/context/privacy audit | pass | Listener 无 ThreadLocal；列表无正文/目标；clientPk 仅显式条件；错误清洗 signed URL/Auth/Secret |
| staged diff/POM hash | pass | 18 个 Ticket 白名单文件；用户 POM 未 staged 且哈希不变 |

E2E 为 `not-required`：用户明确不执行 E2E。Controller 路由、权限与 OperLog 使用集中合同测试验证；本 Ticket 未启动完整 Web 应用或浏览器，也未把真实 MySQL Mapper 验证描述为 E2E。

## 6. 双轴审查

- **标准轴：** `68473cbbb..63a05b8ee` 编译、8 项聚焦单测、真实 MySQL 8.4 Mapper/Service 测试、81 项 admin 回归和 41 模块 package 通过。列表分页一次批量加载 Delivery，避免逐行查询；父子删除和附件解绑处于同一事务。
- **规范轴：** 明文永久保存、同 query 权限详情、列表脱敏、duplicate 零 Delivery、Event best-effort、Client 非隔离、`sys_notify_log` 真实 ref_type 和 TEMP 兜底均与 ADR/Spec/Ticket 一致。
- **审查修正：** 新测试补 `@Tag("dev")` 防止 Surefire 空跑；LambdaWrapper 单测改为检查条件段而非依赖表缓存；增加一次性 MySQL 真实 Mapper 测试后才关闭 Ticket。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `68473cbbb9a36e8298ef954c3f43d0d18df24915` |
| Implementation/source/result SHA | `63a05b8eec82b5cc65927c326c51ca8f25a811a6` |
| Candidate branch/workspace | `current`；candidate not-applicable |
| Method/conflicts | `direct-parent`；无冲突 |
| Integration checks | focused/full admin tests、disposable MySQL 8.4、41-module package、path/context/privacy audits 全部通过 |
| E2E disposition/result | `not-required`：用户明确不做 E2E |

## 8. 偏差与决策

无 Spec、ADR、schema、安全语义、路径或发布偏差。一次性 MySQL 数据目录已在验证后关闭并永久删除；未触碰共享或生产数据库。

## 9. 残余风险与交付定位

- **残余风险：** 完整正文和目标永久明文、全局 query 权限及 clean 权限具有已批准的高事故半径；生产授权和首次清空仍需独立运维批准。Event 是 best-effort，极端宕机窗口允许缺日志且不得据此重试 Provider。
- **后续 Ticket：** T-10 收口既有 SMS/Mail 调用；T-11 建立列表/详情/删除页面并确保 HTML 只作文本/隔离预览。
- **Source commit / Parent result：** `63a05b8eec82b5cc65927c326c51ca8f25a811a6`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/evidence/T-09.md</Path>`
