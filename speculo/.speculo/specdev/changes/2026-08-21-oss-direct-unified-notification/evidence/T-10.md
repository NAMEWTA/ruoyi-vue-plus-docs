# Evidence: T-10 — 现有通知调用收口

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/10-notify-caller-migration.md</Path>`
- **Lead：** `cursor-agent`
- **Workspace/branch：** `current/backend` / `main`
- **Base/result SHA：** `63a05b8eec82b5cc65927c326c51ca8f25a811a6` / `b1ded45d8e7c926650c4c054522e242319890c4a`
- **状态：** done

## 1. 实现摘要

Captcha、Workflow 和 Demo 的实际 Mail/SMS 发送统一迁移到 `NotifyClient`。调用方直接解析 PHONE/EMAIL，不向 common 传 USER；验证码短信携带 Provider 模板参数和完整正文快照，验证码邮件携带完整文本，且只在 Provider 接受后缓存验证码。验证码使用分钟业务幂等键，默认窗口继续由 T-07 的五分钟配置控制。

Workflow 继续通过 `MessageService` 发送站内信，邮件和短信改走 NotifyClient；三种渠道按当前线程顺序调用，以便 Event 快照可记录可用的请求来源 `clientPk`，但 Client 不参与 Provider 路由、数据隔离、所有权或幂等。外部通知失败继续被 Workflow 吞并并记录安全错误分类，不影响流程主逻辑。

Demo Mail 附件由无效本地占位路径改为 `ossId/ossIds`，触发 T-08 附件快照；Demo SMS 显式选择 `config1/config2` 并提供完整模板快照。SMS 黑名单增删属于 Provider 管理操作，按批准偏差继续直接复用 common-sms，不属于通知发送直调。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead
- **Dispatch Packet/checkpoint：** Lead direct；backend base `63a05b8eec82b5cc65927c326c51ca8f25a811a6`
- **允许动作：** current workspace changes、implementation commit、direct-parent verification；无 worktree/candidate
- **返回：** backend commit `b1ded45d8e7c926650c4c054522e242319890c4a`；11 个 Ticket 文件；用户 POM bundle profile 改动保持 dirty
- **Lead 独立核对：** pass；重读验证码缓存顺序、幂等指纹、Workflow 失败语义与上下文线程、Demo 附件、模块直接依赖和调用扫描
- **只读 Agent findings：** 无；本计划未启用 subagent

## 3. 修改范围与路径所有权

- **Admin：** `CaptchaController`、新增 `common-notify` 直接依赖、集中 caller tests
- **Workflow：** `FlwCommonServiceImpl` 和 POM；站内信路径不变
- **Demo：** Mail/SMS Controller 和 POM；黑名单 Provider 管理路径不变
- **既有用户改动：** `ruoyi-admin/pom.xml` 使用交互式 selective staging；只提交六行 notify 依赖，用户 bundle profile patch 完整保留为唯一未提交项
- **集成方式：** current workspace direct-parent，无 candidate/worktree、无冲突

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-015/016 | Captcha、Workflow、Demo 均构造单渠道 NotifyRequest；Mail/SMS 原子 API 仅在 Adapter 和黑名单管理使用 | pass |
| AC-017 | 业务调用同步获得 NotifyResult/NotifyDeliveryException；Captcha 失败不缓存，Workflow 保持吞并语义 | pass |
| AC-023/024 | Demo Mail 仅传 ossId/ossIds，通知栈在 Provider 前创建附件快照 | pass |
| AC-025 | Caller 统一经过 Dispatcher/Event；模板短信提供 code params 和完整 contentSnapshot | pass |
| AC-031 | Workflow 不构造 SYSTEM/client scope；同线程解析器仅记录可用请求来源，无 Client 隔离或路由 | pass |

## 5. Workspace Verification

本 Ticket 在 `current-workspace` 完成实现、验证和 direct-parent 集成。

| 命令或步骤 | 结果 | 摘要 |
|---|---|---|
| TDD red focused caller tests | expected fail | 8 个编译错误均为旧构造器/附件签名，证明测试先于实现约束迁移目标 |
| T-10 focused caller tests | pass | 9 tests，0 failure/error/skip；Captcha SMS/Mail 成败、Workflow 三渠道/失败、Demo、静态合同 |
| `sh mvnw -pl ruoyi-admin -am -Dmaven.test.skip=false test` | pass | 90 tests，0 failure/error；3 个外部 Redis/MySQL 条件测试按设计 skip |
| `sh mvnw -DskipTests package` | pass | 41/41 reactor modules SUCCESS |
| Adapter 外发送直调扫描 | pass | MailBuilder 发送为零；SmsFactory 仅剩 Demo blacklist add/remove，属于已批准 Provider 管理例外 |
| staged diff/POM audit | pass | 11 个 Ticket 文件；admin POM 只暂存 notify dependency，用户 profile hunks未暂存 |

E2E 为 `not-required`：用户明确不执行 E2E。代表性入口以 ruoyi-admin 聚焦/全量测试和静态扫描验证；未启动 Provider、完整 Web 应用或浏览器，也未伪报实际邮件/短信送达。

## 6. 双轴审查

- **标准轴：** `63a05b8ee..b1ded45d8` 编译、9 项聚焦测试、90 项 admin 回归及 41 模块 package 通过。Captcha 仅成功后缓存；Workflow 无异步 ThreadLocal 丢失；POM 依赖表达实际模块合同。
- **规范轴：** 一个请求一个 Channel、物理 Target、模板完整快照、Provider 同步/Event observe、Client 非隔离、站内信独立、附件 ossId 快照均与 ADR/Spec/Ticket 一致。
- **审查修正：** RedisUtils 静态初始化不适合纯单测，Controller 增加最小可覆写缓存接缝，测试仍验证成功后缓存且失败零缓存；Workflow 错误日志不输出正文或异常详情。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `63a05b8eec82b5cc65927c326c51ca8f25a811a6` |
| Implementation/source/result SHA | `b1ded45d8e7c926650c4c054522e242319890c4a` |
| Candidate branch/workspace | `current`；candidate not-applicable |
| Method/conflicts | `direct-parent`；无冲突 |
| Integration checks | focused/full admin tests、41-module package、direct-call/POM/privacy audits 全部通过 |
| E2E disposition/result | `not-required`：用户明确不做 E2E |

## 8. 偏差与决策

- `DEV-T10-001`：按当前仓库真实位置使用 `controller/CaptchaController.java`，只纠正 Ticket 旧路径。
- `DEV-T10-002`：Demo 附件参数使用 ossId/ossIds；SMS 黑名单视为 Provider 管理，不纳入通知发送直调收缩。

两项偏差均未改变公共 Notify 契约、数据库、Client 语义或发布范围。

## 9. 残余风险与交付定位

- **残余风险：** 未连接真实 Mail/SMS Provider；模板 ID、Provider 配置及实际送达必须在部署环境验证。验证码正文按用户批准会永久明文进入监控表。
- **后续 Ticket：** T-11 建立监控列表、完整详情、Delivery、附件和删除页面，并确保 HTML 不执行。
- **Source commit / Parent result：** `b1ded45d8e7c926650c4c054522e242319890c4a`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/evidence/T-10.md</Path>`
