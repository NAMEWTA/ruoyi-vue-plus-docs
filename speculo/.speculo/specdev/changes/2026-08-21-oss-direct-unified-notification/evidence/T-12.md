# Evidence: T-12 — 收紧通知附件入口授权

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/12-review-attachment-authorization.md</Path>`
- **Lead：** `codex`
- **Workspace/branch：** `current/backend main + current/frontend main`
- **Base/result SHA：** backend `a56a6f907dc1fad2bde4daa92adfeafbcea3613f` / `4d5a3b5bce660e1d529383006b8252ff0161f430`；frontend `866e5ba1a75c9d308ce752f32fa6b4158763feed` / `83cfa4f4bb50f0a3882f69469f1f43cef0023f32`
- **状态：** done

## 1. 实现摘要

Demo 邮件附件入口增加 `system:oss:download`，内部快照 SPI 不读取登录 ThreadLocal。通知监控附件改用校验 `notifyLogId/ossId` 归属并受 `system:notify:query` 控制的专用短链。

## 2. Lead Dispatch And Candidate Return

Lead 在 current workspace 直接实施；未使用 subagent、worktree 或 candidate。两个聚焦 commit 均已由 Lead 重读并验证。

## 3. 修改范围与路径所有权

后端 Demo/通知 Controller 与前端通知 API/详情抽屉均属于本 Ticket 扩展后的 writable paths；未修改既有用户 dirty POM。

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-013/023/024 | HTTP 边界显式权限、附件归属校验、内部 SPI 无全局权限耦合 | pass |

## 5. Workspace Verification

current-workspace：后端 102 tests（0 failure/error，3 skip）、36-module package、前端 4 Vitest、lint/build 均通过。E2E not-required，用户已明确不建设 E2E。

## 6. 双轴审查

标准轴 pass：权限位于 HTTP 边界。规范轴 pass：内部业务授权后仍可复用快照 SPI。

## 7. Integration Verification

direct-parent；backend/frontend commits 如页首；无冲突，最终全量门禁通过。

## 8. 偏差与决策

为闭合监控附件入口，Ticket writable paths 增补前端通知 API/详情路径；安全语义未扩大。

## 9. 残余风险与交付定位

真实角色矩阵需在发布环境验收；本地未执行部署或 Provider 调用。
