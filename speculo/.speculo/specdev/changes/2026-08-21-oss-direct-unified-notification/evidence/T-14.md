# Evidence: T-14 — OSS 生命周期幂等与可恢复删除

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/14-review-oss-deletion-state.md</Path>`
- **Lead：** `codex`
- **Workspace/branch：** `current/backend main`
- **Base/result SHA：** `b2d50248acfb13c46fc490f248c43500e0f4f7cf` / `d981d0d217e9825136dd0e3700f55ec245e8097a`
- **状态：** done

## 1. 实现摘要

OSS 删除改为 `ACTIVE -> PENDING -> metadata removed` 两阶段；首次只持久化 PENDING，后续任务才调用 Provider。失败保留 PENDING 可重试，合法 bind 可取消；重复 unbind 不重置过期时间，PENDING 对象拒绝签发下载 URL。

## 2. Lead Dispatch And Candidate Return

Lead direct；无 subagent/worktree/candidate。Schema 位于前置 T-13 的同一追加 DDL checkpoint。

## 3. 修改范围与路径所有权

修改 SysOss domain/VO/Mapper、生命周期 manager/task/error 与 admin lifecycle tests。

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-007/008/010/011 | 15 个 lifecycle tests 覆盖两阶段、Provider 失败、bind 取消、重复 unbind、PENDING 下载拒绝 | pass |

## 5. Workspace Verification

current-workspace：最终 102 tests 与 36-module package 通过；E2E not-required。清理任务日志使用“已处理”，不误报首次即删除完成。

## 6. 双轴审查

标准轴 pass：外部副作用只发生在已提交 PENDING 之后。规范轴 pass：复用现有 lifecycle manager 与行锁 Mapper。

## 7. Integration Verification

direct-parent；result `d981d0d217e9825136dd0e3700f55ec245e8097a`，无冲突。

## 8. 偏差与决策

无。

## 9. 残余风险与交付定位

清理默认关闭且 dry-run；生产首次启用前必须审核 PENDING 列表和 Provider 权限。
