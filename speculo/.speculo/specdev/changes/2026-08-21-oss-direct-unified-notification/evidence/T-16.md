# Evidence: T-16 — 业务 OSS 引用与下载授权接入

- **Change：** `2026-08-21-oss-direct-unified-notification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-21-oss-direct-unified-notification/ticket/16-review-business-oss-adoption.md</Path>`
- **Lead：** `codex`
- **Workspace/branch：** `current/backend main + current/frontend main`
- **Base/result SHA：** backend `d09fb314036c2b92395a7bb2271d48769d05926d` / `fc902e0820a869d1044b59629b74c00ab14b64c3`；frontend `ca35cdd69f768924128729da61db3e48b5272c3b` / `5d6ed4b2ce0b066c3a0ad3216e3f8d7d4200d426`
- **状态：** done

## 1. 实现摘要

头像更新在动态数据源事务中绑定新 OSS、解绑旧 OSS；公告增改删 reconcile `oss://id` 与 `sys_notice` 引用；公告业务端点只为正文实际引用对象签发 URL，Editor 通过注入 resolver 回显。

## 2. Lead Dispatch And Candidate Return

Lead direct；后端/前端各一聚焦 commit，无 worktree/candidate。

## 3. 修改范围与路径所有权

修改 system user/notice service/controller 和前端 notice/Editor/ossContent 接缝。

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-009/010/013/028 | 新旧引用收敛、业务归属过滤、前端业务 resolver | pass |

## 5. Workspace Verification

current-workspace：backend 102 tests/package，frontend tests/lint/build 通过；vue-tsc 无新增诊断。E2E not-required。

## 6. 双轴审查

标准轴 pass：业务行与引用同事务。规范轴 pass：跨模块复用 `OssService`，业务 HTTP 授权不退化为通用 ossId ACL。

## 7. Integration Verification

direct-parent；result 如页首，无冲突。

## 8. 偏差与决策

Editor 的 generation 防竞态与 resolver 在同一文件/commit 完成，T-17 复用该 checkpoint。

## 9. 残余风险与交付定位

历史正文没有 `oss://` marker 时无法自动补建引用；保持兼容，不做猜测迁移。
