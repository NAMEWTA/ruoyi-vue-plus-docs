# Evidence: T-03 — CI 与真实外部服务验收

- **Change：** `2026-08-24-upstream-fork-upgrade-remediation`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-24-upstream-fork-upgrade-remediation/ticket/03-ci-and-real-service-acceptance.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-24-upstream-fork-upgrade-remediation/spec.md</Path>`
- **Goal Plan：** 不适用
- **Lead：** codex
- **Workspace/branch：** repository current workspace / `main`
- **Base/implementation-or-source/candidate/result SHA：** `aa4752fe2d51ce705d442c9567e8e33f3676b736` / `null` / `null` / `null`
- **状态：** review

## 1. 实现摘要

新增四 job GitHub Actions、可移植的子模块快照校验、full/core 产物断言、一次性 Redis/MySQL/S3 服务脚本，以及真实 presigned single/multipart MinIO 测试。主 Compose 的 9 个显式容器名统一使用 `ruoyi-namewta-*` 前缀，并使用独立 bridge 网络、4xxxx 宿主机端口、profiles、healthcheck、fresh SQL、utf8mb4 初始化客户端和仓库外 secret。指定远程主机已只部署 MySQL、Redis、MinIO；本地 Git 忽略配置以 `dev,local` 启动后端并连接远端 middleware，Vite 通过 `/dev-api` 代理到本地后端。父仓新增严格两选项的本地启动脚本，前台完成依赖刷新并以 `exec` 运行 Vite 或 Spring Boot，保留实时日志和 `Ctrl+C` 生命周期。真实上传验收发现的 Complete 字符串重载问题已修复：`ossId` 进入 `data`；已完成票据可恢复同一 ID 且不重复传输，上传组件保留具体本地错误并抑制 request adapter 已展示的重复提示。Action 版本和输入按官方 `action.yml` 核对。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead
- **Dispatch Packet/checkpoint：** Lead direct / `aa4752fe2d51ce705d442c9567e8e33f3676b736`
- **允许动作：** local workflow/scripts/tests；remote CI、commit、push、required checks 未授权
- **返回：** parent/child working trees dirty；CI candidate 未产生远程 run
- **Lead 独立核对：** pass；核对 workflow inputs、shell 生命周期、容器 cleanup、测试属性名和 secret 边界
- **只读 Agent findings：** 无

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>.github/workflows/quality-gates.yml</Path>` | writable | snapshot/frontend/backend/external-services jobs |
| `<Path>scripts/ci/verify-submodules.sh</Path>` | writable | SHA、初始化与 dirty 检查 |
| `<Path>scripts/ci/verify-admin-bundle.sh</Path>` | writable | fat jar 模块集合断言 |
| `<Path>scripts/ci/run-external-services.sh</Path>` | writable | disposable Redis 8.6.3/MySQL 8.4.9/pgsty MinIO 2026-08-04 |
| `<Path>scripts/start-dev.sh</Path>` | writable | 从父仓根目录选择并以前台进程启动本地前端或后端 |
| `<Path>scripts/README.md</Path>` | writable | 记录本地启动菜单、前置条件和前台日志语义 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/oss/client/MinioOssClientIntegrationTest.java</Path>` | writable | 真实 S3 数据面验收 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/oss/upload/OssUploadHttpContractUnitTest.java</Path>` | writable | 固定 Complete 的 `data=ossId` HTTP 合同 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/oss/upload/OssUploadServiceUnitTest.java</Path>` | writable | 固定完成态 Resume 不再签发上传请求 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/src/main/java/org/dromara/system/controller/system/SysOssUploadController.java</Path>` | writable | 避免 `R.ok(String)` 把 ID 解释为提示文本 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/src/main/java/org/dromara/system/oss/upload/OssUploadContracts.java</Path>` | writable | Resume 暴露状态与完成 ID |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/src/main/java/org/dromara/system/oss/upload/OssUploadService.java</Path>` | writable | 对完成票据返回稳定的幂等恢复结果 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/main/resources/application-local.yml</Path>` | writable / git-ignored | 本地 `dev,local` profiles 覆盖远端 MySQL/Redis，敏感值不进入 Git 或 Evidence |
| `<Path>plus-ui-namewta/src/api/system/oss/types.ts</Path>` | writable | 对齐 Resume 状态合同 |
| `<Path>plus-ui-namewta/src/hooks/oss/useDirectOssUpload.ts</Path>` | writable | 完成态短路、清理恢复记录并复用原 ID |
| `<Path>plus-ui-namewta/src/hooks/oss/useDirectOssUpload.test.ts</Path>` | writable | 完成态恢复和错误提示 red/green 回归 |
| `<Path>plus-ui-namewta/src/components/FileUpload/index.vue</Path>` | writable | 展示可定位的文件上传错误且不双 toast |
| `<Path>plus-ui-namewta/src/components/ImageUpload/index.vue</Path>` | writable | 展示可定位的图片上传错误且不双 toast |
| `<Path>plus-ui-namewta/src/views/system/user/profile/userAvatar.vue</Path>` | writable | 对齐头像上传错误行为 |
| `<Path>ruoyi-vue-plus-namewta/script/docker/docker-compose.yml</Path>` | writable | 用 NAMEWTA 前缀隔离主 Compose 容器名 |
| `<Path>ruoyi-vue-plus-namewta/script/docker/.env.example</Path>` | writable | 声明无敏感值的部署变量合同 |
| `<Path>ruoyi-vue-plus-namewta/script/docker/README.md</Path>` | writable | 记录端口、profiles、fresh 初始化和安全停止方式 |
| `<Path>ruoyi-vue-plus-namewta/script/docker/mysql/conf/namewta-client.cnf</Path>` | writable | 固定初始化客户端为 utf8mb4，保证中文 SQL 可重复导入 |
| `<Path>ruoyi-vue-plus-namewta/script/docker/redis/conf/redis.conf</Path>` | writable | 移除示例弱口令，密码由远程 env 注入 |

- **read-only 修改：** 无
- **未声明路径：** 无
- **生成文件/锁文件：** 无

## 4. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-003 | workflow + services + snapshot | workflow/shell 静态通过，测试编译通过；指定远程主机的真实 MySQL/Redis/MinIO 数据面通过，GitHub run 尚未执行 | partial |
| T-03 OSS 直传合同 | controller/service/hook + local browser | 两端 red/green 回归、完整门禁和已完成真实票据 Resume/Complete 均通过 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| `bash -n scripts/ci/*.sh` | current-workspace | pass | 三个脚本语法通过 |
| Ruby YAML load | current-workspace | pass | workflow 为合法 YAML map |
| 官方 action input review | internet / official action.yml | pass | checkout v7、setup-node v7、pnpm v6、setup-java v5 输入有效；子模块 lockfile 路径已显式设置 |
| 定向 Maven test compile | current-workspace | pass | MinIO 与菜单迁移 2 tests compiled，因无 properties 均 skipped |
| `scripts/ci/verify-submodules.sh` | current-workspace | expected-fail | backend parent=`704d87a6`，checkout=`58aaf3421`；实现尚未提交 |
| `scripts/ci/run-external-services.sh` | current-workspace | not-run | 本机无 Docker command/daemon |
| Ruby YAML + Compose model assertion | current-workspace | pass | 9 个名称唯一且匹配 `ruoyi-namewta-*`；17 个宿主机端口均为 4xxxx；默认服务仅 mysql/redis/minio；无 host network/privileged |
| `git diff --check` + SpecDev implement validator | current-workspace | pass | diff 无空白错误；`0 error(s), 0 warning(s)` |
| `docker compose ... config --quiet` + SHA-256 对照 | remote `172.16.105.9` | pass | 远程解析成功；Compose、MySQL client config、Redis config 与本地哈希一致 |
| fresh MySQL init + SQL assertions | remote `172.16.105.9` | pass | init done、0 restart、0 ERROR；`ry-vue` 86 tables、5 NAMEWTA tables、2 user types、3 retired workspaces 生效，应用账号可读 |
| Redis/MinIO data-plane smoke | remote `172.16.105.9` | pass | Redis 认证写/读/删通过；MinIO bucket 上传、公开只读 GET、删除通过；OSS 两行配置匹配运行时凭据 |
| middleware network check | current-workspace -> remote | pass | `43306/46379/49000/49001` 均可达，MinIO ready endpoint 返回成功 |
| runtime isolation audit | remote `172.16.105.9` | pass | 仅 3 个 `ruoyi-namewta-*` 容器、全部 healthy/0 restart/bridge；旧 `cde-infra` 三容器仍 running，旧 MySQL 数据路径未变 |
| local secret boundary | current-workspace | pass | `application-local.yml` mode `0600` 且命中 backend `.gitignore`；YAML 只覆盖远端 MySQL/Redis，未向仓库或 Evidence 写入敏感值 |
| `corepack pnpm install --frozen-lockfile` | frontend workspace | pass | 使用 lockfile 固定的 pnpm `10.34.5`，依赖无需变更 |
| `./mvnw clean install -Dmaven.test.skip=true -Plocal` | backend workspace | pass | 41 modules reactor BUILD SUCCESS；按用户参考命令跳过测试，不计作测试通过 |
| `../mvnw spring-boot:run -Dmaven.test.skip=true -Pdev -Dspring-boot.run.profiles=dev,local` | local backend | pass / running | profiles 顺序为 `dev,local`；远端 MySQL/Redis 连接成功，Jetty 监听 `0.0.0.0:8080` |
| `corepack pnpm dev` | local frontend | pass / running | Vite `8.1.5` 监听 `http://localhost:80/` |
| direct/proxy client context smoke | current-workspace | pass | `:8080/auth/client/context` 与 `:80/dev-api/auth/client/context` 均 HTTP 200，客户端上下文可用 |
| MinIO browser preflight | current-workspace -> remote | pass | `Origin: http://localhost` + `PUT` 返回 204，允许请求头并回显显式 Origin |
| focused OSS Maven red/green | backend workspace | pass | 修复前 2 个预期失败：Complete `data=null`、完成态 Resume 无状态；修复后相关 14 tests 零失败 |
| `./mvnw test` | backend workspace | pass | 133 tests，0 failure/error；5 个显式 property-gated 外部服务测试 skipped |
| `./mvnw clean package -DskipTests` + full bundle assertion | backend workspace | pass | BUILD SUCCESS，`verify-admin-bundle.sh full` 通过 |
| `./mvnw clean package -Pbundle-core -Dmaven.test.skip=true` + core bundle assertion | backend workspace | pass | BUILD SUCCESS，`verify-admin-bundle.sh core` 通过 |
| frontend OSS Vitest red/green | frontend workspace | pass | 修复前分别命中缺少签名请求和未导出错误解析器；修复后 4/4 通过 |
| `corepack pnpm lint` + `typecheck` + `test` + `build:prod` | frontend workspace | pass | lint/typecheck/build 通过；Vitest 2 files、6 tests 全通过 |
| `corepack pnpm exec playwright test` | frontend preview + local backend | pass | 安装仓库声明的 Chromium 后 Client context 2/2 通过 |
| authenticated completed-ticket Resume/Complete | local Vite + restarted backend + remote middleware | pass | 使用真实登录会话重放用户票据：Resume HTTP 200、`state=COMPLETED`、同一 `completedOssId`、无签名请求；Complete HTTP 200、`data` 为同一 ID、`msg=操作成功` |
| `bash -n scripts/start-dev.sh` + menu negative cases | current-workspace / macOS Bash 3.2 | pass | 语法通过且无未加花括号的命名变量引用；非法输入与 EOF 均退出 2；菜单只有前端/后端两个选项；无 `nohup` 或行尾后台 `&` |
| `bash /absolute/path/scripts/start-dev.sh` option 1 | current-workspace | pass | 按用户实际绝对路径调用，并在 `pnpm` 可用但 `corepack` 不可见的受限 PATH 下回归；frozen install 使用 pnpm `10.34.5`；Vite `8.1.5` 在 80 启动并输出实时日志；`Ctrl+C` 后端口释放、无残留进程 |
| frontend no-auto-open/strict-port probe | isolated local port | pass | `BROWSER=none` + `--strictPort` 启动后没有自动浏览器请求；修正 `--open=false` 被 Vite 当成字符串路径的错误假设 |
| `bash /absolute/path/scripts/start-dev.sh` option 2 | current-workspace + configured middleware | pass | 既有 41 modules reactor install BUILD SUCCESS（tests skipped，不计测试通过）及 `dev,local` HTTP 200 结果保持有效；按用户实际绝对路径复测已通过命令预检并正确识别当前 8080 监听进程，未终止该进程 |
| occupied port protection | current-workspace | pass | 临时前台服务占用 80 后，菜单 1 报告监听 PID 并退出 1；既有监听进程未被终止 |

- **失败后修复与重跑：** 修复 BSD grep 字符类、dirty 子仓检查、pnpm 子模块缓存路径和 clean matrix；首次远程 MySQL init 因客户端未使用 utf8mb4 在中文昵称处中止，失败数据留档后挂载 client config 重跑通过。一次诊断列标题展开了首次生成的 MinIO 密钥，该密钥已立即轮换并验证失效；当前 env/container/database 三方一致。首次本地后端启动因 `application-local.yml` 内部 import 优先级未覆盖 dev datasource 而连接 `localhost` 失败；移除 import，改为显式 `dev,local` profile 顺序后重跑成功。OSS 回归先固定 Complete/Resume 两个后端失败和完成态恢复/错误提示两个前端失败，再完成实现并全量重跑；Playwright 首次受 pnpm 版本解析和缺少 Chromium 阻断，未产生业务失败，改用锁定 pnpm 并安装声明浏览器后 2/2 通过。启动脚本首次非法输入在 macOS Bash 3.2 下因 `$choice` 紧邻中文全角括号被扩展为错误变量名，改用 `${choice}` 后按预期退出 2；用户随后按绝对路径真实启动时，`$command_name` 紧邻中文逗号触发了同一问题，现已将脚本所有命名变量统一为 `${...}` 形式，并以相同绝对路径分别回归前端启动和后端占用保护。用户从交互终端启动又发现脚本强制要求 `corepack`，现已改为优先使用可直接调用的 `pnpm`、缺失时才回退 `corepack pnpm`，并在显式隐藏 `corepack` 的 PATH 下真实启动验证。首次尝试 `--open=false` 被 Vite 解析为待打开路径，改用 `BROWSER=none` 后隔离验证无自动请求。前端首次真实验证的自动浏览器曾使既有页面在后端未启动时产生含会话查询参数的本地代理错误输出，未将其值写入工件；脚本现默认禁止自动打开浏览器以避免启动动作主动触发该路径。
- **供应商差异：** 开源 MinIO `RELEASE.2026-04-17` 不实现桶级 CORS，也不接受 `AbortIncompleteMultipartUpload` 桶生命周期规则；本实例的集群级显式 CORS 已通过实际预检，服务内置 `stale_uploads_cleanup_interval=6h`、`stale_uploads_expiry=24h`。后端启动诊断仍会对这两项 S3 桶能力给出 provider-specific warning。
- **恢复留档：** 首次失败的 MySQL 数据保留于远端 `/data/ruoyi-vue-plus-namewta/runtime/recovery/mysql-data-failed-20260824-101053`，未执行不可恢复删除。
- **未运行检查：** Redis/MySQL/MinIO 五项 Maven 真实集成测试和 GitHub Actions 首次 run；真实服务已手工验收，但 commit/push 未授权。
- **E2E：** required / partial；Playwright Client context 2/2、远程 MySQL/Redis/S3 手工数据面、本地真实后端和 Vite proxy、认证后真实完成票据 Resume/Complete 均通过，Maven external-services E2E pending。

## 6. 双轴审查

### 标准轴

- **固定输入：** `aa4752fe2d51ce705d442c9567e8e33f3676b736` + parent/child working trees
- **结果：** pass
- **Findings 与修正：** 修复 shell portability、pnpm cache path、bundle stale artifact 与迁移测试业务表破坏风险。

### 规范轴

- **固定输入与来源：** Spec、T-03、CR-001、CR-002
- **结果：** pass
- **Findings 与修正：** CI candidate 覆盖合同命令；未执行真实容器与远程 required check 明确保留为 completion gate。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `aa4752fe2d51ce705d442c9567e8e33f3676b736` |
| Implementation/source SHA | `null` |
| Candidate branch/workspace | current |
| Method/conflicts | uncommitted cross-repository candidate |
| Integration checks | local frontend/backend 全量门禁、full/core bundle、两选项前台启动生命周期、真实 middleware 连接、proxy 和完成票据 smoke passed |
| E2E disposition | required |
| E2E result | partial；Playwright 2/2、指定远程 external-services 手工数据面、本地真实后端/Vite proxy 和真实完成票据幂等恢复 passed，Maven disposable suite not-run |
| Parent result/re-read | `null`；parent snapshot intentionally still red |

## 8. 偏差与决策

- **偏差：** `DEV-T03-001` / ticket；用户要求主 Compose 容器名整体使用 `ruoyi-namewta-*`，原 Ticket 未授权该路径。
- **触发证据：** 2026-08-24 用户明确指定 `<Path>ruoyi-vue-plus-namewta/script/docker/docker-compose.yml</Path>` 和目标前缀。
- **处理：** 扩充 T-03 的 expected/writable paths 与容器命名验证，不改变服务键、镜像、端口、卷、网络模式或其他 Compose 文件。
- **批准来源及影响：** 用户于 2026-08-24T09:29:34+08:00 明确批准该范围；变更可通过恢复 9 个 `container_name` 回滚，不涉及远程、数据或运行中容器操作。
- **偏差：** `DEV-T03-002` / ticket；用户要求通过 4xxxx 映射消除端口冲突并批准在指定主机初始化 middleware，与原“网络模式保持不变”约束冲突。
- **触发证据：** 2026-08-24 用户批准已展示的 bridge、profiles、外置 secret、fresh 初始化、远程部署和验收计划。
- **处理：** 修订 T-03 路径与合同；旧 `cde-infra` 只读，新实例使用独立名称、网络、端口和数据根；frontend/backend 不部署。
- **批准来源及影响：** 用户明确回复“执行”；授权本地配置修改和计划内远程部署，不包含 commit、push、旧容器清理或生产数据删除。
- **偏差：** `DEV-T03-003` / ticket；用户在 middleware 部署完成后要求配置本地 dev 账密并启动前后端测试。
- **触发证据：** 2026-08-24 用户明确指定 `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/main/resources</Path>`，并给出 pnpm/Maven 同技术栈启动命令作为参考。
- **处理：** 显式激活 `dev,local`，使用 Git 忽略的 `application-local.yml` 最后覆盖远端 MySQL/Redis；本地启动后端与 Vite，远端仍只运行 middleware。
- **批准来源及影响：** 当前用户请求授权本地配置与进程启动；不包含 commit、push、远端 frontend/backend 容器或旧服务变更。
- **偏差：** `DEV-T03-004` / ticket；真实浏览器直传验收发现 Complete 成功结果因 Java 重载选择进入 `msg` 而不是 `data`，导致前端在对象和元数据均已创建后误报失败，并保留已完成的本地恢复记录。
- **触发证据：** 用户提供的 Complete 响应为 `code=200,data=null,msg=<ossId>`，OSS 管理列表已有同一文件记录；源码确认 `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/src/main/java/org/dromara/system/controller/system/SysOssUploadController.java</Path>` 调用了 `R.ok(String)`，前端随后对空 `data` 执行 `requireData`。
- **处理：** 扩充 T-03 的 expected/writable paths；以 `R.data` 恢复既有响应合同，为完成态 Resume 增加 `state/completedOssId` 并让前端短路遗留票据，同时让上传组件展示未被 request adapter 处理的具体错误。既有数据库记录、对象、配置、管理列表 URL 脱敏和全局内容去重语义保持不变。
- **批准来源及影响：** 用户于 2026-08-24T11:07:27+08:00 明确回复按已给执行计划修改；授权上述本地代码、测试和 Evidence 调整，不包含 commit、push、数据库清理或远程部署变更。
- **偏差：** `DEV-T03-005` / ticket；用户在本地前后端验收后要求把已验证的启动顺序封装为父仓根目录的一键前台启动脚本，原 Ticket 仅授权 CI scripts。
- **触发证据：** 用户要求 `<Path>scripts/start-dev.sh</Path>` 严格提供“启动前端”和“启动后端”两个选项，保持当前终端与实时日志，并批准按已展示计划实施。
- **处理：** 扩充 T-03 expected/writable paths 和本地启动合同；脚本只做环境/端口预检、依赖刷新和前台进程替换，不后台化、不自动终止已有进程、不读取或输出本地 secret。
- **批准来源及影响：** 用户于 2026-08-24T11:59:08+08:00 明确回复“根据这个计划进行实施”；授权脚本、scripts 说明、Ticket/Evidence 和本地启动验证，不包含 commit、push、部署或进程清理。

## 9. 残余风险与交付定位

- **残余风险/已知限制：** 未证明容器镜像在 GitHub runner 上拉取/启动；远程 firewalld 未运行且当前为内网明文端口，仅通过绑定指定内网接口限定；启动器禁止自动打开浏览器，但用户手动打开前端且后端不可用时，现有 SSE 查询参数仍可能出现在 Vite proxy error 中，该应用级日志脱敏不属于本脚本范围；未自动化新文件经组件执行实际 PUT 的浏览器路径，也未覆盖真实后端的 cross-Client role、multipart retry/resume/cancel、signed download 与 notification attachment 全链路。当前页面交互日志仍可见既有 `ResizeObserver`、Element Plus form width、未配置第三方平台和 SSE 警告，未导致 Vite 或后端退出。
- **后续 Ticket：** T-04；提交后首次 CI run 与分支保护需单独授权
- **监控或回滚触发：** 任一 external-services test skipped 或 snapshot mismatch 时 CI 必须失败
- **Source commit：** `null`
- **Parent result：** `null`
- **Source workspace：** repository current workspace
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-24-upstream-fork-upgrade-remediation/evidence/T-03.md</Path>`
