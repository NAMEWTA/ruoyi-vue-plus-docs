# Evidence: T-05 - 以 demo 建立 Domain 与 WebDomainManifest 曳光弹

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/05-demo-manifest-pilot.md</Path>`
- **Spec / Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/spec.md</Path>` / `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/goal-plan.md</Path>`
- **Lead：** `codex:/root`
- **Workspace / branch：** `specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-05` / `speculo/2026-08-25-plus-ui-multi-app-domain-architecture/T-05`
- **Base / source / candidate / result SHA：** `8aae13100c18a5a5a8e5cfaaa95fecbee8f21477` / `89cab310a9ff355733b5b0074aeb0b32b92ef00b` / `89cab310a9ff355733b5b0074aeb0b32b92ef00b` / `89cab310a9ff355733b5b0074aeb0b32b92ef00b`
- **Result tree：** `e2e981620c743b80d5fe5adc137cb42c5377f761`
- **状态：** done

## 1. 实现摘要

激活 `@namewta/platform-app-runtime`、`@namewta/domain-demo` 与 `@namewta/web-domain-demo` 三个 private workspace package。demo API、transport types 与页面实现迁入 domain/web-domain，根 `<Path>src/api/demo/**</Path>` 和已知 demo/tree views 保留为薄兼容 facade。

`WebDomainManifest` 公开 messages namespace、permission contributions 与 stable web registrations。App runtime 只组合所选 domain/manifest，先过滤再验重，拒绝 message namespace、permission ID、registration ID 与 component key 冲突，并返回克隆冻结的贡献快照。根 demo/table/tree 页面通过 `composeAppRuntime -> resolve -> load` 进入 registry；显式 `<Path>src/views/demo/manifest-diagnostic/index.vue</Path>` harness 将选定的 missing registry key 呈现为含 code/App/domain/key 的可见错误。

demo transport 对单 ID 使用 path-segment 编码，对批量 ID 逐项编码并保留分隔逗号。按 `DEV-T05-002` 原样保持既有后端 `PUT/DELETE` 方法，不单边改变 API。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** `native:/root/t01`
- **最终 source checkpoint：** `89cab310a9ff355733b5b0074aeb0b32b92ef00b`；tree `e2e981620c743b80d5fe5adc137cb42c5377f761`；source worktree clean；未在 source 运行或声明 Playwright pass
- **实现提交链：**
  - `e8d075172deeacac090e1b7bcde6b961ec883f5c` - `feat(architecture): add demo manifest pilot`
  - `53b332ededd87d855721b936aa140b5b7d607195` - `fix(architecture): harden demo manifest routing`
  - `89cab310a9ff355733b5b0074aeb0b32b92ef00b` - `test(architecture): label manifest diagnostic harness`
- **Lead 接收：** 固定 diff、branch/tip/tree、ancestor、clean、路径 owner、root manifest/lock、protected path 与生成声明均复核通过

## 3. 修改范围与路径所有权

| 路径 | 所有权 / 结果 |
|---|---|
| `<Path>packages/platform/app-runtime/**</Path>` | `DEV-T05-001` 激活；通用 manifest/registry 合同与测试 |
| `<Path>packages/domains/demo/**</Path>` | T-05 writable；无头 transport/models/service 与 ID 编码 |
| `<Path>packages/web-domains/demo/**</Path>` | T-05 writable；Vue pages/runtime/manifest contributions |
| `<Path>src/api/demo/**</Path>`、`<Path>src/views/demo/**</Path>` | 兼容 facade、registry route adapter 与显式诊断 harness |
| `<Path>package.json</Path>`、`<Path>pnpm-lock.yaml</Path>` | `DEV-T05-001` 精确新增三个 root `workspace:*` consumers 与三个 package importers |
| `<Path>e2e/demo-manifest.spec.ts</Path>` | 有效 registry keep-alive 与显式 diagnostic harness 两场景 |

`pnpm-workspace.yaml`、根 scripts/catalog/外部版本/既有 resolution、全局 permission/router/store 与生成声明未改。domain 无 Vue/DOM/browser concrete 依赖，跨包只走 public exports，architecture graph 无环。

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-010 | selected-only 正反夹具；未选重复 domain/manifest 不影响当前 App | pass |
| AC-011 | namespace、permission ID、registration ID 与 component key 冲突均 fail-fast 并含双方 owner | pass |
| AC-012 staged | registry missing error、可见 adapter 与显式 harness 通过；任意无物理 facade typo key 按 `DEV-T05-003` 由 T-07 关闭 | T-05 scope pass |
| AC-022 | Gate E 全门禁/E2E 通过后才推进 parent 与 T-06 | pass |
| AC-027 | 手工 transport types 与 API path/method/encoding tests | pass |

## 5. Workspace Verification

最终 `source-worktree` checkpoint 非 E2E 检查均 exit 0：targeted Vitest `5 files / 22 tests`；architecture `11 workspaces / 0 violations` 与 `92/92`；lint；typecheck；root unit `16 files / 71 tests` 加 workspace tests；production build（含 demo/tree/manifest-diagnostic chunks 与 10 child builds）；frozen install；11 workspace list；scoped Oxfmt check；diff/protected/generated audit。source 未运行 Playwright。

TDD RED 首先出现旧全量验重、旧 components 合同和未编码 ID 的 `3 suites / 9 failures`，随后逐切片转绿。source 首次并行全门禁中 lint 因 frozen install 同时刷新 `node_modules` 链接而暂时找不到 oxlint，unit/build 并行资源争用导致 exit 137；依赖稳定后串行重跑均 exit 0，不把失败轮写成通过。

## 6. 双轴审查

- **固定输入：** `8aae13100c18a5a5a8e5cfaaa95fecbee8f21477..89cab310a9ff355733b5b0074aeb0b32b92ef00b`
- **标准轴最终结论：** pass，无 findings。已关闭 registry 未接生产 facade、`toSorted` 浏览器兼容、浅不可变、ID 未编码和 API-005 未登记例外；`DEV-T05-003` 后 E2E 不再冒充通用 unknown-key 证明。
- **规范轴最终结论：** pass，无 findings。已关闭 selected-only 反例、manifest 贡献面缺失和 Gate E route adapter 证明；路径、headless/public exports、root/lock deviations 与 staged AC-012 归属一致。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before | `main@8aae13100c18a5a5a8e5cfaaa95fecbee8f21477`；clean，tree `3faee8a1a52691607aa63229407ffb9224838d8f` |
| Candidate | branch `speculo/integration/2026-08-25-plus-ui-multi-app-domain-architecture/T-05`；workspace `specdev-worktree/.integration/2026-08-25-plus-ui-multi-app-domain-architecture/T-05`；fast-forward，无冲突 |
| Candidate/result | `89cab310a9ff355733b5b0074aeb0b32b92ef00b`；tree `e2e981620c743b80d5fe5adc137cb42c5377f761` |
| Full suite | frozen install；11 workspaces；architecture 0 + 92/92；lint；typecheck；root 16 files/71 tests + workspace；production build 3381 modules + 10 child builds，均 exit 0 |
| Playwright | `pnpm test:e2e` exit 0；Chromium `8/8` passed in `11.8s`；含 registry keep-alive 与显式 manifest-diagnostic harness |
| Promotion | 父 HEAD 未漂移后 `git merge --ff-only`；重读 `main@89cab310...`、tree/ancestor/clean 均 pass |
| Candidate cleanup | 已按 transient integration 授权删除 candidate worktree/branch；source worktree/branch 保留 |

## 8. 偏差与决策

- **DEV-T05-001 / approved：** T-05 激活 app-runtime，并只写 root facade 实际消费的三个 `workspace:*` 声明、匹配 root lock specifier 与三个 package importers；T-03 保留策略 owner。
- **DEV-T05-002 / approved compatibility：** 保持现有 demo `PUT/DELETE` 后端合同；禁止新增非 POST 变更端点；协调后端迁移后到期。
- **DEV-T05-003 / approved ownership：** 显式 diagnostic harness 只证明 registry/adapter 可见错误，不代表任意后端未知 key 能穿过旧 views glob。最终 AC-012 外部行为不变，由 T-07 使用无物理 facade typo key 在全局 permission/router 集成中关闭。
- **残余风险：** 全局 dynamic route 仍使用旧 views glob；这是 T-07 的显式 shared-path 工作，不是 Gate E 隐性完成项。

## 9. 残余风险与交付定位

- **前端父分支/result：** `main@89cab310a9ff355733b5b0074aeb0b32b92ef00b`
- **Source workspace：** `specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-05`（cleanup 未授权）
- **后续 Ticket：** `T-06`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/evidence/T-05.md</Path>`
