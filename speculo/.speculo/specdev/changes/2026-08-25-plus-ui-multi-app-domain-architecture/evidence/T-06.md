# Evidence: T-06 - 激活 client-web 第二 App 组合证明

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/06-client-web-proof.md</Path>`
- **Spec / Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/spec.md</Path>` / `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/goal-plan.md</Path>`
- **Lead：** `codex:/root`
- **Workspace / branch：** `specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-06` / `speculo/2026-08-25-plus-ui-multi-app-domain-architecture/T-06`
- **Base / source / candidate / result SHA：** `89cab310a9ff355733b5b0074aeb0b32b92ef00b` / `6b873176daef467d935d01750cd9e88493bade4e` / `6b873176daef467d935d01750cd9e88493bade4e` / `6b873176daef467d935d01750cd9e88493bade4e`
- **Result tree：** `703c5de2c7547331d7135cca08c291f1de3be3a0`
- **状态：** done

## 1. 实现摘要

激活独立 `@namewta/client-web` App、无头 `@namewta/domain-identity-access`、可注入的 `@namewta/web-domain-identity-access`，以及 design tokens 与 shell web-kit。client-web 只组合 identity-access 与 demo，使用 App 注入并严格校验的 ClientContext、按 App/Client 编码的 sessionStorage key、独立绿色主题和 Router-owned shell。

登录流程严格执行 `Client context -> captcha configuration -> login`。非法或禁用 Client 在验证码和登录请求前 fail-close；启用验证码时必须有非空 image/uuid，页面支持刷新并在 pending/失败时保持禁用。登录请求的 body 与 `clientid` header 使用同一 Client。demo 复用既有 domain/web-domain，并由 client App 装配其所需 toolbar、pagination 与 icons；未选择 workflow 的 component key 通过真实 runtime registry 输出稳定诊断。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** `native:/root/t01`
- **最终 source checkpoint：** `6b873176daef467d935d01750cd9e88493bade4e`；tree `703c5de2c7547331d7135cca08c291f1de3be3a0`；source worktree clean；未在 source 运行或声明 Playwright pass
- **实现提交链：**
  - `de1bd3302e048a94c6e1c51307429e593998bd0e` - `feat(architecture): prove isolated client web composition`
  - `82f791eb69929a9b4e35646ed197dc1180a0e935` - `fix(architecture): complete T-06 client proof contracts`
  - `45ba9d8b48cfa63b1ba8f7e90b1a3fc78b5ce8d0` - `test(architecture): prove dual Client session isolation`
  - `6b873176daef467d935d01750cd9e88493bade4e` - `test(architecture): await dual Client terminal states`
- **Lead 接收：** 固定 diff、commit 链、branch tip/tree、ancestor、clean、路径 owner、lock importer 和 protected/generated paths 均独立复核
- **只读审查修正：** 第一轮关闭验证码不可用、demo 宿主组件缺失和硬编码根链接；第二轮补齐双 Client 真登录矩阵；第三轮关闭 E2E 提前通过与调度型 flaky

## 3. 修改范围与路径所有权

| 路径 | 所有权 / 结果 |
|---|---|
| `<Path>apps/client-web/**</Path>` | T-06 writable；独立 config/main/router/composition/shell/theme 与 package build |
| `<Path>packages/domains/identity-access/**</Path>` | T-06 writable；无头 Client/login/session use case 与严格响应解析 |
| `<Path>packages/web-domains/identity-access/**</Path>` | T-06 writable；登录 manifest、页面及可测试状态机 |
| `<Path>packages/web-kit/design-tokens/**</Path>`、`<Path>packages/web-kit/shell-element/**</Path>` | T-06 writable；client theme、App-owned brand navigation、toolbar/pagination |
| `<Path>e2e/client-web-proof.spec.ts</Path>` | T-06 writable；登录、fail-close、registry、shell/theme 与双 Client 矩阵 |
| `<Path>pnpm-lock.yaml</Path>` | `DEV-T06-001`；仅 5 个新 importer 及 client 对既有 icons resolution 的 specifier |

根 `<Path>package.json</Path>`、`pnpm-workspace.yaml`、`src/**`、demo/platform/adapters、catalog、外部版本、既有 importer/resolution 和生成声明均未改；无未声明路径。

## 4. 验收与合同映射

| Contract | 证据 | 结果 |
|---|---|---|
| AC-003 | client 独立 build/login；composition 只含 identity/demo；未选 workflow registry 诊断；不同 shell/theme | pass |
| AC-018 | 同一 BrowserContext 内 admin/client 真登录；两侧 header/body Client ID、网络终态及 token namespace 双向隔离 | pass |
| AC-019 | identity domain 未复制；App/web-domain/web-kit 承担页面、布局、主题与宿主控件；双 App build | pass |
| AC-023 | Gate F candidate 全门禁通过后才推进 parent 并解锁 T-07；admin 兼容入口保持 | pass |

## 5. Workspace Verification

最终 `source-worktree` 非 E2E 检查均 exit 0：identity domain `13/13`、identity web-domain `5/5`、shell `3/3`、client `10/10`；frozen install；16 workspace list；architecture `0 violations` 与 `92/92`；lint；typecheck；root unit `23 files / 103 tests` 加全部 workspace tests；admin 与 client production build；E2E static TypeScript/list；scoped Oxfmt/diff/path/lock/generated audits。source 按 required 模式未运行 Playwright。

TDD RED 先由 identity domain 精确出现 5 个预期失败，覆盖验证码空白规范化及缺失、空值、非字符串 image/uuid；实现后转绿。E2E static TypeScript 首次受 TypeScript 6 `TS5112` 配置发现规则退出 2，使用明确 `--ignoreConfig` 的静态 harness 重跑 exit 0；该工具调用调整未放宽产品检查。

## 6. 双轴审查

- **固定输入：** `89cab310a9ff355733b5b0074aeb0b32b92ef00b..6b873176daef467d935d01750cd9e88493bade4e`
- **标准轴最终结论：** pass，无 findings。已关闭 enabled captcha 不可用、demo 全局组件缺失、shell 根链接、双 Client 终态提前断言和异步 flaky；认证顺序、Client header/body、session 隔离、错误边界、registry 与 lock/path 均通过。
- **规范轴最终结论：** pass，无 findings。双 Client 真登录、双向 token 隔离、selected-only、不同壳层与 App-injected ClientContext 足以关闭 Gate F；临时 remove-all 明确只是 T-06 fail-closed 边界，不冒充 T-07 完整权限迁移。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before | `main@89cab310a9ff355733b5b0074aeb0b32b92ef00b`；clean，tree `e2e981620c743b80d5fe5adc137cb42c5377f761` |
| Candidate | branch `speculo/integration/2026-08-25-plus-ui-multi-app-domain-architecture/T-06`；逻辑 locator `specdev-worktree/.integration/2026-08-25-plus-ui-multi-app-domain-architecture/T-06`（本轮物理 checkout 为 `specdev-worktree/.integration/T-06`）；fast-forward，无冲突 |
| Candidate/result | `6b873176daef467d935d01750cd9e88493bade4e`；tree `703c5de2c7547331d7135cca08c291f1de3be3a0` |
| Full suite | frozen install；16 workspaces；architecture 0 + 92/92；lint；typecheck；root 23 files/103 tests + workspace；admin build 3381 modules + 15 child workspaces，均 exit 0 |
| Playwright | `pnpm test:e2e` exit 0；Chromium `12/12` passed in `14.4s`；client preview `4174`，Playwright-owned admin preview `4173` |
| Promotion | 父 HEAD 未漂移后 `git merge --ff-only`；重读 `main@6b873176...`、tree/ancestor/clean 均 pass |
| Candidate cleanup | 已按 transient integration 授权删除 candidate worktree/branch；source worktree/branch 保留 |

## 8. 偏差与决策

- **DEV-T06-001 / approved：** T-06 只写其目录 manifests 机械生成的 5 个新增 lock importer/specifier；修正阶段 client importer 增加仓库既有 `@element-plus/icons-vue@2.3.2` specifier。root manifest、workspace/catalog、外部版本、既有 importer/resolution 均保持只读。
- **T-06/T-07 边界：** T-06 仅证明最小 identity 登录与 fail-closed demo 权限宿主；完整注册/OAuth、401、getInfo/getRouters 动态权限、AccessEvaluator 和全局 typo-key fail-visible 仍由 T-07 独占。

## 9. 残余风险与交付定位

- **已知限制：** client 首屏 chunk 约 `1.34 MB`，Vite 报 >500 kB 非阻塞警告；后续领域装配时需要按实际性能基线做 vendor/code splitting，不能通过抬高阈值掩盖。
- **前端父分支/result：** `main@6b873176daef467d935d01750cd9e88493bade4e`
- **Source workspace：** `specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-06`（cleanup 未授权）
- **后续 Ticket：** `T-07`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/evidence/T-06.md</Path>`
