# Evidence: T-10 - 迁移 system-admin 身份与组织治理

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/10-system-identity-governance.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `codex:/root`
- **Source workspace / branch：** `specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-10` / `speculo/2026-08-25-plus-ui-multi-app-domain-architecture/T-10`
- **Base / source / candidate / result SHA：** `39f7eaef8bff2002ba6824a2616e2050991e6959` / `9ce426c20be49209d24b40083e14eede4ea31109` / `9ce426c20be49209d24b40083e14eede4ea31109` / `9ce426c20be49209d24b40083e14eede4ea31109`
- **状态：** done

## 1. 实现摘要

system-admin 已提取 Client、用户、用户类型、角色、菜单、部门和岗位的无头 service/models，并将 11 个治理页面迁入显式可选的 system web-domain。根 API/types/views 保留为兼容 facade；admin-web 选择该 domain/manifest，client-web 不选择。当前会话 profile 页面仍由宿主静态路由承载，但其 transport 已通过 domain service 复用。

## 2. Lead Dispatch And Candidate Return

- 原 implementation owner `native:/root/t01` 未形成可接收 checkpoint；Lead 在既有 T10 source worktree 接管实现、修复并创建 commit。
- **Source checkpoint：** 首提交 `4a0964232b181c34ee112cbac15996344c5eae4a`，按首轮审查追加修复提交 `9ce426c20be49209d24b40083e14eede4ea31109`；worktree clean。
- **双轴审查：** 固定输入 `39f7eaef8bff2002ba6824a2616e2050991e6959..9ce426c20be49209d24b40083e14eede4ea31109`；规范轴与标准轴均 PASS。

## 3. 修改范围与路径所有权

| 路径 | 所有权 / 结果 |
|---|---|
| `<Path>packages/domains/system-admin/**</Path>` | T10 writable；七组治理服务/models；T09 `public/user/**` 字节不变 |
| `<Path>packages/web-domains/system-admin/**</Path>` | T10 writable；治理页面、host runtime、manifest 与 `./pages` 公开入口 |
| `<Path>src/api/system/{client,user,userType,role,menu,dept,post}/**</Path>` | 兼容 facade；原路径/方法保持 |
| `<Path>src/views/system/{client,user,userType,role,menu,dept,post}/**</Path>` | 兼容 facade；旧 component keys/name/props/expose 保持 |
| `<Path>package.json</Path>`、`<Path>pnpm-lock.yaml</Path>`、`<Path>src/router/adminManifestRegistry*</Path>` | `DEV-T10-001` 限定的 workspace importer 与 selected-only wiring |
| `<Path>e2e/system-identity.spec.ts</Path>` | T10 required E2E；仅 Lead 在 candidate 运行 |

## 4. 验收与合同映射

| Contract | Source 证据 | 当前结果 |
|---|---|---|
| AC-009 / AC-010 | domain/manifest tests、admin selected registry、client unselected E2E | source/candidate pass |
| AC-017 / AC-018 | permission evaluator host port、Client headers、失败不回退 unit/E2E | source/candidate pass |
| AC-021 | lint/typecheck/unit/architecture/双 build/full E2E | source/candidate pass |

## 5. Workspace Verification

- **source-worktree：** `specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-10`。
- `pnpm lint`、`pnpm typecheck`、`pnpm test`：exit 0；root Vitest `36 files / 153 tests`，全部 workspace tests 通过。
- `pnpm architecture:check`：21 workspaces / 0 violations；`pnpm architecture:test`：`92/92`。
- `pnpm build:prod`：exit 0；admin/client 分别转换 `3436` / `1718` modules。
- domain transport matrix：65 条调用；domain `2 files / 7 tests`，web-domain `1 file / 3 tests`。
- T09 `public/user/index.ts` 与 test 的 Git blob 分别为 `0a330cf...` / `78c3c71...`，SHA-256 分别为 `fe03ccd...` / `b83bdfb...`，与 base 完全一致。
- source 按 required-worktree 合同未运行 E2E；Lead 已在 parent-candidate 完成专用和 full dual-App E2E。

## 6. 双轴审查

- **固定输入：** `39f7eaef8bff2002ba6824a2616e2050991e6959..9ce426c20be49209d24b40083e14eede4ea31109`
- **标准轴：** PASS；确认浏览器兼容、确认取消与 service rejection 边界、具体 DTO/VO/PageResult、manifest permission inventory、selected-only composition 和 65 条 transport 合同均符合工程标准。
- **规范轴：** PASS；确认真实 Client 选择/role-menu query/PUT mutation E2E 合同、七切片类型合同、README 依赖/资源说明、T09 seam 与路径所有权均符合 Spec。

## 7. Integration Verification

- candidate 从 parent `39f7eaef8bff2002ba6824a2616e2050991e6959` fast-forward 到 `9ce426c20be49209d24b40083e14eede4ea31109`，tree `1672fcb46b94acbb1f5f71cefaa1a8aa77415987`；frozen install 通过。
- 完整静态 Gate：21 workspaces / 0 architecture violations、architecture `92/92`、lint、typecheck、root `36/153` 与全部 workspace tests、admin/client build `3436/1718` modules，全部 exit 0。
- required E2E：T10 专用 Chromium `3/3`；full attempt 1 为 `31/32`，唯一失败是既有 workflow 全局“确定”定位器的并发时序歧义，原场景单独复跑 `1/1` 后，full attempt 2 `32/32`。未修改既有 workflow 测试，父分支只在第 2 次完整通过后推进。
- Lead 将前端 `main` fast-forward 到 result `9ce426c20be49209d24b40083e14eede4ea31109`；候选 worktree/branch 已按授权删除，source worktree/branch 保留且 clean。

## 8. 偏差与决策

- `DEV-T10-001`：仅允许两个实际 workspace 依赖、机械 lock importer 与最小 admin registry host ports；candidate 必须复核结构化 diff 和 T09 seam 字节不变。
- `DEV-T10-002`：保留全部既有 PUT/DELETE 路径与方法；仅在后端协调迁移并更新全部消费者后到期。

## 9. 残余风险与交付定位

- 当前会话 profile 仍为宿主静态页面；T15 在 facade 零引用且 full E2E 后再决定收缩。
- T11 才激活 dict/config/notice/OSS/message/social-resource 管理切片。
- **Source commits：** `4a0964232b181c34ee112cbac15996344c5eae4a`、`9ce426c20be49209d24b40083e14eede4ea31109`
- **Parent result：** `plus-ui-namewta main@9ce426c20be49209d24b40083e14eede4ea31109`，tree `1672fcb46b94acbb1f5f71cefaa1a8aa77415987`
