# Evidence: T-11 - 迁移 system-admin 资源与内容服务

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/11-system-resource-services.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `native:/root/t01`
- **Base / source / candidate / result SHA：** `9ce426c20be49209d24b40083e14eede4ea31109` / `b79687427b9b00d6e5ab5990f104d4a4bb8dc984` / `b79687427b9b00d6e5ab5990f104d4a4bb8dc984` / `b79687427b9b00d6e5ab5990f104d4a4bb8dc984`
- **状态：** done；Gate G5 已关闭，source worktree/branch 保留

## 1. 实现摘要

T11 将 dict type/data、config、notice、OSS、OSS config、message 与 social 的 46 项 transport/models 提取到既有 system-admin domain，增加最小 public dict/menu contracts；五个管理页迁入同一 web-domain/manifest，旧 API/types/views 保留 facade。OSS URL/presigned request、公告富文本与字典缓存继续复用宿主安全实现。

## 2. Lead Dispatch And Candidate Return

- source-worktree：`specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-11`
- 初始 checkpoint：`6bcbc31bb7282071473f8a90c93033ef3222e4f6`；安全修复 `fa61f2504476ac75852bdac4107da344597f1597`；attempt-1 fixture 修复 `b57607587eb90e836a70141965251d1861f4ba3d`；attempt-2 accessible-name 修复 `3074bcfdba0dd49406ed979c27f17cf26157e7f4`；attempt-3 fixture 修复/source checkpoint：`a067d10c15d2ee0353ae305fde97c0b1f00ceb8c`，tree `fac51b711edda99c6e743f165b3d0129720d654c`。
- 首次固定点双轴 `REQUEST CHANGES` 后，`fa61f250` 双轴均 `PASS`；attempt 1 fixture 修复固定点 `b5760758` 双轴也均 PASS。candidate attempts 1/2 均未推进父分支，integration attempts 为 `2`。

## 3. 修改范围与路径所有权

- Ticket writable paths 覆盖 system-admin packages、七组旧 API/views 与专用 E2E；root package/lock/workspace config 均 zero diff。
- `DEV-T11-001` 仅为既有 system runtime 追加 dict cache、OSS download、sanitize/content 与 Editor/ImagePreview typed host ports，并更新 selected/unselected registry assertions。
- T10 `public/user` Git blobs 保持 `0a330cf...` / `78c3c71...`。

## 4. 验收与合同映射

| Contract | Source 证据 | 当前结果 |
|---|---|---|
| AC-009 / AC-010 | domain/manifest/public ports、admin selected/client unselected 声明 | source pass；candidate pending |
| AC-019 | host ports + web-domain 页面，不复制宿主安全实现 | source pass；candidate pending |
| AC-021 | transport/security/unit/architecture/双 build | source pass；required E2E pending |

## 5. Workspace Verification

- **source-worktree：** frozen install 通过，21 workspaces；architecture check 0 baseline violations，architecture tests `92/92`。
- 修复后 domain `5 files / 17 tests`、web-domain `1 file / 4 tests`；full test root `37 files / 162 tests` 及全部 workspace tests 通过。
- full lint、typecheck、build:prod 通过；root package/lock/generated d.ts zero diff。
- Playwright `--list` 识别 `e2e/system-resources.spec.ts` 4 项；按 required-worktree 合同未在 source 运行 E2E。

## 6. 双轴审查

- 固定输入：`9ce426c20be49209d24b40083e14eede4ea31109..6bcbc31bb7282071473f8a90c93033ef3222e4f6`
- 标准轴：`REQUEST CHANGES`。公告附件 URL 绕过 domain 安全校验；OSS 授权查询失败后错误回退旧 HTTPS URL；public menu 仅类型最小化而未运行时投影；字典 API 失败后 loading 不收敛；E2E 未覆盖上传与 social Client 路径。
- 规范轴：`REQUEST CHANGES`。除上述 URL/public seam/E2E 问题外，social transport 仍使用 `unknown`，message E2E 只证明请求发生而未证明结果进入可见或消费状态。
- 已确认通过：46 项 method/path 合同、路径所有权与 `DEV-T11-001`、root package/lock zero diff、T10 `public/user` blob 不变、五页 key/name/facade 主体等价、无 deep import/cycle。
- 满足条件：在 source branch 创建 append-only 修复 commit，覆盖授权失败 fail-close、notice attachment URL 校验、public menu 递归最小投影、social 具体模型、dict loading `finally`，并补齐 upload/download/social/message 的浏览器声明；随后对新固定点重新执行两个独立审查轴。
- 修复事实：OSS 授权失败不再回退旧 URL；notice attachment 逐 URL 结构化校验；menu 递归最小投影；social 精确类型；dict loading 使用 `finally`；E2E 扩为 4 项并覆盖消息可见消费、signed upload/complete、真实 download、social list/binding/unlock Client 失败。
- 复审固定输入：`9ce426c20be49209d24b40083e14eede4ea31109..fa61f2504476ac75852bdac4107da344597f1597`；规范轴 `PASS`（无 findings），标准轴 pending。规范复审定向验证 domain `17/17`、web-domain `4/4`、architecture `21 packages / 0`，Playwright `--list` 4 项；未运行 E2E，worktree clean。

## 7. Integration Verification

### Attempt 1

- parent before `9ce426c20be49209d24b40083e14eede4ea31109`；candidate `fa61f2504476ac75852bdac4107da344597f1597`，tree `a8c485693b55f5dc9bea54e9e5b633e30e4e9644`，method `fast-forward`。
- candidate setup 首次在错误 checkout 执行快进，使 `main` 短暂指向 source；Lead 立即将 parent checkout detach、把 candidate 精确快进到 source、再以 expected-old-value `update-ref` 把 `main` 恢复到 `9ce426c` 并重验 clean。提交由 source/candidate refs 全部保留，无代码丢失，验证期间 parent 保持旧结果。
- frozen install（21 workspaces）、architecture `0 + 92/92`、lint、typecheck、root `37 files / 162 tests` 与全部 workspace tests、admin/client production builds 均通过。
- targeted `e2e/system-resources.spec.ts` 为 `2/4`：permission/client-unselected 通过；message/social 两项因不真实的 top-level menu 未渲染 Navbar、以及无 accessible title 的真实 ElMessageBox locator 超时。full E2E 未运行，parent 未推进。
- 恢复：废弃本轮 transient candidate，source 追加真实 Layout 菜单夹具与 dialog role/text locator 修复，重新双轴审查并从未漂移 parent 重建 candidate。

### Attempt 1 Source Fix

- append-only commit `b57607587eb90e836a70141965251d1861f4ba3d` 只修改 `e2e/system-resources.spec.ts`：使用真实 `Layout` 父菜单与相对子路由，按 dialog role + 可见解除文案定位 titleless 确认框。
- source Gate：Oxfmt、Oxlint、Playwright `--list` 4 项、architecture `21 / 0 + 92/92`、lint、typecheck、root `37 files / 162 tests` 与全 workspace tests、build:prod、diff-check 全部通过；按合同未在 source 运行浏览器。
- 复审固定输入：`9ce426c20be49209d24b40083e14eede4ea31109..b57607587eb90e836a70141965251d1861f4ba3d`；标准轴与规范轴均 `PASS`。标准轴确认追加提交仅修改专用 E2E 的真实 `Layout` parent/relative child fixture、既有静态 profile 路径与 role+可见文案 dialog locator；生产代码保持此前已通过状态，上传下载、消息、权限、Client/method、失败可见与 unknown-request 终态断言均未削弱。规范轴独立确认累计 diff 满足 AC-009/010/019/021、Ticket path ownership 与 `DEV-T11-001/002`，root package/lock、client-web、全局 router/request 无变化，T-10 public user blobs 字节不变；聚焦 Vitest `5 files / 15 tests`、Playwright `--list` 4 项与 diff-check 通过。两个 reviewer 均未运行浏览器 E2E，worktree clean。

### Attempt 2

- parent before 仍为 `9ce426c20be49209d24b40083e14eede4ea31109`；从该 parent 全新创建 transient candidate，以 `--ff-only` 精确推进到 `b57607587eb90e836a70141965251d1861f4ba3d`，tree `5ac0653ca77a63dbeeb8de771cefa450286079ac`。
- frozen install（21 workspaces）、architecture `21 / 0 + 92/92`、lint、typecheck、root `37 files / 162 tests` 与全部 workspace tests、admin/client production builds 均通过；生成声明噪音已精确清理。
- targeted `e2e/system-resources.spec.ts` 为 `3/4`：message/config/dict 与 social/permission/client exclusion 均通过；OSS 列表和 `proof.txt` 已可见，但真实 icon-only 下载按钮只有 tooltip、没有 accessible name，`getByRole('button', { name: '下载' })` 无法定位并在等待 download 时超时。full E2E 未运行，parent 未推进。
- 恢复：停止并确认释放 4173/4174，清理 transient candidate worktree/branch，保留 source branch。源修复必须为真实下载按钮补可访问名称并保持强 E2E locator；随后重新双轴审查并从未漂移 parent 构建 attempt 3。

### Attempt 2 Source Fix

- append-only commit `3074bcfdba0dd49406ed979c27f17cf26157e7f4` 只修改 web-domain system-admin 的 `OssPage.vue` 与 `index.test.ts`：为真实下载/删除 icon-only row action 增加 `aria-label`，保留 tooltip、icon、权限与行为，并以源码回归断言锁定可访问名称。
- source Gate：scoped Oxfmt/Oxlint、web-domain `5/5`、package typecheck、frozen install、architecture `21 / 0 + 92/92`、root lint/typecheck、root `37 files / 163 tests` 与全 workspace tests、build:prod、Playwright `--list` 4 项、diff-check 全部通过；未在 source 运行浏览器 E2E。
- 新固定输入：`9ce426c20be49209d24b40083e14eede4ea31109..3074bcfdba0dd49406ed979c27f17cf26157e7f4`；标准轴与规范轴均 `PASS`。两个 reviewer 独立确认 append 仅为真实 ElButton 补准确 `aria-label` 与 raw-source guard，原权限指令、tooltip/icon/style、row click、E2E locator/download event/filename/signed URL/Client/unknown 断言全部保持；累计安全、类型、loading、host ports、package/lock/path ownership 与 T-10 public user blobs 无回归。规范轴聚焦 Vitest `1 file / 5 tests`、Playwright `--list` 4 项及 diff-check 通过；未运行浏览器 E2E，worktree clean。

### Attempt 3

- parent before 仍为 `9ce426c20be49209d24b40083e14eede4ea31109`；从该 parent 全新创建 transient candidate，以 `--ff-only` 精确推进到 `3074bcfdba0dd49406ed979c27f17cf26157e7f4`，tree `3f02741c46ca5462237e69e594107e34131d367e`。
- frozen install（21 workspaces）、architecture `21 / 0 + 92/92`、lint、typecheck、root `37 files / 163 tests` 与全 workspace tests、admin/client production builds 均通过；声明生成噪音精确清理，candidate tree 未漂移。
- targeted T11 Playwright 为 `3/4`：可访问下载 action、真实 download/filename、上传 UI/transfer/complete、message、social、permission 与 client exclusion 均推进到最终 strict-unknown；失败仅为 fixture 未处理页面真实发出的两次 `GET /system/dict/data/list` 和上传完成后的 `GET /resource/oss/listByIds/8`。full E2E 未运行，parent 未推进。
- 根因与恢复：strict-unknown 正确拒绝伪造绿色；问题是 E2E fixture 未描述字典页自动数据查询与上传组件完成后的 OSS 回查，而非生产 transport 错误。停止 4173/4174，清理 candidate worktree/branch并保留 source；`DEV-T11-003` 只允许 append 两个具体 fixture/Client 记录并要求新固定点双轴 PASS 后构建第 4 次 candidate。

### Attempt 3 Source Fix

- append-only commit `a067d10c15d2ee0353ae305fde97c0b1f00ceb8c` 只修改 `e2e/system-resources.spec.ts`：为 `GET /system/dict/data/list` 返回具体 PageResult 字典行，为上传后 `GET /resource/oss/listByIds/8` 返回具体 OSS row，并将两类请求加入 Client-scoped expected facts；`state.unknown === []` 与既有 interaction/locator 保持。
- source Gate：scoped Oxfmt/Oxlint、Playwright `--list` 4 项、architecture `21 / 0 + 92/92`、full lint/typecheck、root `37 files / 163 tests` 与全 workspace tests、build:prod、diff-check 全部通过；未在 source 运行浏览器 E2E，worktree clean。
- 新固定输入：`9ce426c20be49209d24b40083e14eede4ea31109..a067d10c15d2ee0353ae305fde97c0b1f00ceb8c`；规范轴 `PASS`，确认两个具体 GET fixture、Client facts、strict unknown 与既有强断言符合 `DEV-T11-003`，production/package/lock/router/locator/permission 零增量。标准轴 `REQUEST CHANGES`（Low）：字典 fixture 把真实 `DictDataVO.dictCode: string` 写成数值 `1`，E2E JSON 不经类型检查，必须改为 `'1'` 后在新固定点复审；其余 response shape、GET method、Client 记录、listByIds/signed URL 与 unknown 空终态均通过。
- append-only commit `c2b9bedddbd0c4133a0df71c287b15c7063d422c` 仅把 fixture `dictCode: 1` 改为真实 DTO 合同的 `dictCode: '1'`，tree `182778e7652cb97ce835b5ca1b5750b589df4269`；scoped format/lint、Playwright `--list` 4 项、architecture `21 / 0 + 92/92`、full lint/typecheck、root `37 / 163` 与全 workspace tests、build:prod、diff-check 全部通过，未运行浏览器 E2E，worktree clean。最终固定输入 `9ce426c..c2b9bedd` 的规范轴与标准轴均 `PASS`；两个 reviewer 确认累计 DEV-T11-003 exact GET fixtures、Client/method/path/strict-unknown 强断言保持，production/locator/permission/package/lock zero diff。

### Attempt 4

- parent before 仍为 `9ce426c20be49209d24b40083e14eede4ea31109`；从该 parent 全新创建 transient candidate，以 `--ff-only` 精确推进到 `c2b9bedddbd0c4133a0df71c287b15c7063d422c`，tree `182778e7652cb97ce835b5ca1b5750b589df4269`。
- frozen install（21 workspaces）、architecture `21 / 0 + 92/92`、lint、typecheck、root `37 files / 163 tests` 与全 workspace tests、admin/client production builds 均通过；生成声明噪音精确清理，candidate tree 未漂移。
- targeted T11 Playwright 为 `3/4`：permission/client exclusion 与 social 两项通过，主链在上传文件选择后因 `getByText('system-resource-proof.txt', { exact: true })` 同时匹配对话框内两个可见 link 而触发 strict-mode；未进入 confirm 后 upload transfer/listByIds/strict-unknown 终态。full dual-App E2E 未运行，parent 未推进。
- 恢复与阻断：停止并确认释放 4173/4174，清理 transient candidate worktree/branch，保留 source branch；`main` 保持 `9ce426c`。Speculo config 与 Goal Plan 的 integration attempt 上限均为 `4`，不得自行创建第 5 候选。解除阻断需要用户明确授权提高两处上限，之后才能用有明确 dialog/row scope 的强 locator 修复并重新双轴审查。

## 8. 偏差与决策

- T13 才把 devtools 消费切换到 public dict/menu；T11 只建立稳定 seam，不越界修改 devtools。
- **Source commit：** `b79687427b9b00d6e5ab5990f104d4a4bb8dc984`
- **Parent result：** `b79687427b9b00d6e5ab5990f104d4a4bb8dc984`

### Attempt 5 Authorization

- 用户于 `2026-08-26T18:06:09+0800` 明确授权把 Speculo config 的 `max_integration_attempts` 与 Goal Plan 的 `integration_attempt_limit` 同步从 `4` 提高为 `5`；change/Ticket/Map/worktree 已解除 blocked 并恢复执行。
- `DEV-T11-004` 将 source 变化限定为专用 E2E 中真实“上传文件”dialog scope：file input 与确认前同名文件可见断言必须在 dialog 内定位；确认后列表、PUT transfer、complete、`GET /resource/oss/listByIds/8`、Client method/path 与 `state.unknown === []` 保持不变。
- 第 5 candidate 必须从未漂移的 `main@9ce426c20be49209d24b40083e14eede4ea31109` 重建，并在双轴复审后依次通过完整 non-browser Gate、targeted T11 `4/4` 与 full dual-App Playwright 才能推进。
- fixed point `cfc09f4e5b2e6ac1c333ba8f385b840744bf5bea` 的规范轴 `PASS`；标准轴 `REQUEST CHANGES`（Medium）：确认后 page-wide `.first()` 可能在 dialog 关闭动画中命中旧链接，未严格证明 OSS list refresh。`DEV-T11-005` 只允许等待 dialog hidden、证明 list request 精确增加一次并在 OSS table row 内断言新文件，修复后重新双轴复审。
- append-only review-fix commit `521c51e0dc634af2e040bb36a571c33591aeaf03`（tree `3fa3229eaff3998fab39c959f36e35ba34ef1926`）先固定确认前 list request 次数，确认后等待 dialog hidden、list request 精确 `+1` 并在 role=row 内断言新文件；scoped format/lint、Playwright `--list` 4 与 diff-check 通过。固定输入 `9ce426c..521c51e` 的标准轴与规范轴均 `PASS`，确认 production/package/lock zero diff，transfer/complete/listByIds/Client/permission/strict-unknown 强断言保持；未在 source 运行浏览器 E2E，worktree clean。
- attempt 5 candidate 已从未漂移的 parent `9ce426c20be49209d24b40083e14eede4ea31109` 创建并以 `--ff-only` 精确推进到 source/candidate `521c51e0dc634af2e040bb36a571c33591aeaf03`，tree `3fa3229eaff3998fab39c959f36e35ba34ef1926`；candidate worktree clean，完整 Gate 与浏览器验证 pending。

### Attempt 5 Result

- candidate 的 frozen install（21 workspaces）、architecture `21 / 0 + 92/92`、lint、typecheck、root `37 files / 163 tests` 与全 workspace tests、admin/client production builds 全部通过；生成声明噪音精确清理，candidate tree 保持 `3fa3229eaff3998fab39c959f36e35ba34ef1926`。
- 管理端/Client preview 在 `4173/4174` 启动后，targeted `e2e/system-resources.spec.ts` 为 `3/4`。三个权限/社交/client exclusion 场景通过；主场景在确认前失败：真实 upload dialog 内部本身有两个同名 link，因此 `uploadDialog.getByText(... exact)` 仍触发 strict-mode。没有执行 confirm，`DEV-T11-005` 的 dialog hidden/list refresh/table row 证据未到达；full dual-App suite 未运行，parent 未推进。
- Lead 停止服务并确认两端口空闲，移除 transient candidate worktree/branch，保留 source `521c51e0dc634af2e040bb36a571c33591aeaf03`；`main` 保持 `9ce426c20be49209d24b40083e14eede4ea31109`。config/Goal Plan 上限均为 `5`，不得自行创建第 6 candidate。下一 source 修复应在 dialog 内对有可访问名称的同名 link 使用明确单元素 locator，同时保留全部确认后刷新强证据，但须先取得两处上限 `5 -> 6` 的明确授权。

### Attempt 6 Authorization

- 用户于 `2026-08-26T22:04:40+0800` 明确授权把 Speculo config 的 `max_integration_attempts` 与 Goal Plan 的 `integration_attempt_limit` 同步从 `5` 提高为 `6`；change/Ticket/Map/worktree 解除 blocked 并恢复执行。
- `DEV-T11-006` 将 source 变化限定为专用 E2E 的确认前文件名断言：在真实 upload dialog 内使用有准确 accessible name 的 link locator，并通过明确单元素选择消除两个同名 link 的 strict-mode 歧义。
- `DEV-T11-005` 的确认后 dialog hidden、OSS list request 精确 `+1`、table row，以及 transfer/complete/`listByIds/8`、Client method/path、permission 与 `state.unknown === []` 全部保持。新 source fixed point 必须双轴 PASS；第 6 candidate 必须从未漂移 `main@9ce426c20be49209d24b40083e14eede4ea31109` 重建，并依次通过完整 non-browser Gate、targeted T11 `4/4` 与 full dual-App Playwright 后才可推进。
- append-only source commit `b79687427b9b00d6e5ab5990f104d4a4bb8dc984`（tree `f0968f3f3c41aa75fa9af118f6e710a4027cc3b3`）只把确认前 exact-text 断言改为 upload dialog 内 exact accessible-name link 的 `.first()`；Oxfmt、scoped Oxlint、Playwright `--list` 4 项与 diff-check 通过，source worktree clean。固定输入 `9ce426c20be49209d24b40083e14eede4ea31109..b79687427b9b00d6e5ab5990f104d4a4bb8dc984` 的标准轴与规范轴均 `PASS`、findings `0`：两轴独立确认 locator 命中真实 FileUpload link seam 且只消除确认前同名元素歧义，`DEV-T11-005` 的 dialog hidden/list `+1`/table row 与 transfer/complete/listByIds/Client/permission/strict-unknown 强断言完整，production/package/lock 与最新 source 增量的其他路径均零变化；未在 source 运行浏览器 E2E。
- attempt 6 candidate 已从未漂移 parent `9ce426c20be49209d24b40083e14eede4ea31109` 新建，并以 `--ff-only` 精确推进到 source/candidate `b79687427b9b00d6e5ab5990f104d4a4bb8dc984`，tree `f0968f3f3c41aa75fa9af118f6e710a4027cc3b3`；parent 仍为旧 SHA，candidate worktree clean，完整 Gate 与浏览器验证 pending。

### Attempt 6 Result

- 完整 parent-candidate Gate 全部通过：frozen install 覆盖 21 workspaces；architecture check 为 `21 packages / 0 violations`，反向规则 `92/92`；root/workspace lint 与 typecheck 通过；root Vitest `37 files / 163 tests` 和全部 workspace tests 通过；admin/client production builds 通过。构建产生的三行生成声明噪音被精确移除，candidate HEAD/tree 保持 `b79687427b9b00d6e5ab5990f104d4a4bb8dc984` / `f0968f3f3c41aa75fa9af118f6e710a4027cc3b3` 且 clean。
- 管理端/Client preview 在 `127.0.0.1:4173/4174` 运行；targeted `e2e/system-resources.spec.ts` 为 `4/4`（5.1s），确认前具名 link locator、确认后 dialog hidden/list request `+1`/OSS table row、transfer/complete/listByIds、Client/permission/social/strict-unknown 全部到达终态；随后 full dual-App Playwright 为 `36/36`（41.8s，workers=1）。
- Lead 停止 previews 并确认 4173/4174 释放；重验 candidate/source/main clean、SHA/tree 与 ancestor 后，将前端 `main` 以 `--ff-only` 从 `9ce426c20be49209d24b40083e14eede4ea31109` 推进到 result `b79687427b9b00d6e5ab5990f104d4a4bb8dc984`。随后移除 transient candidate worktree/branch；T11 source worktree/branch 按用户决定保留。未 push、部署、发布或清理 source。

## 9. 残余风险与交付定位

- T13 才把 devtools 消费切换到 public dict/menu；T11 已建立稳定 seam，但不越界修改 devtools。
- T11 的本地 source worktree/branch 按用户决定保留；transient candidate 已移除。未 push、部署或发布。
- **Source / candidate / result：** `b79687427b9b00d6e5ab5990f104d4a4bb8dc984`；**tree：** `f0968f3f3c41aa75fa9af118f6e710a4027cc3b3`。
