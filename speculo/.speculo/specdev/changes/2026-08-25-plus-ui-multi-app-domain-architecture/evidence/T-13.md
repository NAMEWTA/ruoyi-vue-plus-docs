# Evidence: T-13 - 迁移代码生成与开发工具领域

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/13-devtools-domain.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `codex:/root`
- **Base / source / candidate / result SHA：** `8144f98bd8a005c1386e7de8462fd354776b5d34` / `a73007e54dcb517fccf4e5470e679dfee7ac0c00` / `a73007e54dcb517fccf4e5470e679dfee7ac0c00` / `a73007e54dcb517fccf4e5470e679dfee7ac0c00`
- **状态：** done；Gate G6 已关闭，source worktree/branch 保留

## 1. 实现摘要

T13 激活 headless devtools domain 与 devtools web-domain，迁移代码生成 transport、模型、配置、预览、下载、manifest 和旧入口 facade。devtools 只通过 system-admin public dict/menu seam 取元数据；admin 显式选择，client-web 不选择。宿主 ZIP adapter 在保存前验证响应，拒绝 JSON、HTML、plain text、截断和网络错误 payload，并只呈现清洗后的错误。

## 2. Lead Dispatch And Candidate Return

- source-worktree：`specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-13`；branch：`speculo/2026-08-25-plus-ui-multi-app-domain-architecture/T-13`。
- attempt 1 至 6 的 progressive browser findings 均在 source 以 append-only commits 修复、重新双轴审查，并从未漂移 parent 重建候选；失败候选均未推进 parent。
- attempt 6 fixed point `aca2047310ec8a9b5205c72e4bd8585b7b8a5ac2` 唯一失败是等待不存在的 `tree-root-value` test-id，而 accessibility snapshot 已显示唯一 `textbox "根节点值"`。
- 用户解除上限 blocker 后，attempt 7 只改一行 E2E locator。最终 source fixed point `a73007e54dcb517fccf4e5470e679dfee7ac0c00`，tree `37895e4c70901b47dfe40f17c9fe47b5c907687c`，worktree clean。

## 3. 修改范围与路径所有权

- devtools packages、旧 tool API/views facades 与专用 E2E 位于 Ticket writable paths。
- `DEV-T13-001` 只增加两个 devtools root workspace dependencies、机械 lock importers、additive admin registry selection/typed host ports，并通过 system-admin public dict entry 增加 projected dict-type catalog seam。
- `src/plugins/download.ts` 仅硬化既有 ZIP 下载路径并补聚焦测试；通用 request/axios、OSS download、resolver/router/App、client-web 和无关 lock/config zero diff。
- `DEV-T13-006` 的最终 source 只把不存在的 test-id lookup 改为唯一 accessible textbox name；生产代码与既有强断言 zero diff。

## 4. 验收与合同映射

| Contract | Source 证据 | 当前结果 |
|---|---|---|
| AC-009 / AC-010 | devtools domain/manifest、admin selected、client unselected | source/candidate pass；result integrated |
| AC-019 | Web runtime 通过 typed system public/host ports 组合 | architecture/no-deep/no-cycle 与双 build pass |
| AC-021 | ZIP 反向 unit、full Gate、required preview/download E2E | Gate G6 pass |

## 5. Workspace Verification

- **source-worktree：** scoped Oxlint exit 0；Playwright `--list` 加载 5 条 devtools scenarios；`git diff --check` exit 0；worktree clean。
- 两个独立审查轴均在最终 source SHA 上完成；按 required-worktree 合同未在 source 运行浏览器 E2E。
- **current-workspace/parent-candidate：** frozen install、完整 architecture、lint、typecheck、root/workspace unit、双 App production build、targeted 与 full Playwright 均由 Lead 运行并通过，详见第 7 节。

## 6. 双轴审查

- 标准轴：`PASS` on `a73007e54dcb517fccf4e5470e679dfee7ac0c00`，无 findings。确认 locator 匹配唯一生产 FormItem/input 的 accessible name，不依赖 Element Plus 内部 DOM。
- 规范轴：`PASS` on 同一 fixed point，无 findings。确认 tree radio checked、`aria-controls` 精确 listbox、完整 root/params/columns payload、metadata Client IDs、ZIP zero-save、retry/race、client exclusion 与 strict-unknown 强断言全部保留。
- source checks：scoped Oxlint、Playwright `--list` 5 项、`git diff --check` 全部 exit 0；source clean。

## 7. Integration Verification

### Attempts 1-6

- attempts 1-5 的完整 non-browser Gate 均通过，但 targeted browser 分别逐步暴露 preview/dialog locator、Element Plus hidden option/transition、placeholder click、radio wrapper 等真实浏览器定位问题；每次都未推进 parent。
- attempt 6 完整 non-browser Gate 通过：27-workspace architecture `0 + 92/92`、lint/typecheck、root `48 files / 232 tests`、全部 workspace unit 与双 App build；targeted `4/5`，未运行 full E2E，未推进 parent。
- 所有 transient candidates 均删除；source retained。达到当时上限 6 后停止，用户随后明确授权上限 7。

### Attempt 7 Result

- 从未漂移的 clean `main@8144f98bd8a005c1386e7de8462fd354776b5d34` 创建 fast-forward candidate；candidate/source 均为 `a73007e54dcb517fccf4e5470e679dfee7ac0c00`，tree `37895e4c70901b47dfe40f17c9fe47b5c907687c`。
- 完整非浏览器 Gate 通过：`pnpm install --frozen-lockfile`；architecture `27/0 + 92/92`；lint/typecheck；root Vitest `48 files / 232 tests` 与全部 workspace tests；admin/client production builds，client transformed 1718 modules。
- build 生成的 `src/types/components.d.ts` 三行声明噪声恢复到 HEAD，candidate 保持 clean 且 tree 不变。
- Lead 双预览环境下 targeted devtools Playwright `5/5`（12.8s），随后 full dual-App Playwright `47/47`（约 1.3m）。预览停止，`4173/4174` 端口释放。
- Lead 重验 source/candidate/main clean 和 expected parent SHA 后，以 `git merge --ff-only a73007e54dcb517fccf4e5470e679dfee7ac0c00` 推进 `plus-ui-namewta/main`。result 为 `a73007e54dcb517fccf4e5470e679dfee7ac0c00`，tree `37895e4c70901b47dfe40f17c9fe47b5c907687c`，ancestor verification 通过。
- transient candidate worktree/branch 已删除；source worktree/branch 保留。未 push、部署、发布或清理 source。

## 8. 偏差与决策

- `DEV-T13-001` 按批准的 public seam、composition、lock 与 safe-download 边界实现，没有扩大到通用 transport 或 client-web。
- `DEV-T13-002` 至 `DEV-T13-006` 完整记录 7 次 candidate 的根因、授权和强断言保留要求；最终修改只修正测试与真实 accessibility tree 不一致的定位假设，没有降低验收标准。

## 9. 残余风险与交付定位

- Element Plus 的内部结构可能随版本变化；最终 E2E 依赖公开 accessibility roles/names 和控件 `aria-controls` 所属关系，较内部 class/test-id 假设更稳定。
- **Source / candidate / parent result：** `a73007e54dcb517fccf4e5470e679dfee7ac0c00`；tree `37895e4c70901b47dfe40f17c9fe47b5c907687c`。
- Gate G6 closed；T15 可在 T11-T14 全部完成后进入 contract 收缩。
