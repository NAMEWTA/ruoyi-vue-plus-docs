# Evidence: T-16 - 引入 OpenAPI transport 合同生成与漂移检查

- **Change：** `2026-08-25-plus-ui-multi-app-domain-architecture`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-25-plus-ui-multi-app-domain-architecture/ticket/16-openapi-contracts.md</Path>`
- **Lead / implementation owner：** `codex:/root` / `codex:/root`
- **Base SHA：** `50ff2207791c87d10ff8f618090a6d82f160b645`
- **状态：** integrated；Gate I closed

## 1. 实现摘要

新增 private `@namewta/api-contracts` 与 package-local `@namewta/tooling-openapi`。工具从明确 source 执行完整读取、OpenAPI 3.0/3.1 结构验证，把 snapshot 与机器 provenance 完整写入 immutable digest revision，最后只以一次原子 rename 切换 `current.json`；generate/check 解析当前 revision 后校验 backend commit 元数据、generator、raw digest 和结构统计，check 再内存生成并逐字节比较，绝不覆盖当前输出。七个 headless domain 各自从 api-contracts 导入生成 schema，真实 HTTP service response 经 domain-owned mapper 投影到既有业务模型后才返回。

## 2. Lead Dispatch And Candidate Return

- source-worktree：`specdev-worktree/2026-08-25-plus-ui-multi-app-domain-architecture/T-16`
- branch：`speculo/2026-08-25-plus-ui-multi-app-domain-architecture/T-16`
- base：`50ff2207791c87d10ff8f618090a6d82f160b645`（T15 Gate H result）。
- initial source：`40627f50bd58fc495b046434da51c9248b83ca6c`；初轮双轴审查不通过，未进入 candidate。
- second-review source：`a33d5b75a54e6c5a43967616926575271b4f5891`，tree `f5e3ff8ea8e1f381e70377407845767ad090f8cf`；规格轴 PASS，标准轴不通过，未进入 candidate。
- third-review source：`b7d2a283768bfa5bc6266ec5afd85b514eb5f632`，tree `ba36624fd58754cd9da88dcf3bede6cbcdac8785`；双轴不通过，未进入 candidate。
- fourth-review source：`6d06f89d911f24a0deed64cc71f836d9b3626c75` 标准轴 PASS、规格轴 FAIL，未进入 candidate。
- fifth-review source：`e48375c11ff16cd9fe2658e22b50ee5c2d5cb380` 标准轴 PASS、规格轴 FAIL，未进入 candidate。
- final reviewed source：`22c10d202c069d851e61de83bf4ff5f376da8419`，tree `0a4e07f09a2992bfa8b71ed3f863bfe7410fd8cb`，append-only error-cause remediation 后 worktree clean。
- candidate/result：Attempt 1 source/candidate/result 均为 `22c10d202c069d851e61de83bf4ff5f376da8419`，tree `0a4e07f09a2992bfa8b71ed3f863bfe7410fd8cb`；parent 从 `50ff220` fast-forward，transient candidate 已删除，source 保留。

## 3. 修改范围与路径所有权

- api-contracts 保存 raw snapshot、provenance、651675-byte 生成类型和只读类型入口；无反向 domain/app 依赖。
- tooling/openapi 固定 `openapi-typescript@7.13.0` 与其受支持的局部 peer `typescript@5.9.3`，提供 fetch/generate/check CLI 与 7 个正反测试；workspace 消费生成物仍由 TypeScript 6.0.3 typecheck。
- demo/identity/workflow/system/AI/devtools/operations 的真实 service 调用生成 transport mapper；mapper 返回既有 `DemoVO`、`ClientAuthContext`、`WorkflowTask`、`UserSummary`、`SnailOpenApiUser`、`TableVO`、`OperLogVO`，endpoint/method/public service signatures 不变。
- `.oxfmtrc.json` 精确忽略 immutable revision 下的 raw snapshot 与 generated output；architecture 仅允许 `@namewta/tooling-openapi/devDependencies/typescript/5.9.3` 四元组脱离 catalog，并有 other-package/other-field 反向 fixture。
- `DEV-T16-001/002/003/004/005/006/007` 覆盖 package/lock、五轮 review remediation 与精确 architecture ratchet；root package/scripts、workspace/catalog、apps、web-domains 和后端保持零 diff。

## 4. 验收与合同映射

| Contract | Source 证据 | 当前结果 |
|---|---|---|
| AC-027 | 前序手工 transport 不追溯改写；新 mapper 可并存 | source + integration pass |
| AC-028 | pinned source/generator、offline check、domain mapper/model ownership | source + integration pass |

## 5. Workspace Verification

- **source-worktree generation：** backend clean detached worktree `a98d6edcc591550221dd983e293d43e3aac36d23` 构建成功；本地 `/v3/api-docs` 连续三次 raw SHA-256 均为 `d72883b9b089200e92962048ba55f0c003ff20f1527bcd3f3238e39df411bc9e`。snapshot 为 OpenAPI `3.1.0`、308 paths、304 schemas、52 tags；五组覆盖为 demo `70/37`、common `11/10`、system `128/136`、gen `12/11`、workflow `56/65`。
- **source-worktree deterministic check：** fetch 从已验证临时源完整写入 bundle digest `a1f65734...` revision 后以单一 `current.json` 原子激活；该身份精确绑定 raw SHA `d72883b9...` 与 canonical provenance。generate 与后续多次 offline check 的输出均为 651675 bytes、SHA-256 `cb1d2e0857f7ed99319423ac10a59d50c4795fac92840ff4c88178853ecb383f`。
- **source-worktree negative tests：** tooling node:test `7/7` 覆盖 invalid/missing source 保留 active revision、合法 immutable revision + atomic pointer 激活、同 raw/不同 backend commit 形成独立 revision、未激活 orphan revision 不影响生成、三项合法 provenance 身份字段篡改、intentional source drift、regenerate closing drift、manual generated edit，以及标准/单斜杠大小写 HTTP scheme 的 credential/query 脱敏；所有临时目录由 test hook 清理。
- **formatter/peer ratchet：** Oxfmt 对 raw/generated 精确路径报告全部被 ignore；离线 check 仍为生成 SHA `cb1d2e0...`。frozen install 无 peer warning，lock 解析为 `openapi-typescript@7.13.0(typescript@5.9.3)`；architecture positive/negative fixtures 验证例外不能扩散。
- **source-worktree full Gate：** frozen install 识别 30 workspaces；architecture `30/0 + 94/94`；完整 lint、typecheck、unit/API tests 全绿；development 与 production 双 App build 通过，admin 3473 modules、client 1720 modules。admin dev artifact `/dev-api=7` 且 `/prod-api=0`，prod 反向为 `/prod-api=7` 且 `/dev-api=0`；生成的 admin declaration 3 行噪声已移除，source clean。
- **source-worktree E2E disposition：** not-required；T16 不修改 App UI、路由、endpoint/method 或浏览器交互，service mapper 行为由 API unit/type/双 build 覆盖；Gate H 的 47/47 仍是最近浏览器基线。
- **security/provenance review：** snapshot 只有 `http://127.0.0.1:18080` 本地诊断 server，无 securitySchemes、无 examples；5 个 defaults 均为分页或 demo 参数，无 credential value。敏感词命中仅为 schema 字段名。

## 6. 双轴审查

初轮 fixed point `40627f50bd58fc495b046434da51c9248b83ca6c` 双轴均不通过。append-only source `a33d5b75a54e6c5a43967616926575271b4f5891` 的规格轴 PASS、标准轴 FAIL。`b7d2a283768bfa5bc6266ec5afd85b514eb5f632` 又因剩余 page assertion、raw-only revision 身份与 `HTTPS:/` 分流绕过而双轴 FAIL。`6d06f89d911f24a0deed64cc71f836d9b3626c75` 的标准轴 PASS，规格轴因前导空白 URL 泄漏 FAIL；`e48375c11ff16cd9fe2658e22b50ee5c2d5cb380` 的标准轴 PASS，规格轴因 error cause 泄漏 FAIL。最终 source `22c10d202c069d851e61de83bf4ff5f376da8419` 对同一 tree 获得 Spec/Intent 与 Standards/Maintainability 双 PASS。

## 7. Integration Verification

Attempt 1 从未漂移 parent `50ff2207791c87d10ff8f618090a6d82f160b645` 建立并 fast-forward 到 exact reviewed source。fresh frozen install 识别 30 workspaces；OpenAPI offline check 为 651675 bytes / `cb1d2e0...`；architecture `30/0 + 94/94`、完整 lint/typecheck/unit/API 全绿；development/production 双 App build 通过，admin artifact 分别为 `/dev-api=7,/prod-api=0` 与 `/dev-api=0,/prod-api=7`。构建生成的 3 行 declaration 噪声移除后 candidate clean。E2E 按 Ticket 为 not-required。Lead 随后以 `--ff-only` 推进 `main`，source/candidate/result SHA/tree 完全一致；transient candidate worktree/branch 已删除，source worktree/branch 保留。

## 8. 偏差与决策

- `DEV-T16-001`：见 Ticket/Goal Plan/status。结构化 diff 确认 root package/workspace 零变化，lock 只增加 api-contracts/openapi tooling importers、七个 workspace links 与 `openapi-typescript@7.13.0` 的依赖图。
- `DEV-T16-002`：初轮双轴 finding 触发精确 Oxfmt ignore、机器 provenance transaction/check、真实 service mapper、局部 TS5.9、URL 脱敏、temp cleanup 与直接受影响 README 整改。
- `DEV-T16-003`：完整 architecture Gate 暴露共享 catalog TS6 与 generator peer TS5.9 冲突；新增精确四元组例外和正反 fixture，不修改 catalog 或根 package/scripts。
- `DEV-T16-004`：第二轮标准轴 finding 触发 workflow/operations 字段级 domain projection、URL parser 规范化 protocol 脱敏，以及 immutable digest revision + 单 current pointer 原子激活；新增额外字段排除、大小写 scheme、未激活 revision 与 last-known-good 反向测试。
- `DEV-T16-005`：第三轮双轴 finding 触发剩余 page assertion 清零、缺失分页数据显式归一化、bundle-digest revision、合法 provenance 字段篡改与 `HTTPS:/` 单斜杠 URL 脱敏测试；完整 source Gate 再次通过。
- `DEV-T16-006`：第四轮规格轴 finding 触发 URL-parser-first 分类及前导空格/Tab 反向测试；控制字符正则初版被 lint 拒绝后改为 `trimStart()`，完整 source Gate 再次通过。
- `DEV-T16-007`：第五轮规格轴 finding 触发 HTTP userinfo fail-fast、URL parse/fetch 原始 cause 清除与完整 `util.inspect(error)` secret 扫描；完整 source Gate 再次通过。

## 9. 残余风险与交付定位

- OpenAPI snapshot/生成文件约 1 MB，后端 schema 变化会产生大 diff；check 保证必须显式 regenerate/review，不会静默覆盖。
- 局部 TS5.9 是受控 generator peer；升级 generator 或 workspace TypeScript 时必须同步调整/删除精确 architecture exception，并重新运行 provenance/determinism/full Gate。
- backend diagnostics 已停止并删除 detached worktree；未改后端 source、未访问生产 endpoint、未 push/deploy/release。
