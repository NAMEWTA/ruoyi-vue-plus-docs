---
schema_version: 1
artifact: code-review
change: 2026-08-28-plus-ui-shared-navigation-permission-runtime
review_id: CR-001
fixed_point: d2961dbb444b9f036ad84c26ee8bcd69d973955c
head: efb8e0d7fae86cfd09c1f55204e8b486a499a3cc
status: request-changes
standards_result: request-changes
specification_result: request-changes
spec_sources:
  - "<Path>{roots.state}/specdev/changes/2026-08-28-plus-ui-shared-navigation-permission-runtime/spec.md</Path>"
  - "<Path>{roots.state}/specdev/changes/2026-08-28-plus-ui-shared-navigation-permission-runtime/ADR.md</Path>"
  - "<Path>{roots.state}/specdev/changes/2026-08-28-plus-ui-shared-navigation-permission-runtime/goal-plan.md</Path>"
  - "<Path>{roots.state}/specdev/changes/2026-08-28-plus-ui-shared-navigation-permission-runtime/ticket/02-build-web-permission-host.md</Path>"
  - "<Path>{roots.state}/specdev/changes/2026-08-28-plus-ui-shared-navigation-permission-runtime/ticket/03-extract-server-menu-runtime.md</Path>"
  - "<Path>{roots.state}/specdev/changes/2026-08-28-plus-ui-shared-navigation-permission-runtime/ticket/04-migrate-admin-navigation-composition.md</Path>"
  - "<Path>{roots.state}/specdev/changes/2026-08-28-plus-ui-shared-navigation-permission-runtime/ticket/05-contract-legacy-and-validate.md</Path>"
  - "<Path>{roots.state}/specdev/changes/2026-08-28-plus-ui-app-routing-permission-eli5/01_Admin动态路由与权限为何仍在App.md</Path>"
standards_sources:
  - "<Path>.agents/skills/engineering-standards/SKILL.md</Path>"
  - "<Path>.agents/skills/engineering-standards/references/rules/architecture-and-boundaries.md</Path>"
  - "<Path>.agents/skills/engineering-standards/references/rules/testing.md</Path>"
  - "<Path>.agents/skills/engineering-standards/references/rules/quality-gates.md</Path>"
  - "<Path>.agents/skills/engineering-standards/references/typescript/core.md</Path>"
  - "<Path>.agents/skills/plus-ui-frontend-conventions/SKILL.md</Path>"
  - "<Path>.agents/skills/plus-ui-frontend-conventions/references/permission-routing.md</Path>"
created_at: 2026-08-28T14:43:27+08:00
---

# Code Review CR-001：共享导航与权限运行时完成后复核

## Fixed Input

- **Diff：** `git diff d2961dbb444b9f036ad84c26ee8bcd69d973955c...efb8e0d7fae86cfd09c1f55204e8b486a499a3cc`
- **Commits：** `git log d2961dbb444b9f036ad84c26ee8bcd69d973955c..efb8e0d7fae86cfd09c1f55204e8b486a499a3cc --oneline`
- **Scope：** `<Path>plus-ui-namewta/**</Path>`；36 个文件，新增 800 行、删除 420 行。
- **当前检出：** `<Path>plus-ui-namewta/</Path>` 仅有 `main@efb8e0d7fae86cfd09c1f55204e8b486a499a3cc`，无其他 worktree，工作树 clean。
- **评审目的：** 判断最终实现是否满足 change 的共享边界、失败关闭、manifest-only、导航恢复、遗留收缩和全量质量门禁要求。
- **不在范围：** 后端产品代码、父仓库并行归档改动、远程 CI、部署和发布。

## Verdict

最终实现的架构方向和主行为成立：共享层没有接管 Admin Router、Pinia、Layout、白名单、manifest 选择或诊断 UI，旧 permission Store、私有权限指令、本地动态页面 glob 与空 `dynamicRoutes` 已删除，正式恢复链路保持唯一。

但本次审查不能给出无条件批准。存在一个直接影响 `AC-002`/`AC-012` 的权限指令失败关闭缺口，以及一个违反 TypeScript 边界 Ratchet、会影响后续 App 接入质量的菜单类型缺口。因此结论为 `request-changes`。

## 标准轴

| Severity | Path / block | Finding | Authority | Satisfied when |
|---|---|---|---|---|
| medium | `<Path>plus-ui-namewta/packages/domains/admin/src/index.ts</Path>` 的 `IdentityMenu`/`parseMenus`；`<Path>plus-ui-namewta/apps/admin-web/src/store/modules/navigation.ts</Path>` 的 `projectMenus`/`generateRoutes` | 菜单领域合同仍是 `Readonly<Record<string, unknown>>`，入口只验证数组项是对象，随后通过多次 `as unknown` 在 IdentityMenu、ServerRouteNode 和 RouteRecordRaw 之间穿透。新公共边界没有完成递归验证与缩窄；后续 App 会复制同一断言，后端 RouterVo 漂移也只能在运行时暴露。 | `TS-001`、`TS-002`、`TS-005`、`DEC-002` | domain-admin 拥有递归、终端无关的 ServerMenuNode，并在 `parseMenus` 入口验证已知字段；Admin 使用显式适配器接入 Vue Router，生产边界不再依赖双重断言。 |
| low | `<Path>plus-ui-namewta/e2e/system-resources.spec.ts</Path>` 的 social failure 断言；`<Path>plus-ui-namewta/e2e/workflow-runtime.spec.ts</Path>` 的 participant action 确认操作 | 第一次完整 Playwright 在 5 workers 下因同名 Element Plus 消息/按钮同时存在而 strict locator 失败，结果为 37/39；两条单 worker 定向重跑 2/2、第二次完整重跑 39/39。当前没有稳定产品回归，但定位器和过渡时序存在非确定性，降低 `AC-015` 门禁可信度。 | `TEST-003`、`TEST-004`、`DELIVERY-003` | locator 限定到具体消息或对话框 owner，必要时等待前一个 overlay/transition 关闭；完整套件连续或在 CI 并发配置下稳定通过。 |

### 标准轴补充说明

推荐的菜单边界不是公共 Store，也不是让 Platform 依赖 Vue Router。更合适的所有权是：

```text
HTTP/JSON unknown
  -> domain-admin 递归校验 ServerMenuNode
  -> platform-app-runtime 纯投影
  -> Admin 显式适配 RouteRecordRaw
  -> navigation Store 保存 App 自有投影
```

`ParentView` 默认键仍带有 RuoYi RouterVo 协议色彩，但当前展平为 opt-in、键可覆盖，且各 Vue Web App 预期消费同一后端菜单协议。本次仅记为观察项，不构成 finding；未来若接入不同菜单协议，应由调用方显式提供键或移除默认值，不能继续泛化为所有 App 的导航公理。

## 规范轴

| Severity | Requirement | Finding | Source | Satisfied when |
|---|---|---|---|---|
| medium | `AC-002`、`AC-012`：非法绑定显式失败且不放行；身份缺失时移除受限元素；异常不得默认展示受限元素 | `<Path>plus-ui-namewta/packages/web-kit/permission/src/index.ts</Path>` 的 mounted hook 先调用 `requireValues` 和 `requireEvaluator`，异常时直接抛错，只有正常求值后才执行 DOM 移除。Vue 在元素插入 DOM 后调用 mounted 指令，因此非法绑定或 provider 返回 undefined 时可能留下受限元素。当前测试只断言 `toThrow`，没有断言异常同时移除元素。 | `<Path>{roots.state}/specdev/changes/2026-08-28-plus-ui-shared-navigation-permission-runtime/spec.md</Path>`；`<Path>{roots.state}/specdev/changes/2026-08-28-plus-ui-shared-navigation-permission-runtime/ticket/02-build-web-permission-host.md</Path>` | 异常路径先移除元素再抛出稳定错误；新增真实 Vue 生命周期或 custom renderer/component test，证明非法绑定和 evaluator 缺失时 DOM 中不存在元素，同时保留显式错误。 |

### 权限指令时序证据

当前实现：

```text
Vue hostInsert(element)
  -> directive mounted
       -> requireValues 可能 throw
       -> requireEvaluator 可能 throw
       -> 只有正常求值且拒绝时 removeChild
```

因此“抛错”不等于“失败关闭”。后端仍是最终鉴权者，故该问题不等同于服务端越权，但它违反了已锁定的前端可见性合同，并会让公共 Web Kit 的 future consumer 在缺失身份时显示本应隐藏的控件。

最低满足行为应为：

```text
invalid binding / missing evaluator
  -> remove element
  -> throw stable error
```

## 符合项与边界判断

以下实现符合 ELI5、ADR 和 Spec 的“共享纯能力，保留 App 装配”原则：

- `<Path>plus-ui-namewta/packages/web-kit/permission/</Path>` 只包装 `v-hasPermi`、`v-hasRoles` 和注入的 evaluator provider，不读取 Admin Store、Router 或会话持久化。
- `<Path>plus-ui-namewta/apps/admin-web/src/directive/index.ts</Path>` 继续由 Admin 决定是否安装权限宿主，并从 App 会话状态提供 evaluator。
- `<Path>plus-ui-namewta/packages/platform/app-runtime/src/routeAssembler.ts</Path>` 只进行确定性菜单投影、特殊组件解析接缝和结构化诊断，不依赖 Vue、DOM、Pinia、Router 单例或 Element Plus。
- `<Path>plus-ui-namewta/apps/admin-web/src/store/modules/navigation.ts</Path>` 继续拥有侧栏、顶栏、默认路由和动态导航状态；没有建立公共 Pinia Store。
- `<Path>plus-ui-namewta/apps/admin-web/src/permission.ts</Path>` 继续拥有 Token、白名单、NProgress、错误反馈、Router 实例和 replacement 策略；共享恢复函数只约束调用顺序。
- `<Path>plus-ui-namewta/apps/admin-web/src/router/adminManifestRegistry.ts</Path>` 继续表达 Admin 编译期选择的 domain/web-domain manifests。
- `<Path>plus-ui-namewta/apps/admin-web/src/router/manifestDiagnostic.ts</Path>` 继续拥有诊断页面和 Element Plus 通知呈现。
- 动态领域页面执行 manifest-only；未知或未选择组件键不会回退到任意 App 本地页面加载。
- 源码扫描和架构 Ratchet 阻止旧 permission Store、私有指令、本地 views glob、`dynamicRoutes`、`filterAsyncRouter`、`filterDynamicRoutes`、`loadView` 和 `usePermissionStore` 回流。

权限 Web Kit 的提取不是只为假想第二个 App：`packages/web-domains` 中已有 28 个 Vue 页面、150 处 `v-hasPermi`/`v-hasRoles` 使用，App 私有实现无法诚实表达这些共享页面的宿主合同。当前 37 行宿主保持最小职责，没有形成公共 UI 框架。

## Acceptance Coverage

| Contract | Review result | Evidence / note |
|---|---|---|
| AC-001 | pass | Web Kit 公开安装入口存在；Admin 显式注入 provider；架构依赖方向通过。 |
| AC-002 | request-changes | 正常允许、拒绝、超管、通配符通过；非法绑定与身份缺失只证明抛错，未证明 DOM 失败关闭。 |
| AC-003 | pass | 指令复用 Platform Permission evaluator，无第二套权限算法。 |
| AC-004 | pass | Platform Permission 无 Vue/DOM；Web Kit 位于 Web 层。 |
| AC-005 | pass | 恢复顺序保持 `getInfo -> getRouters -> addRoute -> replace`；单元和 E2E 通过。 |
| AC-006 | pass | 前端只投影服务端 Client 裁剪菜单，不恢复本地二次权限补偿。 |
| AC-007 | pass | Layout、ParentView、InnerLink、空/畸形 children 和多级展平由表驱动测试覆盖。 |
| AC-008 | pass | 已选 manifest 页面可解析；Admin 静态页面仍由 App 路由拥有。 |
| AC-009 | pass | 未知键生成稳定诊断，无本地 views fallback。 |
| AC-010 | pass | 重复名称由共享纯函数返回结构化诊断，Element Plus 只存在于 Admin 呈现层。 |
| AC-011 | pass | Store 和 8 个布局/导航消费者完成 navigation 语义迁移。 |
| AC-012 | request-changes | 导航与未知组件路径失败关闭成立；权限指令异常路径未保证受限元素移除。 |
| AC-013 | pass | legacy 正式源码零匹配；架构反向用例通过。 |
| AC-014 | pass | T-01 至 T-05 Evidence、source/candidate/result checkpoint 完整。 |
| AC-015 | pass-with-risk | 所有门禁最终通过；Playwright 首次并行运行有 2 条波动，重跑闭合但需稳定 locator。 |

## Verification

| Command | CWD | Exit | Result |
|---|---|---:|---|
| `pnpm architecture:check` | `<Path>plus-ui-namewta/</Path>` | 0 | 26 packages，0 violation。 |
| `pnpm architecture:test` | `<Path>plus-ui-namewta/</Path>` | 0 | 99/99 passed。 |
| `pnpm lint` | `<Path>plus-ui-namewta/</Path>` | 0 | 根级与 25 个激活 workspace project 通过。 |
| `pnpm typecheck` | `<Path>plus-ui-namewta/</Path>` | 0 | 完整 TypeScript/Vue workspace 通过。 |
| `pnpm test` | `<Path>plus-ui-namewta/</Path>` | 0 | Workspace suites 通过；App Runtime 22/22、Web Kit 14/14、Admin 38/38。 |
| `pnpm build:dev` | `<Path>plus-ui-namewta/</Path>` | 0 | development App build 与 non-Admin package build 通过。 |
| `pnpm build:prod` | `<Path>plus-ui-namewta/</Path>` | 0 | production workspace build 与 gzip 通过；保留既有 ineffective dynamic import 警告。 |
| `pnpm test:e2e`（第一次） | `<Path>plus-ui-namewta/</Path>` | 1 | 37/39；两条 strict locator 并行时序失败。 |
| `pnpm exec playwright test e2e/system-resources.spec.ts:350 e2e/workflow-runtime.spec.ts:557 --workers=1` | `<Path>plus-ui-namewta/</Path>` | 0 | 两条失败用例定向重跑 2/2。 |
| `pnpm test:e2e`（第二次完整） | `<Path>plus-ui-namewta/</Path>` | 0 | 39/39 passed。 |
| `git status --short` | `<Path>plus-ui-namewta/</Path>` | 0 | 无输出，产品工作树 clean。 |

## Recommended Repair Boundary

应修复：

1. Web Kit 权限指令的异常失败关闭，并补真实生命周期测试。
2. domain-admin 的递归菜单模型/验证，以及 Admin 到 Vue Router 的显式类型适配。
3. 两条 E2E 的 overlay/message locator 作用域与等待条件。

不应借此次修复继续迁移：

- Admin navigation Store；
- Admin Router 或根级守卫；
- `adminManifestRegistry`；
- Layout、白名单、导航投影或诊断 UI；
- 公共 Pinia Store、公共 Router 或配置驱动的导航总工厂。

## Summary

- **Standards findings：** 2（medium 1，low 1）
- **Specification findings：** 1（medium 1）
- **Most severe standards finding：** 菜单公共边界仍依赖宽泛模型和双重断言。
- **Most severe specification finding：** 权限指令非法绑定/evaluator 缺失时没有先移除 DOM 元素。
- **Overall：** `request-changes`
- **Next Work：** `<Path>{roots.workflows}/specdev/T-tickets/T-tickets.md</Path>`；可建立一个小型修复 Ticket，保持当前共享/App 所有权不变。
