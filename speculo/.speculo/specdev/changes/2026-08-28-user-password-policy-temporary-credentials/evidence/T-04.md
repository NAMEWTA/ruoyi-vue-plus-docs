# Evidence: T-04 — 收敛全部密码写入并提供可编辑重置候选

- **Change：** `2026-08-28-user-password-policy-temporary-credentials`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-28-user-password-policy-temporary-credentials/ticket/04-converge-password-write-paths.md</Path>`
- **Spec / Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-28-user-password-policy-temporary-credentials/spec.md</Path>` / `<Path>{roots.state}/specdev/changes/2026-08-28-user-password-policy-temporary-credentials/goal-plan.md</Path>`
- **Lead：** `codex:lead`
- **Workspace/branch：** `specdev-worktree/2026-08-28-user-password-policy-temporary-credentials/T-04` / `speculo/2026-08-28-user-password-policy-temporary-credentials/T-04`
- **Base/source/candidate/result SHA：** `65dfb11f96c1ec2ee7a40927bf43fc4ced205ff1` / `29cf4dce083afe18aa59d68b44ae41885ee62cd9` / `966d426d0e94e5ff69cf62c8ec5a71018d911af6` / `966d426d0e94e5ff69cf62c8ec5a71018d911af6`
- **状态：** done

## 1. 实现摘要

公开注册、管理员新增、Excel 新用户导入、管理员重置与个人改密五条明文写入路径均在 BCrypt 前调用 T-03 的同一 `PasswordPolicyService`。移除 `RegisterBody` 的旧固定长度分叉；Excel 新用户逐行独立生成，更新已有用户不改密码。新增受 `system:user:resetPwd` 保护的 candidate POST；它复用 allowed/data scope、只返回候选、不写数据库并设置 `Cache-Control: no-store`。既有 add 初始化响应也返回候选。reset/profile 成功后保持原有会话，不引入注销。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner / checkpoint：** Lead direct / `29cf4dce083afe18aa59d68b44ae41885ee62cd9`
- **允许动作：** T-04 writable paths 内 source commit 与 candidate integration；不清理 worktree/branch
- **返回：** source clean，8 个 targeted tests 与 174 项 reactor gate 通过
- **Lead 独立核对：** pass；candidate 使用与 source 相同 parent/tree 的集成结果 `966d426d0e94e5ff69cf62c8ec5a71018d911af6`
- **只读 Agent findings：** 无

## 3. 修改范围与路径所有权

| 路径 | 结果 |
|---|---|
| register / add / reset / profile | validate-before-BCrypt；弱值返回稳定有序 `data.violations`，失败不写库 |
| Excel import | 每个新用户独立 random；update-existing 不生成、不覆盖密码；摘要只含用户名 |
| candidate | POST `{userId}` -> `{password}`；权限、用户可操作性、data scope 与 reset 对齐；生成不持久化 |
| audit | candidate/profile 禁止 request/response 入业务审计；add/reset 排除 password；导入摘要无明文 |
| compatibility | 存量弱 BCrypt 继续登录；reset/profile 不踢出已有 Token |

生产源码对 `sys.user.initPassword` 的读取扫描为零。ADR-0017 已接受的 raw HTTP logger 边界未在本 Ticket 扩大；数据库业务审计的密码字段为空或被排除。

## 4. 验收与合同映射

| Contract | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001/002/003 | add/reset candidate + structured violations | targeted tests 与真实 HTTP | pass |
| AC-004 | reset/profile Token compatibility | reset 前 Token 继续访问 profile | pass |
| AC-016/017 | 五类写入口 | 注册、add、import、reset、profile 均 validate-before-BCrypt | pass |
| AC-018 | 存量兼容 | 弱 BCrypt 登录成功、下一次写入收敛 | pass |

## 5. Workspace Verification

| 命令或步骤 | 环境 | 结果 | 摘要 |
|---|---|---|---|
| candidate contract 首次编译 | source-worktree | expected red | candidate BO/VO/controller 尚不存在，测试按预期编译失败 |
| `PasswordWritePathUnitTest`、`PasswordCandidateContractUnitTest`、`PasswordImportUnitTest` | source-worktree | pass | 8 tests，0 failure/error |
| `./mvnw -pl ruoyi-api,ruoyi-modules/ruoyi-system,ruoyi-admin -am -Dgroups=dev -Dcache.redis.integration.port=6398 -Dpassword.policy.redis.integration.port=6398 -Dauthorization.session.redis.integration.port=6398 test` | source-worktree | pass | 174 tests，0 failure/error，5 个既有外部条件集成 skip |
| 同一 reactor 命令 | clean candidate | pass | 174 tests，0 failure/error，5 个相同既有 skip |
| `git diff --check` 与静态敏感字段/旧配置扫描 | source + candidate | pass | 无 whitespace、越权路径或生产 `sys.user.initPassword` 读取 |

5 个 skip 均为既有 Notify MySQL、MinIO 或 Redis OSS 条件集成；本 Ticket 要求的真实 MySQL/Redis/Spring 接缝由下一节单独执行，未以 mock 或 skip 替代。

### Lead Candidate E2E

- **环境：** clean candidate JAR；Spring Boot `18085`；MySQL `8.4.11` / `specdev_t04` / `3308`；Redis `6399`。仅为本机直接 JSON HTTP 传输设置 `api-decrypt.enabled=false`，controller、service、BCrypt、数据库和 Token 行为均为真实实现，未改变产品加密合同。
- **存量兼容：** 数据库中旧弱 BCrypt 密码仍可成功登录。
- **公开注册：** 弱值返回稳定有序 violations 且用户数不变；合规值写入 BCrypt。
- **管理员新增：** 弱值拒绝且零写入；合规值写入 BCrypt；初始化 GET 返回 12 位合规候选和 `no-store`。
- **重置候选/确认：** POST candidate 返回 12 位合规候选和 `no-store`，调用前后 hash 相同；弱 reset 保持 hash，合规 reset 生成新 BCrypt，reset 前 Token 仍可访问 profile。
- **个人改密：** 弱值保持 hash；合规值生成新 BCrypt，改密前 Token 仍可访问 profile。
- **Excel：** 真实模板上传创建两个用户，两者均为 BCrypt 且 hash 指纹不同；摘要无明文；再次以 updateSupport 上传后两条 hash 指纹均保持。
- **审计：** candidate/profile 的 request/response audit 长度为 0；add/reset 未落 password；按本轮测试密码扫描业务审计为零命中。

五条入口均通过真实 HTTP 绕过前端校验；数据库原值、BCrypt 写入、Token 兼容与 import 批处理均直接核对。

## 6. 双轴审查

### 标准轴

- **固定输入：** `65dfb11f96c1ec2ee7a40927bf43fc4ced205ff1..29cf4dce083afe18aa59d68b44ae41885ee62cd9`
- **结果：** pass
- **Findings 与修正：** 明文校验均早于 BCrypt/持久化；导入逐用户生成；审计排除与 no-store 明确。

### 规范轴

- **固定输入与来源：** Spec AC-001/002/003/004/016/017/018、Ticket T-04、Goal Plan G-30
- **结果：** pass
- **Findings 与修正：** 五入口、候选只读、存量兼容与永久 reset 会话行为逐项闭合。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before / source | `65dfb11f96c1ec2ee7a40927bf43fc4ced205ff1` / `29cf4dce083afe18aa59d68b44ae41885ee62cd9` |
| Candidate | `speculo/integration/2026-08-28-user-password-policy-temporary-credentials/T-04` / `specdev-worktree/.integration/2026-08-28-user-password-policy-temporary-credentials/T-04` |
| Method/conflicts/tree | tree-equivalent candidate result / none / `24b503d87cbaaf1e06458e5b65e033ee6dacf6ca` |
| Candidate checks | Ticket reactor 174 tests 全绿；真实 Spring/MySQL/Redis 五入口 E2E 全绿 |
| Parent result/re-read | `966d426d0e94e5ff69cf62c8ec5a71018d911af6`；HEAD/tree 与 candidate 相同且包含 source |

后端 `main` 的用户 README/技能文件工作区改动在 fast-forward 前后均原样保留且未暂存；父仓库 gitlink 暂不更新，由 T-08 最终集成统一处理。

## 8. 偏差与决策

- **偏差：** 无产品偏差。一次性 E2E 数据库初始化先并行执行 DDL/DML 导致 DML 在表创建前失败；随后按发布顺序串行重建并成功，不涉及源码或持久环境。
- **记录 / 批准来源：** 本 Evidence / `USER-DECISION:2026-08-28-complete-goal-plan`

## 9. 残余风险与交付定位

- **残余风险：** `sys.user.passwordPolicy` 的 fresh/upgrade DML 与生产发布编排归 T-08；T-04 E2E 使用的策略行仅存在于一次性测试库。
- **Source commit / Parent result：** `29cf4dce083afe18aa59d68b44ae41885ee62cd9` / `966d426d0e94e5ff69cf62c8ec5a71018d911af6`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-28-user-password-policy-temporary-credentials/evidence/T-04.md</Path>`
