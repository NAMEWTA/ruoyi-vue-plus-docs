# Evidence: T-05 — 实现一分钟一次性临时密码认证

- **Change：** `2026-08-28-user-password-policy-temporary-credentials`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-28-user-password-policy-temporary-credentials/ticket/05-implement-temporary-password-authentication.md</Path>`
- **Spec / Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-28-user-password-policy-temporary-credentials/spec.md</Path>` / `<Path>{roots.state}/specdev/changes/2026-08-28-user-password-policy-temporary-credentials/goal-plan.md</Path>`
- **Lead：** `codex:lead`
- **Workspace/branch：** `specdev-worktree/2026-08-28-user-password-policy-temporary-credentials/T-05` / `speculo/2026-08-28-user-password-policy-temporary-credentials/T-05`
- **Base/source/candidate/result SHA：** `966d426d0e94e5ff69cf62c8ec5a71018d911af6` / `afeb8a056dfc1b9fea0cf5dbc09de796db97be25` / `afeb8a056dfc1b9fea0cf5dbc09de796db97be25` / `afeb8a056dfc1b9fea0cf5dbc09de796db97be25`
- **状态：** done

## 1. 实现摘要

新增独立权限 `system:user:temporaryPassword` 的 POST 签发 API。服务端以 T-03 策略生成合规值，仅在本次 `no-store` 响应中返回明文；Redis 按 userId 保存 BCrypt 校验值并设置 60 秒 TTL，重新签发覆盖旧值。password grant 保持永久 BCrypt 优先，仅在永久密码失败时验证临时值；Client/userType 准入完成后再执行 Lua compare-and-delete，唯一赢家进入既有普通 Token 流程。

`SysLoginService` 将账号/锁定判断、一次失败计数和成功清理拆成明确接缝，永久与临时值共同构成一次 grant 判定。临时签发业务审计通过事件 enrich 只落目标 userId 与 TTL，关闭原始 request/response 入库。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner / checkpoint：** Lead direct / `afeb8a056dfc1b9fea0cf5dbc09de796db97be25`
- **允许动作：** T-05 writable paths 内 source commit 与 candidate integration；不清理 worktree/branch
- **返回：** source clean，targeted、185 项 reactor 和真实 Redis test 通过
- **Lead 独立核对：** pass；重读存储、Lua CAS、认证顺序、审计、Git tree 和真实环境结果
- **只读 Agent findings：** 无

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>ruoyi-modules/ruoyi-system/src/main/java/org/dromara/system/password/temporary/**</Path>` | writable | 60 秒 BCrypt store、覆盖与原子消费 |
| `<Path>ruoyi-modules/ruoyi-system/.../SysTemporaryPasswordController.java</Path>` | writable | 独立权限签发 API 与安全审计 |
| `<Path>ruoyi-admin/.../SysLoginService.java</Path>` | writable | permanent-first、Client 准入后临时 CAS 与统一计数 |
| `<Path>ruoyi-admin/src/test/java/org/dromara/test/password/temporary/**</Path>` | writable | 合同、故障、真实 Redis 与认证 E2E |

- **read-only 修改 / 未声明路径 / 生成文件：** 无 / 无 / 无

## 4. 验收与合同映射

| 合同 | 结果 |
|---|---|
| permission/data scope | 无独立权限的普通用户返回 403，Redis 不创建凭据；管理员仍执行 allowed/data-scope 校验 |
| storage/TTL/overwrite | Redis 仅有 BCrypt 值；TTL 实测 60；重发值与 hash 均变化且 TTL 重置；永久 DB hash 指纹全程不变 |
| ordering | 错误密码不消费；正确永久密码不读取/消费临时值并清除失败计数；未准入 Client 不消费 |
| CAS | 同一临时值双并发返回一个 200、一个通用失败，Redis key 删除 |
| expiry/error | 过期临时值与普通错误密码的 code/message 完全相同，不泄露临时状态 |
| session | 临时成功复用 Client 的 604800 秒时长；profile 的 userId/userName 与永久登录一致，无临时标记 |
| audit/log | `sys_oper_log.oper_param` 仅含 `targetUserId`、`expiresInSeconds:60`；response/error 为空；普通日志、异常与 Evidence 无凭据/hash。永久 ADR-0017 raw HTTP sink 按 Spec DEC-014 保持原边界 |

## 5. Workspace Verification

| 命令或步骤 | 环境 | 结果 | 摘要 |
|---|---|---|---|
| temporary password contract 首次编译 | source-worktree | expected red | 8 处缺失类型/入口编译错误，证明测试先于实现 |
| controller/service/audit/auth targeted tests | source-worktree | pass | 10 tests，0 failure/error |
| `TemporaryPasswordRedisIntegrationTest` | source-worktree | pass | 真实 Redis 1 test executed、0 skipped；TTL/CAS store 接缝成立 |
| `./mvnw -pl ruoyi-api,ruoyi-modules/ruoyi-system,ruoyi-admin -am -Dgroups=dev -Dcache.redis.integration.port=6398 -Dpassword.policy.redis.integration.port=6398 -Dauthorization.session.redis.integration.port=6398 -Dtemporary.password.redis.integration.port=6398 test` | source-worktree | pass | 185 tests，0 failure/error，5 个既有外部条件 skip |
| 同一 reactor 命令 | clean candidate | pass | 185 tests，0 failure/error，5 个相同既有 skip；T-05 Redis 1/0 skipped |
| `./mvnw -pl ruoyi-admin -am -DskipTests package` | clean candidate | pass | 可执行 `ruoyi-admin.jar` 构建成功 |
| `git diff --check`、路径与敏感字段扫描 | source + candidate | pass | 无 whitespace、越权路径或凭据/hash Evidence |

5 个 skip 均为既有 Notify MySQL、MinIO 或 Redis OSS 外部条件测试；T-05 所需 Redis 测试明确 executed 且未 skipped。Redis/策略故障关闭、永久路径不访问 Redis、token 签发异常后不恢复等故障分支由单元测试注入验证。

- **E2E：** source-worktree 未运行 required E2E；由 Lead 在 clean candidate 执行。

## 6. 双轴审查

### Lead Candidate E2E

- **环境：** clean candidate JAR；Spring Boot `18086`；MySQL `8.4.11` / `specdev_t04` / `3308`；Redis `6399`。仅为本机直接 JSON HTTP 设置 `api-decrypt.enabled=false`，验证码、controller、service、BCrypt、Client 准入、数据库、Redis 与 Token 均使用真实实现。
- **权限负向：** 普通测试用户签发自身临时密码返回 403，目标 Redis key 不存在。
- **签发/覆盖：** 连续两次签发均返回 60 与 `Cache-Control: no-store`；Redis TTL 均为 60，旧值登录失败且新值仍存在，DB 永久 hash 指纹保持。
- **组合判定：** 普通错误密码仅增加一次失败计数且不消费；正确永久密码成功、清除计数且不消费临时值。
- **Client 顺序：** 仅具 sys_user 域的测试用户在 app Client 被拒绝，凭据仍存在；随后在 pc Client 登录成功并消费。
- **并发/过期：** 双并发恰好一成功；人工缩短一次性测试 key 的 TTL 后，过期登录与普通错误登录返回相同通用错误。
- **普通会话：** 永久与临时登录响应使用相同 Client 时长，profile 身份一致；Redis 与返回对象均无临时会话标记。
- **审计：** 多条成功签发记录的 `oper_param` 均只含目标 userId 与 TTL，`json_result`、`error_msg` 为空。

真实 E2E 通过 API 绕过前端权限指令和表单校验，直接核对 Redis key、TTL、数据库 hash、失败计数、Client 准入、Token profile 与审计记录。测试输出和本 Evidence 不保存凭据、Token 或 Redis hash。

### 双轴审查结果

### 标准轴

- **固定输入：** `966d426d0e94e5ff69cf62c8ec5a71018d911af6..afeb8a056dfc1b9fea0cf5dbc09de796db97be25`
- **结果：** pass
- **Findings 与修正：** Redis 只存 BCrypt，Lua compare-delete 原子，失败计数一次，审计/日志无明文。

### 规范轴

- **固定输入与来源：** Spec AC-005..014/023/024、Ticket T-05、Goal Plan G-30
- **结果：** pass
- **Findings 与修正：** user-scoped、60 秒、覆盖、Client 准入、永久优先、普通会话和故障关闭全部覆盖。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before / source | `966d426d0e94e5ff69cf62c8ec5a71018d911af6` / `afeb8a056dfc1b9fea0cf5dbc09de796db97be25` |
| Candidate | `speculo/integration/2026-08-28-user-password-policy-temporary-credentials/T-05` / `specdev-worktree/.integration/2026-08-28-user-password-policy-temporary-credentials/T-05` |
| Method/conflicts/tree | fast-forward / none / `123888c626a9f88d4e6a18787c69520fa1790595` |
| Candidate checks | Ticket reactor 185 tests 全绿；真实 Spring/MySQL/Redis 签发、覆盖、权限、Client、CAS、过期、session 与 audit E2E 全绿 |
| Parent result/re-read | `afeb8a056dfc1b9fea0cf5dbc09de796db97be25`；HEAD/tree 与 candidate 相同且包含 source |

后端 `main` 的用户 README/技能文件工作区改动在 fast-forward 前后均原样保留且未暂存；父仓库 gitlink 暂不更新，由 T-08 最终集成统一处理。

## 8. 偏差与决策

- **偏差：** 无产品偏差。E2E 初次直接登录因环境启用验证码被拒，随后按现有验证码缓存合同执行；批量登录为避免 `/auth/code` 限流干扰，在一次性 Redis 中预置等价验证码缓存项，认证策略仍真实读取、比较并删除。
- **记录 / 批准来源：** 本 Evidence / `USER-DECISION:2026-08-28-complete-goal-plan`

## 9. 残余风险与交付定位

- **残余风险：** 独立权限 DML 与最终 fresh/upgrade 迁移归 T-08；前端一次展示/复制/清理归 T-07。生产部署、授权和 DML 均未执行。
- **Source commit / Parent result：** `afeb8a056dfc1b9fea0cf5dbc09de796db97be25` / `afeb8a056dfc1b9fea0cf5dbc09de796db97be25`
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-28-user-password-policy-temporary-credentials/evidence/T-05.md</Path>`
