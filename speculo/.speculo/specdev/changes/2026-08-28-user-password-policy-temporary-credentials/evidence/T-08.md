# Evidence: T-08 — 发布密码迁移、长期合同与最终回归

- **Change：** `2026-08-28-user-password-policy-temporary-credentials`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-28-user-password-policy-temporary-credentials/ticket/08-publish-password-migration-and-contracts.md</Path>`
- **Lead：** `codex:lead`
- **Backend workspace/branch：** `specdev-worktree/2026-08-28-user-password-policy-temporary-credentials/T-08` / `speculo/2026-08-28-user-password-policy-temporary-credentials/T-08`
- **Backend base/source/candidate/result：** `afeb8a056dfc1b9fea0cf5dbc09de796db97be25` / `98f1e76e7945b7bd003abf6833d0efecea416a12` / `42e06c0f713e0d724813800505e5bb5b40ab563b` / `42e06c0f713e0d724813800505e5bb5b40ab563b`
- **Backend result tree：** `a8ac9a101f1719f0df640a782491e9072b9388b3`
- **Frontend final result/tree：** `8aa184b353c5a37ee555feb8be808fe9ba885297` / `cc19bdd95108b81cc463b1ef293a40a42cc4418e`
- **状态：** done

## 1. 实现与迁移合同

`script/sql/namewta/DML.sql` 只在尾部追加 `NAMEWTA-PASSWORD-DSL-001`：新增 RANDOM v1 `sys.user.passwordPolicy`，用 MySQL CSPRNG 一次替换并退役 `sys.user.initPassword`，在用户管理菜单下新增独立 `system:user:temporaryPassword` 按钮权限。迁移不新增普通角色授权，不输出生成值；MySQL 8 CHECK preflight 对 key、ID、permission 及 NULL 冲突失败关闭。

SQL README 同步 fresh、upgrade、重复执行、发布、缓存刷新、回滚和前向补偿步骤。回滚只处理配置、菜单和角色菜单关系，明确禁止修改 `sys_user.password`。长期 `docs/upstream/customization-map.md` 只记录当前稳定不变量，不复制 Ticket 历史。

| 数据合同 | 已验证结果 |
|---|---|
| policy config ID | `2093282875312267265`，唯一、合法 JSON、长度小于 500、默认 RANDOM |
| temporary menu ID | `2093282875312267266`，parent `1761400000000000100`，permission 精确且普通角色授权为 0 |
| legacy compatibility | 不再为 `123456`，满足 8–30 与四类字符；重复执行不再次改值 |
| append-only | 历史前缀 15,370 bytes，SHA-256 `637d6ab8aa4a813536597d2e2e49ac98f0a932284325a6023a77d09f8b86d8e6` |
| 保留范围 | `ry_vue.sql`、`DDL.sql`、用户密码和非本迁移业务数据均未改写 |

## 2. Lead Dispatch And Candidate Return

- **Implementation owner / checkpoint：** Lead direct / `98f1e76e7945b7bd003abf6833d0efecea416a12`
- **允许动作：** backend T-08 writable paths 内 source commit/candidate integration；父仓仅 customization map、change 工件与两个 gitlink
- **返回：** backend source/candidate clean；migration、全量测试、bundle 与真实 MySQL/Redis/HTTP 通过
- **Lead 独立核对：** pass；重读 SQL 历史前缀、Git tree、数据库演练、前端最终 Gate 与父仓 dirty/index 边界
- **只读 Agent findings：** 无

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>ruoyi-vue-plus-namewta/script/sql/namewta/DML.sql</Path>` | shared:T-08 | append-only policy、legacy 兼容与独立 permission 迁移 |
| `<Path>ruoyi-vue-plus-namewta/script/sql/namewta/README.md</Path>` | shared:T-08 | fresh/upgrade/repeat/release/rollback runbook |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/migration/password/**</Path>` | writable | 静态前缀与真实 MySQL migration tests |
| `<Path>docs/upstream/customization-map.md</Path>` | shared:T-08 | 长期密码、临时凭据、Client 失效和发布边界 |

- **read-only 修改：** `ry_vue.sql`、`DDL.sql`、前后端产品实现均无 T-08 修改
- **未声明路径 / 生成文件：** 无 / 无

## 4. 验收与合同映射

| Contract | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-005/006 | permission DML + real HTTP/Redis | 独立 permission、普通角色 0 grant、60 秒临时登录且永久 hash 不变 | pass |
| AC-019 | static parser + MySQL fresh/upgrade | RANDOM v1、JSON <500、legacy 安全值、缓存刷新 runbook | pass |
| AC-023 | audit rows + SQL/docs scan | 业务审计只有 target user ID/TTL，Evidence 无凭据 | pass |
| G-50 | backend/frontend/parent candidate | full/core、190 tests、真实外部服务、48 browser tests、精确 gitlinks | pass |

## 5. Workspace Verification

### TDD 与 Backend Candidate

| 命令或步骤 | 环境 | 结果 |
|---|---|---|
| `PasswordMigrationUnitTest` 首次执行 | source | expected red：4 项均因迁移 marker 尚不存在失败 |
| `./mvnw -pl ruoyi-admin -am -Dtest=PasswordMigrationUnitTest,PasswordMigrationMySqlIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false -Dpassword.migration.mysql.integration.url=jdbc:mysql://127.0.0.1:3308/specdev_t04 test` | source + clean candidate / MySQL 8.4.11 | 5 passed、0 skipped；fresh、upgrade、repeat、冲突、NULL 冲突、rollback 均执行 |
| `./mvnw test` | source + clean candidate | 190 tests，0 failure/error；11 项条件型外部集成测试 skipped，相关 MySQL/Redis 均另行强制执行 |
| `./mvnw -DskipTests package -Pbundle-full` | source | pass |
| `./mvnw package -Pbundle-core -Dmaven.test.skip=true` | source | pass；core profile 按仓库 README 使用不编译测试的正确调用 |
| `./mvnw -pl ruoyi-admin -am -DskipTests package` | clean candidate | pass |
| `./mvnw test` | ancestry-reconciled clean candidate tree | 190 tests，0 failure/error，11 项条件型外部集成 skip |
| `./mvnw clean package -Pbundle-full -DskipTests` | ancestry-reconciled clean candidate tree | pass |
| `./mvnw clean package -Pbundle-core -Dmaven.test.skip=true` | ancestry-reconciled clean candidate tree | pass |

首个 `bundle-core` 尝试只传 `-DskipTests`，profile 移除 demo/workflow 后 Maven 仍编译全量测试而失败；这不是产品失败，也未被记作绿色。按仓库明确合同改用 `-Dmaven.test.skip=true` 后通过。

- **运行环境：** source-worktree 执行 TDD/非 E2E checks；真实服务与最终 required E2E 由 Lead 在 clean parent-candidate 执行。
- **E2E：** source-worktree 未运行 required E2E。

## 6. 双轴审查

### Lead 真实环境演练

#### MySQL 8.4.11

- **Fresh：** 新建隔离库，串行执行 `ry_vue.sql -> DDL.sql -> DML.sql`；policy 唯一/合法/<500/RANDOM、legacy 合规且非默认值、permission owner 正确、普通角色授权 0。
- **Upgrade：** 先执行 upstream、DDL 和变更前 DML，再只应用追加块；预置用户密码、sentinel 和其他数据保持不变。
- **Repeat：** 第二次执行后 legacy value/remark、policy/menu 行数及既有数据均不变。
- **Rollback：** 删除新增 policy/menu/role relationship 并恢复升级前 legacy 合规值；用户密码和 sentinel 保持。
- **Conflict：** 注入错误 policy ID 与 NULL permission collision 后，CHECK preflight 以非零退出；legacy/menu/policy 在写入前保持原值。

#### Redis 与 HTTP

- clean backend candidate 在真实 Redis 上强制执行 cluster cache、authorization session、password policy、temporary password 四组集成测试：5 tests、0 skipped。
- clean JAR 在隔离 fresh DB + Redis 上完成公开 context、普通永久密码登录、临时签发、首次临时登录、重复拒绝和永久 BCrypt 不变验证。
- 临时认证签发普通 604800 秒会话；签发业务审计只有 target user ID 与 TTL，request/result/error 均无凭据明文。
- 永久 ADR-0017 的 raw HTTP runtime logging 边界未在本 change 扩大；测试终端曾显示隔离凭据，因此 Evidence 不复制任何密码、Token 或响应正文。

### Frontend Final Gate

所有命令均在 clean candidate `8aa184b353c5a37ee555feb8be808fe9ba885297` 执行：

| 命令 | 结果 |
|---|---|
| `pnpm --filter @namewta/tooling-openapi openapi:check` | pass；657722 bytes，SHA-256 `7b0a6405d4eee0d61e10056e14eebe566ad0cabc444f960424629939b45f2186` |
| `pnpm architecture:check` / `pnpm architecture:test` | pass；26 packages、0 violation、99/99 tests |
| `pnpm lint` / `pnpm typecheck` / `pnpm test` | pass；所有激活 workspace 绿色 |
| `pnpm build:dev` / `pnpm build:prod` | pass；仅既有 ineffective dynamic import warning |
| `pnpm test:e2e` | 最终 48/48 passed |

完成审计在同一 frontend commit 上重新执行 OpenAPI check、architecture check/test、lint、typecheck 与 workspace tests，结果仍全部通过；build 与 48/48 Playwright 由该不可变 tree 的上述 Lead Gate 继续承担。Speculo `--stage complete` 结构校验为 0 error、0 warning；因 change 横跨两个子模块，Git SHA/祖先关系分别在 backend/frontend 仓库核对，不使用会把父仓误当成唯一产品仓库的单一 `--repo .` 参数。

首轮全量浏览器回归为 47/48：既有 `system-resources` fixture 未声明 T-06 新增的 `/auth/client/context` 启动请求。T-07 source 只补齐合法策略投影 mock，单项和 clean candidate 全量均通过，形成并晋级 `8aa184b353c5a37ee555feb8be808fe9ba885297`；没有修改产品逻辑或放宽断言。

### 双轴审查结果

### 标准轴

- **固定输入：** backend `afeb8a056dfc1b9fea0cf5dbc09de796db97be25..98f1e76e7945b7bd003abf6833d0efecea416a12`；frontend `8aa184b353c5a37ee555feb8be808fe9ba885297`
- **结果：** pass
- **Findings 与修正：** NULL-safe preflight、历史 prefix hash、幂等/补偿、敏感信息边界和发布顺序均通过；最终 E2E fixture 缺口已在 T-07 source 修正并全量重跑。

### 规范轴

- **固定输入与来源：** Spec AC-005/006/019/023、Ticket T-08、Goal Plan G-50、DML/README/customization map
- **结果：** pass
- **Findings 与修正：** 数据启用、独立授权、迁移失败关闭、用户密码不参与回滚及三仓精确组合全部满足。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Backend candidate | merge commit，0 conflicts，candidate/result `42e06c0f713e0d724813800505e5bb5b40ab563b`；显式包含 T-04 candidate/source，tree 保持 `a8ac9a101f1719f0df640a782491e9072b9388b3` |
| Frontend candidate | fast-forward，0 conflicts，candidate/result `8aa184b353c5a37ee555feb8be808fe9ba885297` |
| Parent gitlinks | backend `42e06c0f713e0d724813800505e5bb5b40ab563b`；frontend `8aa184b353c5a37ee555feb8be808fe9ba885297` |
| Parent integration result | `fa70322dd7ef86d169e5d60a2d90b2bbf2f6d926`（tree `a95114292b5500f23be11f5603cceedeb043b871`）；仅更新 backend gitlink 与本 change 完成工件，frontend gitlink 保持已验证结果 |
| Contract closure | `AC-001` 至 `AC-024` 全部 passed，无 deferred 或 skipped-as-passed |

父仓和两个子仓原有、非本 change 的 dirty 内容在快进与父提交前后均保留；没有清理 source worktree、推送、部署、执行生产 DML 或授予生产角色权限。

## 8. 偏差与决策

- **偏差：** 无产品偏差。core bundle 首次使用不适配该 profile 的 `-DskipTests`，按 README 改用 `-Dmaven.test.skip=true` 后通过；frontend 全量 E2E 首轮暴露旧 fixture 缺口，回到 T-07 source 修正后 48/48。完成审计发现 T-04 历史 result 仅与 source tree 等价但无祖先关系，已用零产品 diff 的双父 candidate 和最终 merge 修正，并在相同 tree 上重跑 190 tests 与两个 clean bundle。
- **记录 / 批准来源：** 本 Evidence 与 LOG-023 / `USER-DECISION:2026-08-28-complete-goal-plan`

## 9. 残余风险与交付定位

- 本地实现和隔离环境 Gate 已完成，G-50 关闭。
- 生产 DML、部署、权限授予、远程推送及 source branch/worktree 清理仍未授权，也未执行。
- 生产发布必须重新做备份与 preflight，按 DML -> backend -> frontend -> permission grant 顺序推进；失败时使用已演练补偿，绝不修改用户密码。
