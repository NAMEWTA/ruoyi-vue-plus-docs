# Evidence: T-03 - 构建跨合法 Client 的全局 OpenAPI 授权快照

- **Lead：** `codex:/root`
- **Workspace/branch：** `specdev-worktree/2026-08-30-openapi-common-module/T-03` / `speculo/2026-08-30-openapi-common-module/T-03`
- **Base/source/candidate/result：** `5772a424a6e5e8e57775f2191eb1d40b7b7465d3` / `f77f09239ebf30142e5efc3443d6ebb929b62c28` / `f77f09239ebf30142e5efc3443d6ebb929b62c28` / `f77f09239ebf30142e5efc3443d6ebb929b62c28`
- **状态：** done

## 1. 实现摘要

新增 system 唯一 `OpenApiAuthorizationResolver` Bean 和专用只读 mapper。查询仅纳入正常且未删除的用户、Client、登录域、角色，以及正常登录域关系和菜单；每个合法 Client 的默认角色与显式用户角色形成并集，按实际授权角色构建标准 `LoginUser`、permission 和 `dataScopeRoleMap`。机器身份使用隔离的 `openapi:{userId}` 登录标识，`clientPk/clientKey` 保持为空，不读取在线 Session，也不补写权限。

实际变更仅位于 Ticket 允许的 authorization owner 包、专用 mapper/XML 和定向测试，无普通登录 service、公共 mapper 或 `LoginUser` 修改。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead
- **Dispatch Packet/checkpoint：** Lead direct；base `5772a424a6e5e8e57775f2191eb1d40b7b7465d3`
- **允许动作：** source worktree 修改、非 E2E 检查、implementation commit
- **返回：** source commit、clean 状态、路径清单、测试结果与恢复条件
- **Lead 独立核对：** pass；读取 mapper/service/test diff、branch tip 与 candidate 结果
- **只读 Agent findings：** 无

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/src/main/java/org/dromara/system/openapi/authorization/**</Path>` | writable:T-03 | 权威授权快照 resolver |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/src/main/resources/mapper/system/openapi/**</Path>` | writable:T-03 | 只读授权查询 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/openapi/authorization/**</Path>` | writable:T-03 | 授权负向矩阵 |

- **read-only 修改：** 无
- **未声明路径：** 无
- **生成文件/锁文件：** 无

## 4. 验收与合同映射

| 合同 | 接缝 | 结果 |
|---|---|---|
| AC-013/014/015/016/019 | resolver + mapper contract | pass；无效用户/无合法 Client 失败关闭，合法 Client 默认与显式角色并集，权限不作 fallback |
| AC-017 | superadmin matrix | pass；保留 `superadmin` 与 `*:*:*` |
| 数据权限 | defensive snapshot test | pass；roles、permissions、RoleDTO list 和 dataScope map 均不可变或防御复制 |

## 5. Workspace Verification

| 命令 | 环境 | 结果 |
|---|---|---|
| `./mvnw -pl ruoyi-admin -am test -Dtest=SystemOpenApiAuthorizationResolverTest -Dsurefire.failIfNoSpecifiedTests=false` | source-worktree | pass；4 tests，0 failure/error/skipped |
| `./mvnw -pl ruoyi-modules/ruoyi-system,ruoyi-admin -am test -Dtest='*Authorization*' -Dsurefire.failIfNoSpecifiedTests=false` | parent-candidate | pass；7 tests，0 failure/error，1 个既有真实 Redis 条件测试 skipped；36 modules success |
| `git diff --check` + writable-path review | source/candidate | pass |

E2E 为 not-required：本 Ticket 是无 HTTP 的只读 resolver；Servlet 调用闭环由 T-09 验证。

## 6. 双轴审查

- **标准轴：** pass；查询有界、状态过滤显式、结果防御复制、异常不返回部分授权。
- **规范轴：** pass；只从 system 权威关系构建快照，单 Client 登录链和普通 mapper 未变化。

## 7. Integration Verification

- **Parent before：** `5772a424a6e5e8e57775f2191eb1d40b7b7465d3`
- **Candidate：** `speculo/integration/2026-08-30-openapi-common-module/T-03`，fast-forward，无冲突，tree `23138f386bd05bb6e4acc637e3f2802cd31ded99`
- **Parent result：** 后端 `main@f77f09239ebf30142e5efc3443d6ebb929b62c28`；source 为 ancestor；main clean

## 8. 偏差与决策

- **偏差：** 无
- **记录：** 不适用

## 9. 残余风险与交付定位

- **残余风险：** 未连接真实 MySQL 验证 mapper SQL 和并发状态变化；真实 Redis 测试因环境条件跳过。最终真实依赖与多节点门禁属于 T-12/发布环境。
- **后续 Ticket：** T-04、T-05、T-12
- **Source commit / Parent result：** `f77f09239ebf30142e5efc3443d6ebb929b62c28`
- **Source workspace：** `specdev-worktree/2026-08-30-openapi-common-module/T-03`
- **Evidence：** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-03.md</Path>`
