# Evidence: T-08 - 补齐角色、菜单与数据范围变更的机器 Session 失效

- **Lead：** `codex:/root`
- **Change：** `2026-08-30-openapi-common-module`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/08-rbac-menu-session-invalidation.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/{change}/spec.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/{change}/goal-plan.md</Path>`
- **Workspace/branch：** `specdev-worktree/2026-08-30-openapi-common-module/T-08` / `speculo/2026-08-30-openapi-common-module/T-08`
- **Base/source/candidate/result：** `05a3739a28b4678bee25878cee516e6591519f0a` / `a5d9507d69189596864d297d60ba88f2ac6eb7a2` / `a5d9507d69189596864d297d60ba88f2ac6eb7a2` / `a5d9507d69189596864d297d60ba88f2ac6eb7a2`
- **Candidate tree：** `97f4788ad4ed35628280682db9de5e73fb5dea70`
- **状态：** done

## 1. 实现摘要

在角色与菜单 service 命令中接入 T-05 `OpenApiMachineSessionInvalidator`。角色影响集合同时覆盖显式 `sys_user_role` 用户和以该角色为有效 Client 默认角色的登录域用户，并使用 `LinkedHashSet` 去重；菜单影响集合先由 menu-role 关系解析角色，再复用显式角色与默认角色用户查询。删除和关系替换均在破坏关系前捕获集合，失效在权威写完成后、成功返回前执行。

角色基础信息只在 roleKey、状态、数据范围、菜单/部门严格模式或 Client 所属等授权语义变化时失效；状态、删除、菜单/数据范围权限和授权用户写入口全部覆盖。菜单状态、权限标识、Client 所属和删除全部覆盖。新增机器失效只使用窄接口，不直接访问 Token、Redis key 或 Sa-Token 静态 API；既有 Controller 浏览器 Session 注销条件与调用点保持原状。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead
- **Dispatch Packet/checkpoint：** Lead direct；base `05a3739a28b4678bee25878cee516e6591519f0a`
- **允许动作：** source worktree 修改、非 E2E 检查、implementation commit
- **返回：** commit `a5d9507d69189596864d297d60ba88f2ac6eb7a2`；source-worktree clean；3 个文件均在 writable owner 内
- **Lead 独立核对：** pass；读取两个 service 全量 diff、入口矩阵、测试报告、branch tip 与 candidate tree
- **只读 Agent findings：** 无；本票未使用子 Agent

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/src/main/java/org/dromara/system/service/impl/SysRoleServiceImpl.java</Path>` | writable:T-08 | 角色语义、状态、权限、授权用户和删除失效 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/src/main/java/org/dromara/system/service/impl/SysMenuServiceImpl.java</Path>` | writable:T-08 | 菜单语义和删除按关联角色用户失效 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/openapi/invalidation/RbacOpenApiSessionInvalidationTest.java</Path>` | writable:T-08 | 精确集合、删除前捕获、失败传播、事务和调用点合同 |

- **预期但未修改：** `SysRoleController`；现有浏览器 Session 行为已经满足兼容要求，保持不动可避免重复副作用
- **read-only 修改：** 无；T-05 Session SPI、现有 Controller 与既有授权失效测试仅作为核对输入
- **未声明路径：** 无
- **生成文件/锁文件：** 无；`target/**` 未提交

## 4. 验收与合同映射

| 合同 | 接缝 | 结果 |
|---|---|---|
| AC-018 | service interaction + failure path | pass；权威写后、成功返回前按精确 userId 注销，异常原样传播 |
| AC-030 | existing browser-session source contract | pass；Controller 既有浏览器 kickout 行为保持，机器路径不直接操作 Token/Redis/Sa-Token |
| transaction boundary | reflection contract | pass；12 个触及的复合写方法使用 `@DSTransactional` |
| affected-user bounds | mapper interaction | pass；显式角色用户与有效默认角色用户合并去重，菜单删除前捕获 role 集合，不做全局 Token 扫描 |

## 5. Workspace Verification

| 命令 | 环境 | 结果 |
|---|---|---|
| `./mvnw -pl ruoyi-admin -am test -Dtest='RbacOpenApiSessionInvalidationTest,IdentityOpenApiSessionInvalidationTest,AuthorizationInvalidationCallSiteUnitTest' -Dsurefire.failIfNoSpecifiedTests=false` | source-worktree | pass；13 tests，0 failure/error/skipped；36 modules success |
| `./mvnw -pl ruoyi-admin -am package -DskipTests` | source-worktree | pass；36 modules success |
| `./mvnw -pl ruoyi-admin -am test -Dtest='RbacOpenApiSessionInvalidationTest,IdentityOpenApiSessionInvalidationTest,AuthorizationInvalidationCallSiteUnitTest,*OpenApiMachineSession*' -Dsurefire.failIfNoSpecifiedTests=false` | parent-candidate | pass；RBAC 5 + identity 6 + browser authorization 2 + machine-session 6 = 19 tests，0 failure/error/skipped；36 modules success |
| `git diff --check` + writable-path/direct-token review | source/candidate | pass |

E2E 为 not-required：本票是 service 副作用接缝，使用交互、反射与源码合同测试验证。没有连接真实 Redis/MySQL、执行 DDL/DML、启用 OpenAPI、推送或部署。

## 6. 双轴审查

- **标准轴：** pass；影响集合来自权威关系表并去重，删除前捕获，复合写事务统一，失效错误不降级，未引入无界扫描。
- **规范轴：** pass；覆盖 role/menu/data-scope/authorized-user 全写入口，保持普通浏览器 Session 行为，并把启用态唯一 Bean 装配留给 T-12。

## 7. Integration Verification

- **Parent before：** `05a3739a28b4678bee25878cee516e6591519f0a`
- **Candidate：** `speculo/integration/2026-08-30-openapi-common-module/T-08`，fast-forward，无代码冲突，tree `97f4788ad4ed35628280682db9de5e73fb5dea70`
- **Parent result：** 后端 `main@a5d9507d69189596864d297d60ba88f2ac6eb7a2`；source 为 ancestor；main clean

## 8. 偏差与决策

- **装配边界：** 与 T-07 一致，两个 service 使用关闭态 no-op 与可选 setter，保持默认关闭和直接构造测试兼容；T-12 负责启用态唯一 Bean 与缺失依赖失败关闭。
- **精确性决策：** 角色集合包含显式分配和有效 Client 默认角色两条来源；菜单由删除前 role-menu 快照向用户投影。权限关系写后返回行数不能可靠表达先删后增，因此在已捕获实际影响集合后执行有界幂等注销。
- **Controller 决策：** Ticket 允许必要调整，但现有浏览器失效调用与机器路径互不重复，故不修改 Controller，以源码合同测试证明兼容性。

## 9. 残余风险与交付定位

- **残余风险：** 真实 Redis 故障回滚、多节点 Session 传播和数据库事务时序未实测；T-12 尚未完成启用态 Bean 唯一性与缺失依赖 context gate。
- **后续 Ticket：** T-09、T-12
- **恢复边界：** OpenAPI 仍默认关闭；若真实环境发现失效异常，保持关闭并以前向修复处理，不清理或改写已保留工作树历史。
- **Source commit / Parent result：** `a5d9507d69189596864d297d60ba88f2ac6eb7a2`
- **Source workspace：** `specdev-worktree/2026-08-30-openapi-common-module/T-08`
- **Evidence：** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-08.md</Path>`
