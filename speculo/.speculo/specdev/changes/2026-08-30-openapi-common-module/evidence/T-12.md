# Evidence: T-12 - 完成默认关闭装配、失败关闭启动门禁与发布回归

- **Lead：** `codex:/root`
- **Change：** `2026-08-30-openapi-common-module`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/12-openapi-assembly-release-gate.md</Path>`
- **Workspace/branch：** `specdev-worktree/2026-08-30-openapi-common-module/T-12` / `speculo/2026-08-30-openapi-common-module/T-12`
- **Backend base/source/candidate/result：** `f5a381c5411b26870a6d63bf57e78cf091016386` / `bb80f15d48e80d46c4806091076d42b49522652d` / `bb80f15d48e80d46c4806091076d42b49522652d` / `bb80f15d48e80d46c4806091076d42b49522652d`
- **Backend candidate tree：** `780cd38c21558bf8ebf81b9d8ddbbf960d1c8dc5`
- **Frontend verified result/tree：** `ea32aa1b1c9911e430f406631199e30589ba007b` / `5aebefaebdc1418a05a9677b9298157d8aee6e78`
- **状态：** done

## 1. 实现摘要

新增唯一 Spring Boot 自动配置入口，在 `openapi.enabled=true` 时装配真实 Handler registry、签名器、Redis nonce/双限流、Sa-Token 机器 Session bridge/invalidator、事件发布器和 Servlet gateway filter。开关缺失或为 false 时上述 Bean 均不注册；管理 Controller 继续消费既有相同条件，不产生关闭态入口。

启用态启动校验要求 32-byte 标准 Base64 KEK、合法 KEK version、正数时间窗口与限额，并依赖唯一 credential/authorization SPI、Redis、Sa-Token 和 MVC mapping。缺失、重复或无效均阻止启动，错误指出配置键或缺失类型但不回显 key material。Filter 只处理 REQUEST、禁用 async，位于 Sa-Token context 之后且不改变普通请求。

admin 配置只新增 `OPENAPI_*` 环境变量占位符，默认 `OPENAPI_ENABLED=false`，KEK/version 默认空。模块 README 与父仓 customization map 记录迁移、配置、菜单、双 UI、回滚及生产批准点；未写入真实 secret，未执行迁移或启用。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead；未使用子 Agent
- **Source checkpoint：** clean commit `bb80f15d48e80d46c4806091076d42b49522652d`，tree `780cd38c21558bf8ebf81b9d8ddbbf960d1c8dc5`
- **Candidate：** backend 独立 integration worktree 从 `main@f5a381c...` 创建并 `--ff-only` 到 source；一次候选尝试全部通过
- **Lead 核对：** 6 个实现文件均在 T-12 writable scope；POM、前置业务实现、SQL、前端源码和 lockfile 未修改

## 3. 修改范围与路径所有权

| 路径 | 改动目的 |
|---|---|
| `<Path>ruoyi-vue-plus-namewta/ruoyi-common/ruoyi-common-openapi/src/main/java/org/dromara/common/openapi/config/**</Path>` | conditional assembly 与 fail-closed startup validation |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-common/ruoyi-common-openapi/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</Path>` | Spring Boot 自动配置发现入口 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-common/ruoyi-common-openapi/README.md</Path>` | 配置、迁移、恢复与批准边界 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/main/resources/application.yml</Path>` | 默认关闭环境变量映射，无 secret |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/src/test/java/org/dromara/test/openapi/assembly/**</Path>` | disabled/invalid/missing/duplicate/complete context 与关闭态 MockMvc |
| `<Path>docs/upstream/customization-map.md</Path>` | common、system、SQL/menu、manifest/profile 和发布定制点 |

## 4. 验收与合同映射

| 合同 | 结果 |
|---|---|
| AC-001/030 | 默认不装配 gateway/state/registry；关闭态带伪签名头的普通 MVC 请求仍返回原响应 |
| AC-018 | 装配唯一 machine session invalidator，T-07/T-08 写后失效回归包含于 backend full suite |
| AC-023-026 | T-11 的 current/target scope、manifest/profile 与 App composition 测试包含于 frontend full suite |
| AC-028/029 | T-02 HTTP 日志脱敏/非 JSON 与 stream 回归包含于 backend full suite；启动错误不回显 KEK |
| 启用态 fail-closed | 缺 KEK、非法 KEK/version/限额、缺 Redis、缺或重复 credential SPI 均启动失败；完整依赖只装配一套 gateway |
| full/core | 两个 profile 均构建 41 模块，admin 依赖树都解析 `ruoyi-common-openapi:6.0.0` |

## 5. Workspace Verification

| 命令 | 环境 | 结果 |
|---|---|---|
| `./mvnw -pl ruoyi-admin -am test -Dtest='*OpenApiAssembly*' -Dsurefire.failIfNoSpecifiedTests=false` | source-worktree；non-E2E check | green；7 tests，0 failure/error/skipped |
| `./mvnw -pl ruoyi-admin -am test -Dtest='*OpenApi*' -Dsurefire.failIfNoSpecifiedTests=false` | source-worktree；non-E2E check | green；44 tests；只作为实现期回归，不计 required E2E |
| `./mvnw test` | backend parent-candidate | pass；41 reactor projects；admin 234 tests、0 failure/error、11 infrastructure skips |
| `./mvnw -Pbundle-full -DskipTests package` | backend parent-candidate | pass；41 projects；admin executable jar 含 common-openapi，模块 jar 含 auto-config/imports |
| `./mvnw -Pbundle-core -Dmaven.test.skip=true package` | backend parent-candidate | pass；41 projects；与 full 的 admin dependency tree 均含同一 common-openapi artifact |
| `pnpm architecture:check` | clean frontend candidate | pass；24 workspace packages，0 violation |
| `pnpm test` | clean frontend candidate | pass；23 workspace projects；system domain 63、system web-domain 20、admin App 41 tests |
| `pnpm typecheck` / `pnpm lint` | clean frontend candidate | pass；各 23 workspace projects |
| `pnpm build:prod` | clean frontend candidate | pass；3370 modules；独立 OpenAPI admin/workspace chunks |
| `git diff --check`、secret scan、commit/tree/clean | source/candidates/results | pass |

Source-worktree E2E disposition 为 `not-run`。Required E2E 仅由 candidate 通过：backend candidate 运行 Spring context、关闭态 MockMvc 和 T-09 七场景签名网关 MockMvc；frontend clean candidate 运行 component/workflow/App integration tests。所有外部依赖使用 test double；未宣称真实 MySQL、Redis、KEK、浏览器或多节点验证。

## 6. 双轴审查

- **标准轴：** pass；装配只依赖 common/public SPI，不反向依赖 system 实现；默认关闭；启用失败关闭；无本地状态回退、明文 secret、生产开关或迁移执行。
- **规范轴：** pass；唯一自动配置入口、条件 Controller、filter order、full/core 同 artifact、双 scope UI、迁移/恢复和批准边界均可由测试与文档重建。

## 7. Integration Verification

- **Backend parent before：** `main@f5a381c5411b26870a6d63bf57e78cf091016386`
- **Backend candidate：** `speculo/integration/2026-08-30-openapi-common-module/T-12`，fast-forward，无冲突，tree `780cd38c21558bf8ebf81b9d8ddbbf960d1c8dc5`
- **Backend parent result：** `main@bb80f15d48e80d46c4806091076d42b49522652d`；main/source/candidate 同 commit、同 tree、clean
- **Frontend verified result：** `main@ea32aa1b1c9911e430f406631199e30589ba007b`；clean T-11 integration candidate 与 main 同 tree
- **Parent composition：** 仅允许两个已验证 gitlink、T-12 customization map 与本 change Speculo 工件进入隔离父仓 candidate；父仓其他 dirty 路径不纳入

## 8. 偏差与决策

- **前端扫描环境：** 首次在 frontend main 根运行 architecture check 时，工具把按授权保留的 `specdev-worktree/T-10`、`T-11` 当作普通源码扫描，产生 329 条仅指向嵌套 worktree 的无效违规。未清理 worktree；改在与 main 同 commit/tree 的 clean T-11 integration candidate 重跑全部五项门禁并通过。
- **测试发现：** 新 assembly 测试首次绿色实际执行 0 tests，定位为缺少仓库默认 `@Tag("dev")` 后修正并确认执行 7 tests；不把零执行计为证据。
- **Spring Boot 4 包名：** 增强 MockMvc 测试首次使用旧 `WebMvcAutoConfiguration` 包名而编译失败，改用当前工作树已有的 `org.springframework.boot.webmvc.autoconfigure` 后通过。

## 9. 残余风险与交付定位

- **未执行：** 真实 MySQL/Redis、多节点时序、完整 Playwright、生产 DDL/DML、KEK 下发、远程 push/PR/deploy、`OPENAPI_ENABLED=true`。
- **生产批准点：** 数据库备份/迁移、secret provider 配置、KEK/version 下发与启用必须另行批准；本 change 完成不扩大该权限。
- **恢复边界：** 首先设置 `OPENAPI_ENABLED=false`；保留 additive schema/menu，修复后前向恢复。
- **Source commit / Backend result：** `bb80f15d48e80d46c4806091076d42b49522652d`
- **Evidence：** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-12.md</Path>`
