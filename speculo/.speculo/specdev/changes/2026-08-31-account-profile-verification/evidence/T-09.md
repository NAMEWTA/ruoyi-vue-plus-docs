# Evidence: T-09 - 交付管理查询、审核上下文与高风险 Profile 覆盖命令

- **Change：** `2026-08-31-account-profile-verification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/09-admin-profile-operations.md</Path>`
- **Lead：** `codex:/root`
- **Workspace/branch：** `specdev-worktree/2026-08-31-account-profile-verification/T-09` / `speculo/2026-08-31-account-profile-verification/T-09`
- **Base/source/candidate/result SHA：** `7635e962bfb12e3718c52741ea05433ca3d98b06` / `a850f288d2aad4ad8a08c4f04d634afada32ce94` / `12192f90a268f8ef690ed7931d701e5bb43f1f5c` / `12192f90a268f8ef690ed7931d701e5bb43f1f5c`
- **状态：** done

## 1. 实现摘要

个人与企业叶子模块新增 `/profile/person/archive`、`/profile/enterprise/archive` 管理 API，覆盖分页查询、全量详情、审核上下文、材料读取、直建、流程覆盖审批/驳回、核心字段修订、绑定暂停/恢复/解绑、负责人指定和档案注销。查询返回不可变申请快照、current/历史版本、绑定事件、决定与材料引用；注销档案默认排除，显式筛选后只读可见。

所有写操作使用 POST，要求对应 query/review/material/manage/override 能力并关闭 `@Log` 请求和响应正文。覆盖决定先锁定并提升 `decisionVersion`，再按 `businessId` 终止 workflow；终止失败整体回滚，迟到事件不能覆盖管理员决定。直建与修订分别追加 `ADMIN_CREATE`、`ADMIN_OVERRIDE` 来源，绑定操作追加状态事件；材料、版本、快照和审计不物理删除。企业负责人指定还要求目标为正常 system 用户且具有 active 个人档案。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead direct
- **Dispatch/checkpoint：** source tip `a850f288d2aad4ad8a08c4f04d634afada32ce94`
- **返回：** source clean；person 62、enterprise 46 个模块测试通过
- **Lead 独立核对：** pass；逐项复核能力闭合、明文/材料读取边界、workflow 决定围栏、版本与绑定事件、注销终态、日志和无物理删除
- **只读 Agent findings：** 无；本 Ticket 未使用 subagent

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>ruoyi-modules/ruoyi-profile/ruoyi-profile-person/src/main/java/org/dromara/profile/person/admin/**</Path>` | writable | 个人管理 HTTP、合同、服务、MyBatis 查询与高风险命令 |
| `<Path>ruoyi-modules/ruoyi-profile/ruoyi-profile-person/src/test/java/org/dromara/profile/person/admin/**</Path>` | writable | HTTP 合同、服务和 MySQL E2E |
| `<Path>ruoyi-modules/ruoyi-profile/ruoyi-profile-enterprise/src/main/java/org/dromara/profile/enterprise/admin/**</Path>` | writable | 企业管理 HTTP、合同、服务、MyBatis 查询与负责人命令 |
| `<Path>ruoyi-modules/ruoyi-profile/ruoyi-profile-enterprise/src/test/java/org/dromara/profile/enterprise/admin/**</Path>` | writable | HTTP 合同、服务和 MySQL E2E |

- **read-only 修改/共享路径偏差：** 无；24 个文件、3579 行新增均位于 Ticket writable admin 子树。
- **schema/DML 变化：** 无；复用 T-02 业务表与 T-01 workflow 公开门面。
- **物理删除或敏感正文日志：** 无；无 DELETE/PUT/PATCH/export/data-scope，所有写 `@Log` 均禁用 request/response body。

## 4. 验收与合同映射

| Contract | 验证接缝 | 结果 |
|---|---|---|
| AC-024 | HTTP contract/service | review 能力闭合读取快照与材料，不依赖额外 query/material 能力 | pass |
| AC-025/026 | service/MySQL/workflow fault | waiting 覆盖先终止流程；失败回滚；高 decisionVersion/管理员来源阻断迟到事件 | pass |
| AC-027/028 | service/MySQL transaction | 直建生成 `ADMIN_CREATE` 快照/首版/可选绑定；核心、材料、身份键和目标资格失败不留残片 | pass |
| AC-029 | service/MySQL | 核心修订新增 `ADMIN_OVERRIDE` current 版本，旧版本 superseded、快照不变 | pass |
| AC-030/031 | HTTP/MySQL | suspend/resume/unbind 追加事件；revoke 默认排除、筛选可见、全量只读且不可恢复 | pass |
| AC-039/041 | HTTP contract/repository | 六能力在服务端校验；有权明文与材料全量读取，无部门数据范围，无权零数据 | pass |
| AC-042 | MySQL/static scan | 驳回、解绑、版本替换、注销均保留历史行、材料引用和操作审计 | pass |
| AC-043 | annotation/static scan | 写调用有审计但请求/响应敏感正文不入操作日志 | pass |
| AC-044 | dependency/JAR scan | 仅建立档案与绑定；没有 system 用户角色、菜单、Client 或登录域写入 | pass |

## 5. Workspace Verification

| 命令或步骤 | 环境 | 结果 |
|---|---|---|
| `./mvnw -Pdev -pl :ruoyi-profile-person,:ruoyi-profile-enterprise -am test` | source-worktree | person 62、enterprise 46 tests，0 failure/error/skip；required E2E `not-run` as source acceptance |
| `./mvnw -Pdev -pl :ruoyi-profile-person,:ruoyi-profile-enterprise -am test` | final parent-candidate | person 62、enterprise 46 tests，0 failure/error/skip；BUILD SUCCESS |
| `PersonAdminMySqlE2ETest` + `EnterpriseAdminMySqlE2ETest`，`-Dprofiles.active=e2e` | final parent-candidate + fresh isolated MySQL | 1 + 1 tests，0 failure/error；覆盖真实持久化、直建/修订、绑定状态、注销/历史/材料 ACL、终止成功/失败回滚与决定围栏 |
| `./mvnw -Pbundle-core -Dmaven.test.skip=true clean package` | final parent-candidate | 46 reactor projects，BUILD SUCCESS |
| HTTP method/log/SQL/data-scope/dependency scan | source + final parent-candidate | 无 DELETE/PUT/PATCH/export/物理删除/data-scope/内部 system 或 workflow 依赖；写日志禁载荷 |
| JAR/ancestry/worktree 核验 | parent-candidate/backend main | admin JAR 含 person/enterprise profile，不含 workflow/demo/job/ai；source 为 result 祖先；source/candidate/main clean |
| 临时数据库清理后独立复核 | 授权开发 MySQL | `temporary_database_residue=0` |

- **连接资源：** 只读取 `<Path>temp/relase/deployment-profile.json</Path>` 与 `<Path>temp/relase/namewta-dev-deployment.md</Path>`，按 `10/20/30/40/50/60` 导入官方六份初始化 SQL 后运行隔离 E2E。未安装、启动或连接本机 MySQL。
- **E2E 参数：** 根 POM 以 `${profiles.active}` 绑定 Surefire groups，因此正式 required E2E 使用 `-Dprofiles.active=e2e`；先前 `-Pdev` 与 `-Dgroups=e2e` 的 0-test 诊断运行不计入验收。
- **E2E 纠正记录：** 既有应用库缺少 `profile_operation_audit`，首次探测在业务写入前失败；随后改用 fresh 隔离库。首次隔离运行暴露 `JsonUtils` 静态上下文依赖，改为显式注入 `JsonMapper`。真实 E2E 又暴露原始 JDBC seed 未被 MyBatis dirty 标记提交，fixture 改为 `commit(true)`/`rollback(true)`。最终候选在企业审计标题纠正后重新完整通过 1+1，所有临时库均由 trap 删除并复核零残留。

## 6. 双轴审查

- **标准轴：** `7635e962bfb12e3718c52741ea05433ca3d98b06..a850f288d2aad4ad8a08c4f04d634afada32ce94`，路径、API 方法、授权、事务、MyBatis JSON 映射、workflow 公开门面、日志和测试检查通过。
- **规范轴：** AC-024 至 AC-031、AC-039、AC-041 至 AC-044 与 candidate tree 对照通过；没有双决定、注销恢复、历史覆盖、物理删除、导出、数据范围或 system RBAC 副作用。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `7635e962bfb12e3718c52741ea05433ca3d98b06` |
| Source SHA | `a850f288d2aad4ad8a08c4f04d634afada32ce94` |
| Candidate/result/tree | `12192f90a268f8ef690ed7931d701e5bb43f1f5c` / `12192f90a268f8ef690ed7931d701e5bb43f1f5c` / `2b2eb414720d42857b451c49c1a92774d81ce018` |
| Method/conflicts | merge-commit；无冲突 |
| Source commits | `5d59b169c`、`5e4f5742f`、`d35311557`、`a850f288d` |
| Integration checks | 108 模块测试、2 required E2E、46 模块 core bundle、静态/JAR/ancestry/clean 检查均 pass |
| Parent result/re-read | backend `main@12192f90a268f8ef690ed7931d701e5bb43f1f5c`；HEAD/tree 与 candidate 完全一致 |

初始集成命令因 shell 当前目录仍在 backend root 而先在父 `main` 创建 `443463e9784727b73ce0ba89ebd7c1085a882c2e`；未执行 reset/revert。Lead 把候选分支快进到该精确 SHA/tree，之后 JSON mapper、E2E 事务 fixture 与审计标题三个修正均以 source commit + candidate merge commit 方式推进，最终全部 Gate 只认 final candidate `12192f90a`。

## 8. 偏差与决策

- **集成执行偏差：** 首个 merge 因 cwd 错误先落 parent；候选随后快进到完全相同 commit/tree，后续验收均在候选完成，无额外代码差异。
- **E2E 环境偏差：** 既有应用库 schema 不完整，未发生业务写入；改用发布资料授权的 fresh 隔离数据库，不对现有库执行迁移。
- **测试诊断偏差：** 两次 Surefire 参数错误执行 0 tests，明确排除；修正为 `-Dprofiles.active=e2e` 后在最终候选通过真实 2/2。
- **合同/路径偏差：** 无；实现和测试均保持在 Ticket writable admin 子树。

## 9. 残余风险与交付定位

- **残余风险：** T-10/T-12/T-13/T-14 仍需完成 transport、管理页面、真实动态菜单与全栈 Playwright；完整 Warm-Flow 引擎定义链与发布装配由 T-14 关闭。
- **生产动作：** 无；未部署、未 push、未迁移现有业务库，source/candidate worktree 与 branch 未清理。
- **Source commit：** `a850f288d2aad4ad8a08c4f04d634afada32ce94`
- **Parent result：** `12192f90a268f8ef690ed7931d701e5bb43f1f5c`
