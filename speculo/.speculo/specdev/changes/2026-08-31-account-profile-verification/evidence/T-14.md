# Evidence: T-14 - 完成 admin-web 显式组合与 Profile 发布验收

- **Change：** `2026-08-31-account-profile-verification`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/14-admin-composition-and-release-gate.md</Path>`
- **Lead：** `codex:/root`
- **状态：** done
- **后端 base/source/candidate/result：** `12192f90a268f8ef690ed7931d701e5bb43f1f5c` / `dc3ec6ae9df9beb48c5e415f758356fa2e3d974c` / `e2a73d5c1a14810f2dc289c96089c5a2fc4b912a` / `e2a73d5c1a14810f2dc289c96089c5a2fc4b912a`
- **前端 base/source/candidate/result：** `ae4e2b404615f0fcdba9f1feadf9fe058348f990` / `987395479675aa4144847340f908aabfc5cad8e3` / `e47c558afd815075c71863e6e52bcf3a17ca30c4` / `e47c558afd815075c71863e6e52bcf3a17ca30c4`

## 1. 实现摘要

admin-web 已显式组合 profile domain、manifest、transport 和运行时，注册材料标签、个人/企业列表详情及两个 workflow review formPath。管理员候选账户改走 `UserService` 兼容窄接口，不借用 `system:user:list`；个人候选排除已有有效个人绑定，企业候选还要求有效个人实名且排除已有企业绑定。

core 装配下 workflow 为可选依赖，查询、草稿和直建能力可装配；管理员流程决定在任何仓储状态变更前检查 workflow 并失败关闭。full 装配新增两套独立已发布流程定义，workflow FINISH 会透传 `profileDecision`，个人/企业驳回均落为 `INVALID` 而不发布档案。材料标签树接受对应 override 权限，`profile_material_ref.oss_id` 已进入 OSS 载体治理清单。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead direct
- **后端 dispatch/checkpoint：** `dc3ec6ae9df9beb48c5e415f758356fa2e3d974c`
- **前端 dispatch/checkpoint：** `987395479675aa4144847340f908aabfc5cad8e3`
- **返回：** 两个 source-worktree 均 clean；后端聚焦回归、前端 package 测试均通过
- **Lead 独立核对：** pass；复核 admin 显式组合、封闭候选账户、workflow 决定、流程种子、OSS 载体与 core/full 装配
- **只读 Agent findings：** 无；未使用 subagent

## 3. 修改范围与路径所有权

T-14 除原定 App 组合外接管了发布 Gate 实际发现的最小伴随修复：封闭账户检索、材料树 override、个人决定枚举映射、core 可选 workflow、驳回终态透传、两套流程种子、schema 回归和 OSS 载体登记。没有修改 system 权限、角色、菜单授予、Client、登录域、现有 DDL 或物理删除策略。

父仓库 `60-namewta-dml.sql` 的流程种子属于发布阻断修复；仅在精确命名的隔离开发库 fresh 演练，没有修改业务库或生产环境。源、候选和产品 main 工作树在提升时均 clean；未使用 subagent。

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>apps/admin-web/</Path>`、`<Path>packages/profile/</Path>`、`<Path>packages/web-domains/profile/</Path>`、`<Path>e2e/profile-management.spec.ts</Path>` | writable | admin 显式组合、运行时、管理面修正与浏览器回归 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-admin/</Path>`、`<Path>ruoyi-vue-plus-namewta/ruoyi-api/</Path>`、`<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-profile/</Path>` | writable | 封闭账户候选、装配门禁、profile 管理与测试 |
| `<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-system/</Path>`、`<Path>ruoyi-vue-plus-namewta/ruoyi-modules/ruoyi-workflow/</Path>` | writable | 兼容用户搜索桥接、workflow 终态变量透传 |
| `<Path>release-artifacts/docker/infrastructure/mysql/init/60-namewta-dml.sql</Path>` | shared owner | 两套已发布 Warm-Flow 流程定义 |

## 4. 验收与合同映射

| 合同 | 验证接缝 | 结果 |
|---|---|---|
| AC-001/002/005/007/038 | application + ProcessEvent + MySQL | 两类申请、不可变提交、完成发布与驳回 INVALID 回归通过 |
| AC-024/025/031/039 | admin service/controller + closed user seam | 权限闭合、合格候选、workflow 缺失前置失败和管理操作通过 |
| AC-034/040/041/042 | material HTTP/OSS governance/static scan | 标签 override、材料访问、OSS 引用登记、无生产 DELETE 通过 |
| AC-043/044 | workflow termination/system regression | 终止失败整体失败；system RBAC/Client/login 无改动 |

## 5. Workspace Verification

| 命令或步骤 | 环境 | 结果 |
|---|---|---|
| 后端聚焦测试与 `git diff --check` | backend source-worktree | pass；source clean |
| profile domain/web-domain/admin package tests | frontend source-worktree | pass；source clean |
| E2E disposition | source-worktree | not-run；跨资源验证只在 parent-candidate 执行 |
| ancestry/path/clean | source-worktree + parent-candidate + 产品 main | source 为 candidate 祖先；candidate=result；各产品树 clean |

## 6. 双轴审查

- **标准轴：** Java/Spring、Vue/TypeScript、兼容 API、可选装配、事务失败关闭、路径边界、敏感日志与无物理删除审查通过。
- **规范轴：** AC-001/002/005/007/024/025/031/034/038/039/040/041/042/043/044 映射通过；管理员能力闭合、两套流程独立，待审内容不污染 current。

## 7. Integration Verification

### 真实资源与数据库门禁

所有连接信息只在进程内从 `<Path>temp/relase</Path>` 读取，未写入 Evidence、Git 或命令日志。没有安装或启动本地 MySQL、Redis、MinIO 或其他中间件。

| 门禁 | 结果 |
|---|---|
| 六文件 fresh 基座 `10 -> 20 -> 30 -> 40 -> 50 -> 60` | 远端隔离 MySQL 成功；两套 definition、6 nodes、4 skips 可查询 |
| person/enterprise MySQL + Redis E2E | person 9 + enterprise 9，18 tests，0 failure/error/skip |
| schema contract + isolated MySQL integration | 6 tests，0 failure/error；24 张 profile 表、参考数据、流程定义和唯一约束通过 |
| 隔离资源清理 | 精确删除 `namewta_profile_t14_20260902`；Redis 测试键由夹具清理 |
| OSS/workflow 边界 | 复用 T-04 的真实 OSS ACL/lifecycle Evidence 与 T-09 的 workflow/MySQL/OSS 管理闭环 Evidence |

### 后端候选验证

| 命令或步骤 | 结果 |
|---|---|
| `./mvnw -pl ruoyi-admin -am -Dprofiles.active=dev -Dnamewta.sql.root=... test` | candidate 351 tests，0 failures，0 errors，24 条外部条件测试按设计 skip |
| profile schema isolated integration | 6/6 pass |
| profile MySQL/Redis E2E | 18/18 pass |
| `./mvnw -Pbundle-core ... -Dmaven.test.skip=true package` | pass；fat JAR 含 person/enterprise，不含 workflow |
| `./mvnw -Pbundle-full ... -Dmaven.test.skip=true package` | pass；fat JAR 含 person/enterprise/workflow |
| workflow null-variable regression | 1/1 pass |
| `git diff --check` / JSON contract | pass |

### 前端候选验证

| 命令或步骤 | 结果 |
|---|---|
| `pnpm install --offline --frozen-lockfile` | 391 packages 全部复用本机 store，0 download |
| `pnpm lint` | 26 packages architecture，0 violation；workspace lint pass |
| `pnpm test` | workspace pass；profile domain 10、profile web-domain 23、admin-web 42 均通过 |
| `pnpm typecheck` | workspace 全部通过 |
| `pnpm exec playwright test e2e/profile-management.spec.ts` | Chromium 1/1 pass，生产构建启动；动态菜单、三管理面和 workflow REJECT payload 通过 |

Playwright 使用确定性的受控 API 路由验证前端组合，不声称浏览器直接连接真实后端；真实持久化、Redis、OSS 和 workflow 边界分别由本票后端 E2E 与前序 required Evidence 证明。

### 静态、安全与回归审计

- 生产 profile 源码没有 `DeleteMapping`、HTTP DELETE 或 `delete from profile_*`；扫描命中仅为隔离 E2E fixture 清理。
- 明文/材料管理入口继续由 profile 权限保护，敏感写入口 `@Log` 禁止保存请求/响应正文；没有新增日志敏感原文。
- 无 system 角色、菜单授予、Client、登录和注册合同改动；新增 `UserService` 方法为兼容 default method。
- 无隐式前端全量 glob；componentKey 唯一，profile 由 admin App 显式选择。
- core 缺 workflow 时管理员决定在 repository 写入前失败，单元测试验证无部分状态。

### 候选提升

| 仓库 | Parent before | Candidate/result | Tree | 方法 |
|---|---|---|---|---|
| backend | `12192f90a268f8ef690ed7931d701e5bb43f1f5c` | `e2a73d5c1a14810f2dc289c96089c5a2fc4b912a` | `f9ee8cbde9174332e8480724aa8b4d3c6549a213` | merge-commit，无冲突；main `--ff-only` |
| frontend | `ae4e2b404615f0fcdba9f1feadf9fe058348f990` | `e47c558afd815075c71863e6e52bcf3a17ca30c4` | `23769564784b1d09f7002dde4bce9f885f2daf20` | merge-commit，无冲突；main `--ff-only` |

## 8. 偏差与决策

- **实现偏差：** 发布 Gate 发现的封闭候选检索、材料树 override、个人覆盖枚举、core 可选 workflow、驳回变量透传、流程种子与 OSS 载体登记已按 `.status.json` 两条 T-14 deviation 接管。
- **决策边界：** 所有伴随修复均为 AC 闭环所需的最小范围；未修改 system 授权模型、Client、登录域、业务 DDL 或生产数据。
- **资源决策：** 验证只使用 `<Path>temp/relase</Path>` 声明的外部资源；未安装或启动本地中间件，隔离数据库已精确清理。

## 9. 残余风险与交付定位

G7 通过，代码与父仓库基座达到本地发布候选状态。未执行 push、PR、远程合并、部署、生产迁移、生产权限赋予或 worktree/branch 清理；这些动作仍需单独授权。菜单默认不向普通角色授予，正式环境上线需按既定 DDL/DML、backend、frontend、人工赋权顺序执行并观察。
