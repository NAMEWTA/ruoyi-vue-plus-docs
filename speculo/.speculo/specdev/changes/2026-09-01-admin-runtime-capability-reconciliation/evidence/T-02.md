# Evidence: T-02 - 显式启用 OpenAPI 发布配置并验收 Admin 最终体验

- **Change：** `2026-09-01-admin-runtime-capability-reconciliation`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/02-enable-openapi-release-and-verify-admin.md</Path>`
- **Workspace/branch：** current / aggregate `main`
- **Parent before/source checkpoint：** `5a52ff9e94335522396b623811d509e2fbd604c2` / `97d67cf3aac1ce0dfdd38a5c8e3b1b235c7f3e8d`
- **状态：** in_progress

## 1. 实现摘要

- `x-admin-environment` 现在以默认关闭方式透传 `OPENAPI_ENABLED`、`OPENAPI_KEK_VERSION`、`OPENAPI_KEK`，两个 admin 服务继续继承同一锚点。
- 公开 `.env.example` 只提供 `OPENAPI_ENABLED=false` 和非 secret 占位符。
- 新增 Node 合同测试，锁定单一共享声明、两实例继承、default-off 和占位符边界。

## 2. Lead Dispatch And Candidate Return

- **Implementation/integration owner：** Lead `codex:/root`
- **执行模式：** current/direct-parent；未创建 subagent、source worktree 或 candidate worktree
- **Aggregate source checkpoint：** `97d67cf3aac1ce0dfdd38a5c8e3b1b235c7f3e8d`
- **Lead 核对：** 三个 writable/shared path、测试红绿、提交 diff、无真实 KEK 和 source checkpoint 已读取
- **Result：** pending；目标 Compose、双实例和登录浏览器 E2E 尚未完成

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 目的 |
|---|---|---|
| `<Path>release-artifacts/docker/docker-compose-backend.yml</Path>` | writable/shared:T-02 | 在共享 admin environment 透传三项 OpenAPI 变量 |
| `<Path>release-artifacts/.env.example</Path>` | writable/shared:T-02 | default-off 与公开占位符 |
| `<Path>release-artifacts/tests/release-config.test.mjs</Path>` | writable/shared:T-02 | 双实例同源及 secret 边界回归 |

- backend/frontend read-only 路径未修改；两个子仓在验证后保持 clean。
- 聚合仓其他用户 change 与全局状态保持未暂存、未修改。

## 4. 验收与合同映射

| 合同 | 当前结果 |
|---|---|
| AC-001/002/004 | Spring assembly `7/7` pass；default-off、合法配置和非法配置 fail-closed 已覆盖 |
| AC-012 | Node release `18/18` pass；共享锚点和公开占位符已覆盖 |
| AC-014 | Admin `41` tests、system web-domain `23` tests、两包 typecheck、Admin production build pass |
| AC-003/015/016 | pending；需要目标双实例 HTTP/log 与登录浏览器 |

## 5. Workspace Verification

| current-workspace Gate | 结果 | 摘要 |
|---|---|---|
| 新 Node test 红灯 | pass | 修改前唯一失败：共享锚点中 `OPENAPI_ENABLED` 数量为 0 |
| `node --test release-artifacts/tests/release-config.test.mjs` | pass | `18/18` |
| `bash release-artifacts/scripts/verify-release.sh` | partial pass | Node/Shell/Nginx 台账通过；本机无 Docker CLI，Compose 解析明确 skip |
| OpenAPI assembly Maven gate | pass | `7/7`，0 failure/error/skip |
| Admin package tests | pass | 8 files / 41 tests |
| system web-domain package tests | pass | 7 files / 23 tests |
| Admin + system typecheck | pass | 两包 `vue-tsc --noEmit` 均 exit 0 |
| Admin `pnpm build:prod` | pass | 3370 modules；生成 OpenAPI Admin/Workspace chunks |
| Speculo implement validator / diff check | pass | `0 error / 0 warning`；无 whitespace error |

## 6. 双轴审查

### 标准轴

- 配置只在现有共享锚点声明一次，避免双实例漂移；未引入额外脚本或重复服务级配置。
- 默认关闭时 KEK/version 允许空值，目标启用时由既有 Spring startup validator 校验合法性。
- 测试检查公开值而不输出、生成或提交真实 secret。

### 规范轴

- 实现严格限定三个 T-02 writable path，未改变 OpenAPI HTTP/wire contract、Nacos 鉴权或生成器退役源码。
- 本地 gates 支持 AC-001/002/004/012/014；不以静态结果替代 AC-003/015/016 的真实 E2E。
- 两实例同源配置、server1-first rollout 和无 secret Evidence 约束保持不变。

## 7. Integration Verification

| 项目 | 当前结果 |
|---|---|
| Aggregate parent before | `5a52ff9e94335522396b623811d509e2fbd604c2` |
| Aggregate source checkpoint | `97d67cf3aac1ce0dfdd38a5c8e3b1b235c7f3e8d` |
| Tree SHA | `eb7a6366f4edaedc8237537f6fa6b61800457a8f` |
| Method/conflicts | current/direct-parent；无 Git 冲突；attempt 1 |
| Verification/E2E/result | local partial passed / pending / pending |

## 8. 偏差与决策

- 本机没有 Docker CLI；`verify-release.sh` 按既有合同明确跳过 Compose parsing。目标服务器接管后必须在任何配置写入前补跑真实 Compose `config`。
- 第一次从 monorepo 根直接运行 Vitest 会扫描保留的 SpecDev worktree，并绕过 Admin App alias 配置；该 run 无效。随后从 `apps/admin-web` 与 `packages/web-domains/system` 分别运行包级脚本，得到有效的 41/23 tests。
- 真实 KEK/version 尚未生成或读取；进入远端动作前仍需按部署 Skill 选择后台或可见终端模式。

## 9. 残余风险与交付定位

- 待完成：远端只读发布定位、Compose 渲染、权限受限私密配置、server1/server2 逐实例 health/HTTP/log、重新登录 Admin 浏览器及最终 DB/secret reconcile。
- 首实例失败时恢复上一配置或 `OPENAPI_ENABLED=false`，不得推进 server2；生产/CDE 持续排除。
- 未执行 push、生产/CDE 写入、普通角色授权、volume 删除或工作树清理。
