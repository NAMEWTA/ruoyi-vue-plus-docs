# Evidence: T-02 - 显式启用 OpenAPI 发布配置并验收 Admin 最终体验

- **Change：** `2026-09-01-admin-runtime-capability-reconciliation`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/02-enable-openapi-release-and-verify-admin.md</Path>`
- **Workspace/branch：** current / aggregate `main`
- **Parent before：** `5a52ff9e94335522396b623811d509e2fbd604c2`
- **Final source/result：** `ae3689ec419fb97e0a216e4ab44b85a3517d3bd9`
- **状态：** done

## 1. 实现摘要

- release `x-admin-environment` 以 default-off 方式透传 `OPENAPI_ENABLED`、`OPENAPI_KEK_VERSION`、`OPENAPI_KEK`；两个 Admin 实例继承同一锚点，公开 `.env.example` 只有关闭默认值和占位符。
- OpenAPI 自动配置明确注入业务 `requestMappingHandlerMapping`，避免 Actuator 的第二个同类型 Bean 造成启动歧义；Admin assembly 回归覆盖双候选运行时。
- 凭据不存在时 Controller 返回业务 `404`，接口目录仍返回业务 `200`，使个人设置显示“尚未创建凭据”而不是“能力不可用”。
- 目标开发环境使用权限受限私有配置显式启用 OpenAPI，并按 server1 -> server2 滚动到最终镜像；默认安装仍保持关闭。

## 2. Lead Dispatch And Candidate Return

- **Implementation/integration owner：** Lead `codex:/root`
- **执行模式：** current-workspace/direct-parent；未创建 subagent、source worktree 或 candidate worktree
- **Release implementation：** `97d67cf3aac1ce0dfdd38a5c8e3b1b235c7f3e8d`
- **HandlerMapping correction：** backend `b694dd4b84dd442f7f2e3247c57a184aa2698e50`，aggregate `22941077a547bdf3afc86c93fedf704e55cd2b42`
- **Missing credential correction：** backend `1300301098d0b27805f4224ef83e5b49262e4ef5`，aggregate/final result `ae3689ec419fb97e0a216e4ab44b85a3517d3bd9`
- **Result tree：** `b0eb14947cc080d12bf32f65ab619d98da97401a`
- **Lead 核对：** 产品修改均位于 T-02 writable/shared paths；backend/frontend 最终 clean；无 Git 冲突；direct-parent attempt 3 passed

## 3. 修改范围与路径所有权

| 路径组 | 所有权 | 目的 |
|---|---|---|
| release backend Compose、`.env.example`、release config test | writable/shared:T-02 | default-off、双实例同源和公开 secret 边界 |
| `OpenApiAutoConfiguration`、`OpenApiAssemblyContextTest` | writable/shared:T-02 | 选择业务 MVC mapping 并覆盖 Actuator 双候选 |
| `SysOpenApiCredentialController`、其 Admin contract test | writable/shared:T-02 | 缺失凭据使用业务 404 |

- `application.yml`、Admin manifest/profile 和动态菜单源码保持只读；前端使用已有 `a2d79cf7bc3e95f69b4aa2fc814e4ba0156687e2`。
- 聚合仓其他用户 change 与全局状态保持未暂存、未修改。

## 4. 验收与合同映射

| 合同 | 最终结果 |
|---|---|
| AC-001/002/004 | default-off、合法装配、非法配置 fail-closed 和 Actuator 双 mapping 回归通过 |
| AC-003 | 两实例和 LB 路由存在；未认证业务 401，不再业务/路由 404 |
| AC-012 | Node `18/18`、目标 Compose parse、双实例同源和无公开真实 secret 通过 |
| AC-014 | manifest/profile tests、typecheck/build 与真实管理页通过，生成器入口仍关闭 |
| AC-015 | server1-first 双实例滚动、健康、HTTP、日志与配置一致性通过 |
| AC-016 | 登录浏览器四项用户体验全部通过 |

目标运行时断言：

- 四文件 Compose `config --quiet` 通过，两个 Admin image 均渲染为最终标签；私有 env 和 OpenAPI override 均为 `0600 root:root`。
- 两个实例均为 healthy、restart count 0、同一 image ID；OpenAPI enable/version/KEK 存在性一致，关键启动/secret 日志扫描均为 0。
- LB 未认证 `/namewta/prod-api/system/openApi/self/interfaces` 返回业务 `401`，证明路由存在且认证边界正常。
- 登录浏览器中本人 credential 为 HTTP 200/业务 404，interfaces 为 HTTP 200/业务 200/空数组；UI 显示“尚未创建 OpenAPI 凭据”和接口空态，不显示“OpenAPI 暂不可用”。
- `OpenAPI管理` 精确 1 项且位于系统管理，页面 `/namewta/system/openApi` 可打开；`Nacos配置中心` 精确 1 项且位于系统监控，旧精确名称 `配置中心` 为 0；`系统工具` 与 `代码生成` 均为 0。

## 5. Workspace Verification

| current-workspace Gate | 结果 | 摘要 |
|---|---|---|
| Node release 红绿与最终测试 | pass | 修改前共享锚点缺少变量；最终 `18/18` |
| `verify-release.sh` | pass with declared local skip | Node/Shell/Nginx 台账通过；本机无 Docker，目标服务器补齐真实 Compose parse |
| OpenAPI assembly | pass | 初始 `7/7`；加入双 HandlerMapping 回归后 `8/8` |
| 缺失凭据 Controller contract | pass | 红灯确认旧实现无异常；最终 `4/4` |
| Admin reactor full suite | pass | HandlerMapping 修复后 `329` 项；最终 `330` 项，均 0 failure/error、`23` skip |
| 前端 package tests | pass | Admin `41` tests；system web-domain `23` tests |
| 前端 typecheck/build | pass | Admin/system 两包 typecheck；Admin production build 3370 modules |
| JAR package/transfer | pass | Maven package 成功；本地与远端 SHA-256 同为 `57df1f20d453ca51cd8173edf8bf48d6e70e1c1bd589c9e3828ce44b4d7f1a47` |
| Diff/Speculo | pass | 产品 diff check 无 whitespace error；实施期 validator `0 error / 0 warning` |

最终镜像为 `namewta/namewta-admin:6.0.0-openapi-ae3689e`，image ID `sha256:604129e84f48d3168dc3768fcb633fd408cc574f6dc78f729f33174ac9a88074`。前端 index SHA-256 为 `129520b54e5f0cf133d08ad98976843ae32de3a70426b4942c362721c06ac155`，入口脚本使用 `/namewta/assets/` 前缀。

## 6. 双轴审查

- **标准轴：** 共享 Compose 锚点避免实例漂移；Spring qualifier 只消除真实候选歧义；404 helper 同时约束本人和管理员查询，测试覆盖本人路径；未引入重复配置或旁路。
- **规范轴：** default-off、fail-closed、普通角色不自动授权、Nacos 独立认证、生成器退役与无 secret Evidence 全部保持；所有 deviation 在原 Ticket 精确接缝内收敛。
- `application.yml` 与前端产品源码没有为部署问题做特例修改；真实 secret 只验证存在性，从未进入 Git、Evidence、浏览器持久化或日志正文。

## 7. Integration Verification

| 项目 | 最终结果 |
|---|---|
| Aggregate parent before | `5a52ff9e94335522396b623811d509e2fbd604c2` |
| Aggregate source/result | `ae3689ec419fb97e0a216e4ab44b85a3517d3bd9` |
| Tree SHA | `b0eb14947cc080d12bf32f65ab619d98da97401a` |
| Method/conflicts/attempts | current/direct-parent；无 Git 冲突；3 attempts |
| Full suite / E2E | passed / passed |

T-01 数据库最终态、重放指纹和普通角色 0 增量继续成立；T-02 未再执行数据库写入。最终配置、两实例、LB、登录 API 与页面断言在同一 result 上复核通过。

## 8. 偏差与决策

1. 首次错误使用另一 Compose project；命令在目标变更前失败，只留下未使用镜像，没有改动 NAMEWTA 实例。后续固定 project `namewta-backend` 和四文件路径。
2. 第一轮正确 project 的 server1 候选进入重启循环，原因是 Actuator 增加第二个 `RequestMappingHandlerMapping`；server1 恢复，server2 未推进。加入 qualifier 与 `8/8` assembly 回归后重建。
3. 第二轮候选两实例健康，但浏览器发现缺失凭据的 `R.ok(null)` 被误判为能力不可用；以红绿 Controller contract 改为业务 404，再构建最终候选并重新逐实例滚动。
4. 首次前端归档使用根 `/` base，页面停在 loader；正确 `/namewta/` build 第一次即时 LB 探针遇到瞬时 502并自动回退，第二次等待连接窗口后 4 秒通过。
5. 失败候选、前端 rollback 目录与旧镜像按恢复边界保留；用户未授权 cleanup，因此未删除。数据库继续遵循 T-01/ADR-005 的明确无备份决定。

## 9. 残余风险与交付定位

- **开发环境：** `/data/namewta-data` 的 NAMEWTA 环境已完成；同机 CDE、生产、远程 Git、volume 和广泛 cleanup 均未触碰。
- **恢复资产：** 旧镜像、失败候选和前端 rollback 目录保留，可在运行回归时恢复；私有配置仍为 `0600`。
- **合法空态：** 当前身份的实时可调用接口目录为 0 项，这是权限目录空态，不是 OpenAPI 不可用。
- **敏感信息：** 登录 Token、账号凭据、KEK、AppSecret、数据库/Redis 凭据和响应敏感正文均未写入 Evidence。
