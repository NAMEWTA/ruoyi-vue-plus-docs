# Evidence：T-01 建立唯一且可跟踪的 MySQL 发布基座

- **Lead：** `codex:/root`
- **Workspace/branch：** `current-workspace` / `main`
- **Base/source/result：** `129c7fdcdbb4fd7066f9d70f1af7804beda940e2` / `9dc99e715d8ba542dcee547205504cf9d74c32a4` / `9dc99e715d8ba542dcee547205504cf9d74c32a4`
- **状态：** done

## 1. 实现摘要

六份 MySQL 8.4 SQL 已进入 `release-artifacts/docker/infrastructure/mysql/init/` 并由 Git 跟踪。`stage-mysql` 保留命令名，但只校验固定清单、顺序、非空和 Nacos 资产，不复制、删除或生成 SQL。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** `codex:/root`，current-workspace 直接实施。
- **返回：** 父仓实现提交 `9dc99e7`、六文件摘要、发布合同结果和偏差处理记录。
- **Lead 独立核对：** pass；复核 diff、Git 跟踪、忽略矩阵和命令零写入。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 目的 |
|---|---|---|
| `release-artifacts/docker/infrastructure/mysql/init/*.sql` | writable:T-01 | 唯一 MySQL 基座 |
| `release-artifacts/scripts/release-manage.sh` | writable:T-01 | 只读 stage 校验 |
| `release-artifacts/tests/*mysql*` | writable:T-01 | 基座与 SQL 合同 |

未改动现场数据库、真实配置或其他未声明运行数据。

## 4. 验收与合同映射

| 合同 | 接缝 | 结果 |
|---|---|---|
| AC-001/002/007 | Git 跟踪、忽略矩阵、固定六文件 | pass |
| AC-003/009 | 连续 stage、缺文件负向合同 | pass；正常零写入，缺失时非零退出 |

最终摘要：10=`5030f797...bf11`、20=`2faf7a5d...6684`、30=`1b02e85e...ad38`、40=`16fb7f84...325d`、50=`cf3112ed...4477`、60=`df2ac287...c502`。

## 5. Workspace Verification

| 命令 | 环境 | 结果 |
|---|---|---|
| `node --test release-artifacts/tests/*.test.mjs` | current-workspace | pass，31/31 |
| `bash release-artifacts/scripts/verify-release.sh` | current-workspace | pass；按设计提示本机无 Docker |
| `git diff --check` | current-workspace | pass |

E2E 为 not-required，由 T-06 统一执行隔离 MySQL 初始化。

## 6. 双轴审查

- **标准轴：** pass；清单集中、缺失失败、stage 无副作用。
- **规范轴：** pass；六份 SQL 是唯一事实源，只支持 MySQL 8.4。

## 7. Integration Verification

- **Parent before：** `129c7fdcdbb4fd7066f9d70f1af7804beda940e2`
- **方法：** current-workspace `direct-parent`，无冲突。
- **Parent result：** `9dc99e715d8ba542dcee547205504cf9d74c32a4`

## 8. 偏差与决策

预检发现 50/60 落后于后端随后完成的管理端运行能力 SQL；已先等价接管后端最新内容再删除旧目录。偏差已闭合，无活动 deviation。

## 9. 残余风险与交付定位

MySQL 历史语法弃用告警由 T-06 记录，不影响基座导入。结果提交 `9dc99e7`，Evidence 位于本文件。
