# Evidence：T-03 将 SQL 合同测试迁移到唯一基座

- **Lead：** `codex:/root`
- **Workspace/branch：** 父仓与后端 `current-workspace` / `main`
- **Backend source/result：** `6927ee9786ba87f432127f4feda61c0f43c01849`
- **Parent result：** `9dc99e715d8ba542dcee547205504cf9d74c32a4`
- **状态：** done

## 1. 实现摘要

新增 `SqlBaselinePaths`，默认从聚合工作区定位唯一 SQL 根，独立检出支持 `-Dnamewta.sql.root`；缺目录或文件明确失败。所有 Java SQL 测试和资源不再读取 `script/sql`。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** `codex:/root`，两个 current-workspace 串行实施。
- **返回：** 后端 `6927ee9`、父仓 `9dc99e7`、全量 Maven 测试结果。
- **Lead 独立核对：** pass；复核后端 diff、子模块指针和旧路径扫描。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 目的 |
|---|---|---|
| 后端测试公共 `SqlBaselinePaths` | writable:T-03 | 统一基座定位 |
| 后端 SQL 合同测试与资源 | writable:T-03 | 消费唯一 SQL 根 |
| 父仓子模块指针与发布合同 | writable:T-03/Lead | 固定跨仓结果 |

无第三份 SQL 副本。

## 4. 验收与合同映射

AC-010：后端活动测试和配置旧路径扫描为零；缺失输入明确失败；42 个 Maven reactor 模块全部成功。

## 5. Workspace Verification

| 命令 | 环境 | 结果 |
|---|---|---|
| `./mvnw test` | 后端 current-workspace | pass，42 模块 |
| 旧 `script/sql` 活动引用扫描 | 父仓/后端 current-workspace | pass，零引用 |
| 发布合同 | 父仓 current-workspace | pass，31/31 |

E2E 为 not-required，真实 SQL 导入由 T-06 执行。

## 6. 双轴审查

- **标准轴：** pass；统一解析器、缺失失败、无重复基座。
- **规范轴：** pass；MySQL-only 与 50/60 分类合同一致。

## 7. Integration Verification

后端 `main@6927ee9` 已由父仓 `main@9dc99e7` 的 gitlink 固定；current-workspace direct-parent，无冲突。

## 8. 偏差与决策

首次全量测试发现密码测试仍冻结历史前缀，已改为完整基座内容合同后重跑通过；无活动偏差。

## 9. 残余风险与交付定位

独立后端检出需显式提供 `-Dnamewta.sql.root`，这是预期的缺失失败合同。结果为后端 `6927ee9`、父仓 `9dc99e7`。
