# Evidence：T-06 打通发布回归与 MySQL 全新初始化门禁

- **Lead：** `codex:/root`
- **Workspace/branch：** `current-workspace` / `main`；E2E 位于用户指定开发服务器隔离资源
- **Parent result：** `9dc99e715d8ba542dcee547205504cf9d74c32a4`
- **Backend result：** `6927ee9786ba87f432127f4feda61c0f43c01849`
- **状态：** done

## 1. 实现摘要

`verify-release.sh` 聚合全部发布合同，CI 新增 `release-contracts`；外部服务 CI 复用受保护初始化器并显式传入唯一 SQL 根。真实 E2E 将预期表数校正为 87。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** `codex:/root`，current-workspace 实施；远端只执行隔离验证。
- **返回：** 31/31 合同、42 模块测试、两套打包、MySQL 8.4.9 初始化及资源清理结果。
- **Lead 独立核对：** pass；校验命令退出码、关键表、回放拒绝和隔离目录消失。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 目的 |
|---|---|---|
| `release-artifacts/scripts/verify-release.sh` | writable:T-06 | 聚合发布门禁 |
| `.github/workflows/ci.yml` | writable:T-06 | CI 发布合同与外部服务初始化 |
| `release-artifacts/tests/**` | writable:T-06 | 最终跨边界合同 |

远端仅使用 `/data/namewta-mysql-baseline-validation-v003` 和同名临时容器；未修改现场服务。

## 4. 验收与合同映射

| 合同 | 证据 | 结果 |
|---|---|---|
| AC-001 至 AC-007、AC-009 至 AC-011、AC-013、AC-014 | Node/Shell/Git/文档合同 | pass，31/31 |
| AC-008、AC-012 | 隔离 MySQL 8.4.9 六文件顺序导入 | pass |
| 后端回归与产物 | Maven test、full/core package、JAR 校验 | pass |

## 5. Workspace Verification

| 命令 | 环境 | 结果 |
|---|---|---|
| `bash release-artifacts/scripts/verify-release.sh` | current-workspace | pass |
| `./mvnw test` | 后端 current-workspace | pass，42 模块 |
| full/core `clean package` 与 JAR 校验 | 后端 current-workspace | pass |
| 六文件初始化、断言、回放拒绝、清理 | 远端隔离 parent-candidate 等价资产 | pass，required E2E |

本机未安装或保留 Docker；全部容器验证均在用户指定服务器执行。

## 6. 双轴审查

- **标准轴：** pass；失败自动清理、secret 不输出、CI 与本地门禁同源。
- **规范轴：** pass；AC-001 至 AC-014 全覆盖，required E2E 实际运行。

## 7. Integration Verification

current-workspace direct-parent 无冲突；父仓结果 `9dc99e7` 固定后端 `6927ee9`。最终 87 张表，六类关键表齐全，`gen_table` 与 `gen_table_column` 均为 0，启用的 `minio` PRIVATE 默认配置数量为 1，第二次初始化被明确拒绝。

## 8. 偏差与决策

首轮 E2E 发现脚本预期 86、实际 87，初始化器自动回滚本次数据库和账号；修正合同后重新验证通过。偏差已闭合。

## 9. 残余风险与交付定位

MySQL 8.4 对历史整数显示宽度和 AI 重复索引给出弃用 warning，属于后续清理候选，不阻塞发布。临时容器和 `/data/namewta-mysql-baseline-validation-v003` 已删除，现场 MySQL、Compose、网络、卷和应用未触达。
