# Evidence: T-02 - schema and persistence

- Owner: `codex:/root`
- Status: `done`
- Root checkpoint: release gate validation captured 2026-09-05; final root documentation commit is recorded by T-13
- Backend result: `751ecd439` (latest ruoyi-third integration checkpoint; supersedes `2dacefcfb`)

## Implementation

Added the five `third_*` tables to the NAMEWTA MySQL init DDL, including provider code uniqueness, provider/endpoint uniqueness, credential scope/envelope fields, invocation detail, and provider plus endpoint statistic dimensions. Entity, read-row, mapper, and XML boundaries are module-owned.

## Verification

| Check | Result |
|---|---|
| `./mvnw.cmd -q -pl ruoyi-modules/ruoyi-third -am -DskipTests compile` | pass |
| DDL static review and `git diff --check` | pass |
| Static schema contract (`ThirdSchemaContractTest`) | pass; 1 test, 0 failures/errors/skips |
| MySQL 8.4.9 isolated constraint/FK/upsert integration (`ThirdSchemaMySqlIntegrationTest`) | pass; 1 test against randomized table names, 0 failures/errors/skips |
| Canonical 10/20/30/40/50/60/61 fresh-init | pass; disposable remote Docker/MySQL 8.4.9 ran the ordered stack, imported all seven SQL files, created 116 tables, applied MinIO runtime defaults, and completed rollback-safe cleanup |

## Read-only runtime probe (2026-09-05)

The configured database reported `8.4.9` (`MySQL Community Server - GPL`). The integration test created only randomized, isolated copies of the five third-party tables, verified provider/endpoint ownership, credential active-scope uniqueness and soft-delete recreation, statistic aggregate uniqueness, invalid-scope checks, and cleanup. It did not modify the application schema.

The canonical release initializer was executed in an isolated, disposable Docker environment. MySQL reported 8.4.9, the full database contained 116 tables, all five `third_*` tables were present, the `三方接口管理` menu row was present with four child pages and 15 `third:*` permissions, and the temporary database/container/network were removed after verification. The application schema and production services were not touched.

## 2. Lead Dispatch And Candidate Return

Lead-owned current-workspace execution; the schema implementation checkpoint is `751ecd439`. The fresh-init gate was run by the Lead against an isolated remote release environment and returned with exit code 0.

## 3. 修改范围与路径所有权

T-02 owns the NAMEWTA release DDL and module persistence boundary. The verification used the existing `50-namewta-ddl.sql` and the ordered release initializer; no product source or shared SQL outside the declared paths was changed.

## 4. 验收与合同映射

AC-001/002/005/013/017 are covered by the five-table schema, FK/scope/unique constraints, randomized integration checks, and the canonical fresh-init table/menu counts.

## 5. Workspace Verification

`current-workspace` static schema checks and Maven compile passed. The required MySQL E2E ran in an isolated release environment with MySQL 8.4.9 and completed successfully.

## 6. 双轴审查

Implementation and contract review passed: persistence types stay behind DAO/Mapper/XML boundaries, credentials remain encrypted, and provider/endpoint/statistic uniqueness is enforced by the database.

## 7. Integration Verification

Direct-parent backend result `751ecd439` passed module checks. The canonical seven-file fresh-init and the exact-schema MySQL integration both passed; temporary infrastructure was cleaned up.

## 8. 偏差与决策

No deviation. The earlier unavailable-environment note is superseded by the isolated remote Docker verification recorded above.

## 9. 残余风险与交付定位

No production migration or provider data was executed. Release deployment still requires the separately approved migration and credential/role operations described by the Goal Plan.
