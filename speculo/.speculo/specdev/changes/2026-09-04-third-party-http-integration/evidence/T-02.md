# Evidence: T-02 - schema and persistence

- Owner: `codex:/root`
- Status: `done`
- Root checkpoint: `c49f562`
- Backend result: `ec8cd78a` (latest ruoyi-third integration checkpoint; supersedes `d5f7dc0b`)

## Implementation

Added the five `third_*` tables to the NAMEWTA MySQL init DDL, including provider code uniqueness, provider/endpoint uniqueness, credential scope/envelope fields, invocation detail, and provider plus endpoint statistic dimensions. Entity, read-row, mapper, and XML boundaries are module-owned.

## Verification

| Check | Result |
|---|---|
| `./mvnw.cmd -q -pl ruoyi-modules/ruoyi-third -am -DskipTests compile` | pass |
| DDL static review and `git diff --check` | pass |
| MySQL 8.4 fresh-init and duplicate/rollback scenarios | not run; read-only JDBC probe confirmed MySQL 8.4.9, but the reachable `ry-namewta` database is an existing environment with no `third_*` tables and is not an approved fresh-init target |

## Read-only runtime probe (2026-09-05)

The configured database reported `8.4.9` (`MySQL Community Server - GPL`). A read-only `information_schema` query returned no `third_*` tables. No DDL, DML, migration, or schema reset was executed.

The release environment must execute fresh-init before this ticket is considered externally verified.
