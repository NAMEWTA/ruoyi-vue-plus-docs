# Evidence: T-03 - provider management

- Owner: `codex:/root`
- Status: `done`
- Backend result: `751ecd439` (latest ruoyi-third integration checkpoint; supersedes `2dacefcfb`)

## Implementation

Provider CRUD, immutable provider-code identity, status changes, logical deletion protection, trusted HTTP(S) origin validation, provider-level limits, permissions, logging, and cache eviction are implemented through `controller/admin -> usecase/impl -> service -> dao`. Provider-code rename is rejected because endpoint identity and cache keys are provider-code based.

## Verification

| Check | Result |
|---|---|
| Backend module compile | pass |
| Provider lifecycle guard tests | pass; rename is rejected and logically deleted provider records remain protected |
| Controller route and permission source review | pass |
| MySQL/MVC permission integration with fresh data | pending release environment |

No real provider URL or credential was seeded.
