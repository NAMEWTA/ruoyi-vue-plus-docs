# Evidence: T-05 - credential security

- Owner: `codex:/root`
- Status: `done`
- Backend result: `2dacefcfb` (latest ruoyi-third integration checkpoint; supersedes `8d9d03238`)

## Implementation

Provider and endpoint credentials are separate records. Endpoint credentials override provider credentials by type. AES-GCM ciphertext, nonce, auth tag, and key version are persisted; only the supported `v1` envelope is accepted and unknown versions fail closed. The master key is read only from configuration. Management responses expose summaries and never plaintext or ciphertext. Logically deleted credentials cannot be reactivated through a forged identifier.

## Verification

| Check | Result |
|---|---|
| `ThirdCredentialCryptoTest` and credential lifecycle guard | 4 tests pass, including tamper rejection, missing-key fail-closed, unknown-version rejection, and deleted-record reactivation rejection |
| Backend compile | pass |
| MVC/log secret canary and persistence encryption check | pending release environment |
