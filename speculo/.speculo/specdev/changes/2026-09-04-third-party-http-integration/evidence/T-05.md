# Evidence: T-05 - credential security

- Owner: `codex:/root`
- Status: `done`
- Backend result: `ff8753773` (latest ruoyi-third integration checkpoint; supersedes `a7ae94b47`)

## Implementation

Provider and endpoint credentials are separate records. Endpoint credentials override provider credentials by type. AES-GCM ciphertext, nonce, auth tag, and key version are persisted; the master key is read only from configuration. Management responses expose summaries and never plaintext or ciphertext.

## Verification

| Check | Result |
|---|---|
| `ThirdCredentialCryptoTest` | 2 tests pass, including tamper rejection and missing-key fail-closed |
| Backend compile | pass |
| MVC/log secret canary and persistence encryption check | pending release environment |
