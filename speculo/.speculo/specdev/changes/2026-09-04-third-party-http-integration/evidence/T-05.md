# Evidence: T-05 - credential security

- Owner: `codex:/root`
- Status: `done`
- Backend result: `3bdfd61e` (latest ruoyi-third integration checkpoint; supersedes `ff8753773`)

## Implementation

Provider and endpoint credentials are separate records. Endpoint credentials override provider credentials by type. AES-GCM ciphertext, nonce, auth tag, and key version are persisted; only the supported `v1` envelope is accepted and unknown versions fail closed. The master key is read only from configuration. Management responses expose summaries and never plaintext or ciphertext.

## Verification

| Check | Result |
|---|---|
| `ThirdCredentialCryptoTest` | 3 tests pass, including tamper rejection, missing-key fail-closed, and unknown-version rejection |
| Backend compile | pass |
| MVC/log secret canary and persistence encryption check | pending release environment |
