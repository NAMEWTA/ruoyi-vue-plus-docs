# Evidence: T-06 - configuration snapshot and invalidation

- Owner: `codex:/root`
- Status: `done`
- Backend result: `3bdfd61e` (latest ruoyi-third integration checkpoint; supersedes `ff8753773`)

## Implementation

DB remains the fact source. `ThirdConfigCacheAdapter` stores provider/endpoint snapshots in the `third:config:` Redis namespace behind `ThirdConfigSnapshotPort`, evicts after provider, endpoint, or credential writes, and publishes invalidation events. Gateway runtime validation rechecks snapshot schema before use. Redis or snapshot uncertainty returns `CONFIG_UNAVAILABLE` and never uses an unknown stale snapshot.

## Verification

| Check | Result |
|---|---|
| Backend compile | pass |
| Cache failure paths source review | pass |
| Redis multi-instance invalidation and fail-closed integration | pending; Redis/Docker unavailable |
