# Evidence: T-06 - configuration snapshot and invalidation

- Owner: `codex:/root`
- Status: `done`
- Backend result: `d5f7dc0b` (latest ruoyi-third integration checkpoint; supersedes `3bdfd61e`)

## Implementation

DB remains the fact source. `ThirdConfigCacheAdapter` stores provider/endpoint snapshots in the `third:config:` Redis namespace behind `ThirdConfigSnapshotPort`, evicts after provider, endpoint, or credential writes, and publishes invalidation events. Gateway runtime validation rechecks snapshot schema before use. Redis or snapshot uncertainty returns `CONFIG_UNAVAILABLE` and never uses an unknown stale snapshot.

## Verification

| Check | Result |
|---|---|
| Backend compile | pass |
| Cache failure paths source review | pass |
| Redis multi-instance invalidation and fail-closed integration | pending; the existing remote Redis is reachable, but a second local app instance hit the shared cache invalidation acknowledgement timeout during startup, so the required isolated multi-instance proof is not claimed |

## Runtime probe (2026-09-04)

The existing `18080` process is an older JAR and returned `404` for `/third/provider/list`. A current classpath launch loaded `ruoyi-third`, opened HTTP `18081`, and connected to the configured MySQL and Redis services. It then exited during the unrelated system OSS startup runner because the shared Redis cache invalidation acknowledgement timed out. No database mutation or cache cleanup was performed.
