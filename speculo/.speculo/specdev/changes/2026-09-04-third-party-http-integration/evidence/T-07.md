# Evidence: T-07 - RestClient gateway pipeline

- Owner: `codex:/root`
- Status: `done`
- Backend result: `ff8753773` (supersedes `a7ae94b47`)

## Implementation

`ThirdHttpClientFactory` centralizes JDK client connection/read timeouts, redirect policy, and Spring `RestClient` conversion. Dynamic metadata calls use the fixed gateway pipeline; fixed typed integrations may use the `@HttpExchange` marker. Provider adapters are selected by a duplicate-checked registry and may explicitly prepare/map requests. The final implementation keeps the layered chain intact and exposes persistence, crypto, resilience, configuration, and observability through ports instead of adapter-to-DAO imports.

## Verification

| Check | Result |
|---|---|
| Backend module compile | pass |
| URI/header/body whitelist and adapter registry source review | pass |
| `ParameterizedTypeReference<T>` JavaType response conversion | pass |
| `validate-module-mode.mjs ... --mode layered` | pass (68 Java files) |
| Transport failure attempt-count preservation | pass; failures record elapsed time and actual attempts |
| Caller header/path/metadata rejection and response-body redaction | pass; configured headers remain server-controlled |
| Local HTTP status, redirect, and adapter integration | pending; local provider test stack unavailable |
