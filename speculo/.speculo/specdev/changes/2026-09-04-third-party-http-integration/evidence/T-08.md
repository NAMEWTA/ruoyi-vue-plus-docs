# Evidence: T-08 - resilience and limits

- Owner: `codex:/root`
- Status: `done`
- Backend result: `8d9d03238` (latest ruoyi-third integration checkpoint; supersedes `650a5bd98`)

## Implementation

Connect/read timeout is mandatory. Provider and endpoint rate/concurrency gates use Redis/Redisson before HTTP construction. Unknown gate state fails closed. Only explicitly idempotent endpoints receive at most three attempts; leases are released in `finally`, and failure categories distinguish rejection and timeout.

## Verification

| Check | Result |
|---|---|
| Backend compile and local-profile tests | pass; 26 tests, including 5 loopback Gateway tests, endpoint adapter ownership, physical-attempt logging, typed transport, sink health, and bounded retry policy coverage |
| Local-profile security tests | pass |
| Redis limiter/concurrency and retry HTTP attempt counter | pending; Redis/local HTTP unavailable |
