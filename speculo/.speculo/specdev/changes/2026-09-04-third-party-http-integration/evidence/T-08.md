# Evidence: T-08 - resilience and limits

- Owner: `codex:/root`
- Status: `done`
- Backend result: `751ecd439` (latest ruoyi-third integration checkpoint; supersedes `2dacefcfb`)

## Implementation

Connect/read timeout is mandatory. Provider and endpoint rate/concurrency gates use Redis/Redisson before HTTP construction. Unknown gate state fails closed. Only explicitly idempotent endpoints receive at most three attempts; leases are released in `finally`, and failure categories distinguish rejection and timeout.

## Verification

| Check | Result |
|---|---|
| Backend compile and local-profile tests | pass; 32 `ruoyi-third` tests, including 5 loopback Gateway tests, 3 Spring context tests, endpoint adapter ownership, physical-attempt logging, typed transport, sink health, bounded retry policy, and provider/credential lifecycle guard coverage |
| Local-profile security tests | pass |
| Real Redis limiter/concurrency and fail-closed gate (`ThirdRedisIntegrationTest`) | pass; 2 tests, 0 failures/errors/skips; provider limits bound endpoint limits, rejected calls have zero outbound count, leases release, and Redis shutdown returns `CONFIG_UNAVAILABLE` |
| Retry HTTP attempt counter (`ThirdGatewayApplicationContextTest`) | pass; bounded idempotent retry is two physical attempts, while non-idempotent timeout is one |
