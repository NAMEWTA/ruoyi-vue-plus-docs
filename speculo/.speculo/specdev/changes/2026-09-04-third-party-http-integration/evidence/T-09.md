# Evidence: T-09 - observability and statistics

- Owner: `codex:/root`
- Status: `done`
- Backend result: `ff8753773` (latest ruoyi-third integration checkpoint; supersedes `a7ae94b47`)

## Implementation

Every gateway attempt enters `ThirdInvocationRecorderAdapter` through `ThirdInvocationRecorderPort`. Invocation detail stores sanitized request/response summaries and attempt count; statistics retain provider and endpoint aggregates with success, failure, timeout, rejected, and quota fields. `ThirdLogSanitizerAdapter` enforces an immutable sensitive-key blacklist, endpoint-additive redaction, and a UTF-8-safe 16 KiB cap before writing to the existing `SysLogEventSink`. Recorder persistence and sink failures are best effort and do not replace the synchronous gateway result.

## Verification

| Check | Result |
|---|---|
| Backend module compile and local-profile tests | pass; 8 tests |
| Recorder/sanitizer source review | pass |
| MySQL aggregate/upsert and log sink canary | pending; MySQL service unavailable |
