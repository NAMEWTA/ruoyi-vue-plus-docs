# Evidence: T-09 - observability and statistics

- Owner: `codex:/root`
- Status: `done`
- Backend result: `5401cbc08` (latest ruoyi-third integration checkpoint; supersedes `ec8cd78a`)

## Implementation

Every gateway attempt enters `ThirdInvocationRecorderAdapter` through `ThirdInvocationRecorderPort`. Invocation detail stores sanitized request/response summaries and attempt count; statistics retain provider and endpoint aggregates with success, failure, timeout, rejected, and quota fields. `ThirdLogSanitizerAdapter` enforces an immutable sensitive-key blacklist, endpoint-additive redaction, valid bounded JSON values, and a UTF-8-safe 16 KiB cap before writing to the existing `SysLogEventSink`. Recorder persistence and sink failures are best effort and do not replace the synchronous gateway result.

## Verification

| Check | Result |
|---|---|
| Backend module compile and local-profile tests | pass; 19 tests, including the immutable outbound-log sanitization canary |
| Recorder/sanitizer source review | pass |
| MySQL aggregate/upsert and production log sink persistence | pending; MySQL service and application-context sink are not available in the current gate |
