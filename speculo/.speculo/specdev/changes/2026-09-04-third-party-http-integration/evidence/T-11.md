# Evidence: T-11 - third-party administration pages

- Owner: `codex:/root`
- Status: `done`
- Frontend result: `55deeacd` (latest web-domain checkpoint; supersedes `fdfba0f`)

## Implementation

Added `@namewta/web-domain-third` with exactly four views: provider, endpoint, invocation, and statistics. Credential replacement/removal is a permission-guarded nested dialog in provider/endpoint workflows; no standalone credential page is registered. Forms and tables use the typed domain service, explicit component keys, permission metadata, masked credential fields, and provider/endpoint edit, enable/disable, and delete controls.

## Verification

| Command | Result |
|---|---|
| `pnpm --filter @namewta/web-domain-third typecheck` | pass |
| `pnpm --filter @namewta/web-domain-third lint` | pass |
| `pnpm --filter @namewta/admin-web typecheck` | pass |
| `pnpm --filter @namewta/web-domain-third test` | pass; manifest test verifies exactly four page registrations |

Browser routing and permission behavior are verified by T-12 when the backend/menu stack is available.
