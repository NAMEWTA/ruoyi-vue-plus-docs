# Evidence: T-12 - Admin composition and menu

- Owner: `codex:/root`
- Status: `done`
- Frontend result: `183f1ea` (supersedes `fdfba0f`)
- Root menu result: `961c485`

## Implementation

Admin composition explicitly registers the third domain and web-domain in `package.json`, `application/services.ts`, and `adminManifestRegistry.ts`. The root release artifact adds the `三方接口管理` menu below System Management with provider, endpoint, credential, invocation, and statistics permissions matching backend routes and component keys.

## Verification

| Check | Result |
|---|---|
| Admin typecheck | pass |
| Admin lint | pass |
| Admin production build | pass |
| Menu/manifest static key and permission review | pass |
| MySQL fresh-init, backend authorization, and browser permission E2E | pending; Docker/service stack unavailable |

The working tree retains a pre-existing `apps/admin-web/vite.config.ts` change; it was not reverted.
