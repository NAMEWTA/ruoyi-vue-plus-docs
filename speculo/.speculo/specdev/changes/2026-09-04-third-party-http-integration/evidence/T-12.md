# Evidence: T-12 - Admin composition and menu

- Owner: `codex:/root`
- Status: `done`
- Frontend result: `cc6a6f2` (latest web-domain/Admin integration checkpoint; supersedes `d45a480`)
- Root menu result: current root menu DML change (uncommitted at evidence capture)

## Implementation

Admin composition explicitly registers the third domain and web-domain in `package.json`, `application/services.ts`, and `adminManifestRegistry.ts`. The root release artifact adds the `三方接口管理` menu below System Management with exactly four child pages: provider, endpoint, invocation, and statistics. Credential query/add/remove permissions are function items under the provider workflow and are consumed by the nested credential dialog; no standalone credential menu is seeded.

## Verification

| Check | Result |
|---|---|
| Admin typecheck | pass |
| Admin lint | pass |
| Admin production build | pass |
| Full workspace typecheck/lint/test/build and architecture | pass; architecture reports 101/101 after registering `third -> ruoyi-third` |
| Menu/manifest static key and permission review | pass |
| Direct unauthenticated management HTTP (`ThirdAdminPermissionHttpTest`) | pass; 1 test covers six management endpoints, all return JSON `401` and no UseCase is invoked |
| Browser permission E2E (`third-party-management.spec.ts`) | pass; 2 tests cover full permissions and list-only permissions across the four menu pages; list-only users cannot mutate or resolve direct child routes |
| Canonical MySQL 8.4 fresh-init | pass; isolated remote Docker/MySQL 8.4.9 imported the ordered stack and verified the third-party menu/permission rows |

The backend and frontend implementation worktrees are clean after their commits. The root worktree still contains pre-existing `docs/README.md` and `docs/error/` changes; they were not touched or staged.

## 2. Lead Dispatch And Candidate Return

Lead-owned current-workspace execution; frontend result `cc6a6f2` and root menu integration were verified directly by the Lead.

## 3. 修改范围与路径所有权

T-12 owns Admin App composition, manifest registration, package lock changes, and `61-third-dml.sql`. Product domain/web-domain sources remain read-only inputs.

## 4. 验收与合同映射

AC-015/016 are covered by the System Management menu, four child pages, explicit component keys, backend permissions, and full/list-only browser scenarios.

## 5. Workspace Verification

`current-workspace` Admin typecheck, lint, build, architecture (101/101), direct permission HTTP, and Playwright browser E2E (2/2) passed. Canonical fresh-init also verified one parent menu, four children, and 15 `third:*` permissions.

## 6. 双轴审查

App -> web-domain -> domain composition and server-side permissions agree on route/component keys. List-only users cannot mutate or resolve direct child routes.

## 7. Integration Verification

Direct-parent frontend result `cc6a6f2` passed. Root SQL and menu DML were verified in the isolated MySQL 8.4.9 fresh-init and the temporary environment was removed.

## 8. 偏差与决策

No deviation. The earlier unavailable Docker note is superseded by the isolated remote release verification.

## 9. 残余风险与交付定位

Production role assignment and enabling live providers remain separately approved deployment actions.
