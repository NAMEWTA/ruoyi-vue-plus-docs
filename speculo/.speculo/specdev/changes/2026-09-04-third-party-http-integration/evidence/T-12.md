# Evidence: T-12 - Admin composition and menu

- Owner: `codex:/root`
- Status: `done`
- Frontend result: `d45a480` (latest web-domain/Admin integration checkpoint; supersedes `fa3b8b6`)
- Root menu result: `bed64ec`

## Implementation

Admin composition explicitly registers the third domain and web-domain in `package.json`, `application/services.ts`, and `adminManifestRegistry.ts`. The root release artifact adds the `三方接口管理` menu below System Management with exactly four child pages: provider, endpoint, invocation, and statistics. Credential query/add/remove permissions are function items under the provider workflow and are consumed by the nested credential dialog; no standalone credential menu is seeded.

## Verification

| Check | Result |
|---|---|
| Admin typecheck | pass |
| Admin lint | pass |
| Admin production build | pass |
| Full workspace typecheck/lint/test/build and architecture | pass; architecture reports 101/101 after registering `third -> ruoyi-third` |
| Menu/manifest static key and permission review | pass |
| MySQL fresh-init, backend authorization, and browser permission E2E | pending; the current runtime has reachable services but no approved fresh-init/reset, test role assignment, or browser E2E session |

The backend and frontend implementation worktrees are clean. The root worktree still contains pre-existing `docs/README.md` and `docs/error/` changes; they were not touched or staged.
