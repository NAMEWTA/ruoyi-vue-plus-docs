# Evidence: T-13 - engineering sync and release gate

- Owner: `codex:/root`
- Status: `in_progress`
- Backend result: `751ecd439` (supersedes `2dacefcfb`)
- Frontend result: `cc6a6f2` (supersedes `d45a480`)
- Root SQL/CI/SpecDev result: current root working tree at evidence capture

## Skill and structure synchronization

The project module map and `ruoyi-module-guide` register `ruoyi-third` and its layered guide. The module root documents `controller/admin -> usecase/impl -> service -> dao -> mapper -> XML`; runtime infrastructure is isolated behind `port`, `adapter`, and pure `support` packages, with no adapter-to-DAO or controller-to-service bypass. Public cross-module access remains limited to `ruoyi-api` gateway contracts. Frontend ownership remains domain/web-domain/App with explicit exports and composition. All 13 Ticket documents contain the mandatory `必须加载的 Skill 与工程基线` section.

## Verified checks

| Area | Result |
|---|---|
| Backend root `./mvnw.cmd -P local -q test` | pass; exit 0 after backend `751ecd439`; `ruoyi-third` contributes 32 tests with 0 failures/errors |
| Layered module-mode validation | pass; 71 Java files |
| Spring application-context Gateway/SPI | pass; `ThirdGatewayApplicationContextTest` 3/3, covering no-data startup, duplicate/missing adapter startup validation, typed responses, provider error mapping, 503/no-retry, 302 refusal, SSRF zero-request rejection, and timeout/retry attempt counts |
| Backend HTTP permission | pass; `ThirdAdminPermissionHttpTest` rejects six unauthenticated management endpoints with JSON `401` and zero UseCase calls |
| MySQL 8.4.9 isolated schema integration | pass; `ThirdSchemaContractTest` 1/1 and `ThirdSchemaMySqlIntegrationTest` 1/1, with randomized tables, FK/scope/soft-delete/statistic uniqueness and cleanup |
| Redis/Redisson integration | pass; `ThirdRedisIntegrationTest` 2/2, with two-client invalidation, missing-ACK timeout, provider-bounded endpoint limits, zero-send rejection, lease release and `CONFIG_UNAVAILABLE` fail-closed behavior |
| Production SysLog pipeline | pass; `ThirdOutboundSysLogPersistenceTest` 1/1 launches an isolated JVM using production Logback config and verifies two JSON events, 16 KiB cap and mandatory secret redaction |
| Full frontend workspace | pass; `pnpm typecheck`, `pnpm lint`, `pnpm test`, and `pnpm build:prod`; Admin reports 40 tests |
| `pnpm architecture:check` | pass; 101/101, including `third -> ruoyi-third` |
| Browser permission E2E | pass; Playwright `third-party-management.spec.ts` 2/2 for full and list-only permissions, four menu pages, hidden mutations/secrets and direct-route rejection |
| Isolated full release package | pass; backend `751ecd439` export completed all 47 reactor projects and contains `ruoyi-third-6.0.0.jar` |
| Isolated core release package | pass; all 47 reactor projects; contains `ruoyi-third-6.0.0.jar` and excludes job/ai/demo/workflow |
| Bundle assertion script | pass for both full and core artifacts; `ruoyi-third` is a required artifact |
| Release-artifacts static contract suite | pass; 31 tests for the ordered 10/20/30/40/50/60/61 baseline |
| SpecDev validators and Skill coverage | pass; tickets/implement 0 errors and 0 warnings; 13/13 Ticket Skill sections present |
| `git diff --check` | pass for root scope, backend and frontend |

## Runtime and remaining release check

The MySQL test ran against MySQL 8.4.9 using randomized table names and removed only the five tables it created. The Redis test used randomized keys and channels; it did not clear shared namespaces. HTTP tests use a deterministic loopback server and require no external provider credential. The SysLog test writes to a temporary isolated log path. Browser E2E intercepts the management API and validates actual route/menu/permission behavior without assigning a production role.

Full/core packaging was run from `D:\Document\code\namewta-release-verify-751ecd439-zip` because a running legacy `ruoyi-admin` process locks the source workspace JAR. Verified copies are `D:\Document\code\namewta-full-admin-751ecd439.jar` and `D:\Document\code\namewta-core-admin-751ecd439.jar`.

The sole remaining required gate is canonical MySQL 8.4 fresh-init of the full 10/20/30/40/50/60/61 stack. Docker CLI is unavailable, and the reachable `ry-namewta` database is not an approved reset target. `scripts/ci/run-external-services.sh` now runs the third MySQL/Redis tests under `-Pdev` and requests exact-schema mode in its disposable `namewta_ci` database, but that CI path was not executed on this host. This is why the change remains `in_progress`.

No real Qichacha endpoint, production credential, deployment, role assignment, production migration, or destructive shared-cache operation was executed.
