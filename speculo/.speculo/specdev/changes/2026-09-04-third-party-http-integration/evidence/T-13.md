# Evidence: T-13 - engineering sync and release gate

- Owner: `codex:/root`
- Status: `in_progress`
- Backend result: `5401cbc08` (supersedes `b78b8c5e5`)
- Frontend result: `1fa13dc` (supersedes `55deeacd`)
- Root SQL/menu: `bed64ec`

## Skill and structure synchronization

The project module map and `ruoyi-module-guide` now register `ruoyi-third` and its layered guide. The module root documents `controller/admin -> usecase/impl -> service -> dao -> mapper -> XML`; runtime infrastructure is isolated behind `port`, `adapter`, and pure `support` packages, with no adapter-to-DAO or controller-to-service bypass. Public cross-module access remains limited to `ruoyi-api` gateway contracts. Frontend package ownership remains domain/web-domain/App with explicit exports and composition.

## Verified checks

| Area | Result |
|---|---|
| Backend module compile | pass |
| Backend root `./mvnw.cmd -P local -q test` | pass; exit 0 after the latest backend commit; the `ruoyi-third` module contributes 19 local-profile tests (0 failures/errors) |
| Layered module-mode validation | pass (68 Java files) |
| Backend local-profile crypto/path/sanitizer/origin/loopback Gateway tests | 19 passing; includes 302 refusal, bounded retry, and outbound SysLog sanitization canary |
| Frontend domain/web-domain/admin typecheck and lint | pass |
| Admin production build | pass |
| `pnpm architecture:check` | pass |
| New third domain/web-domain build and Vitest gates | pass; domain route contract and four-page manifest tests pass |
| `git diff --check` on changed repositories | pass |
| Isolated full release package | pass; clean package from backend `5401cbc08` export completed all 47 reactor projects and `ruoyi-admin` repackage succeeded |
| Isolated core release package | pass; `-Pbundle-core -Dmaven.test.skip=true` from the same `5401cbc08` export completed all 47 reactor projects and `ruoyi-admin` repackage succeeded |
| Full/core `BOOT-INF/lib` composition | pass; both packages contain `ruoyi-third-6.0.0.jar`; full also contains job/ai/demo/workflow, while core excludes those optional modules |

## Runtime and required release checks

The host has a running legacy `ruoyi-admin` process on `18080`; its current JAR predates this module and returned `404` for `/third/provider/list`. A current classpath launch on `18081` loaded `ruoyi-third`, opened the HTTP listener, and connected to the configured MySQL and Redis services. A read-only JDBC probe confirmed MySQL `8.4.9`, but the existing `ry-namewta` database has no `third_*` tables and was not changed. The launch exited during the existing system OSS startup runner because the shared Redis cache invalidation acknowledgement timed out for the second instance. Docker CLI is not installed, and no isolated MySQL 8.4 fresh-init, counting local provider, SysLog sink, browser permission session, or approved test-role setup is available.

The full/core package gate was run separately from an isolated export of backend `5401cbc08` because the original workspace's running legacy process locks `ruoyi-admin/target/ruoyi-admin.jar`. Both clean package commands completed all 47 reactor projects with `ruoyi-admin` repackage success, and direct `BOOT-INF/lib` inspection confirmed `ruoyi-third-6.0.0.jar` in both packages. The full package contains job/ai/demo/workflow; the core package excludes those optional modules.

MySQL fresh-init/constraints, isolated Redis multi-instance invalidation and limiter behavior, full local HTTP status/redirect/retry application-context integration, SysLog persistence canary, and browser permission E2E therefore remain pending. The deterministic loopback unit test now proves the normal request and pre-send rejection paths without external credentials. No real Qichacha endpoint, production credential, deployment, role assignment, migration, or destructive cache operation was executed. The change must remain `in_progress` until those gates run in an approved release environment.
