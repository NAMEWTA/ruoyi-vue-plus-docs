# Evidence: T-13 - engineering sync and release gate

- Owner: `codex:/root`
- Status: `in_progress`
- Backend result: `d5f7dc0b` (supersedes `3bdfd61e`)
- Frontend result: `1fa13dc` (supersedes `55deeacd`)
- Root SQL/menu: `bed64ec`

## Skill and structure synchronization

The project module map and `ruoyi-module-guide` now register `ruoyi-third` and its layered guide. The module root documents `controller/admin -> usecase/impl -> service -> dao -> mapper -> XML`; runtime infrastructure is isolated behind `port`, `adapter`, and pure `support` packages, with no adapter-to-DAO or controller-to-service bypass. Public cross-module access remains limited to `ruoyi-api` gateway contracts. Frontend package ownership remains domain/web-domain/App with explicit exports and composition.

## Verified checks

| Area | Result |
|---|---|
| Backend module compile | pass |
| Layered module-mode validation | pass (68 Java files) |
| Backend local-profile crypto/path/sanitizer tests | 12 passing |
| Frontend domain/web-domain/admin typecheck and lint | pass |
| Admin production build | pass |
| `pnpm architecture:check` | pass |
| New third domain/web-domain build and Vitest gates | pass; domain route contract and four-page manifest tests pass |
| `git diff --check` on changed repositories | pass |

## Runtime and required release checks

The host has a running legacy `ruoyi-admin` process on `18080`; its current JAR predates this module and returned `404` for `/third/provider/list`. A current classpath launch on `18081` loaded `ruoyi-third`, opened the HTTP listener, and connected to the configured MySQL and Redis services. It exited during the existing system OSS startup runner because the shared Redis cache invalidation acknowledgement timed out for the second instance. Docker CLI is not installed, and no isolated MySQL 8.4 fresh-init, counting local provider, SysLog sink, browser permission session, or approved test-role setup is available.

MySQL fresh-init/constraints, isolated Redis multi-instance invalidation and limiter behavior, local HTTP status/redirect/retry integration, SysLog persistence canary, and browser permission E2E therefore remain pending. No real Qichacha endpoint, production credential, deployment, role assignment, migration, or destructive cache operation was executed. The change must remain `in_progress` until those gates run in an approved release environment.
