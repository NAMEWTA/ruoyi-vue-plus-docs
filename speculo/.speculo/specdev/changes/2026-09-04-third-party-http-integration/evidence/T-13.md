# Evidence: T-13 - engineering sync and release gate

- Owner: `codex:/root`
- Status: `in_progress`
- Backend result: `ff8753773` (supersedes `a7ae94b47`)
- Frontend result: `183f1ea` (supersedes `fdfba0f`)
- Root SQL/menu: `c49f562`, `961c485`

## Skill and structure synchronization

The project module map and `ruoyi-module-guide` now register `ruoyi-third` and its layered guide. The module root documents `controller/admin -> usecase/impl -> service -> dao -> mapper -> XML`; runtime infrastructure is isolated behind `port`, `adapter`, and pure `support` packages, with no adapter-to-DAO or controller-to-service bypass. Public cross-module access remains limited to `ruoyi-api` gateway contracts. Frontend package ownership remains domain/web-domain/App with explicit exports and composition.

## Verified checks

| Area | Result |
|---|---|
| Backend module compile | pass |
| Layered module-mode validation | pass (68 Java files) |
| Backend local-profile crypto/path/sanitizer tests | 8 passing |
| Frontend domain/web-domain/admin typecheck and lint | pass |
| Admin production build | pass |
| `pnpm architecture:check` | pass |
| New third domain/web-domain build and Vitest gates | pass; domain route contract test passes, web-domain has no unit test files |
| `git diff --check` on changed repositories | pass |

## Required release checks not run

Docker is not installed in this workspace. MySQL 8.4 fresh-init/constraints, Redis multi-instance invalidation and limiter behavior, local HTTP status/redirect/retry integration, SysLog persistence canary, and browser permission E2E therefore remain pending. No real Qichacha endpoint, production credential, deployment, role assignment, or migration was executed. The change must remain `in_progress` until those gates are run in the release environment.
