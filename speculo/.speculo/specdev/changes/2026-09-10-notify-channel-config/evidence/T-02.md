# Evidence T-02

- MAIL scene bindings seeded for five scenes; hot subject/body use locked `${name}` tokens; dispatch renders bound template not caller snapshots.
- Test: `DispatchNotificationServiceTest.boundMailUsesRenderedTemplateNotCallerSnapshots` — caller title/content ignored; rendered `验证码 1234`.
- Disabled account fail-closed: `disabledAccountFailsClosedWithoutProviderSend`.
- Save rejects unknown tokens via `NotifyTemplateRenderer.validate`.
