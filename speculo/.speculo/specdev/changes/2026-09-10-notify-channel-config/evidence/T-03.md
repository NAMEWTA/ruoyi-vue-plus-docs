# Evidence T-03

- SMS binding requires vendor template code + declared variable mapping; dispatch uses `NotifyTemplateContent` on the bound account `providerKey` only.
- Test: `boundSmsUsesVendorTemplateOnBoundAccountOnly` — `SMS_BOUND` template, params mapped, other enabled account never resolved.
- SMS4J blends registered from DB accounts (`Sms4jBlendRegistry`); YAML `config-type: interface`; no default blend when providerKey blank.
- Vendor config must be the factory class (`AlibabaConfig` / `TencentConfig`), not an anonymous `BaseConfig` subclass — `AlibabaFactory.createSms` checkcasts and would CCE. `Sms4jBlendRegistry.vendorConfig` uses `ProviderFactoryHolder.requireForSupplier` + `factory.getConfigClass()`.
- Tests: `Sms4jBlendRegistryTest.vendorConfigUsesAlibabaAndTencentClassesNotGenericBaseConfig`, `upsertRegistersBlendRetrievableByConfigKeyWithoutClassCast`, `removeUnregistersBlend`.
- Disable: `NotifyConfigService.registerSms` calls `smsBlendRegistry.remove` when `enabled=N`; `NotifyConfigServiceTest.disablingSmsAccountUnregistersBlend`.
