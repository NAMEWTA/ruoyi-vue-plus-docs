# Evidence T-04

- Three quota layers in `NotifySendPlanner`: account/minute, template/minute (≤ account on save), per-scene recipient intercept.
- Tests: `secondSendWithinAccountMinuteCapFailsClosed`; `templateMinuteMaxCannotExceedAccountCap`.
- YAML `restricted` / `minute-max` / `account-max` removed from application-dev.yml and application-prod.yml (see yaml-account-scan.txt).
- auth-captcha seed: restricted Y, 1/min, 30/day.
