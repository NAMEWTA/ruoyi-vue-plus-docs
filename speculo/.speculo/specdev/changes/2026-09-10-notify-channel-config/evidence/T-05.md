# Evidence T-05

- Captcha `templateCode=auth-captcha` with `code` + `expireMinutes`; no hardcoded sentence.
- Enterprise transfer: `Map.of("code", code)`.
- Person-rebind SMS: empty params (IN_APP keeps SAFE_TEXT).
- Workflow/notice: wrapper `${title}/${content}/${path}`; workflow sceneCode `workflow-task`.
- Tests: CaptchaNotifyCallerUnitTest, DemoNotifyCallerUnitTest, NotifyCallerMigrationContractUnitTest, WorkflowNotifyCallerUnitTest, EnterpriseTransferServiceTest, PersonRebindNotificationTest — all green.
