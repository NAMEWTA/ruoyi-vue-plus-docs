# Evidence T-06

- POST `/notify/config/test/account` and `/notify/config/test/template` with `notify:config:test`.
- Tests: `NotifyTestSendServiceTest` disabled/unbound fail-closed; template submit uses scene examples without providerKey.
- Frontend: ConfigPage mail/SMS TABs, token occupancy, no SMS free-text body, test-send dialog; web-domain registration `notify/config/index`.
- Skills: notification.md, notify index, common-mail/sms facts, ruoyi-notify layered in 03-backend-module-modes.
- `validate-skill-facts.mjs` and `validate-module-mode.mjs ... --mode layered` exit 0.
